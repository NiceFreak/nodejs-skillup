# 部署链路 · 讲稿

> **对外表达材料，不进展板、不 import 到前端。** 要点由 AI 列出（都是已发生的事实），
> 走查怎么讲、协作复盘怎么写由本人完成——同 `AGENTS.md` 与 `plan/w5-display-board.md §3.3`
> （掌握证据 vs 对外表达）。前端只放可视化的链路图，这里放「怎么讲这条链路」。

## 走查顺序（讲的时候按这个链路过）

1. 一句话定位：零后端展板 → Pages 子路径；顺带点明**为什么后台上不了 Pages**（纯静态托管，跑不了 Express/Mongo）。
2. 指着链路图讲：构建开关 → `dist` → 拷进 Pages 仓 → **合并 main 才发布**（不是推分支就上线）。
3. 收在「沉淀」：一次性流程 → `deploy-showcase-pages` 技能。

## 关键决策点（讲到每个岔路口的取舍）

| 决策 | 为什么 | 证据 / 落点 |
|---|---|---|
| 只发布**零后端展板**，排除管理后台 | Pages 是纯静态托管，跑不了 Express/Mongo | `VITE_SHOWCASE_ONLY` 隐藏 `#/admin`、导航、页脚 |
| 放在**子路径** `/skillup-week8/` | 与已有 `/viz/`、`/screenplay/` 并列，语义清晰 | `--base=/skillup-week8/` |
| 用 **hash 路由** | 子路径静态托管下 hash 路由天然无 SPA 刷新 404 | `App.tsx` 的 `#/showcase` |
| 笔记链接**改为板内跳转** | 相对 `.md` 链接在静态站点点不动；改切板对移动端最顺手 | `MarkdownNotes.tsx` 的 `a` 渲染器 |
| CORS **本地被 dev proxy 隐藏** | 本地同源转发，后端「零改动」；跨域部署才需 CORS | `vite.config.ts` proxy；后端上线属 backlog |

## AI 协作分工（我主导、AI 辅助）

| 部分 | 归属 | 谁做 |
|---|---|---|
| 构建开关、部署产物、构建/拷贝脚本 | 白名单脚手架 / 胶水 | AI 辅助搭 |
| 关键决策（发什么、放哪、隐藏什么） | 判断 | 本人定方向 |
| `deploy-showcase-pages` 技能 | 白名单工具沉淀 | AI 辅助搭，本人定范围 |
| 端到端链路的走查与复盘 | 核心表达 | 本人 |

## 复盘 · 这次和 AI 怎么协作的（待本人填写）

> 留给本人写，AI 不代笔。几个可选切入问题：
>
> - 这条链路里，哪些是我**主动定方向**、哪些是 AI 提出我否决/采纳的？
> - 我在哪一步靠**追问**把 AI 的默认方案纠偏了（例：子路径 / 隐藏后台 / 链接修法）？
> - 「AI 辅助、核心判断我来」这条 `AGENTS.md` 红线，在这次协作里是怎么守住的？
> - 如果脱离这次对话，我能独立把这条链路**从空白重建**吗？卡点在哪？
>
> _（正文待补）_
