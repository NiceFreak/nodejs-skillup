# 展板状态核查线（独立线，不与学习主线同步开展）

> 建立：2026-08-20（Asia/Shanghai）。**本人决定单开一条线**——展板核查不必跟着每日学习主线走。
> 上游：[`visualization-optimization-roadmap.md`](./visualization-optimization-roadmap.md) 第八轮「落地范围的补正」（发现与证据）
> 优先级入口：根 [`BACKLOG.md`](../../BACKLOG.md) **P2-4**（本线是它的执行文档；档位仍以 BACKLOG 为准）
> 边界：`week8-fullstack/src/frontend/` 白名单展示资产（`AGENTS.md` §2）；不改后端学习代码，不部署。

---

## 1. 这条线是什么，不是什么

**是什么**：一条**按块推进、可随时暂停**的核查队列。每次消化一块板，做完记一行，下次从下一块继续。
它的节奏由**余量**决定，不由学习日程决定——这正是「单开一条线」的意思。

**不是什么**（三条，缺一条这条线就会开始和学习抢时间）：

1. **不是新增的并行线。** 2026-08-17 已拍板：W10 的并行线只有**英语 + 项目叙述**（`week10-plan.md` §5）。
   本线不进这个名单，也不占每日主线的时间盒。
2. **不是每日必做项。** 按 `LEARNING-PROTOCOL.md` §7 的固定优先级，它属「上周遗留实验与优化」那一档——
   **只有当天主线达到止步条件后才允许进入**。当天主线没收口，这条线就该是零进展，且零进展不算欠账。
3. **不是收口日的活。** 收口日（每周 D5）本来就满，把八块板的判定塞进去，
   结果只会是「凭印象铺开改」——那恰好和这条线要修的失效同型。

**一句话**：**它排队，不排期。**

---

## 2. 范围：八块板

| # | tab | 组件 | 组件末次改动 | 建成时的口径 | 维度① 数据时效 | 维度② 表达形态 |
|---|---|---|---|---|---|---|
| 1 | 认证与授权 | `AuthBoard.tsx` | 2026-08-12 | 旧（图形只负责关系） | 待核 | 视觉编码：**可答，小改候选**；阅读负担：**实测 0.97 屏 · 无欠项** |
| 2 | OAuth2 流程 | `OAuth2Panel.tsx` | 2026-07-29 | 旧 | 待核 | 视觉编码：**可答**（凭据卡一处留裁决，§5.2）；阅读负担：**实测 0.73 屏 · 无欠项** |
| 3 | 数据库聚合 | `W3Board.tsx` | 2026-07-29 | 旧 | 待核 | 视觉编码：**可答，局部改候选**；阅读负担：**实测 0.74 屏 · 无欠项** |
| 4 | Node.js 运行时 | `W5Board.tsx` | 2026-08-12 | 旧 | 待核 | 视觉编码：**可答，无欠项**；阅读负担：**实测 1.48 屏均 · 8/8 超一屏，轻度** |
| 5 | 测试闭环 | `W6Board.tsx` | 2026-07-30 | 旧 | 待核 | 视觉编码：**可答，小改候选**（另加常驻折叠的证据矩阵，§5.2）；阅读负担：**实测 1.51–2.21 屏，中度** |
| 6 | 测试闭环（Day 4 子板） | `W6Day4Board.tsx` | 2026-07-30 | 旧 | 待核 | 视觉编码：**可答，局部改候选**；阅读负担：**实测 5.12 屏 · 减负候选**（全站第三重） |
| 7 | 部署上线 | `W9Board.tsx` | 2026-08-18（仅两张 `HBarChart` 按新口径） | 旧为主 | 待核 | 视觉编码：**可答**；阅读负担：**减负候选**（十三块，均 2.03 屏 · 11/13 超一屏 · `evidence` 实测 6.08） |
| 8 | 可观测性 | `W10Board.tsx` | 2026-08-20 | **新**（全程按加严判据） | **优先**（⑤ 那四格「未实测」） | 视觉编码：**可答，无欠项**；阅读负担：**均 2.88 屏 · 7/7 超一屏 · 全站最重**（§5.1 第 1 条） |

**明确不在范围**（写下来，免得下次又把范围铺大）：

- **「学习笔记」tab（`MarkdownNotes.tsx`）**：Markdown 正文以阅读为主，roadmap「明确保留文字的页面与内容」已排除。
- **管理后台的 `Dashboard.tsx`**：它属 8080 admin 应用，不是展板；**2026-08-20 首次列范围时误把它算进来过，已剔除**。
- **实验的环境、样本量、命令、原始测量与「事实 / 推断 / 未测量」限定语**：这些本来就该是文字（同上）。

> `reviewOnly` 标记（部署上线 / 可观测性 / 面试准备）只影响**能不能进公开无门禁的发布目标**，不影响是否要核查。
> 「面试准备」板（`InterviewBoard.tsx`，末改 8/12）同属旧口径，但它是**材料索引**而非关系图。
> **2026-08-21 判定：不纳入**——`iv-matrix` 已把它的验收句编码成矩阵，实测 0.98 屏也无减负需求；
> 它与 `W6Day4Board` 共用 `evidenceSets.ts`，那处的量出了图这块跟着受益。依据见 §5.2。

---

## 3. 两个维度，两种触发方式（不要混用）

| 维度 | 问什么 | 判据 | 触发方式 |
|---|---|---|---|
| **① 数据时效** | 这一格的结论和**最新一次实测**还对得上吗？ | 对不上就翻档；`yarn verify:board` 保持全过 | **事件驱动**：每次有新实测、新收口，就核**受影响的那几格**——不必等排到这条线 |
| **② 表达形态** | A. 遮住 `h3` 与结论段，还能答出验收句吗？B. 能答出以后，是否仍要读完多屏正文才知道重点？ | 加严后的验收判据（roadmap 第八轮）+ 四条肯定面 + 阅读负担三问 | **排队驱动**：按本文件 §4 一块一块消化 |

**四条肯定面**（roadmap「目标与约束」，此处只作复检提问用，不重新定义）：

1. 同类对象 ≥ 3 个要相互比较 → 有没有让读者在脑子里建表的地方？
2. 量可比且差距本身是结论 → 差距是结论的那几处，出图了吗？
3. 信息有序或分层 → 用的是位置编码，还是「第一 / 第二」这类序号词？
4. 一块板的正文超过一屏 → 有没有一个不读正文就能读到的锚（一眼读数 / 矩阵 / 尺）？

**阅读负担三问**（2026-08-21 补入；它和“有没有图”不是同一件事）：

1. **主路径**：一块只回答一个问题时，读者能否先看到“问题 → 判断 → 必要边界”，还是摘要、命令、原始证据、复盘过程同时争抢注意力？
2. **重复量**：结论已经由位置、长度或矩阵编码后，紧邻正文是否又把同一结论完整说一遍？保留限定语不等于重复讲图。
3. **文案可读性**：一句是否同时压入结论、原因、反例、限定和动作；各卡是否反复使用同一种高度压缩的判断句。这里把本人所说的“AI 感”落实为可观察的句法与信息层级问题，**不推断文字来源**。

> 第 4 条只有“长页必须有锚”，是必要条件，不是长页通过条件。锚存在后仍把全部细节默认摊开，
> 依然属于阅读负担欠项。专题高度、字数等代理指标只用于定位高密度区域，不能替代人工判断，
> 也不能靠压字数凑过验收。

> 维度 ① 是**跟着学习走的**（新数据一到就该修），维度 ② 才是这条线的主体。
> 把两者混成一件事，会让「今天有没有新实测」决定「今天要不要返工八块板」——那是没道理的耦合。

---

## 4. 工作单元与两个阶段

**一个工作单元 = 15–20 分钟 = 一块板**（`AGENTS.md` 的小单元口径）。

### 阶段一：只判定，不改（八块，八个单元）

对一块板：遮住 `h3` 与结论段 → 自问该块的验收句 → 答得出 / 答不出 → 四条肯定面各过一遍 →
再过阅读负担三问 →
在 §5 记一行：**欠什么、欠多少、值不值得改**。

**阶段一不许动代码。** 这是整条线最容易破的一条纪律：看到一处不顺眼就顺手改，
结果是判定没做完、改动铺开、规模再次估不出来。

### 阶段二：按判定结果决定改哪几块（规模到阶段一末尾才知道）

阶段一全部做完后，拿着八行判定一次性决定：**改哪几块、每块改到什么程度、要不要全改**（大概率不必全改）。
届时更新 `BACKLOG.md` P2-4 的档位与规模，再排具体执行。

---

## 5. 进度与判定记录（每块一行，做完即填）

> **进度**：视觉编码 8/8 判完（§5、§5.2）；阅读负担已有全量实测（§5.1），但三问的判断仍是人闸。
> 「面试准备」判定不纳入（§5.2）。**阶段二未开始，本线不关闭。**

> 2026-08-21 判据纠正前写下的 `AuthBoard` 至 `W6Day4Board` 六行，**只对视觉编码有效**；
> 其中“无表达欠项 / 不值得改”不是阅读负担结论。补核时更新原行，不另造一份互相冲突的表。

| 日期 | 板 | 遮标题后能否答出验收句 | 先破的是哪项判据 | 欠什么（一句话） | 值不值得改 | 单元数估计 |
|---|---|---|---|---|---|---|
| 2026-08-21 | `AuthBoard` | 能；参与层、步骤轨道、凭据卡与结果卡仍能说明两道门及停止层 | 第 1 条：三个结果路径需要相互比较 | 总览一次只显示一条路径，比较 401 / 403 / 200 的停止位置要来回切换并依赖记忆 | 值得，作为低成本小改候选；不影响当前验收 | 1 |
| 2026-08-21 | `OAuth2Panel` | 能；三方位置、六步顺序、信道颜色与五类凭据卡共同说明流程和边界 | 无；四条肯定面均通过 | 无表达欠项 | 不值得为改而改；当前形态已承载结论 | 0 |
| 2026-08-21 | `W3Board` | 能；分层、pipeline 与月份靠位置表达，两个 explain 仍可从表中读出结论 | 第 2 条：量可比且差距本身是结论 | 两个 explain 专题只列前后数字，`14→5`、`15→0`、`12ms→3ms` 的幅度没有被长度或位置编码 | 值得；阶段二只改共享 `ExplainVisual`，其余三个专题不动 | 2 |
| 2026-08-21 | `W5Board` | 能；统一职责图与八个专题各自的阶段、时间轴、批次、缓冲、链路或对照图仍能承载判断 | 无；四条肯定面均通过 | 无表达欠项 | 不值得为改而改；长页中的机制文字已有明确视觉锚，不影响当前验收 | 0 |
| 2026-08-21 | `W6Board` | 能；总闭环、覆盖拓扑、CI 来源与生命周期、全栈请求轨道仍能说明三天如何形成可重复证据 | 第 1 条：Day 3 的三条结果路径需要相互比较 | 全栈轨道一次只显示 admin / member / 无 Token 中的一条，比较三个停止层仍要切换并依赖记忆 | 值得，作为 Day 3 局部小改候选；不影响当前验收 | 1 |
| 2026-08-21 | `W6Day4Board` | 能；四维框架、四周因果链、三种业务终点、十层交付物、限制与还债时间线仍能完成整体收束 | 第 2 条：量可比且差距本身是结论 | W3 与 W5 两组实验只列前后数字，`15→0`、`12ms→3ms`、`1154ms→102ms`、`378ms→2–3ms` 等量级差没有被长度或位置编码 | 值得；阶段二只改 `EvidenceLayers` 中 W3 / W5 的数值呈现，W6 类别型对照及其他区域不动 | 2 |
| 2026-08-21 | `W9Board` | 能；十三个专题各自已有矩阵、停止轨道、生命周期轴、分层链路或数量图，故问题不是单纯缺图 | 阅读负担先破：有锚后仍把摘要、限制、命令、证据和复盘大面积同层展开 | 13 个专题中 11 个正文区超过一个 1000px 桌面视口；`evidence` 约 4.2 屏，`release` 2.7、`boundary` 2.4、`failure` 2.3、`urlface` 2.1；每个专题还重复出现证据图例、词表与 13 行路线图。结论图能读，但完整说明没有形成清晰的渐进层级 | 值得；阶段二应先做信息减负与文案松绑，而不是继续加图。优先处理五个高密度专题与共享外壳；`spoken`（0.7 屏）和 `exposure`（0.9 屏）不因同属 W9 被一刀切改 | 4–6 |

> **阶段二意向（2026-08-21，本人确认）**：当前已发现的五个优化候选
>（`AuthBoard`、`W3Board`、`W6Board`、`W6Day4Board`、`W9Board`）均可以做；仍按阶段纪律等阶段一
> 完整收口后统一冻结执行范围与顺序，阶段一不提前改代码。前六块此前只过了视觉编码问法，
> 阅读负担尚未补核，不能用“已有视觉锚”推出“正文可读”。

### 5.1 阅读负担实测（2026-08-21，八块板 + 面试板全部 45 个专题）

8/21 的判据补正把「阅读负担」立成一维，但**只有 `W9Board` 有量**：其余六块记「待补核」，
`W10Board` 暂不进入。这一节把量补齐——**不是估，是把展板真的跑起来量的**。

**方法（可复现）**：`yarn build:showcase` 产物 → 本地静态服务 → Chromium 1280×1000 →
逐 tab 逐专题点开、把「揭示 / 展开 / 开始逐步核对」类闸门全部点开 →
量**专题正文区**的渲染高度（`#w9-topic-panel` / `#w10-topic-panel` / `.w5-stage` /
`.authk-stage` / `.oauth-flow` / `.w6-board` 减去总闭环与 Day 条），以 1000px 为一屏折算。
门禁只在本地 `localStorage` 里塞了一对占位 token / user（门禁是存在性检查），**没有后端、没有部署、没有改一行展板代码**。

**先验证方法本身**——拿 W9 已有的七个数字对照：

| 专题 | 8/21 已记 | 本次实测 | 差 |
|---|---|---|---|
| `release` 发布变更单 | 2.7 | **2.75** | +0.05 |
| `boundary` 信任边界 | 2.4 | **2.38** | −0.02 |
| `failure` 故障分叉 | 2.3 | **2.32** | +0.02 |
| `urlface` URL 面 | 2.1 | **2.18** | +0.08 |
| `spoken` 讲得出来才算会 | 0.7 | **0.72** | +0.02 |
| `exposure` 服务边界 vs 暴露边界 | 0.9 | **0.89** | −0.01 |
| `evidence` 契约销账与闸门 | 4.2 | **6.08** | **+1.88** |

**七格里六格差 ≤ 0.08 屏，且「13 个专题中 11 个超过一屏」完全复现**（超一屏的正好是那 11 个，
`spoken` 0.72 与 `exposure` 0.89 是仅有的两个不到一屏）。两次独立测量对上，方法可信。
**唯一的大差是 `evidence`：把它内部的闸门全部点开是 6.08 屏，不是 4.2 屏**——
原记录多半停在部分折叠态。这一格因此比原判还重，`W9Board` 减负的首位不变，量级要往上修。

**全量结果**（专题正文区，全部展开，1000px = 一屏）：

| 板 | 专题数 | 平均 | 超过一屏 | 最重的一个专题 |
|---|---|---|---|---|
| 8 · 可观测性 `W10Board` | 7 | **2.88 屏** | **7/7** | ⑤ 演练分档与定位 **5.91 屏** |
| 5/6 · 测试闭环 `W6Board`（含 Day 4 子板） | 4 | **2.73 屏** | **4/4** | Day 4 · W3–W6 整体总结 **5.12 屏** |
| 7 · 部署上线 `W9Board` | 13 | **2.03 屏** | 11/13 | `evidence` 契约销账与闸门 **6.08 屏** |
| 4 · Node.js 运行时 `W5Board` | 8 | **1.48 屏** | **8/8** | 知识点 3 线程池与慢点诊断 **2.47 屏** |
| （候选）面试准备 `InterviewBoard` | 5 | **0.98 屏** | 2/5 | 板块 2 两处硬伤 **1.10 屏** |
| 1 · 认证与授权 `AuthBoard` | 4 | **0.97 屏** | 1/4 | 知识点 4 RBAC 与 401/403 **1.06 屏** |
| 3 · 数据库聚合 `W3Board` | 5 | **0.74 屏** | 0/5 | 知识点 2 聚合 pipeline **0.87 屏** |
| 2 · OAuth2 `OAuth2Panel` | 1 | **0.73 屏** | 0/1 | 全板 **0.73 屏** |

> `AuthBoard` 那一行只是下半的四个知识点；它上面还有一段常驻的 `auth-master` 总览，
> 连总览一起量是 2.65–2.84 屏。总览本身是这块板判「能答出验收句」的依据，不算减负对象，故分开记。

**三条这次才看出来的事**

1. **`W10Board` 的阅读负担是全站最重的，比 `W9Board` 还重。** 平均 2.88 屏 vs 2.03 屏，
   **7/7 全部超过一屏**（`W9Board` 是 11/13）。8/21 决定「暂不进入 W10」的理由是它按新判据建、
   视觉编码没问题——**这个理由只对第一问成立**。它的矩阵、阈值尺、证据链确实是全站最好的，
   但**「编码好」和「读起来省力」是两件事**，这块板恰好是前者最好、后者最差。
   建议：**W10 必须进阶段一的阅读负担这一维**，不能因为它是新口径就跳过。
2. **`W6Day4Board` 5.12 屏被漏掉了。** 它现在的判定只有「第 2 条：量欠图，2 单元」，
   但它是**全站第三重的单个专题**，仅次于 W9 `evidence`（6.08）和 W10 ⑤（5.91），
   比 W9 里最重的那几个常规专题（`release` 2.75、`boundary` 2.38）还高一倍。
   它的 2 单元估计只覆盖了数值呈现，没覆盖减负。
3. **`W3Board` 与 `OAuth2Panel` 实测确认没有阅读负担欠项**（0.74 / 0.73 屏，无一超过一屏）。
   这两块「待补核」可以直接销掉：`OAuth2Panel` 两维都无欠项的结论成立，
   `W3Board` 只剩「改共享 `ExplainVisual`」那一项。`AuthBoard`（0.97）与面试板（0.98）同理，
   都在一屏上下，减负不是它们的问题。

**这份数据不回答的事**：它只量**篇幅**，不量「主路径是否清晰」「同一结论是否被重复讲」
「一句里是否压了结论 + 原因 + 反例 + 限定」。§3 已经写明「专题高度、字数等代理指标只用于
定位高密度区域，不能替代人工判断，也不能靠压字数凑过验收」——**这份表就是那种定位工具**，
它指出该看哪几块，不代替看。三问的判断仍然是人闸。

### 5.2 视觉编码那一维：另外两块的判定与三处补正

8/21 的六行只覆盖到 `W6Day4Board`，`W10Board` 未进入、「面试准备」的纳入判定仍挂着。补上：

- **8 · 可观测性 `W10Board`（视觉编码）：可答，无欠项。** 三张 `w10-matrix`、`w10-rulers` 阈值尺
  （红线 / 余量 / 当前值 / 出事点落在同一根轨道上）、`w10-corridor` 三道绿灯 × 四条实例、
  `w10-chain` 五条红态证据链、`w10-tier-grid` 三档。四条肯定面都不破。
  **但见 §5.1 第 1 条：它的阅读负担是全站最重的**——两维结论相反，这正是判据补正想区分开的情况。
- **（候选）面试准备 `InterviewBoard`：判定不纳入。** `iv-matrix`（学习周 × 面试稿章节 + 状态，
  `role="table"`）已经把它的验收句编码成矩阵；它的 `iv-metrics` 与 `W6Day4Board` 读同一份
  `evidenceSets.ts`，那处的量出了图这块跟着受益；实测 0.98 屏，也没有减负需求。
- **补正一：`W6Board` 的证据精度矩阵是常驻折叠的。** 全板唯一一张「测试 / 观察结果 × 能证明 ×
  没有证明」矩阵（`w6-matrix-table`）包在 `<details>` 里且**没有 `open` 属性**，展示态和复习态都默认收起。
  §3 第 4 条要的是「不读正文就能读到的锚」，**要点开才看得到的锚不满足这一条**。
  这一处独立于已记的「Day 3 三条结果路径要切换比较」，是同一块板上的第二处，改法也更便宜。
- **补正二：`W6Day4Board` 的九条纠错折叠不是欠项。** `Corrections` 在复习态默认全收起、展示态全展开
  （`setOpenIds(review ? [] : 全开)`），这是主动回忆的有意设计，**与上一条的常驻 `<details>` 性质不同**，
  不要在阶段二一起「打开」。
- **补正三（与已记结论不一致，留裁决）：`OAuth2Panel` 的凭据卡可能破第 1 条。**
  已记「四条肯定面均通过、无欠项」。但 `oauth-creds` 是并排五张卡，每张只有 `code` + 一句
  `boundary` 文字；「`client_secret` 只在后信道、绝不过浏览器」这条验收句要么读文字、
  要么播到第 4 步看高亮，**五个凭据用来比较的那一维（属哪条信道 / 过不过浏览器）没有被编码**。
  数据是现成的（`OAUTH_STEPS[].creds` 与 `OAUTH_CREDS` 同一套 key，交叉即得矩阵）。
  **两种判法都说得通**：说它通过，是因为六步时序那张全览已经承载了主结论、凭据只是附注；
  说它不通过，是因为凭据边界本身就是这块板的验收句之一。**按谁是验收句来裁——这一条留给本人。**
  实测 0.73 屏也说明：即便要改，它是全站最便宜的一块。

### 5.3 跨块的两条共性（比逐块返工便宜）

逐行判定之外，八行合起来能看出两条横贯多块的欠口——**一次做完覆盖面大于逐块修**：

1. **互斥切换**：`AuthBoard` 三条结果路径、`W6Board` Day 3 三条轨道（两条已记在案），
   加上 `W3Board` / `W5Board` / `W6Day4Board` 的知识点与实验组 `nav`，都是「同类对象一次只显示一个，
   要比较就得来回切换并依赖记忆」。这正是第 1 条肯定面要防的事。
   做法上它们同型：给这类切换补一条**把被切开的对象按位置摆在一处的全览**，而不是逐块重新设计。
2. **量的差距只写成数字**：`W3Board` 的 `ExplainVisual` 与 `W6Day4Board` 的 `EvidenceLayers`
   （两条已记在案）读的是同型数据，而 `HBarChart` 已经存在、`W9Board` 已经在用——**属复用不属新造**。
   面试板共用 `evidenceSets.ts`，跟着受益。

**阶段一完成后在这里写一段总结**：八块里几块欠着、总规模、建议档位 → 回写 `BACKLOG.md` P2-4。

---

## 6. 启动、止步与验证

**什么时候可以推进这条线**（满足其一即可，不需要专门排期）：

- 当天学习主线已达到止步条件，且还有一个 15–20 分钟的空档；
- 周复盘后有余量；
- 或者本人明确说「今天推展板核查线」。

**止步条件**：

1. **当天主线未收口** → 本线零进展（不算欠账）。
2. **一次只做一块** → 一个单元结束就停，不连做三块「反正手热」。
3. **阶段一不改代码** → 见 §4。
4. **改动只走白名单最小范围** → 展示资产可由 AI 实现，但不顺带重构无关组件（`AGENTS.md` §3 末段）。
5. **任何改动后 `yarn typecheck` + `yarn verify:board` 必须全过**（8/20 基线 421/421），窄屏与深浅色各核一次。

**不部署**：发布目标是服务器 8081，且**发布是独立授权动作**（`SHOWCASE-DEPLOY-PROTOCOL.md`）；
Pages 上传处于冻结状态，本线不解冻、不上传。

---

## 7. 与学习主线的接口

- **W10 D5（8/21 收口日）只交维度 ①**：W10 板七块按最新数据核一遍、不一致的当场翻档、`verify:board` 保持全过。
  维度 ② **不在 D5 做**（原计划的「抽样 3 块定性」已移交本线）。
- **W11 起**：本线按 §6 的空档推进，不进每日计划的时间盒，不写进当周主线。
- **`LEARNING-STATE.md`**：只保留一行指针（当前进展 X/8），不复制本文件内容。

---

## 8. 退出条件

阶段一八行判定填满，并且阶段二的「改哪几块」有结论且执行完毕 → 本线关闭，
在 roadmap 追加一节收口记录，`BACKLOG.md` P2-4 标完成。
**若阶段一的结论是「都不值得改」，同样是关闭**——判定本身就是产出，不是必须改点什么才算数。

---

## 9. 变更记录

- **2026-08-20 建立**。起因：本人指出「不要过分追求动效」的波及面比 8/18 认为的更广，
  大段文字展示不是 W9/W10 的局部问题，这两周只是集中爆发处（证据三条见 roadmap 第八轮补正）。
  当天本人进一步决定：**核查单开一条线，不与学习同步开展**——遂有本文件。
- **2026-08-20 范围修正**：首次列范围时把 admin 的 `Dashboard.tsx` 误算成展板（它属 8080 管理后台），
  已剔除；同时补入此前漏掉的 `OAuth2Panel.tsx`。核查对象由「九块板」更正为**八块板**（另有「面试准备」待判定是否纳入）。
- **2026-08-21 元数据纠正**：核查 `OAuth2Panel.tsx` 时以 `git log -1` 复核，组件末次改动实际为
  2026-07-29，不是表中原记的 2026-08-12；两者都早于 8/18 新口径，故不改变「旧口径成品」分类。
- **2026-08-21 元数据纠正（续）**：`W3Board.tsx` / `w3Topics.ts` 的末次改动同样由 `git log -1`
  纠正为 2026-07-29；仍早于 8/18，不改变分类。
- **2026-08-21 元数据纠正（再续）**：`W6Board.tsx` 的末次改动经 `git log -1` 复核为
  2026-07-30，不是原记的 2026-08-12；仍早于 8/18，不改变分类。
- **2026-08-21 元数据纠正（Day 4 子板）**：`W6Day4Board.tsx` / `w6Day4.ts` 的末次改动经
  `git log -1` 复核同为 2026-07-30，不是原记的 2026-08-12；仍早于 8/18，不改变分类。
- **2026-08-21 阅读负担实测补齐**：把展板 `build:showcase` 后跑起来，用 Chromium 1280×1000 逐专题量
  正文区高度，覆盖**八块板 + 面试板全部 45 个专题**。方法先拿 W9 已记的七个数字对照验证：
  **六格差 ≤ 0.08 屏、「11/13 超过一屏」完全复现**；唯一大差是 `evidence`——把内部闸门全部点开是
  6.08 屏而非 4.2 屏，故该格量级往上修。**零代码改动、零部署**，门禁只在本地 `localStorage` 塞占位值。
  三条新结论：① `W10Board` 阅读负担全站最重（均 2.88 屏、7/7 超一屏，高于 W9 的 2.03 与 11/13），
  「按新判据建」只对第一问成立，**它必须进阅读负担这一维**；② `W6Day4Board` 5.12 屏是全站第三重，
  原判的 2 单元只覆盖了数值呈现没覆盖减负；③ `W3Board` 0.74 / `OAuth2Panel` 0.73 / `AuthBoard` 0.97 /
  面试板 0.98 实测确认无阅读负担欠项，这几块的「待补核」可以销掉。详见 §5.1。
- **2026-08-21 视觉编码补完 + 三处补正**：补上 `W10Board`（可答，无欠项）与「面试准备」的纳入判定（不纳入）；
  补正 `W6Board` 的证据精度矩阵是**常驻折叠**（`<details>` 无 `open`，要点开的锚不满足第 4 条）、
  `W6Day4Board` 的纠错折叠是**主动回忆的有意设计不算欠项**、`OAuth2Panel` 凭据卡是否破第 1 条**留本人裁决**。详见 §5.2。
- **2026-08-21 判据纠正**：本人不接受 `W9Board`“有视觉锚 = 无欠项”的判定，并指出原始问题可能是
  文本过密、过度精炼或呈现出“AI 感”。复核 roadmap 第八轮后确认异议成立：这条线的起因本来就是
  “每块板都充斥巨量文字”，四条肯定面只检查结论有没有被版面承载，没有完整检查承载结论后的阅读负担。
  因此维度②补入“主路径 / 重复量 / 文案可读性”三问，撤回 W9 的“无欠项”；此前六块保留视觉编码结论，
  但阅读负担改为待补核。这里的“AI 感”只记录可观察的统一压缩句式，不推断文本实际来源。
