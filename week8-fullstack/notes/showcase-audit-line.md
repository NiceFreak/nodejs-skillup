# 展板状态核查线（独立线，不与学习主线同步开展）

> 建立：2026-08-20（Asia/Shanghai）。**本人决定单开一条线**——展板核查不必跟着每日学习主线走。
> 上游：[`visualization-optimization-roadmap.md`](./visualization-optimization-roadmap.md) 第八轮「落地范围的补正」（发现与证据）
> 优先级入口：根 [`BACKLOG.md`](../../BACKLOG.md) **P2-4**（本线是它的执行文档；档位仍以 BACKLOG 为准）
> 边界：`week8-fullstack/src/frontend/` 白名单展示资产（`AGENTS.md` §2）；不改后端学习代码，不部署。

---

## 1. 这条线是什么，不是什么

**是什么**：一条**按块推进、可随时暂停**的核查队列。每次消化一块板，做完记一行，下次从下一块继续。
它的节奏由**余量**决定，不由学习日程决定——这正是「单开一条线」的意思。

**不是什么**（三条，缺一条这条线就会开始和学习抢时间）：

1. **不是新增的并行线。** 2026-08-17 已拍板：W10 的并行线只有**英语 + 项目叙述**（`week10-plan.md` §5）。
   本线不进这个名单，也不占每日主线的时间盒。
2. **不是每日必做项。** 按 `LEARNING-PROTOCOL.md` §7 的固定优先级，它属「上周遗留实验与优化」那一档——
   **只有当天主线达到止步条件后才允许进入**。当天主线没收口，这条线就该是零进展，且零进展不算欠账。
3. **不是收口日的活。** 收口日（每周 D5）本来就满，把八块板的判定塞进去，
   结果只会是「凭印象铺开改」——那恰好和这条线要修的失效同型。

**一句话**：**它排队，不排期。**

---

## 2. 范围：八块板

| # | tab | 组件 | 组件末次改动 | 建成时的口径 | 维度① 数据时效 | 维度② 表达形态 |
|---|---|---|---|---|---|---|
| 1 | 认证与授权 | `AuthBoard.tsx` | 2026-08-12 | 旧（图形只负责关系） | 待核 | 视觉编码：**可答，小改候选**；阅读负担：**实测 0.97 屏 · 无欠项** |
| 2 | OAuth2 流程 | `OAuth2Panel.tsx` | 2026-07-29 | 旧 | 待核 | 视觉编码：**可答**（凭据卡一处留裁决，§5.2）；阅读负担：**实测 0.73 屏 · 无欠项** |
| 3 | 数据库聚合 | `W3Board.tsx` | 2026-07-29 | 旧 | 待核 | 视觉编码：**可答，局部改候选**；阅读负担：**实测 0.74 屏 · 无欠项** |
| 4 | Node.js 运行时 | `W5Board.tsx` | 2026-08-12 | 旧 | 待核 | 视觉编码：**可答，无欠项**；阅读负担：**实测 1.48 屏均 · 8/8 超一屏，轻度** |
| 5 | 测试闭环 | `W6Board.tsx` | 2026-07-30 | 旧 | 待核 | 视觉编码：**可答，小改候选**（另加常驻折叠的证据矩阵，§5.2）；阅读负担：**实测 1.51–2.21 屏，中度** |
| 6 | 测试闭环（Day 4 子板） | `W6Day4Board.tsx` | 2026-07-30 | 旧 | 待核 | 视觉编码：**可答，局部改候选**；阅读负担：**实测 5.12 屏 · 减负候选**（全站第三重） |
| 7 | 部署上线 | `W9Board.tsx` | 2026-08-18（仅两张 `HBarChart` 按新口径） | 旧为主 | 待核 | 视觉编码：**可答**；阅读负担：**减负候选**（十三块，均 2.03 屏 · 11/13 超一屏 · `evidence` 实测 6.08） |
| 8 | 可观测性 | `W10Board.tsx` | 2026-08-20 | **新**（全程按加严判据） | **优先**（⑤ 那四格「未实测」） | 视觉编码：**可答，无欠项**；阅读负担：**均 2.88 屏 · 7/7 超一屏 · 全站最重**（§5.1 第 1 条） |

**明确不在范围**（写下来，免得下次又把范围铺大）：

- **「学习笔记」tab（`MarkdownNotes.tsx`）**：Markdown 正文以阅读为主，roadmap「明确保留文字的页面与内容」已排除。
- **管理后台的 `Dashboard.tsx`**：它属 8080 admin 应用，不是展板；**2026-08-20 首次列范围时误把它算进来过，已剔除**。
- **实验的环境、样本量、命令、原始测量与「事实 / 推断 / 未测量」限定语**：这些本来就该是文字（同上）。

> `reviewOnly` 标记（部署上线 / 可观测性 / 面试准备）只影响**能不能进公开无门禁的发布目标**，不影响是否要核查。
> 「面试准备」板（`InterviewBoard.tsx`，末改 8/12）同属旧口径，但它是**材料索引**而非关系图。
> **2026-08-21 判定：不纳入**——`iv-matrix` 已把它的验收句编码成矩阵，实测 0.98 屏也无减负需求；
> 它与 `W6Day4Board` 共用 `evidenceSets.ts`，那处的量出了图这块跟着受益。依据见 §5.2。

---

## 3. 两个维度，两种触发方式（不要混用）

| 维度 | 问什么 | 判据 | 触发方式 |
|---|---|---|---|
| **① 数据时效** | 这一格的结论和**最新一次实测**还对得上吗？ | 对不上就翻档；`yarn verify:board` 保持全过 | **事件驱动**：每次有新实测、新收口，就核**受影响的那几格**——不必等排到这条线 |
| **② 表达形态** | A. 遮住 `h3` 与结论段，还能答出验收句吗？B. 能答出以后，是否仍要读完多屏正文才知道重点？ | 加严后的验收判据（roadmap 第八轮）+ 四条肯定面 + 阅读负担三问 | **排队驱动**：按本文件 §4 一块一块消化 |

**四条肯定面**（roadmap「目标与约束」，此处只作复检提问用，不重新定义）：

1. 同类对象 ≥ 3 个要相互比较 → 有没有让读者在脑子里建表的地方？
2. 量可比且差距本身是结论 → 差距是结论的那几处，出图了吗？
3. 信息有序或分层 → 用的是位置编码，还是「第一 / 第二」这类序号词？
4. 一块板的正文超过一屏 → 有没有一个不读正文就能读到的锚（一眼读数 / 矩阵 / 尺）？

**阅读负担三问**（2026-08-21 补入；它和“有没有图”不是同一件事）：

1. **主路径**：一块只回答一个问题时，读者能否先看到“问题 → 判断 → 必要边界”，还是摘要、命令、原始证据、复盘过程同时争抢注意力？
2. **重复量**：结论已经由位置、长度或矩阵编码后，紧邻正文是否又把同一结论完整说一遍？保留限定语不等于重复讲图。
3. **文案可读性**：一句是否同时压入结论、原因、反例、限定和动作；各卡是否反复使用同一种高度压缩的判断句。这里把本人所说的“AI 感”落实为可观察的句法与信息层级问题，**不推断文字来源**。

> 第 4 条只有“长页必须有锚”，是必要条件，不是长页通过条件。锚存在后仍把全部细节默认摊开，
> 依然属于阅读负担欠项。专题高度、字数等代理指标只用于定位高密度区域，不能替代人工判断，
> 也不能靠压字数凑过验收。

> 维度 ① 是**跟着学习走的**（新数据一到就该修），维度 ② 才是这条线的主体。
> 把两者混成一件事，会让「今天有没有新实测」决定「今天要不要返工八块板」——那是没道理的耦合。

---

## 4. 工作单元与两个阶段

**一个工作单元 = 15–20 分钟 = 一块板**（`AGENTS.md` 的小单元口径）。

### 阶段一：只判定，不改（八块，八个单元）

对一块板：遮住 `h3` 与结论段 → 自问该块的验收句 → 答得出 / 答不出 → 四条肯定面各过一遍 →
再过阅读负担三问 →
在 §5 记一行：**欠什么、欠多少、值不值得改**。

**阶段一不许动代码。** 这是整条线最容易破的一条纪律：看到一处不顺眼就顺手改，
结果是判定没做完、改动铺开、规模再次估不出来。

### 阶段二：按判定结果决定改哪几块（规模到阶段一末尾才知道）

阶段一全部做完后，拿着八行判定一次性决定：**改哪几块、每块改到什么程度、要不要全改**（大概率不必全改）。
届时更新 `BACKLOG.md` P2-4 的档位与规模，再排具体执行。

---

## 5. 进度与判定记录（每块一行，做完即填）

> **进度**：视觉编码 8/8 判完（§5、§5.2）；阅读负担已有全量实测（§5.1），但三问的判断仍是人闸。
> 「面试准备」判定不纳入（§5.2）。**阶段二未开始，本线不关闭。**

> 2026-08-21 判据纠正前写下的 `AuthBoard` 至 `W6Day4Board` 六行，**只对视觉编码有效**；
> 其中“无表达欠项 / 不值得改”不是阅读负担结论。补核时更新原行，不另造一份互相冲突的表。

| 日期 | 板 | 遮标题后能否答出验收句 | 先破的是哪项判据 | 欠什么（一句话） | 值不值得改 | 单元数估计 |
|---|---|---|---|---|---|---|
| 2026-08-21 | `AuthBoard` | 能；参与层、步骤轨道、凭据卡与结果卡仍能说明两道门及停止层 | 第 1 条：三个结果路径需要相互比较 | 总览一次只显示一条路径，比较 401 / 403 / 200 的停止位置要来回切换并依赖记忆 | 值得，作为低成本小改候选；不影响当前验收 | 1 |
| 2026-08-21 | `OAuth2Panel` | 能；三方位置、六步顺序、信道颜色与五类凭据卡共同说明流程和边界 | 无；四条肯定面均通过 | 无表达欠项 | 不值得为改而改；当前形态已承载结论 | 0 |
| 2026-08-21 | `W3Board` | 能；分层、pipeline 与月份靠位置表达，两个 explain 仍可从表中读出结论 | 第 2 条：量可比且差距本身是结论 | 两个 explain 专题只列前后数字，`14→5`、`15→0`、`12ms→3ms` 的幅度没有被长度或位置编码 | 值得；阶段二只改共享 `ExplainVisual`，其余三个专题不动 | 2 |
| 2026-08-21 | `W5Board` | 能；统一职责图与八个专题各自的阶段、时间轴、批次、缓冲、链路或对照图仍能承载判断 | 无；四条肯定面均通过 | 无表达欠项 | 不值得为改而改；长页中的机制文字已有明确视觉锚，不影响当前验收 | 0 |
| 2026-08-21 | `W6Board` | 能；总闭环、覆盖拓扑、CI 来源与生命周期、全栈请求轨道仍能说明三天如何形成可重复证据 | 第 1 条：Day 3 的三条结果路径需要相互比较 | 全栈轨道一次只显示 admin / member / 无 Token 中的一条，比较三个停止层仍要切换并依赖记忆 | 值得，作为 Day 3 局部小改候选；不影响当前验收 | 1 |
| 2026-08-21 | `W6Day4Board` | 能；四维框架、四周因果链、三种业务终点、十层交付物、限制与还债时间线仍能完成整体收束 | 第 2 条：量可比且差距本身是结论 | W3 与 W5 两组实验只列前后数字，`15→0`、`12ms→3ms`、`1154ms→102ms`、`378ms→2–3ms` 等量级差没有被长度或位置编码 | 值得；阶段二只改 `EvidenceLayers` 中 W3 / W5 的数值呈现，W6 类别型对照及其他区域不动 | 2 |
| 2026-08-21 | `W9Board` | 能；十三个专题各自已有矩阵、停止轨道、生命周期轴、分层链路或数量图，故问题不是单纯缺图 | 阅读负担先破：有锚后仍把摘要、限制、命令、证据和复盘大面积同层展开 | 13 个专题中 11 个正文区超过一个 1000px 桌面视口；`evidence` 约 4.2 屏，`release` 2.7、`boundary` 2.4、`failure` 2.3、`urlface` 2.1；每个专题还重复出现证据图例、词表与 13 行路线图。结论图能读，但完整说明没有形成清晰的渐进层级 | 值得；阶段二应先做信息减负与文案松绑，而不是继续加图。优先处理五个高密度专题与共享外壳；`spoken`（0.7 屏）和 `exposure`（0.9 屏）不因同属 W9 被一刀切改 | 4–6 |

> **阶段二意向（2026-08-21，本人确认）**：当前已发现的五个优化候选
>（`AuthBoard`、`W3Board`、`W6Board`、`W6Day4Board`、`W9Board`）均可以做；仍按阶段纪律等阶段一
> 完整收口后统一冻结执行范围与顺序，阶段一不提前改代码。前六块此前只过了视觉编码问法，
> 阅读负担尚未补核，不能用“已有视觉锚”推出“正文可读”。

### 5.1 阅读负担实测（2026-08-21，八块板 + 面试板全部 45 个专题）

8/21 的判据补正把「阅读负担」立成一维，但**只有 `W9Board` 有量**：其余六块记「待补核」，
`W10Board` 暂不进入。这一节把量补齐——**不是估，是把展板真的跑起来量的**。

**方法（可复现）**：`yarn build:showcase` 产物 → 本地静态服务 → Chromium 1280×1000 →
逐 tab 逐专题点开、把「揭示 / 展开 / 开始逐步核对」类闸门全部点开 →
量**专题正文区**的渲染高度（`#w9-topic-panel` / `#w10-topic-panel` / `.w5-stage` /
`.authk-stage` / `.oauth-flow` / `.w6-board` 减去总闭环与 Day 条），以 1000px 为一屏折算。
门禁只在本地 `localStorage` 里塞了一对占位 token / user（门禁是存在性检查），**没有后端、没有部署、没有改一行展板代码**。

**先验证方法本身**——拿 W9 已有的七个数字对照：

| 专题 | 8/21 已记 | 本次实测 | 差 |
|---|---|---|---|
| `release` 发布变更单 | 2.7 | **2.75** | +0.05 |
| `boundary` 信任边界 | 2.4 | **2.38** | −0.02 |
| `failure` 故障分叉 | 2.3 | **2.32** | +0.02 |
| `urlface` URL 面 | 2.1 | **2.18** | +0.08 |
| `spoken` 讲得出来才算会 | 0.7 | **0.72** | +0.02 |
| `exposure` 服务边界 vs 暴露边界 | 0.9 | **0.89** | −0.01 |
| `evidence` 契约销账与闸门 | 4.2 | **6.08** | **+1.88** |

**七格里六格差 ≤ 0.08 屏，且「13 个专题中 11 个超过一屏」完全复现**（超一屏的正好是那 11 个，
`spoken` 0.72 与 `exposure` 0.89 是仅有的两个不到一屏）。两次独立测量对上，方法可信。
**唯一的大差是 `evidence`：全部展开是 6.08 屏，默认态是 4.27 屏。**

> **这一句 8/21 当天写错过一次，已更正。** 原话是「原记录多半停在部分折叠态，这一格因此比原判还重，
> 量级要往上修」。**错在把「全展开」当成了读者的实际状态。** `evidence` 里的十七条认知修正在复习态默认收起，
> 而 `deploy` 是 `reviewOnly` 板、**读者只会在复习态见到它**——所以默认态 4.27 屏才是它真实的阅读负担，
> 原记录的 4.2 屏是对的。量级不往上修。（同理不适用于 `W10` ⑤：那一块只有一道专题闸门，
> 点开之后 5.91 屏全部摊开，所以它的全展开数就是读者态。）
> 这条更正把第 2 步内部的先后换了个个儿——见 §5.1.3。

**全量结果**（专题正文区，全部展开，1000px = 一屏）：

| 板 | 专题数 | 平均 | 超过一屏 | 最重的一个专题 |
|---|---|---|---|---|
| 8 · 可观测性 `W10Board` | 7 | **2.88 屏** | **7/7** | ⑤ 演练分档与定位 **5.91 屏** |
| 5/6 · 测试闭环 `W6Board`（含 Day 4 子板） | 4 | **2.73 屏** | **4/4** | Day 4 · W3–W6 整体总结 **5.12 屏** |
| 7 · 部署上线 `W9Board` | 13 | **2.03 屏** | 11/13 | `evidence` 契约销账与闸门 **6.08 屏** |
| 4 · Node.js 运行时 `W5Board` | 8 | **1.48 屏** | **8/8** | 知识点 3 线程池与慢点诊断 **2.47 屏** |
| （候选）面试准备 `InterviewBoard` | 5 | **0.98 屏** | 2/5 | 板块 2 两处硬伤 **1.10 屏** |
| 1 · 认证与授权 `AuthBoard` | 4 | **0.97 屏** | 1/4 | 知识点 4 RBAC 与 401/403 **1.06 屏** |
| 3 · 数据库聚合 `W3Board` | 5 | **0.74 屏** | 0/5 | 知识点 2 聚合 pipeline **0.87 屏** |
| 2 · OAuth2 `OAuth2Panel` | 1 | **0.73 屏** | 0/1 | 全板 **0.73 屏** |

> `AuthBoard` 那一行只是下半的四个知识点；它上面还有一段常驻的 `auth-master` 总览，
> 连总览一起量是 2.65–2.84 屏。总览本身是这块板判「能答出验收句」的依据，不算减负对象，故分开记。

**三条这次才看出来的事**

1. **`W10Board` 的阅读负担是全站最重的，比 `W9Board` 还重。** 平均 2.88 屏 vs 2.03 屏，
   **7/7 全部超过一屏**（`W9Board` 是 11/13）。8/21 决定「暂不进入 W10」的理由是它按新判据建、
   视觉编码没问题——**这个理由只对第一问成立**。它的矩阵、阈值尺、证据链确实是全站最好的，
   但**「编码好」和「读起来省力」是两件事**，这块板恰好是前者最好、后者最差。
   建议：**W10 必须进阶段一的阅读负担这一维**，不能因为它是新口径就跳过。
2. **`W6Day4Board` 5.12 屏被漏掉了。** 它现在的判定只有「第 2 条：量欠图，2 单元」，
   但它是**全站第三重的单个专题**，仅次于 W9 `evidence`（6.08）和 W10 ⑤（5.91），
   比 W9 里最重的那几个常规专题（`release` 2.75、`boundary` 2.38）还高一倍。
   它的 2 单元估计只覆盖了数值呈现，没覆盖减负。
3. **`W3Board` 与 `OAuth2Panel` 实测确认没有阅读负担欠项**（0.74 / 0.73 屏，无一超过一屏）。
   这两块「待补核」可以直接销掉：`OAuth2Panel` 两维都无欠项的结论成立，
   `W3Board` 只剩「改共享 `ExplainVisual`」那一项。`AuthBoard`（0.97）与面试板（0.98）同理，
   都在一屏上下，减负不是它们的问题。

**这份数据不回答的事**：它只量**篇幅**，不量「主路径是否清晰」「同一结论是否被重复讲」
「一句里是否压了结论 + 原因 + 反例 + 限定」。§3 已经写明「专题高度、字数等代理指标只用于
定位高密度区域，不能替代人工判断，也不能靠压字数凑过验收」——**这份表就是那种定位工具**，
它指出该看哪几块，不代替看。三问的判断仍然是人闸。

### 5.1.1 再往里一层：主路径与重复量的实测（2026-08-21）

§5.1 只量了「一个专题有多高」。三问里的**主路径**和**重复量**，有一部分同样能上量——
量完之后，它们从「凭印象说这块太密」变成「指着某一块说它欠在哪」。同一套方法，仍然零代码改动。

**一、主路径：正文根本不在第一屏**

| | 板顶 → 专题正文起点 | 页面顶端 → 专题正文起点 |
|---|---|---|
| `W9Board` | **673px** | **1108px** |
| `W10Board` | **692px** | **1127px** |

也就是说，**打开任何一个专题，第一屏里没有正文**——正文从第二屏开始。
挡在前面的是板头、分档图例和专题条（`W9`：123 + 199 + 266；`W10`：178 + 243 + 214）。

再看正文自己的开头。量每个专题**前 1000px 里编码元素占多少**，低于 20% 的意思是「打开先撞一屏文字」：

- **0%**：`rollback` 改一台在跑的机器、`exposure` 服务边界 vs 暴露边界、
  `W5` 知识点 4 整块读取与 Stream、知识点 6 pipeline 的完成与清理——**这四个专题一个编码元素都没有**。
- **14–20%**：`failure` 故障分叉 15%、`systemd` 18%、`W6` Day 1 15% / Day 2 16% / Day 3 14% / Day 4 18%、
  `W5` 知识点 1 0%、知识点 3 20%、知识点 8 15%。
- 对照另一端：`W10` ④ 阈值从哪来 **82%**、⑦ 红过才算数 **82%**、`W9` 信任边界 **80%**、
  `evidence` 69%、`W10` ② 67%。

**二、重复量：口径要澄清一下**

8/21 记的是「每个专题还重复出现证据图例、词表与 13 行路线图」。实测把这句话拆成两半：

- **专题正文区内部没有跨专题重复**。逐块比对 `#w9-topic-panel` / `#w10-topic-panel` / `.w5-stage`
  的直接子块指纹，13 个 / 7 个 / 8 个专题之间**没有任何一块是重复出现的**。
  也就是说，这不是「同一段话讲了十三遍」。
- **但那句话指的成本是真的，它是常驻外壳**——不是内容重复，是**每切一次专题都要重新承担一次的固定开销**：

| 板 | 常驻外壳合计 | 拆开看 |
|---|---|---|
| `W9Board` | **1343px = 1.34 屏** | 路线图 `w9-stage-plan` **707** · 专题条 266 · 分档图例 199 · 板头 123 · 术语表 48（折叠态） |
| `W10Board` | **1127px = 1.13 屏** | 路线图 `w10-stage-plan` **492** · 分档图例 243 · 专题条 214 · 板头 178 |
| `W6Board` | 412px = 0.41 屏 | 总闭环 `w6-master` 352 · Day 条 60 |

**路线图是这两块板上最大的单个常驻块，而它讲的是「本板共 13 块，当前落地 13 块」——
是建构进度，不是内容。** 它比专题条还高（707 vs 266），全板落地已经 13/13、7/7，
也就是说它现在稳定显示「全都做完了」。

**三、编码与散文的比例：同一块板内部就有 12 倍差**

量每个专题里**顶层编码块占正文的比例**，以及**编码块之外的散文里有多少中文字**：

| 专题 | 总高 | 编码占比 | 编码外散文中文字 |
|---|---|---|---|
| `W9` `evidence` 契约销账与闸门 | 6077px | **12%** | **2661** |
| `W10` ⑤ 演练分档与定位 | 5905px | **17%** | **2509** |
| `W10` ⑦ 红过才算数 | 4346px | 40% | 1653 |
| `W6` Day 4 整体总结 | 5537px | 28% | 923 |
| `W9` `release` 发布变更单 | 2751px | 26% | 900 |
| `W9` `boundary` 信任边界 | 2380px | 34% | 690 |
| **`W10` ④ 阈值从哪来** | 2069px | **83%** | **178** |

> 只列编码占比与散文字数，不列「散文占比」：散文块之间存在包含与溢出，个别专题算出来会超过 100%，
> 那个数不可靠。编码占比取的是**互不嵌套的顶层编码块**，字数是直接计数，两者都稳。

**这张表最有用的一行是最后一行。** `W10` ④「阈值从哪来」是全站编码占比最高的专题——
一根阈值尺几乎承担了全部结论，散文只剩 178 个字。而**同一块板**的 ⑤ 是 17% / 2509 字。
**⑤ 和 ⑦ 不需要重新设计，它们要向同一个文件里的 ④ 看齐。** 目标形态已经存在，不用新造。

**四、由此得到的阶段二排序建议（供本人裁决，未执行）**

1. **先动常驻外壳，因为它最便宜且零内容风险**：把 `w9-stage-plan` / `w10-stage-plan` 折成一行摘要
   （它是元信息，且已经稳定显示「全部落地」），把分档图例收进 `details` 或移到板尾。
   两块合起来 `W9` 每个专题视图省 **约 0.9 屏**（中位专题 2.03 屏的 44%），
   正文起点从 673px 提前到约 470px——**第一屏里开始看得见正文**。这一条不碰任何一个专题的内容。
2. **再动那两个五六屏的专题**：`W9` `evidence`（12% / 2661 字）与 `W10` ⑤（17% / 2509 字）。
   它们合计约 12 屏，占两块板全部正文的四分之一强。参照系是 `W10` ④。
3. **`W6Day4Board` 5.12 屏排第三**，它此前只被记为「量欠图」。
4. **首屏 0% 编码的那四个专题**（`rollback`、`exposure`、`W5` 知识点 4 / 知识点 6）单独看：
   它们篇幅都不大（0.89–1.30 屏），问题不是长，是**打开先撞文字**。属便宜的小改。
5. **`W3Board` / `OAuth2Panel` / `AuthBoard` / 面试板不进减负范围**，实测已确认。

**这一节仍然不回答第三问**（文案可读性：一句里是否压了结论 + 原因 + 反例 + 限定、各卡是否反复用同一种
高压缩判断句）。那一条没有可靠的量，**只能人读**。上面所有数字的作用是把「该读哪几段」缩小到
`evidence`、`W10` ⑤⑦、`W6` Day 4 这四处——**这 4 个专题只占已分解的 32 个专题的 12.5%，
却装了其中 37% 的散文**（7746 / 20807 中文字，分解范围为 `W9` / `W10` / `W6` / `W5` 四块板）。
先读这四处，性价比最高。

### 5.1.2 阶段二 · 第 1 步已执行：折叠常驻外壳（2026-08-21）

本人指令「按建议的顺序开始下一步」→ **阶段二开始，执行范围就此冻结为 §5.1.1 的四步排序**。
本次只做第 1 步（常驻外壳），**一次只做一件，后三步未动**。

**改了什么**：`W9Board` / `W10Board` 的**分档图例**与**建构进度**由常驻 `<section>` 改为默认收起的
`<details>`，`h3` 里那句结论移进 `<summary>` 当一行摘要（`board-fold` 类，摘要 ≥24px 触控高度，
沿用 `.w9-glossary > summary` 既有做法）。**展开后内容一字未删**，专题正文一行未动。

**改前 / 改后（同一套实测方法）**：

| | 改前 | 改后 | 省 |
|---|---|---|---|
| `W9` 常驻外壳 | 1343px（1.34 屏） | **485px（0.48 屏）** | **−858px · −64%** |
| `W10` 常驻外壳 | 1127px（1.13 屏） | **500px（0.50 屏）** | **−627px · −56%** |
| `W9` 专题正文起点（板顶起算） | 673px | **497px** | −176px |
| `W10` 专题正文起点（板顶起算） | 692px | **504px** | −188px |
| `W9` 专题正文起点（页面顶端起算） | 1108px | **932px** | **正文进入第一屏** |
| `W10` 专题正文起点（页面顶端起算） | 1127px | **939px** | **正文进入第一屏** |

§5.1.1 的预测是「`W9` 每专题省约 0.9 屏、正文起点提前到约 470px」。实测 **0.86 屏 / 497px**——
省的量对得上，**正文起点比预测差 27px**（板头 123 + 专题条 266 加上间距就是地板，图例收到 24px 已经到底）。

**专题正文区高度前后一致**（`#w9-topic-panel` 1601px 不变），也就是说这一步**只削了外壳，没碰内容**——
§5.1 那张各专题屏数表不受影响，仍然有效。

**验收**：`yarn typecheck` 过；`yarn verify:board` **421/421，与 8/20 基线同数**；
375px 窄屏无横向溢出、`summary` 触控 54 / 35px（≥24px）；深浅色各核一次，摘要行两色下均可读。
**未部署**——服务器 8081 仍是旧版，发布是独立授权动作。

**一处连带改动（在白名单内，且是收紧不是放宽）**：`scripts/verify-w9-board.mjs` 的 `revealAll()`
现在也会展开所有 `details`，与同文件 `goTab()` 早就在做的事一致（原注释写的就是「让折叠内容也进入采样」）。
不这样改，收起态里的档位标签 `innerText` 读出来是空串，会让「只用三档」这条断言**假红**。
断言条数不变，**采样面反而变大**——此前 `revealAll()` 路径下没被采到的 `w9-glossary` 术语表，现在也进采样了。

**下一步是 §5.1.1 的第 2 步**（`W9` `evidence` 与 `W10` ⑤，参照系 `W10` ④），**尚未开始**。

### 5.1.3 阶段二 · 第 2 步（上半）已执行：`W10` ⑤ 演练分档与定位（2026-08-21）

第 2 步原本写作「`W9` `evidence` 与 `W10` ⑤」两块。**开工前先各量了一次默认态，顺序因此对调**：

| | 读者实际会看到的状态 | 屏数 |
|---|---|---|
| `W9` `evidence` | 复习态默认（十七条认知修正收起） | 4.27 屏 |
| **`W10` ⑤** | **点开专题闸门后全部摊开** | **5.91 屏** |

`W10` ⑤ 才是读者真正遇到的最重一块，且它的参照系 `W10` ④（编码 83% / 散文 178 字）**就在同一个文件里**。
**本轮只做 ⑤**（§6 一次只做一块），`W9` `evidence` 留给下一轮。

**先看清它为什么是 5.91 屏**——逐块量下来，问题不是「缺图」，是**一块里堆了四个问题**：

| 子块 | 高 | 散文中文字 |
|---|---|---|
| 三张演练卡 `w10-drill-list` | **2011px** | **1122** |
| 九格判决 `w10-signals` | 926px | 512 |
| 四 check 表态矩阵 `w10-cvs` | 644px | 278 |
| 残留核零 `w10-closeout` | 535px | 282 |
| 三个盲区 `w10-blinds` | 520px | 600 |
| 两条工具纠错 `w10-drill-corrections` | 428px | 283 |

**改了什么**（内容一字未删，只改层级）：

1. **三张演练卡**：主路径——**档位 · 定位链（注入 / 首查 / 它把范围劈成）· 预测 ↔ 实测那根连线**——保持常驻，
   因为这三层就是本块验收句「故障真来了，它会不会红」的答案。
   **收进渐进层级的是原始证据与过程收尾**：可复核的那一行、偏差归在哪、根因分三层、恢复与收口。
   摘要按每张卡实际有什么动态生成（有的卡没有偏差或根因），不是一句死标签。
   > 这一刀正好落在 roadmap 划的界上：「实验的环境、样本量、命令、原始测量与限定语」**本来就该是文字**，
   > 不在表达形态返工的范围里——所以对它们**只折不改写**，一个字都没动。
2. **两条工具纠错**与**残留核零**两整节收起，各自的 `h3` 结论句留作一行摘要（同第 1 步的 `board-fold` 做法）。
   它们回答的是「哪些工具认知被撞翻了」「机器有没有恢复干净」——都不是本块的验收句。

**结果（实测）**：

| | 改前 | 改后 | |
|---|---|---|---|
| ⑤ 专题正文（读者态） | 5905px（5.91 屏） | **4067px（4.07 屏）** | **−1838px · −31%** |
| 单张演练卡 | 631 / 678 / 678px | **335 / 335 / 335px** | **−49%** |
| 全部展开后 | 5905px | **5941px** | +36px（多出来的是摘要行本身） |

**最后一行是这次最要紧的一行**：展开后比改前还多 36px，说明**内容一字未删**——省下的 1838px 全部来自层级，
不是来自删东西。

**验收**：`yarn typecheck` 过；`yarn verify:board` **421/421，与基线同数**；375px 与 1280px、深浅色各核一次，
`summary` 无一低于 24px；窄屏下卡片塌成单列、「断了两根」的连线转成竖向标记仍然看得见。**未部署。**

> 窄屏自查时我那套临时脚本报 `.w10-tier-grid` 横向溢出——**是误报**：
> `styles.css` 手机档写着 `.w10-tiers { overflow-x: auto }` + `.w10-tier-grid { min-width: 480px }`，
> 宽矩阵横滚是这个仓库既有的做法，且与本次改动无关。以 `verify:board` 自己的溢出断言为准（过）。

**排序的后半段随之更新**：⑤ 降到 4.07 屏之后，**全站最重的单个专题变成了 `W6Day4Board` 的 5.12 屏**
（它在展示态默认全摊开），其次是 `W10` ⑦ 4.35 屏与 `W9` `evidence` 4.27 屏。
原定的第 3 步（`W6Day4Board`）因此仍然排在 `W9` `evidence` 之前——**这个先后是量出来的，不是猜的**。

**下一轮**：第 2 步的下半，`W9` `evidence`（4.27 屏，其中十七条认知修正即使收起仍占 2098px）。**尚未开始。**

### 5.1.4 阶段二 · 第 3 步已执行：`W6Day4Board`（2026-08-21）

⑤ 降到 4.07 屏之后，全站最重的单个专题是 `W6Day4Board` 的 **5.12 屏**——按本线一直在用的
「先后是量出来的」，这一轮做它。（它同时就是原定的第 3 步，顺序没有临时改。）

**逐块量下来，问题同样是「一块里堆了几个问题」**（展示态，读者默认就是这个状态）：

| 子块 | 高 | 散文中文字 |
|---|---|---|
| **九条纠错 `d4-corrections`** | **1735px（全块 34%）** | 479 |
| 十层交付物 `d4-layers` | 639px | 243 |
| 三组受控实验 `d4-evidence` | 510px | 204 |
| 四周因果主线 `d4-mainline` | 401px | 179 |
| 能力归属与还债 `d4-debt` | 395px | 176 |
| 六件没证明 `d4-limits` | 356px | 316 |
| 最终验证 `d4-verify` | 283px | 89 |
| 四个收束维度 `d4-frame` | 235px | 200 |

这块板的验收句写在它自己的板头上：**「四周收束成一条主线，并说清没有证明什么」**。
照这句话切——`frame` / `mainline` / `layers` / `evidence` 是「收束成一条主线」，
`limits` 是「说清没有证明什么」，**这五块全部保持常驻**；
`corrections`（哪些说法被推翻）、`debt`（能力归属）、`verify`（当天运行结果）回答的是另外三个问题，收进渐进层级。

**九条纠错这一节要单独说，因为它牵着一条早先记下的纪律。** §5.2 补正二写过：
复习态逐条先只给初始说法、点开才给结论，**是主动回忆的有意设计，阶段二不许一起「打开」**。
所以这一节做成**按视角给不同默认值的受控 `details`**：

- **展示态收起**（读者默认）——1735px 变成一行摘要，摘要就是那句结论「九条初始说法被推翻，留下的是修正后的版本」。
- **复习态保持展开**——里面逐条的回忆设计一个字没动，也不多套一层点击。

> 顺带排除一个看起来更省事的做法：**展示态不能改成「逐条也收起」**。收起态每条只显示「❌ 初始说法」，
> 九条一并收起等于把九句**已经被推翻的话**并排摆出来当内容——那不是减负，是误导。

**结果（实测）**：

| | 改前 | 改后 | |
|---|---|---|---|
| 展示态 · 整块 `.w6-board` | 5537px | **3184px** | **−2353px · −42%** |
| 展示态 · Day 4 自身正文 | 5125px（5.12 屏） | **2772px（2.77 屏）** | −46% |
| 复习态 · 默认 | ≈5305px | **4675px** | 回忆设计未动，另省 `debt` + `verify` |
| 全部展开后 | 5537px | **5432px** | −105px |

**最后一行比前两步多一句解释**：这次全展开后比改前**少** 105px，不是多。原因是三处小标题由 `h3`
变成 `summary` 里的 `strong`，字号小一档——**文本一个字都没少**：逐块字数比对
`corrections` 479 / `debt` 176 / `verify` 89，与改前**完全一致**。

**验收**：`yarn typecheck` 过；`yarn verify:board` **421/421，与基线同数**；
1280px / 375px、深浅色各核一次，无横向溢出、`summary` 无一低于 24px。**未部署。**

**剩下的排序（都按实测重排过）**：

| 下一批 | 屏数 | 在不在冻结范围里 |
|---|---|---|
| `W10` ⑦ 红过才算数 | **4.35 屏** | **不在**——它是四步排序之外新浮上来的第一名 |
| `W9` `evidence` | 4.27 屏 | 在（第 2 步下半） |
| `rollback` / `exposure` / `W5` 知识点 4 / 知识点 6 | 0.89–1.30 屏 | 在（第 4 步，问题是首屏 0% 编码而不是长） |

> **`W10` ⑦ 要不要纳入，请本人定。** 它在 §5.1.1 的表里就排第三（4346px · 编码 40% · 散文 1653 字），
> 但四步排序当时只点名了 ⑤ 和 `evidence`，它一直没被写进冻结范围。
> 按纪律**不自行扩范围**，所以下一轮仍走第 2 步下半的 `W9` `evidence`。

**另外，这块板在视觉编码那一维仍欠着**：`EvidenceLayers` 里 W3 / W5 两组实验的量级差
（`15→0`、`12ms→3ms`、`1154ms→102ms`、`378ms→2–3ms`）只写成数字，判定是 2 个单元。
**本轮没碰它**——那是另一维的欠项，混在同一次改动里会让 diff 难复核。

### 5.1.5 阶段二 · 第 2 步（下半）已执行：`W9` `evidence` 契约销账块（2026-08-21）

本人指示「`W10` ⑦ 先等一下」，故**⑦ 仍不进范围**，本轮走冻结范围里的第 2 步下半。

这块板的问题和前两块同型——**一块里堆了四个问题**（复习态默认，`deploy` 是 `reviewOnly` 板，读者只有这一种状态）：

| 子块 | 高 | 散文中文字 |
|---|---|---|
| **认知修正 `w9-fix`** | **2098px（全块 49%）** | 747 |
| 契约销账轨道 `w9-settle-track` | 692px | 495 |
| 生产对照 `w9-parity` | 508px | 478 |
| 内存闸门 `w9-mem` | 406px | 141 |
| 契约表之外四笔 `w9-offbook` | 399px | 393 |

这块的验收句是它在 `W9_STAGE_PLAN` 里挂的那一问：**「还欠什么」**，标题是「契约销账与资源闸门」。
照这个切——**销账轨道**（12 条按天销掉、仍欠那一列是空的）、**表外新长出来的四笔**、
**内存闸门**（「资源闸门」写在标题里）三块保持常驻；
**生产对照**（对照真实生产还缺什么）与**认知修正**（哪些说法被推翻）是另外两个问题，收进渐进层级。

**结果（实测）**：

| | 改前 | 改后 | |
|---|---|---|---|
| 复习态默认（读者唯一会见到的状态） | 4268px（4.27 屏） | **1712px（1.71 屏）** | **−2556px · −60%** |
| 全部展开后 | 6077px | **6017px** | −60px |
| `parity` / `fix` 两节字数 | 478 / 747 | **478 / 747** | 一字未删 |

**−60% 是四轮里最大的一次**，也是唯一一次把一块从「四屏以上」直接压到两屏以内。

**但这一轮有一个前三轮没有的代价，必须写下来**：

> **`deploy` 只有复习态**，所以收起 17 条认知修正**确实多了一次点击**——
> 这和第 3 步 `W6Day4Board` 的处理**不一样**。那块板展示态与复习态并存，
> 我把折叠只加在展示态、复习态保持展开，所以「主动回忆」那条路径一次点击都没多。
> 这块板没有这个余地：不折就一点省不下（`fix` 占全块 49%），折了就多一次点击。
> **我选了折**，理由是这块的验收句是「还欠什么」，17 条认知修正不回答它。
> **这是个取舍不是定论。** 若认为「最值得复习的那张表不该藏在一次点击后面」，
> 反悔的成本极低——把 `w9-fix` 那一处 `<details className="w9-fix board-fold">` 改回 `<div className="w9-fix">`
> 即可，块高会回到 3814px（3.81 屏，仍比改前省 454px，因为生产对照那一节的折叠不受影响）。

**验收**：`yarn typecheck` 过；`yarn verify:board` **421/421，与基线同数**；
1280px / 375px、深浅色各核一次，`summary` 无一低于 24px。**未部署。**

> 窄屏自查脚本报 `.w9-settle-col` 横向溢出，**又是那个误报**：`.w9-settle-track { overflow-x: auto }`
> 是这条五列轨道本来就有的横滚设计（同 `W10` 的 `.w10-tiers`），与本次改动无关。

**验收脚本第三次同类补正**：`goTopic()` 现在也展开所有 `details`——与 `goTab()`、`revealAll()` 一致。
不改则「生产对照 鉴权进已做」这条断言读不到收起态的正文而**假红**（本轮真的红过一次，见提交历史）。
三个入口现在口径统一，断言条数始终 421。

**四轮下来的账**：

| 块 | 改前 | 改后 | |
|---|---|---|---|
| `W9` `evidence` | 4.27 屏 | **1.71 屏** | −60% |
| `W6Day4Board` | 5.12 屏 | **2.77 屏** | −46% |
| `W10` ⑤ | 5.91 屏 | **4.07 屏** | −31% |
| `W9` / `W10` 常驻外壳 | 1.34 / 1.13 屏 | **0.48 / 0.50 屏** | −64% / −56% |

**剩下的**：`W10` ⑦ 4.35 屏（**本人指示先等**）、`W10` ⑤ 4.07 屏（已改过一轮，未再动）、
第 4 步四个首屏 0% 编码的小专题（`rollback` 1.30 / `W5` 知识点 4 1.29 / 知识点 6 1.12 / `exposure` 0.89 屏，
问题是**打开先撞文字**而不是长）。下一轮走第 4 步。

### 5.1.6 `W10` ⑦「红过才算数」已执行（2026-08-21，解冻后补做）

本人 8/21 先说「⑦ 先等一下」，W10 整周收口完成后解冻。**开工前先确认了一件事**：
main 那次收口翻了 W10 的数据（`THRESHOLD_RULERS` 的磁盘 caveat、`DRILLS` 的类 2 `rootCause`、
两条 `BLIND_SPOTS`），**三处全在 ④ 和 ⑤，没有一处落在 ⑦**——所以 ⑦ 的事实不受这次翻档影响，可以直接按原判定动。

**逐块量**（4346px / 散文 2721 字）：

| 子块 | 高 | 散文中文字 |
|---|---|---|
| 五条红态证据链 `w10-chain` | 1135px | 555 |
| 弄红作用在哪一环 `w10-levers` | 949px | 648 |
| 四个检查的频率与身份 `w10-units` | 618px | 401 |
| 这一天踩出的五条工具行为 `w10-gotchas` | 599px | 416 |
| 静默常绿 `w10-selfwatch` | 475px | 426 |
| 报红文案 `w10-alertfmt` | 298px | 193 |

**这一轮我故意折得比能折的少，理由要写下来。** ⑦ 的验收句是**「一个检查凭什么可信」**，
而这块板自己给的答案有**两半**：

1. **它红过**——五条完整的「绿 → 弄红 → 报红 → 还原 → 绿」（`chain`），
   加上「弄红作用在链条的哪一环」（`levers`）。后者决定那条红的证明力：
   杠杆作用在判据上而不是真实条件上，红了也不算数——这正是 W10 那条「假输入能红 ≠ 真条件该红」。
2. **它现在还在跑**——`selfwatch` 那一节讲的是**静默常绿**：timer 没排程、脚本自己挂了，
   看到的是一片安静，和「一切正常」长得一模一样。

**按篇幅算，`selfwatch` 475px 是很划算的折叠对象；按验收句算，折掉它等于把答案砍掉一半。**
所以它保持常驻。收进渐进层级的只有三节：`units`（频率与身份，是配置事实）、
`alertfmt`（报红输出格式）、`gotchas`（当天踩坑）——这三节回答的都是别的问题。

**结果（实测）**：

| | 改前 | 改后 | |
|---|---|---|---|
| ⑦ 专题正文（读者态） | 4346px（4.35 屏） | **3000px（3.00 屏）** | **−1346px · −31%** |
| 全部展开后 | 4346px | **4261px** | −85px（三处标题字号） |
| `units` / `alertfmt` / `gotchas` 字数 | 401 / 193 / 416 | **401 / 193 / 416** | 一字未删 |

**验收**：`yarn typecheck` 过；`yarn verify:board` **421/421**；1280px / 375px、深浅色各核一次，
`summary` 无一低于 24px。窄屏报 `.w10-chain` 溢出仍是那个老误报（`.w10-chain { overflow-x: auto }`
是五列证据链本来的横滚设计）。**未部署。**

**五轮下来的账**：

| 块 | 改前 | 改后 | |
|---|---|---|---|
| `W9` `evidence` | 4.27 屏 | **1.71 屏** | −60% |
| `W6Day4Board` | 5.12 屏 | **2.77 屏** | −46% |
| `W10` ⑤ | 5.91 屏 | **4.07 屏** | −31% |
| `W10` ⑦ | 4.35 屏 | **3.00 屏** | −31% |
| `W9` / `W10` 常驻外壳 | 1.34 / 1.13 屏 | **0.48 / 0.50 屏** | −64% / −56% |

**一件新浮上来的事，留给本人定**：⑦ 降到 3.00 屏之后，**`W10` ⑤ 的 4.07 屏又成了全站最重**。
⑤ 已经做过一轮（5.91→4.07），它剩下的重量集中在 `w10-signals` 九格判决（926px）
与三张演练卡的常驻主路径上——**要不要对 ⑤ 做第二轮，是一个新问题，不在原来那四步里**，
本轮不自行扩范围。

**下一轮**：第 4 步，四个首屏 0% 编码的小专题（`rollback` 1.30 / `W5` 知识点 4 1.29 /
知识点 6 1.12 / `exposure` 0.89 屏）——它们的问题是**打开先撞文字**，不是长，改法与前五轮不同。

### 5.2 视觉编码那一维：另外两块的判定与三处补正

8/21 的六行只覆盖到 `W6Day4Board`，`W10Board` 未进入、「面试准备」的纳入判定仍挂着。补上：

- **8 · 可观测性 `W10Board`（视觉编码）：可答，无欠项。** 三张 `w10-matrix`、`w10-rulers` 阈值尺
  （红线 / 余量 / 当前值 / 出事点落在同一根轨道上）、`w10-corridor` 三道绿灯 × 四条实例、
  `w10-chain` 五条红态证据链、`w10-tier-grid` 三档。四条肯定面都不破。
  **但见 §5.1 第 1 条：它的阅读负担是全站最重的**——两维结论相反，这正是判据补正想区分开的情况。
- **（候选）面试准备 `InterviewBoard`：判定不纳入。** `iv-matrix`（学习周 × 面试稿章节 + 状态，
  `role="table"`）已经把它的验收句编码成矩阵；它的 `iv-metrics` 与 `W6Day4Board` 读同一份
  `evidenceSets.ts`，那处的量出了图这块跟着受益；实测 0.98 屏，也没有减负需求。
- **补正一：`W6Board` 的证据精度矩阵是常驻折叠的。** 全板唯一一张「测试 / 观察结果 × 能证明 ×
  没有证明」矩阵（`w6-matrix-table`）包在 `<details>` 里且**没有 `open` 属性**，展示态和复习态都默认收起。
  §3 第 4 条要的是「不读正文就能读到的锚」，**要点开才看得到的锚不满足这一条**。
  这一处独立于已记的「Day 3 三条结果路径要切换比较」，是同一块板上的第二处，改法也更便宜。
- **补正二：`W6Day4Board` 的九条纠错折叠不是欠项。** `Corrections` 在复习态默认全收起、展示态全展开
  （`setOpenIds(review ? [] : 全开)`），这是主动回忆的有意设计，**与上一条的常驻 `<details>` 性质不同**，
  不要在阶段二一起「打开」。
- **补正三（与已记结论不一致，留裁决）：`OAuth2Panel` 的凭据卡可能破第 1 条。**
  已记「四条肯定面均通过、无欠项」。但 `oauth-creds` 是并排五张卡，每张只有 `code` + 一句
  `boundary` 文字；「`client_secret` 只在后信道、绝不过浏览器」这条验收句要么读文字、
  要么播到第 4 步看高亮，**五个凭据用来比较的那一维（属哪条信道 / 过不过浏览器）没有被编码**。
  数据是现成的（`OAUTH_STEPS[].creds` 与 `OAUTH_CREDS` 同一套 key，交叉即得矩阵）。
  **两种判法都说得通**：说它通过，是因为六步时序那张全览已经承载了主结论、凭据只是附注；
  说它不通过，是因为凭据边界本身就是这块板的验收句之一。**按谁是验收句来裁——这一条留给本人。**
  实测 0.73 屏也说明：即便要改，它是全站最便宜的一块。

### 5.3 跨块的两条共性（比逐块返工便宜）

逐行判定之外，八行合起来能看出两条横贯多块的欠口——**一次做完覆盖面大于逐块修**：

1. **互斥切换**：`AuthBoard` 三条结果路径、`W6Board` Day 3 三条轨道（两条已记在案），
   加上 `W3Board` / `W5Board` / `W6Day4Board` 的知识点与实验组 `nav`，都是「同类对象一次只显示一个，
   要比较就得来回切换并依赖记忆」。这正是第 1 条肯定面要防的事。
   做法上它们同型：给这类切换补一条**把被切开的对象按位置摆在一处的全览**，而不是逐块重新设计。
2. **量的差距只写成数字**：`W3Board` 的 `ExplainVisual` 与 `W6Day4Board` 的 `EvidenceLayers`
   （两条已记在案）读的是同型数据，而 `HBarChart` 已经存在、`W9Board` 已经在用——**属复用不属新造**。
   面试板共用 `evidenceSets.ts`，跟着受益。

**阶段一完成后在这里写一段总结**：八块里几块欠着、总规模、建议档位 → 回写 `BACKLOG.md` P2-4。

---

## 6. 启动、止步与验证

**什么时候可以推进这条线**（满足其一即可，不需要专门排期）：

- 当天学习主线已达到止步条件，且还有一个 15–20 分钟的空档；
- 周复盘后有余量；
- 或者本人明确说「今天推展板核查线」。

**止步条件**：

1. **当天主线未收口** → 本线零进展（不算欠账）。
2. **一次只做一块** → 一个单元结束就停，不连做三块「反正手热」。
3. **阶段一不改代码** → 见 §4。
4. **改动只走白名单最小范围** → 展示资产可由 AI 实现，但不顺带重构无关组件（`AGENTS.md` §3 末段）。
5. **任何改动后 `yarn typecheck` + `yarn verify:board` 必须全过**（8/20 基线 421/421），窄屏与深浅色各核一次。

**不部署**：发布目标是服务器 8081，且**发布是独立授权动作**（`SHOWCASE-DEPLOY-PROTOCOL.md`）；
Pages 上传处于冻结状态，本线不解冻、不上传。

---

## 7. 与学习主线的接口

- **W10 D5（8/21 收口日）只交维度 ①**：W10 板七块按最新数据核一遍、不一致的当场翻档、`verify:board` 保持全过。
  维度 ② **不在 D5 做**（原计划的「抽样 3 块定性」已移交本线）。
- **W11 起**：本线按 §6 的空档推进，不进每日计划的时间盒，不写进当周主线。
- **`LEARNING-STATE.md`**：只保留一行指针（当前进展 X/8），不复制本文件内容。

---

## 8. 退出条件

阶段一八行判定填满，并且阶段二的「改哪几块」有结论且执行完毕 → 本线关闭，
在 roadmap 追加一节收口记录，`BACKLOG.md` P2-4 标完成。
**若阶段一的结论是「都不值得改」，同样是关闭**——判定本身就是产出，不是必须改点什么才算数。

---

## 9. 变更记录

- **2026-08-20 建立**。起因：本人指出「不要过分追求动效」的波及面比 8/18 认为的更广，
  大段文字展示不是 W9/W10 的局部问题，这两周只是集中爆发处（证据三条见 roadmap 第八轮补正）。
  当天本人进一步决定：**核查单开一条线，不与学习同步开展**——遂有本文件。
- **2026-08-20 范围修正**：首次列范围时把 admin 的 `Dashboard.tsx` 误算成展板（它属 8080 管理后台），
  已剔除；同时补入此前漏掉的 `OAuth2Panel.tsx`。核查对象由「九块板」更正为**八块板**（另有「面试准备」待判定是否纳入）。
- **2026-08-21 元数据纠正**：核查 `OAuth2Panel.tsx` 时以 `git log -1` 复核，组件末次改动实际为
  2026-07-29，不是表中原记的 2026-08-12；两者都早于 8/18 新口径，故不改变「旧口径成品」分类。
- **2026-08-21 元数据纠正（续）**：`W3Board.tsx` / `w3Topics.ts` 的末次改动同样由 `git log -1`
  纠正为 2026-07-29；仍早于 8/18，不改变分类。
- **2026-08-21 元数据纠正（再续）**：`W6Board.tsx` 的末次改动经 `git log -1` 复核为
  2026-07-30，不是原记的 2026-08-12；仍早于 8/18，不改变分类。
- **2026-08-21 元数据纠正（Day 4 子板）**：`W6Day4Board.tsx` / `w6Day4.ts` 的末次改动经
  `git log -1` 复核同为 2026-07-30，不是原记的 2026-08-12；仍早于 8/18，不改变分类。
- **2026-08-21 阅读负担实测补齐**：把展板 `build:showcase` 后跑起来，用 Chromium 1280×1000 逐专题量
  正文区高度，覆盖**八块板 + 面试板全部 45 个专题**。方法先拿 W9 已记的七个数字对照验证：
  **六格差 ≤ 0.08 屏、「11/13 超过一屏」完全复现**；唯一大差是 `evidence`——把内部闸门全部点开是
  6.08 屏而非 4.2 屏，故该格量级往上修。**零代码改动、零部署**，门禁只在本地 `localStorage` 塞占位值。
  三条新结论：① `W10Board` 阅读负担全站最重（均 2.88 屏、7/7 超一屏，高于 W9 的 2.03 与 11/13），
  「按新判据建」只对第一问成立，**它必须进阅读负担这一维**；② `W6Day4Board` 5.12 屏是全站第三重，
  原判的 2 单元只覆盖了数值呈现没覆盖减负；③ `W3Board` 0.74 / `OAuth2Panel` 0.73 / `AuthBoard` 0.97 /
  面试板 0.98 实测确认无阅读负担欠项，这几块的「待补核」可以销掉。详见 §5.1。
- **2026-08-21 `W10` ⑦ 解冻并执行（§5.1.6）**。开工前确认 main 那次 W10 收口翻的三处数据
  （④ 的磁盘 caveat、⑤ 的类 2 `rootCause`、两条盲区）**没有一处落在 ⑦**，事实不受影响。
  **本轮故意折得比能折的少**：⑦ 的验收句「一个检查凭什么可信」有两半——「它红过」（五条证据链 + 弄红作用在哪一环）
  与「它现在还在跑」（静默常绿）。`selfwatch` 475px 按篇幅是划算的折叠对象，按验收句折掉它等于砍掉一半答案，
  故保持常驻；只折 `units` / `alertfmt` / `gotchas` 三节。
  实测 4346→**3000px（−31%）**，全展开 4261px（少 85px，三处标题字号），三节字数 401 / 193 / 416 与改前一致。
  `verify:board` 421/421、深浅色与窄屏各核一次、未部署。
  **新浮上来一件留给本人定的事**：⑦ 降下来后 `W10` ⑤ 的 4.07 屏又是全站最重，
  而 ⑤ 已做过一轮——**要不要对它做第二轮不在原四步里，本轮不自行扩范围**。下一轮走第 4 步。
- **2026-08-21 阶段二第 2 步（下半）：`W9` `evidence`（§5.1.5）**。本人指示 `W10` ⑦ 先等，故走冻结范围内的这一块。
  按验收句「还欠什么」切：销账轨道 / 表外四笔 / 内存闸门常驻，生产对照与认知修正收进渐进层级。
  实测复习态 4268→**1712px（−60%，四轮最大）**；全展开 6017px 比改前少 60px（三处标题字号），
  `parity` 478 字 / `fix` 747 字与改前一致。`verify:board` 421/421、深浅色与窄屏各核一次、未部署。
  **代价要记一笔**：`deploy` 只有复习态，收起 17 条认知修正**多了一次点击**——
  与第 3 步不同（那块板复习态保持展开，回忆路径零额外点击）。这块没这个余地：
  不折省不下（`fix` 占 49%），折了就多一次。选了折，因为验收句是「还欠什么」而它不回答这一问；
  **这是取舍不是定论，反悔只需把那一处 `details` 改回 `div`**，块高回到 3814px。
  验收脚本第三次同类补正：`goTopic()` 也展开 `details`（同 `goTab` / `revealAll`），
  不改则「生产对照 鉴权进已做」假红——本轮真红过一次。三个入口口径统一，断言恒为 421 条。
- **2026-08-21 阶段二第 3 步：`W6Day4Board`（§5.1.4）**。⑤ 降下来后它是全站最重（5.12 屏），
  按「先后是量出来的」这一轮做它。按板头那句验收句切：`frame` / `mainline` / `layers` / `evidence` /
  `limits` 五块常驻，`corrections` / `debt` / `verify` 收进渐进层级。
  **九条纠错做成按视角给不同默认值的受控 `details`**：展示态收起、**复习态保持展开**——
  §5.2 补正二记的「复习态逐条回忆是有意设计、不许一起打开」因此一个字没动。
  （排除过「展示态逐条也收起」：收起态每条只显示「❌ 初始说法」，九条并排等于把九句已被推翻的话当内容。）
  实测展示态 5537→**3184px（−42%）**，Day 4 正文 5125→**2772px**；全展开 5432px，比改前少 105px，
  差值来自三处 `h3`→`summary strong` 字号，**逐块字数 479 / 176 / 89 与改前完全一致**。
  `verify:board` 421/421、深浅色与窄屏各核一次、未部署。
  剩余按实测排：`W10` ⑦ 4.35 屏（**不在冻结的四步范围内，是否纳入待本人定**）、`W9` `evidence` 4.27 屏
  （第 2 步下半，下一轮走这个）、四个首屏 0% 编码的小专题（第 4 步）。
  本轮**未碰**这块板视觉编码那一维的「量欠图」欠项（2 单元），两维混在一次 diff 里不好复核。
- **2026-08-21 阶段二第 2 步（上半）：`W10` ⑤（§5.1.3）**。开工前各量一次默认态，**顺序对调**——
  `W9` `evidence` 复习态默认 4.27 屏，`W10` ⑤ 点开闸门后 5.91 屏全摊开，后者才是读者遇到的最重一块。
  改法：三张演练卡的**主路径（档位 · 定位链 · 预测↔实测连线）保持常驻**，把原始证据 / 偏差 / 根因 /
  恢复收口收进渐进层级（roadmap 已把这类内容划在表达形态返工范围之外，故**只折不改写**）；
  「两条工具纠错」与「残留核零」两整节收起，`h3` 结论留作摘要。
  实测 5905→**4067px（−31%）**，单卡 678→**335px（−49%）**，而**全部展开后是 5941px（比改前多 36px）
  ——内容一字未删**。`verify:board` 421/421、深浅色与窄屏各核一次、未部署。
  连带更正 §5.1 里写错的一句：`evidence` 的量级**不往上修**，4.2 屏原记录是对的（错在把全展开当成读者态）。
  ⑤ 降下来之后全站最重变成 `W6Day4Board` 5.12 屏，原定第 3 步顺序不变。第 2 步下半（`W9` `evidence`）未开始。
- **2026-08-21 阶段二开始，执行第 1 步（§5.1.2）**：本人指令「按建议的顺序开始下一步」，
  执行范围冻结为 §5.1.1 的四步排序，本次**只做第 1 步**。`W9` / `W10` 的分档图例与建构进度
  改为默认收起的 `<details>`，结论句移进 `<summary>` 作一行摘要，内容一字未删、专题正文一行未动。
  实测：常驻外壳 `W9` 1343→**485px**（−64%）、`W10` 1127→**500px**（−56%）；
  专题正文起点（页面顶端起算）1108→**932px** / 1127→**939px**，**正文进入第一屏**。
  `typecheck` 过、`verify:board` **421/421 与基线同数**、375px 无溢出、深浅色各核一次、**未部署**。
  连带把 `verify-w9-board.mjs` 的 `revealAll()` 补成也展开 `details`（同文件 `goTab()` 既有做法），
  否则收起态的档位标签会让断言假红；断言条数不变，采样面变大。第 2–4 步未开始。
- **2026-08-21 主路径与重复量上量（§5.1.1）**：同一套实测方法再往里一层。
  ① **主路径**：`W9` / `W10` 的专题正文起点在板顶下方 673 / 692px（页面顶端下方 1108 / 1127px），
  **第一屏里没有正文**；另有四个专题（`rollback`、`exposure`、`W5` 知识点 4 / 6）首屏编码占比为 **0%**。
  ② **重复量口径澄清**：逐块指纹比对确认**专题正文区内部没有跨专题重复**，8/21 记的「重复出现图例、
  词表与路线图」实际是**常驻外壳**——每切一次专题重新承担一次，`W9` 1343px（1.34 屏）、`W10` 1127px、
  `W6` 412px；其中路线图是两块板上最大的单个常驻块（707 / 492px），且内容是「本板共几块、落地几块」
  的建构进度，现已稳定显示全部落地。③ **编码 / 散文分解**：`W9` `evidence` 编码占比 12% / 散文 2661 字、
  `W10` ⑤ 17% / 2509 字，而**同一块板**的 `W10` ④ 是 **83% / 178 字**——⑤⑦ 不必新设计，向 ④ 看齐即可。
  据此给出阶段二排序建议（先动常驻外壳，`W9` 每专题省约 0.9 屏且零内容风险 → 再动那两个五六屏的专题
  → `W6Day4` → 首屏 0% 编码的四个小专题），**仅为建议，未执行、未改代码**。
- **2026-08-21 视觉编码补完 + 三处补正**：补上 `W10Board`（可答，无欠项）与「面试准备」的纳入判定（不纳入）；
  补正 `W6Board` 的证据精度矩阵是**常驻折叠**（`<details>` 无 `open`，要点开的锚不满足第 4 条）、
  `W6Day4Board` 的纠错折叠是**主动回忆的有意设计不算欠项**、`OAuth2Panel` 凭据卡是否破第 1 条**留本人裁决**。详见 §5.2。
- **2026-08-21 判据纠正**：本人不接受 `W9Board`“有视觉锚 = 无欠项”的判定，并指出原始问题可能是
  文本过密、过度精炼或呈现出“AI 感”。复核 roadmap 第八轮后确认异议成立：这条线的起因本来就是
  “每块板都充斥巨量文字”，四条肯定面只检查结论有没有被版面承载，没有完整检查承载结论后的阅读负担。
  因此维度②补入“主路径 / 重复量 / 文案可读性”三问，撤回 W9 的“无欠项”；此前六块保留视觉编码结论，
  但阅读负担改为待补核。这里的“AI 感”只记录可观察的统一压缩句式，不推断文本实际来源。
