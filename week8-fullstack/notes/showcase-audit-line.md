# 展板状态核查线（独立线，不与学习主线同步开展）

> 建立：2026-08-20（Asia/Shanghai）。**本人决定单开一条线**——展板核查不必跟着每日学习主线走。
> 上游：[`visualization-optimization-roadmap.md`](./visualization-optimization-roadmap.md) 第八轮「落地范围的补正」（发现与证据）
> 优先级入口：根 [`BACKLOG.md`](../../BACKLOG.md) **P2-4**（本线是它的执行文档；档位仍以 BACKLOG 为准）
> 边界：`week8-fullstack/src/frontend/` 白名单展示资产（`AGENTS.md` §2）；不改后端学习代码，不部署。

---

## 1. 这条线是什么，不是什么

**是什么**：一条**按块推进、可随时暂停**的核查队列。每次消化一块板，做完记一行，下次从下一块继续。
它的节奏由**余量**决定，不由学习日程决定——这正是「单开一条线」的意思。

**不是什么**（三条，缺一条这条线就会开始和学习抢时间）：

1. **不是新增的并行线。** 2026-08-17 已拍板：W10 的并行线只有**英语 + 项目叙述**（`week10-plan.md` §5）。
   本线不进这个名单，也不占每日主线的时间盒。
2. **不是每日必做项。** 按 `LEARNING-PROTOCOL.md` §7 的固定优先级，它属「上周遗留实验与优化」那一档——
   **只有当天主线达到止步条件后才允许进入**。当天主线没收口，这条线就该是零进展，且零进展不算欠账。
3. **不是收口日的活。** 收口日（每周 D5）本来就满，把八块板的判定塞进去，
   结果只会是「凭印象铺开改」——那恰好和这条线要修的失效同型。

**一句话**：**它排队，不排期。**

---

## 2. 范围：八块板（阶段一 2026-08-20 全部判完，结论见 §5）

| # | tab | 组件 | 组件末次改动 | 建成时的口径 | 维度① 数据时效 | 维度② 表达形态 |
|---|---|---|---|---|---|---|
| 1 | 认证与授权 | `AuthBoard.tsx` | 2026-08-12 | 旧（图形只负责关系） | 待核 | **已判定**（上半达标 / 下半欠） |
| 2 | OAuth2 流程 | `OAuth2Panel.tsx` | 2026-08-12 | 旧 | 待核 | **已判定**（流程达标 / 凭据欠） |
| 3 | 数据库聚合 | `W3Board.tsx` | 2026-08-12 | 旧 | 待核 | **已判定**（量欠图） |
| 4 | Node.js 运行时 | `W5Board.tsx` | 2026-08-12 | 旧 | 待核 | **已判定 · 达标**（四条全不破） |
| 5 | 测试闭环 | `W6Board.tsx` | 2026-08-12 | 旧 | 待核 | **已判定**（矩阵常驻折叠，欠锚） |
| 6 | 测试闭环（Day 4 子板） | `W6Day4Board.tsx` | 2026-08-12 | 旧 | 待核 | **已判定**（量欠图） |
| 7 | 部署上线 | `W9Board.tsx` | 2026-08-18（仅两张 `HBarChart` 按新口径） | 旧为主 | 待核 | **已判定**（分块不一，欠板级锚；规模最大） |
| 8 | 可观测性 | `W10Board.tsx` | 2026-08-20 | **新**（全程按加严判据） | **优先**（⑤ 那四格「未实测」） | **已判定 · 达标**（② 不必改） |

**明确不在范围**（写下来，免得下次又把范围铺大）：

- **「学习笔记」tab（`MarkdownNotes.tsx`）**：Markdown 正文以阅读为主，roadmap「明确保留文字的页面与内容」已排除。
- **管理后台的 `Dashboard.tsx`**：它属 8080 admin 应用，不是展板；**2026-08-20 首次列范围时误把它算进来过，已剔除**。
- **实验的环境、样本量、命令、原始测量与「事实 / 推断 / 未测量」限定语**：这些本来就该是文字（同上）。

> `reviewOnly` 标记（部署上线 / 可观测性 / 面试准备）只影响**能不能进公开无门禁的发布目标**，不影响是否要核查。
> 「面试准备」板（`InterviewBoard.tsx`，末改 8/12）同属旧口径，但它是**材料索引**而非关系图。
> **2026-08-20 判定：不纳入**——`iv-matrix`（学习周 × 面试稿章节 + 状态）已经把它的验收句编码成矩阵；
> 它的 `iv-metrics` 与第 6 块读同一份 `evidenceSets.ts`，那处的量出了图这块跟着受益。依据见 §5 候选块一段。

---

## 3. 两个维度，两种触发方式（不要混用）

| 维度 | 问什么 | 判据 | 触发方式 |
|---|---|---|---|
| **① 数据时效** | 这一格的结论和**最新一次实测**还对得上吗？ | 对不上就翻档；`yarn verify:board` 保持全过 | **事件驱动**：每次有新实测、新收口，就核**受影响的那几格**——不必等排到这条线 |
| **② 表达形态** | **遮住 `h3` 与结论段，还能答出这块板的验收句吗？** | 加严后的验收判据（roadmap 第八轮）+ 四条肯定面 | **排队驱动**：按本文件 §4 一块一块消化 |

**四条肯定面**（roadmap「目标与约束」，此处只作复检提问用，不重新定义）：

1. 同类对象 ≥ 3 个要相互比较 → 有没有让读者在脑子里建表的地方？
2. 量可比且差距本身是结论 → 差距是结论的那几处，出图了吗？
3. 信息有序或分层 → 用的是位置编码，还是「第一 / 第二」这类序号词？
4. 一块板的正文超过一屏 → 有没有一个不读正文就能读到的锚（一眼读数 / 矩阵 / 尺）？

> 维度 ① 是**跟着学习走的**（新数据一到就该修），维度 ② 才是这条线的主体。
> 把两者混成一件事，会让「今天有没有新实测」决定「今天要不要返工八块板」——那是没道理的耦合。

---

## 4. 工作单元与两个阶段

**一个工作单元 = 15–20 分钟 = 一块板**（`AGENTS.md` 的小单元口径）。

### 阶段一：只判定，不改（八块，八个单元）

对一块板：遮住 `h3` 与结论段 → 自问该块的验收句 → 答得出 / 答不出 → 四条肯定面各过一遍 →
在 §5 记一行：**欠什么、欠多少、值不值得改**。

**阶段一不许动代码。** 这是整条线最容易破的一条纪律：看到一处不顺眼就顺手改，
结果是判定没做完、改动铺开、规模再次估不出来。

### 阶段二：按判定结果决定改哪几块（规模到阶段一末尾才知道）

阶段一全部做完后，拿着八行判定一次性决定：**改哪几块、每块改到什么程度、要不要全改**（大概率不必全改）。
届时更新 `BACKLOG.md` P2-4 的档位与规模，再排具体执行。

---

## 5. 进度与判定记录（每块一行，做完即填）

| 日期 | 板 | 遮标题后能否答出验收句 | 四条肯定面里先破的是哪条 | 欠什么（一句话） | 值不值得改 | 单元数估计 |
|---|---|---|---|---|---|---|
| 2026-08-20 | 1 · 认证与授权 `AuthBoard.tsx` | **上半能，下半不能** | **第 1 条**（下半兼破第 4 条） | 三条路径与四个专题都只能互斥切换；下半结论由 `authk-conclusion` 一句文字承担 | **值得，只改下半** | 2 |
| 2026-08-20 | 2 · OAuth2 流程 `OAuth2Panel.tsx` | **流程能，凭据不能** | **第 1 条** | 六步时序与三条信道已由版面承载；五个凭据只有并排的 `boundary` 文字，比较轴没编码 | **值得，规模最小** | 1 |
| 2026-08-20 | 3 · 数据库聚合 `W3Board.tsx` | **大体能** | **第 2 条** | 四类视觉各自承载了结论；只有 explain 那两块的量（14→5、15→0、12ms→3ms）差距是结论却只排成文字表 | **值得，一处即可** | 1 |
| 2026-08-20 | 4 · Node.js 运行时 `W5Board.tsx` | **能** | 四条都不破 | **旧口径下唯一四条全不破的一块**：ms 轴 + 按时钟生长的柱 + worker 槽位 + 队列 + 判断表。只剩「八个知识点互斥切换」这条共性 | **不值得单独改** | 0 |
| 2026-08-20 | 5 · 测试闭环 `W6Board.tsx` | **大体能** | **第 4 条** | 生命周期矩阵与时间线都在，但全板唯一的「证据精度矩阵」包在 `<details>` 里、两种模式下都默认折叠——锚读不到 | **值得，规模最小** | 1 |
| 2026-08-20 | 6 · 测试闭环 D4 子板 `W6Day4Board.tsx` | **大体能** | **第 2 条** | 十层职责表与三组实验对照表都在；但 378ms→2–3ms、1154ms→102ms 这种量级差只写成数字 | **值得，与第 3 块同一改法** | 1 |
| 2026-08-20 | 7 · 部署上线 `W9Board.tsx` | **分块不一** | **第 4 条**（兼破第 1 条） | 十三块里有轴 / 矩阵 / 图的那几块能，其余几块结论挂在 h3 上；30872 中文字、39 个 h3，**没有一个板级锚** | **值得，但只补板级锚 + 点名的几块，不逐块返工** | 3–4 |
| 2026-08-20 | 8 · 可观测性 `W10Board.tsx` | **能** | 四条都不破 | 维度 ② 已达标（矩阵 ×3、阈值尺、五条证据链、三档格子）。仍欠的是维度 ①，那归 D5 | **② 不必改** | 0 |

> 八块一次做完的原因见 §9（本人 8/20 明确要求「把这些全部查证完先」，§6 止步条件第 2 条当次作废）。
> 下面每块一段依据；第 1 块写得最长是因为它是第一块、要把方法定下来，其余各块按同一四问压缩。

**第 1 块 · 认证与授权**

- **上半 `auth-master` 判「能」**：遮住 `h3`「登录凭据如何变成一次受保护请求」与它下面那句 `<p>` 之后，
  版面本身仍答得出验收句——七段链路是有序 `ol`，四个参与者是 lane 位置编码，
  「不属于本路径 / 已走过 / 当前 / 还没走到」是四种可区分状态，**状态码 `em` 落在自己的停止行上**
  （`stopAt` = 4 / 5 / 6）。结论（这条路径停在哪一层）由位置承担，不由标题承担。
- **下半 `authk-stage` 判「不能」**：遮住 `authk-conclusion`（`核心判断` + `映射回业务`）之后，
  剩下 actors / steps / artifacts 三排卡片，**它们承载的是过程，不是结论**。
  「明文密码只在请求与 Service 内存中短暂停留」这类验收句，只能从 `boundary` 的文字里读出来。
  两个源文件共 2278 个中文字，其中八类纯文字字段就占 77 个字段、1181 个中文字。
- **先破第 1 条**：三条结果路径是 `role="tablist"`，一次只看得见一条，而三者 `stopAt` = 4 / 5 / 6 **各差一格、差距本身就是结论**；
  四个知识点由 `authk-nav` 互斥展开，四组凭据边界永远在不同的框里。
- **下半兼破第 4 条**：远超一屏，锚只有上半 `auth-master-outcome` 的大状态码；
  且 `outcomes` 只有 login / jwt / rbac 三个专题有，**register 连这一处结果对照都没有**。
- **第 2 条不破**（这块板几乎没有量，不 import `charts.tsx` 是对的）；**第 3 条基本不破**
  （唯一序号词是「知识点 1 / 2 / 3 / 4」，而这四个专题实际是一条依赖链：注册 → 登录 → 签发 → 授权）。

**第 2 块 · OAuth2 流程**

- **流程判「能」**：`oauth-steps` 把六步**全部同时列出**，每步带 `from → to`、`carries` 和信道 chip，
  配 `oauth-legend` 三类信道图例（颜色 + 文字双编码，不违反「颜色不得单独承载信息」）。
  遮住 `h3` 与 `FrameNarration` 之后，「哪几步过浏览器、哪一步后端直连第三方」仍读得出来。
- **凭据判「不能」**：`oauth-creds` 是并排的五张卡，每张 `code` + 一句 `boundary` 文字。
  这块板真正的验收句是「`client_secret` 只在第 4 步的后信道、绝不过浏览器；`state` 必须在 callback 比对」——
  它现在只能从文字里读，或者靠逐帧播到第 4 步才高亮。
- **先破第 1 条**：五个凭据是同类对象 ≥ 3，卡片虽然对齐，但**用来比较的那一维（过不过浏览器 / 属哪条信道）根本没有编码**。
- **第 4 条不破**：全板 667 个中文字，是八块里最小的一块，`oauth-steps` 全览本身就是锚。
- **改法已现成**：`OAUTH_STEPS[].creds` 与 `OAUTH_CREDS` 是同一套 key，交叉一下就是「五凭据 × 六步 / 三信道」矩阵，
  **不需要新数据、不需要新组件**。故估 1 个单元。

**第 3 块 · 数据库聚合**

- **判「大体能」**：五个知识点各有专属视觉，且都承载各自的结论——
  `w3-pipeline-strip`（阶段序号 + 输入 → 输出 + 保留字段）、`w3-layers`（分层归属 + 交接物）、
  `w3-timeline`（自然月半开区间，`out / in / edge-open` 三态落在位置上）、
  `w3-metrics`（`role="table"` 的 explain 前后对照）。遮住 `h3` 与 `w5-conclusion` 之后，这四类都还答得出。
- **先破第 2 条**：`w3-metrics` 里 `highlight` 的那几行——`totalDocsExamined` 14 → 5、`collectionScans` 3 → 0、
  `totalDocsExamined` 15 → 0、`executionTime` 12ms → 3ms——**差距本身就是结论**（索引让扫描量塌下来），
  现在却只是两列对齐的数字。`charts.tsx` 的 `HBarChart` 已经存在、W9 板已经在用，属复用不属新造。
- **第 1 条局部破**：五个知识点由 `w5-knowledge-nav` 互斥切换（与第 1 块同型，见「两条共性欠口」）。
- **第 3 条不破**（管道、分层、月边界全是位置编码）；**第 4 条不破**（每个知识点单独成屏，且各有视觉锚）。
- `w3-proof-boundary` 的「观察结果 / 能证明 / 没有证明」三栏是文字——**这是对的**，
  roadmap「明确保留文字」那条把边界限定语排除在外，不算欠账。

**第 4 块 · Node.js 运行时**

- **判「能」**。八块里四条肯定面全不破的有两块（这块和第 8 块），而这块是**旧口径下唯一做到的**——
  第 8 块本来就是按新判据建的。证据是它的编码方式而不是篇幅：
  `w5-tp-track` 的柱长按当前帧时钟算（`width: (shown / axisMax) * 100%`）、`w5-tp-axis` 是真的 ms 轴、
  `w5-tp-schematic` 用槽位数量表示 worker 占用与队列长度、`w5-cpu-tracks` 有独立时间轴、
  `w5-ownership-grid` 与 `w5-jt-grid` 是 `role="table"` 的对照。
- **第 1 条不破**：`JudgmentTable` 把三类「慢」现场并排成三张同构卡（典型观测 / 如何区分 / 当前不能定位），
  `topic.runs` 让 SIZE=2 / SIZE=4 在同一根轴上比。
- **第 2 条不破**：这块板的量全部出了图，且 `SM_CHUNKS` 那处还专门注明「示意用块数（无绝对单位）」，
  没有把没测过的东西画成有刻度的量——这正是判据想要的克制。
- **第 3、4 条不破**：八个知识点各自成屏，轴与槽位就是不读正文也能读到的锚。
- **唯一欠的是共性那条**：八个知识点仍由 `w5-nav-groups` 互斥切换，`w5-conclusion` 的核心判断仍是一句文字。
  但这两处**不构成单独返工这块板的理由**，故判 0 单元。

**第 5 块 · 测试闭环**

- **判「大体能」**：`w6-lifecycle` 是真矩阵（资源 × beforeAll / suite 内 / afterAll 四列）、
  `w6-suite-axis` 是有序资源时间线、`w6-topology-grid` 用格子表示覆盖缺口与补链、
  `w6-parallel-grid` 把两条认证流并排到授权阶段分叉。遮住 `h3` 之后这些都还读得出。
- **先破第 4 条，而且是一处很具体的破法**：全板唯一一张「证据精度矩阵」
  （`w6-matrix-table`，`role="table"`，观察结果 / 能证明 / 没有证明）**被包在 `<details>` 里，没有 `open` 属性**——
  展示态和复习态都默认折叠。**一个要点开才看得到的锚，等于没有锚**。
  这与第 6 块复习态的折叠不是一回事：那边折叠是主动回忆的有意设计，这边是常驻折叠。
- **第 1 条局部破**：`w6-day-switch` 把 Day 1 / 2 / 3 / 4 四天互斥切开；
  `w6-parallel` 只有两条流，不到 ≥ 3，不触发这一条。
- **第 2、3 条不破**：这块板的量是「3 suites / 9 tests」这类计数，没有比较价值，不出图是对的。
- **改法**：把矩阵从 `<details>` 里提出来（或给它一个常驻的一眼读数），1 个单元。

**第 6 块 · 测试闭环 D4 子板**

- **判「大体能」**：`d4-layer-table`（十层 × 职责 / 交付物，`role="table"`）、
  `d4-metrics`（基线 / 前 / 后三列指标对照）、`d4-limits` 六件没证明、`d4-debt-chain`，结构都在。
- **先破第 2 条，而且比第 3 块更明显**：`evidenceSets.ts` 里的
  并发 `/ping` 峰值约 378ms → 约 2–3ms、`maxHeartbeatGap` 1154ms → 102ms——
  **两个量级的差就是「CPU 阻塞 vs worker 卸载」这条结论本身**，现在只是两列数字。
  这份数据同时被「面试准备」板读，改一处两处都受益。
- **折叠不算欠口**：`Corrections` 在复习态默认全折叠（`setOpenIds(review ? [] : 全开)`），
  展示态全开——这是主动回忆的有意设计，**与第 5 块的常驻 `<details>` 性质不同**，不记为欠账。
- **第 1 条局部破**：`d4-set-switch` 把三组实验互斥切开，而这三组正是「数据层 / 运行时层 / 验证层各一组」的对照。
- **第 3、4 条不破**：因果主线与十层表都是位置编码，且各自可作锚。

**第 7 块 · 部署上线**

- **判「分块不一」，这是八块里唯一不能给单一结论的一块**。十三块中：
  **有编码的**——信任边界四条路径判据矩阵（`w9-matrix`）、公网可达性表（`w9-reach`）、
  证书 90 天生命轴（`w9-life-axis`，`role="img"`）、systemd 快 / 慢失败的 60 秒重启密度时间线、
  身份矩阵（`w9-idm-grid`）、口述错误轴（`w9-spoken-axis`）、验收链覆盖表（`w9-acc-grid`）、
  反代 header 变形表（`w9-distort`），外加两张 `HBarChart`（8/18 按新口径补的那两张）。
  **主要靠文字的**——故障分叉、改一台在跑的机器、发布变更单、契约销账与资源闸门、服务边界 vs 暴露边界。
- **先破第 4 条**：30872 个中文字、十三块、**39 个 `h3`**，而这些 `h3` 大量是整句结论
  （「外部现象相同，用第一个动作的**结果**分叉」「加了入口 ≠ 加了业务」「200 只证明有东西在应答」）。
  遮住它们，那几块就没有别的地方承载结论了——**这正是第八轮那句「结论被写成了一句大标题」的原始现场**。
  板尾虽有 `W9_STAGE_PLAN` 的「共十三块、落地几块」，但那是建构进度，不是内容锚。
- **兼破第 1 条**：十三块之间没有任何一处对照，读者要靠滚动记住十三块各自讲了什么。
- **值得改，但改法要限定**：8/14 它刚做过一轮截图核对与存量清扫，逐块返工的成本高于收益（BACKLOG 原判仍成立）。
  建议只做两件：**补一个板级锚**（十三块 × 「这块的问题 / 已用哪种编码」一览），
  以及**点名补上面列的那五块**里最值得的两三块。估 3–4 个单元。

**第 8 块 · 可观测性**

- **判「能」，符合它「全程按加严判据建」的口径**：三张 `w10-matrix`、`w10-rulers` 阈值尺
  （红线、余量、当前值、出事点都落在同一根轨道上）、`w10-corridor` 三道绿灯 × 四条实例、
  `w10-chain` 五条红态证据链、`w10-tier-grid` 三档、`w10-lever-grid`、`w10-blind-grid` 三个盲区。
  29 个 `h3` 对应七块，但每块都另有承载结论的编码，遮住标题不塌。
- **四条肯定面都不破**：同类对象一律成矩阵或成尺；量（磁盘 / 内存 / 证书）走阈值尺的位置编码；
  分档用位置不用序号词；每块都有一眼读数。
- **它仍欠的是维度 ①不是 ②**：⑤ 里四格「未实测」要不要翻档，属数据时效，**归 D5 处理**（§7）。
  本线对这块板的结论是：**表达形态不必改**。

**候选块 · 「面试准备」`InterviewBoard.tsx` 要不要纳入**

§2 把它挂起来，说「先判定要不要纳入」。**判定结果：不纳入阶段二的返工范围**，理由两条：

1. 它的验收句是「哪几周的材料已经能讲、哪几处还欠」，而 `iv-matrix`
   （学习周 × 面试稿章节 + `state`，`role="table"`）**已经把这条结论编码成矩阵了**——它不欠这一维。
2. 它的 `iv-metrics` 读的是 `evidenceSets.ts`，与第 6 块**同一份数据**。
   那一处的量出了图，这块板跟着受益，不必单列一个工作单元。

> **以上八行加一条候选判定都是 AI 预判；`遮住 h3 自问验收句` 那道闸的强制力等级仍然是「人」**
> （roadmap 第八轮「配套的复核仪式」明确说不写成脚本断言，因为代理指标只会被凑数满足）。
> 我给的依据都是可复核的事实——组件结构、`role="table"` / 轴 / 柱长的实际写法、`stopAt` 与指标取值、
> `<details>` 有没有 `open`、字段与字数统计。但「遮住之后本人到底答不答得出」只有自己过一遍才算数。
> **复核后任何一行的结论若不同，直接改掉，不必保留 AI 版本**；总结那一段也要跟着改。

### 阶段一总结（2026-08-20）

**八块里两块达标、五块小欠、一块大欠。**

| 分档 | 板 | 单元数 |
|---|---|---|
| 不必改 | 4 · Node.js 运行时、8 · 可观测性 | 0 |
| 一处即可 | 2 · OAuth2、3 · 数据库聚合、5 · 测试闭环、6 · D4 子板 | 各 1 |
| 两处 | 1 · 认证与授权（只改下半） | 2 |
| 规模最大 | 7 · 部署上线（只补板级锚 + 点名几块） | 3–4 |

**总规模：8–10 个单元**，按 15–20 分钟一个算，约 2.5–3.5 小时，可完全散在空档里。

**比逐块返工更要紧的是两条共性欠口**——它们横贯多块，一次做完比逐块修便宜：

1. **互斥切换**（第 1、3、4、5、6 块都有）：`nav` / `tablist` / `set-switch` 把同类对象切开，
   一次只看得见一个，读者被迫在脑子里建表。这正是第 1 条肯定面要防的事。
   一次性的做法是给这类切换补一条「全览」——把被切开的那几个对象在同一处按位置摆出来，
   而不是逐块重新设计。
2. **量的差距只写成数字**（第 3、6 块，加上「面试准备」共用同一份数据）：
   `explain` 指标与 `evidenceSets` 指标都是「差距本身是结论」，而 `HBarChart` 已经存在、W9 已经在用，
   属复用不属新造。

**对 `BACKLOG.md` P2-4 档位的建议：仍按 P2，不升档。** 理由是判定结果推翻了「范围扩大 = 成本更高」这个预期：
八块里有两块本来就达标（第 4 块尤其值得注意：它建于 2026-08-12，是旧口径下唯一四条全不破的一块），
真正的大头只有第 7 块一块；
而剩下的欠口高度同型，两条共性一次做完能覆盖大半。**换句话说，扩大范围反而让规模变小了**——
因为「八块都欠着」是推断，逐块看过之后它不成立。

**一条要留下的判断**：8/20 那条「只有禁止面的规则会被执行成能不用就不用」现在有了配对的一条——
**按新规则清算存量时，先逐块判定再决定改哪块，判定本身就会把规模改小**。
这次八块判完，规模从「大」回到了「P2 排得下」；而在判定之前，连这条线自己的开篇
（§1「规模再次估不出来」、§4「大概率不必全改」）都只敢说「大概率」，默认它只会更大。

---

## 6. 启动、止步与验证

**什么时候可以推进这条线**（满足其一即可，不需要专门排期）：

- 当天学习主线已达到止步条件，且还有一个 15–20 分钟的空档；
- 周复盘后有余量；
- 或者本人明确说「今天推展板核查线」。

**止步条件**：

1. **当天主线未收口** → 本线零进展（不算欠账）。
2. **一次只做一块** → 一个单元结束就停，不连做三块「反正手热」。
3. **阶段一不改代码** → 见 §4。
4. **改动只走白名单最小范围** → 展示资产可由 AI 实现，但不顺带重构无关组件（`AGENTS.md` §3 末段）。
5. **任何改动后 `yarn typecheck` + `yarn verify:board` 必须全过**（8/20 基线 421/421），窄屏与深浅色各核一次。

**不部署**：发布目标是服务器 8081，且**发布是独立授权动作**（`SHOWCASE-DEPLOY-PROTOCOL.md`）；
Pages 上传处于冻结状态，本线不解冻、不上传。

---

## 7. 与学习主线的接口

- **W10 D5（8/21 收口日）只交维度 ①**：W10 板七块按最新数据核一遍、不一致的当场翻档、`verify:board` 保持全过。
  维度 ② **不在 D5 做**（原计划的「抽样 3 块定性」已移交本线）。
- **W11 起**：本线按 §6 的空档推进，不进每日计划的时间盒，不写进当周主线。
- **`LEARNING-STATE.md`**：只保留一行指针（当前进展 X/8），不复制本文件内容。

---

## 8. 退出条件

阶段一八行判定填满，并且阶段二的「改哪几块」有结论且执行完毕 → 本线关闭，
在 roadmap 追加一节收口记录，`BACKLOG.md` P2-4 标完成。
**若阶段一的结论是「都不值得改」，同样是关闭**——判定本身就是产出，不是必须改点什么才算数。

---

## 9. 变更记录

- **2026-08-20 建立**。起因：本人指出「不要过分追求动效」的波及面比 8/18 认为的更广，
  大段文字展示不是 W9/W10 的局部问题，这两周只是集中爆发处（证据三条见 roadmap 第八轮补正）。
  当天本人进一步决定：**核查单开一条线，不与学习同步开展**——遂有本文件。
- **2026-08-20 阶段一一次做完八块**：本人在同一次会话里说「把这些全部查证完先」，
  故 §6 止步条件第 2 条（一次只做一块）**当次作废**——这是本人的显式指令，不是执行时把纪律忘了。
  第 2–8 块的判定与阶段一总结已填入 §5，「面试准备」的纳入判定填入 §2。**全程零代码改动**，
  阶段二（改哪几块）尚未开始，本线不关闭。进度 **8/8**。
- **2026-08-20 阶段一开始**：本人说「开启展板状态核查」，按 §6 第三条触发。当天主线（W10 D4）已收口，
  故本线可进入；按 §6 止步条件第 2 条**只做第 1 块**（认证与授权），判定已记入 §5，未动任何代码。进度 **1/8**。
- **2026-08-20 范围修正**：首次列范围时把 admin 的 `Dashboard.tsx` 误算成展板（它属 8080 管理后台），
  已剔除；同时补入此前漏掉的 `OAuth2Panel.tsx`。核查对象由「九块板」更正为**八块板**（另有「面试准备」待判定是否纳入）。
