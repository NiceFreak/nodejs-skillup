# 学习展板可视化优化路线

> 状态：三个阶段全部完成；另有可访问性与构建边界审计、W6 Day 3 内容扩展、第五轮「逐帧时间轴统一」、
> 第六轮（Day 4 板 + 面试板）、第七轮「逐帧节奏全板校准」、第八轮「验收判据加严」，
> 以及第九轮「把结论交还给版面」（W2 新板返工 + W3 聚合 + W4 认证链 + W6 全栈轨道）。
> 最后更新：2026-08-22。
> 范围：`week8-fullstack/src/frontend/` 白名单展示资产；不修改后端学习代码，不部署。

## 目标与约束

这轮优化的目标不是增加装饰，而是让用户更快判断：

1. 调用或事件按什么顺序发生。
2. 线程、组件、层和资源分别负责什么。
3. 成功路径与失败路径在哪里分叉。
4. 实验结果能证明什么、不能证明什么。
5. 测试覆盖如何互补，状态和资源如何随时间变化。

实施时继续遵守以下边界：

- 优先复用现有 React、CSS、hash URL、`demo/review` 状态与专题数据，不引入图表框架或新的全局状态层。
- 不把增加卡片、图标、阴影或颜色**当作可视化成果**。
- 不为少量数字创建**没有比较价值**的统计图（判据是有没有比较价值，不是数字多少）。
- ~~图形只负责关系；原始结论、测试条件、未测量项和来源仍保留文字。~~
  **2026-08-18 修订** → **图形负责结论本身**；测试条件、未测量项、来源与可复核证据仍保留文字。
- Markdown 长文继续以阅读为主，不大规模重写现有前端。
- 每个阶段完成后先验证，再进入下一阶段；耗时验证应停止并切换为更小的类型检查、DOM 度量或代表性截图。

**2026-08-18 新增的肯定面**（原来这份清单只有禁止面，执行时被当成「能不用就不用」）：

- **同类对象 ≥ 3 个要相互比较** → 必须有非文字编码：位置、长度或对齐。读者不该在脑子里建表。
- **量可比且差距本身是结论** → 出图。（这条原来就成立，W9 板的两张 `HBarChart` 就是这么来的。）
- **信息有序或分层** → 用位置编码，不靠「第一 / 第二」这类序号词。
- **一块板的正文超过一屏** → 必须有一个不读正文就能读到的锚（一眼读数、矩阵或尺）。

**仍然一条不松的禁止**：视觉承担不了信息的纯装饰（阴影、渐变、图标当项目符号）；
颜色单独承载信息（必须有第二编码）；不表达「随时间变化」的动效；为显得工作量大而加的东西。

## 阶段总览

| 阶段 | 状态 | 核心范围 | 完成标志 |
|---|---|---|---|
| 第一阶段：建立语法与代表页 | 已完成 | 统一视觉 token、认证端到端链路、OAuth2 语义和手机控制条 | 一张代表页能同时表达顺序、职责和 401/403/200 分叉 |
| 第二阶段：复用到高价值关系页 | 已完成 | Node.js 运行时、测试闭环、数据库聚合 | 运行时职责、测试证据和 pipeline 形状不再主要依赖段落说明 |
| 第三阶段：手机与复习任务收口 | 已完成 | 各主要 tab 的移动端语义变体、主动回忆、跨周演进与一致性审查 | 手机不因简单堆叠丢失关系，复习态先做判断再揭示证据 |

## 第一阶段基线（已完成）

### 已落地

- 在 `styles.css` 建立流程、资源、成功、受控拒绝、异常和未测量的视觉 token。
- 在认证页加入“客户端 → Auth Service → Repository + DB → 受保护请求管道”的端到端链路。
- 401、403、200 使用同一张图切换，停止点分别落在认证、授权和资源处理阶段。
- 展示状态默认展示完整模型；复习状态先要求口述两道门、停止层和凭据边界，再显示答案。
- OAuth2 补齐 `state` 的服务端生成、浏览器往返与 callback 比对。
- OAuth2 区分前信道、OAuth 后信道和完成授权后的第一方会话交付。
- 手机 OAuth 控制条恢复为水平按钮；认证主链没有页面级横向溢出。

### 已验证

- `yarn typecheck` 通过。
- `yarn build` 通过。
- Playwright 检查桌面 `1440 × 1000` 和手机 `390 × 844`。
- 认证展示态、复习态、401/403/200 三条路径与 OAuth callback 关键状态通过。
- 两种视口均无页面级横向溢出；本阶段未部署。

### 当前视觉语法

| 语义 | 当前表达 | 使用边界 |
|---|---|---|
| 已有调用或数据流 | 蓝色实线 | 必须有代码、契约或实验支持 |
| 资源或基础设施边界 | 紫色 | 数据库、缓冲区、线程池等非业务参与者 |
| 成功结果 | 绿色 | 通过、完成、200 等成功结论 |
| 受控拒绝或暂停 | 橙色 | 401、403、背压暂停等预期控制结果 |
| 异常或阻断 | 红色 | 非预期错误、资源失败、流程无法继续 |
| 未测量或不在范围 | 灰色 / 虚线 | 不能从当前实验外推的部分 |

线型仍需遵守一个关键限制：虚线表示推断或未验证关系；异步边界必须另带文字标签，不能只靠虚线表达。

## 第二阶段：高价值流程与证据页

第二阶段已按下面顺序完成，并在每个视图后分别通过类型检查。

### 第二阶段完成基线（2026-07-28）

- W5 所有专题复用四区 ownership map：主线程 / event loop、libuv threadpool、OS / kernel I/O、应用资源。
- threadpool 专题显式并列 pool API 与普通网络 I/O 两条路径；event loop、背压与 graceful shutdown 继续用各自的阶段轨道、反馈闭环和状态时间轴下钻。
- W6 新增旧覆盖 → 缺口 → 新 suite 的覆盖拓扑、200 / 403 平行认证链、可展开证据矩阵和 suite 资源时间轴。
- W3 新增真实 customer spending 六阶段 pipeline strip，逐阶段显示输入 / 输出形状，并紧邻保留观察结果、能证明与没有证明。
- W3 复习态从一个已知阶段开始逐步揭示下一 stage；W5 / W6 原有主动回忆门继续控制模型与证据显示。
- `yarn typecheck`、`yarn build` 通过；Playwright 检查桌面 `1440 × 1000` 与手机 `390 × 844` 的代表状态，均无页面级横向溢出。

### 2.1 Node.js 运行时职责与反馈（P0）

认知问题：当前用户需要在多个专题和段落之间拼出“主线程、event loop、threadpool、OS I/O、流和进程”的职责边界。

最小改造：

- 建立统一 ownership map：主线程 / event loop、libuv threadpool、OS / kernel I/O、应用资源四个区域。
- 专题切换时复用同一组参与者，不为每个专题发明另一套颜色或箭头。
- event loop 用阶段环或有序轨道表达调度顺序，同时明确“阶段”不等于独立线程。
- threadpool 与网络 I/O 用分叉调用链表达，避免把所有异步工作都画进 threadpool。
- 背压用“生产者 → 缓冲区 → 消费者 → pause/resume 反馈”闭环表达。
- graceful shutdown 用“收到信号 → 停止接入 → 等待在途任务 → 清理资源 → 退出”的状态时间轴表达。
- 错误传播保留文字化边界，避免用一条红箭头把同步抛错、Promise rejection、Stream error 和进程事件压成同一种机制。

展示状态：默认给出职责全图，选中专题后突出相关参与者和边。

复习状态：先问“慢点属于哪一层、会阻塞谁、证据是什么”，回答展开后再显示 ownership 与未测量边界。

验收重点：用户不读完整段落，也能定位执行归属；但图不能暗示网络 I/O 一定使用 threadpool。

### 2.2 测试闭环与证据矩阵（P0）

认知问题：当前覆盖增量、两条认证流、fixture 和数据库生命周期已有内容，但它们之间的互补关系仍需要逐块阅读。

最小改造：

- 用覆盖拓扑表达“旧测试覆盖什么 → 缺口在哪里 → 新测试补到哪一层”。
- 用两条平行认证链对照 admin 登录后报表 200 与新注册 member 登录后报表 403。
- 用矩阵表达测试 / 观察结果 × 能证明 / 不能证明，至少保留：登录契约、JWT 使用、RBAC 分叉、报表 Controller 是否到达、真实 OAuth2 是否被覆盖。
- 用资源生命周期轴表达 fixture 创建、`JWT_SECRET` 设置、内存库连接、测试执行、清理和恢复。
- 状态码颜色服从统一语法：403 是受控拒绝，不使用异常红色。

展示状态：默认展示覆盖拓扑和两条链，证据矩阵可展开查看精度。

复习状态：先给测试名称与观察结果，要求判断能证明什么；提交或主动揭示后再显示矩阵答案。

验收重点：用户能直接回答“两个测试为什么不能互相替代”，且不会把 200/403 结果外推为数据库、OAuth2 或所有权限场景都正确。

### 2.3 数据库聚合形状与证据边界（P1）

认知问题：pipeline 顺序、每阶段数据形状、索引前后差异及事实 / 推断目前分散在卡片和文本中。

最小改造：

- 用 pipeline strip 表达 `$match → $group / $lookup → $unwind → $project / $sort` 等实际阶段。
- 每个阶段显示输入 / 输出形状的最小样本，只保留影响下一阶段的字段。
- 索引前后用 explain 指标的并排差异表，不把少量指标包装成柱图。
- 用“观察结果 → 能证明 → 不能证明”三列结构区分事实、推断和未测量项。
- `populate` 与 `$lookup` 只在需要解释调用次数或数据往返时使用调用拓扑；其余差异继续用文字表格。

展示状态：默认突出 pipeline 主链和形状变化，证据边界紧邻相关阶段。

复习状态：先隐藏阶段名称或输出形状，要求预测下一阶段结果，再逐步展开。

验收重点：图形不能把一次 explain 结果表达成普遍性能结论，也不能省略数据规模、过滤条件和索引前提。

### 第二阶段不做

- 不重做认证代表页，只修复复用统一语法时发现的不一致。
- 不把 OAuth2 再改成另一套流程组件；现有三参与者时序继续使用。
- 不重写 Markdown 笔记，不给长文增加装饰性图表。
- 不抽象通用图形 DSL。先让三个真实页面证明重复结构值得抽象，再考虑小型组件复用。

## 第三阶段：手机、复习与跨页一致性

### 第三阶段完成基线（2026-07-28）

- 认证 lanes 与 W3 pipeline 在手机端增加连续性轨道，局部横向滚动时可直接看到完整参与者 / stage 顺序；页面本身不横向溢出。
- W5 ownership map 在手机端保留四个稳定区域标签，路径改为可换行关系，不再产生隐藏横向手势。
- W6 证据矩阵按观察结果逐行重复“能证明 / 没有证明”列语义；OAuth2 时序保留当前消息、起点 / 终点和步骤轨道。
- Auth、OAuth2、W3、W5、W6 与 Markdown 笔记均具备展示 / 复习认知任务：复习态先给条件或不完整结构，再由局部 reveal 显示答案。
- `mode/tab/topic` 继续由 hash 保存；OAuth2、笔记与各知识板的 reveal 均保持组件局部状态，不进入分享 URL。
- 展板导航附近新增 W3 数据查询 → W4 身份边界 → W5 运行时 → W6 测试证据的依赖轨道，并把关系图例收敛为全局唯一入口。
- 删除认证页重复图例；专题定位、路径对照、预测揭示和证据展开继续保留。

### 3.1 手机端语义变体

手机端不要求机械缩小桌面图，而要保留相同结论：

- 时序 / 泳道：改成“当前消息 + 明确的起点 / 终点 + 可扫读步骤轨道”，避免四列压成不可读窄栏。
- ownership map：参与者变为稳定的区域标签，选中专题时只突出相关关系。
- 证据矩阵：按测试或观察结果逐行展开，重复显示列语义，避免用户横向记忆表头。
- pipeline：保持阶段顺序，允许局部横向滚动，但页面本身不能横向溢出。
- 任何局部滚动区都要有可发现的下一项，不依赖隐藏手势。

### 3.2 展示 / 复习认知任务

统一规则：

- 展示状态回答“模型是什么”，默认给主链、分叉和证据边界。
- 复习状态回答“你能否从条件推导结果”，先给问题或不完整结构，再逐步揭示。
- 优先询问停止点、职责归属、下一阶段数据形状、覆盖缺口和证据外推边界。
- 单纯隐藏个人文字不再算复习交互。
- `mode/tab/topic` 继续写入 URL；一次性的 reveal 状态保持组件局部状态，不污染分享链接。

### 3.3 跨页面一致性与学习演进

- 审查相同语义是否使用相同颜色、状态码标签、参与者形状和箭头。
- 在展板导航附近增加克制的 W3 → W4 → W5 → W6 学习演进关系，仅表达“数据查询 → 身份边界 → 运行时 → 测试证据”的依赖，不做营销式进度条。
- 检查图例是否需要全局入口；如果各页无需反复解释，则保留一处全局说明并允许局部补充特殊语义。
- 删除只切换内容、却没有改变认知任务的交互；保留专题定位、路径对照、预测后揭示和证据展开。

### 3.4 最终验证矩阵

至少覆盖：

| 视口 | 状态 | 页面 / 交互 |
|---|---|---|
| `1440 × 1000` | 展示、复习 | 认证、OAuth2、数据库、运行时、测试闭环、笔记 |
| `390 × 844` | 展示、复习 | 同上，并检查局部滚动、文字换行和按钮尺寸 |
| 两种视口 | 关键分支 | 401 / 403 / 200、OAuth callback、背压、shutdown、两条测试流、聚合阶段切换 |

验证顺序：

1. `yarn typecheck`。
2. `yarn build`。
3. Playwright DOM 度量：页面级横向溢出、关键元素可见性、路径 / reveal 状态。
4. 只对代表性状态截图；发现问题后补局部截图，不重复全量采集。
5. 若浏览器启动、网络等待或截图任务卡顿，主动终止，改用已有开发服务器、较短超时、DOM 检查或直接构建验证。

## 第四轮：可访问性与构建边界审计（2026-07-28）

三阶段收口后的一次审计，不新增展示内容，只修可测量的缺陷。审计方法是实际构建 + 静态服务 +
Playwright DOM 度量，不依赖本文自述。

### 审计确认仍然成立的部分

- `1440 × 1000` 与 `390 × 844` 共 11 个视图 / 状态，均无页面级横向溢出。手机端唯二超出视口的
  元素（认证 lanes、笔记表格）都在 `overflow-x: auto` 容器内，是设计中的局部滚动。
- 深色模式在各板均成立（逐板截图核对，不只是 token 存在）。
- `prefers-reduced-motion` 在 CSS 与 JS 两侧都已处理。
- admin-only 选择器只占 `styles.css` 的 2.8%，**按板拆 CSS 没有收益，明确不做**。

### 本轮修复

1. 深色模式下 W3 → W4 → W5 → W6 的依赖箭头对比度只有 1.65:1（`--baseline` #383835 on #0d0d0d），
   实测几乎不可见。改用 `--text-muted`。
2. `role="tablist"` 原本是半实现：5 处 tablist，0 处 `tabpanel` / `aria-controls` / 方向键。
   展板主 tab、认证路径、W6 测试链补全为完整 tab 模式（`tabs.ts` 提供键盘契约）；
   登录/注册与图表/表格两处本就不是 tab，改为 `aria-pressed` 按钮组。
3. 手机端点击目标：`summary` 实测仅 14–17px 高（WCAG 2.2 SC 2.5.8 要求 24×24），统一抬到 32px。
   Markdown 正文里的行内链接属于「句子或文本块中的目标」，规范豁免，不为它撑高行距。
4. 手机端字号下限：小号文字统一走 `--fs-s1..s7` 梯子，桌面值不变，手机端整体抬高。
   连带修复背压 stage 在字号抬高后把整行顶出视口的问题（`1fr` 轨道改 `minmax(0, 1fr)`）。
5. `OAuth2FlowPanel` 从 `Dashboard.tsx` 抽到 `OAuth2Panel.tsx`，`Dashboard` 改懒加载。
   之前 Showcase 的一行 import 会把 `charts.tsx` / 报表视图一起拖进 SHOWCASE_ONLY 构建。
   入口 chunk 256.32 → 245.47 kB（gzip 91.27 → 87.92），charts 落到永不请求的 chunk。
   真正的收益是「展板无后端依赖」从 deploy skill 的 grep 约定变成结构约束。
6. 标题改由 `VITE_SHOWCASE_ONLY` 在构建期决定，并内联 favicon。
   deploy skill 里那条 `perl -0pi -e 's#<title>...'` 已经多余，可以删。

### 本轮已知残留

- 桌面认证 tab 比改动前矮 6px（2930 → 2924，0.2%），其余 10 个视图高度逐像素一致；
  视觉无差异，未继续追。
- 三个按钮停在 31px（阈值 32），均高于 WCAG 的 24px 下限。

### 审计判定为「过度打磨」、明确不做

- 认证主链顶部四列 lanes 目前是装饰性的：消息行没有对齐到那四列，实际是列表而非泳道。
  改成真泳道是剩余最大的表达力收益，但属于代表页布局重写，需单独决策，本轮不做。
- 引入图表框架、增加卡片 / 图标 / 阴影 / 颜色、增加新的 reveal 交互。

## W6 Day 3 内容扩展（2026-07-29）

这不是继续做无边界前端润色，而是把当天已验收的全栈学习事实补进 Week 8 展板。新增 `topic=fullstack`
深链，并把 W6 总览从 Day 1 / Day 2 扩展为三日工程闭环。

### 新增表达

- 同一条八段请求轨道切换 admin `200`、member `403`、无 Token `401`；当前停止点与后续未执行阶段显式区分。
- 手机端选择路径后自动把停止点滚动到轨道中央；动画遵守 `prefers-reduced-motion`。
- 身份摘要、JWT `sub` 与数据库当前角色用三段来源图分开，保留实时撤权的查询代价。
- 成功链用四列表格分别呈现代码调用顺序、职责归属、返回值来源和边界判断。
- MongoDB 聚合结果到 React 图表的返回形状单独成轴，明确普通对象数组、Decimal128 DTO 转换与 `fillMonths`。
- 证据边界紧邻结果：真实浏览器三路径为已验证，StrictMode 翻倍为已解释，CORS / 真实 500 / 吞吐为未验证。

### 验证

- `yarn typecheck` 与 `VITE_SHOWCASE_ONLY=1 yarn build` 通过。
- 开发页完成 `1440 × 1000` 浅色 / 深色与 `390 × 844` 截图检查。
- 生产静态预览在桌面 / 手机均无页面级横向溢出、console error 或后端请求；静态标题与 admin 入口隐藏正确。
- 三条路径分别显示 `200 / 403 / 401`，member 手机停止点位于 356px 轨道的中央区间 `98..258`。
- 复习态初始不渲染答案，点击揭示后显示完整契约地图；本轮未部署 Pages。

## 第五轮：逐帧时间轴统一（2026-07-29）

起因：`66acbac` 把 W5 的大数据流生产边界组（知识点 4/5/6）升级为逐帧动画后，展板内部出现了
两种表达档次。知识点 4/5 的可理解性优势不是「有动画」，而是五条可迁移属性：可控节奏、状态机
舞台、逐帧解说、运行计数器、一眼结论图。本轮把这套语法推到除笔记 tab 外的全部展示内容。

### 抽象决策

`useFramePlayer` + `W5Transport` 原本是 `W5Board.tsx` 文件私有的。第二阶段「不做」写明
「先让三个真实页面证明重复结构值得抽象」——该条件已满足（W5 内部 4 处 + 认证 / OAuth2 / W6
三处同构 step 状态），因此抽出 `framePlayer.tsx`：一个 hook、一个控件、一个解说组件。
仍然不抽图形 DSL，各专题的舞台由各自组件决定。

新增 `seek()`（手动定位一律停止自动播放）与帧数变化时的 index 夹取——认证链按 401/403/200
停在不同长度，不夹取会读到 undefined 帧。

### A 档：W5 板内补齐（原本只有 4/5/6 有）

- 知识点 1：`EventLoopTick` 改用统一 transport，删掉自己那套控件；chip 从带 onClick 的 `li`
  改为 `button` + `aria-current`（键盘可达，读屏能播报当前项）。
- 知识点 2：从「一次跑完的 CSS 生长动画」改为按实测时刻取帧（0 / 20 / 100 / 2000 / 2004ms）。
  关键收益是**能停在 100ms 那一帧**——它同时成立「timer 已到期」和「callback 还没执行」，
  正是本知识点的全部结论，之前停不住。
- 知识点 3：柱长改由帧时钟决定，worker 槽位、等待队列与柱状轨道共用同一份状态；
  新增已完成 / worker 占用 / 队列等待计数。可以停在「第一批完成、第二批刚被接走」那一帧。
- 知识点 7：从三根静态柱状图改为主线程组 / Worker 组的双泳道逐帧对照（空闲基线 → 请求到达 →
  计算期间 heartbeat → 计算期间 /ping → 响应完成）。这是本轮单点收益最大的一处：
  「谁被谁挡住」本来就是时间过程，柱状图表达不了。原始读数并排保留在下方。
- 知识点 8：shutdown 六步从静态 `ol` 改为逐帧 + 双路径（正常关停 / deadline 超时），
  复用知识点 6 的模式；新增「接收新连接 / 在途请求 / 数据库连接 / 退出码」四格实时快照。

### B 档：推广到其余 tab

- 认证代表页：端到端链路按路径逐帧推进，参与者泳道高亮当前收发双方；链路步骤区分
  已走过 / 当前 / 本路径未走到 / 不属于本路径四种状态。**状态码改为走到停止点后才揭示**，
  从一开始就摆着的标签变成推导结论。
- OAuth2：原本只有「上一步 / 下一步」，补齐播放 / 暂停 / 重放与 reduced-motion 处理。
- W3 聚合：展示态由播放器沿 pipeline 走并逐阶段解说形状变化；复习态仍由「预测下一阶段」
  控制节奏，播放器不介入。
- W6 全栈轨道：八段轨道加光标与单步推进，未走到的段不再提前显示「通过」；
  单步时把当前段滚进视野（遵守 `prefers-reduced-motion`）。

### C 档：可访问性与同组一致性

- 知识点 4 补 `role="status"` 逐帧解说——同组内 5、6 早就有，4 没有，自动播放时对读屏静默。
- 全局「关系图例」`summary` 桌面态只有 14px 高，低于 WCAG 2.2 SC 2.5.8 的 24×24；
  证据 `summary` 为 23.80px。两者均抬到 24px 下限（手机端上一轮已处理，桌面被漏掉）。

### 口径边界（本轮最容易做错的地方）

知识点 4/5 的动画之所以站得住，是因为帧序列映射到实测数字。推广时按证据类型分别标注：

- 知识点 2 / 3 / 7：有实测数值，标「按实测数值脚本化」。
- 知识点 8：关停顺序取自真实 `server.js` 的实现，**不是一次带计时的实测**，标「模型示意」；
  超时分支是 deadline 契约的推演，页面显式说明。
- 知识点 4：本日仍未实测内存与吞吐，继续标「相对示意，无绝对数值」，不出现任何 MB / ms。

### 验证

- `yarn typecheck` 与 `VITE_SHOWCASE_ONLY=1 yarn build` 通过；入口 chunk 266.48 kB（gzip 96.07）。
- 生产静态预览 + Playwright：`1440 × 1000` 与 `390 × 844` 共 17 条路由 / 状态，
  无页面级横向溢出、无 console error、API 请求数 0（展板无后端依赖成立）。
- 触控目标扫描：两个视口下 `button` / `summary` 均不低于 24×24。
- 16 项交互断言通过：包括知识点 2 停在 100ms 帧时长任务为 blocked、知识点 3 起始帧 4 个任务
  排队、知识点 7 ping 帧 378ms vs 2ms、知识点 8 两条路径退出码 1 / 0、认证切换路径回到第 1 帧、
  W6 光标推进、tick chip 为 button 且带 `aria-current`。
- **本轮已发布 Pages**（与前四轮不同）：源码 PR #59 合并后从 `main` 重新构建，产物同步到
  `nicefreak.github.io` 的 `skillup-week8/`，Pages PR #23 合并，`pages build and deployment` 对 `58b0469` 报 success。
  发布前的视口与静态边界检查是对**生产产物**做的，不是开发服务器。执行会话的网络策略拒绝访问
  `nicefreak.github.io`，线上 URL 未由该会话直接取回核对；发布结论以 GitHub 部署记录为准。

### 本轮修复的两处回归

- 知识点 3 手机端 450 > 390 横向溢出：`.w5-tp-controls` 与 `.fp-transport` 补 `flex-wrap`。
- 知识点 2 柱内标签在窄帧溢出轴外：柱宽低于 22% 时标签改挂到柱子右侧。

### 清理

删除已无对应 DOM 的 `.w5-visual-controls`、`.oauth-nav`、`.oauth-count`、`.w5-tick-ctrl`
以及为旧静态 `ol` 写的 `.w5-shutdown-flow ol/li` 规则；`.w5-replay` 并入 `.fp-replay`。

## 第六轮：新增 W6 Day 4 板与「面试准备」板（2026-07-30，用户单独要求）

第五轮之后本文写的是「展示前端暂无默认后续任务」。本轮不是自发扩展：用户在完成 W1–W6
问答稿更新后明确要求「把最新进展做成高度可视化的内容放进 week8，方便记忆和复盘」，
并要求把面试题本身也放进 Markdown 可读。

### 范围

新增第 6 个 tab `interview`（`#/showcase?tab=interview`），五个板块：

| 板块 | 可视化形态 | 回答什么 |
|---|---|---|
| 1 强项分层 | 七档阶梯 + 自评把握度条 + 三档处理方式 | 面试时主动往哪引 |
| 2 两处答错过的题 | ❌ 旧答法 → ⚡ 漏了哪一层 → ✅ 正确答法 | 哪两句的旧答法与已验收结论矛盾 |
| 3 三组数字 | 实验切换 + 前后指标表 + 能证明 / 不能外推 | 被追问「你怎么知道的」时报什么 |
| 4 追问应答树 | 十条入口 × 三档路线 + 逐帧焦点卡 | 被问到 X 走哪条路、坑在哪 |
| 5 覆盖矩阵 | 堆叠比例条 + W1–W6 × 问答稿章节 + 下一步 | 还缺什么，成本多大 |

同时把 `interview-prep/` 两份问答稿接进笔记 tab（`topic=qa` / `topic=dbqa`），
和已有的 `week6-testing-ci-mental-model.md` 一样直接读源文件，不在前端维护副本。
学习演进导航末尾加一个终点节点「产出 · 讲得出口」。

### 唯一贯穿的语义

三档「面试里怎么处理这一块」在五个板块里用同一套颜色：

```text
lead   主动引    → status-good     可以自己把话题带过去
answer 被问再答  → series-1        不主动挑起，但答得住
hold   暂时收住  → status-serious  答到方法层就停
```

颜色不单独承载信息：每处都同时给了文字标签，符合本文既有的口径。

### 口径边界（本轮最容易做错的地方）

- **强项分层的条形是自评，不是测量。** 组件里和 `reviewNote` 里各写了一次，避免被读成分数。
  这是本板唯一一处非事实数据，必须显式标注。
- **不新增技术结论。** 所有内容来自 `interview-prep/` 两份问答稿与已验收笔记；
  实验数字只用 `day4-overall-technical-summary.md` §4 里可追溯的那三组。
- **DB 维持 ⚠️。** `db-review-sheet.md` 已写完但尚未自测，板块 1 和 5 都按「暂时收住」呈现，
  不因为「材料齐了」就提前升级。

### 验证

- `yarn typecheck` 与 `VITE_SHOWCASE_ONLY=1 yarn build --base=/skillup-week8/` 通过。
- **针对生产静态产物**（本地服务 Pages 仓库根目录、走真实子路径）在 `1440 × 1000` 与
  `390 × 844` 验证 28 项页面级检查：W6 四个 Day × 展示 / 复习、Day 4 全展开态、
  面试板 5 个板块——均无页面级横向溢出、无 console error、后端请求数 0、
  可点击目标 ≥ 24×24。另有 8 项断言：展示态无面试 tab（6 个 tab、无终点节点）、
  复习态有（7 个 tab、有终点节点）、深链自动进复习、切回展示落回默认页、
  Day 4 可直达且四档齐全、纠错在展示态默认展开 / 复习态默认收起、
  **两块板渲染出的实验指标文本完全一致**、问答稿在笔记 tab 可打开。合计 **36/36**。
- 桌面浅色 / 深色与手机浅色截图核对了面试板五个板块与 Day 4 八个分段。
- 验证脚本上踩到的一个坑：只改 hash 的导航不触发重新加载，`waitUntil: "networkidle"`
  会立刻返回，断言可能读到上一屏的 DOM。需要在 hash 导航后显式等待重渲染。
- **已发布**：源码 PR #61 与 Pages PR #25 均已合并，`pages build and deployment` 对 `340b04e4`
  报 success（2026-07-30 10:54Z），本人已初步核验线上更新完成。本会话所在环境的网络策略拒绝访问
  `nicefreak.github.io`，线上 URL 未由本会话直接取回核对，发布结论以 GitHub 的部署记录和本人核验为准。

### 本轮修复的两处自造缺陷

`.mobile-scroll-cue` 的每个变体必须显式给 `grid-template-columns`——不给会退化成单列，
其 `span::after` 连接线（`width: calc(100% + 3px)`）会把手机页面顶到 514px。
首次实现漏了这条，被横向溢出断言抓到。十条入口挤不下文字标签，最终改为序号 + 按路线着色的顶边。

第二处：能力归属时间线的圆点用负 `left` 挂在竖线外侧，贴着容器左边缘时会被裁掉，整条线要向右让出约 9px。这条是截图核对时发现的，断言测不出来。

### 本轮追加（同日，用户澄清后）

用户澄清了两点，都在同一轮内改完：

1. **面试准备板收进复习状态。** 它是个人材料，不该出现在对外 demo 里。
   不变式「interview ⟹ review」由三处共同维持：`Showcase.tsx` 按 mode 过滤 tab 列表
   （方向键与 roving tabindex 也跟着过滤后的列表走，否则会跳到渲染不出来的项），
   `App.tsx` 的 `parseHash` 让带 `tab=interview` 的深链自动进复习状态，
   `changeMode` 在该 tab 上切回展示时把 tab 落回默认页。学习演进的终点节点同样只在复习状态出现。
2. **「最新进展」指的是 W6 Day 4，不是面试稿。** 补上 Day 4 板：
   `w6-day-switch` 从三档变四档，新增 `#/showcase?tab=testing&topic=day4`。

Day 4 板的形状刻意不是「又一份知识点列表」，而是一条论证：

```text
四维框架（契约 / 职责 / 证据 / 限制）
→ 因果主线（四周各解决什么、交出什么）
→ 十层交付物形状
→ 三组受控实验
→ 九条关键纠错
→ 六条当前限制
→ 能力归属时间线
→ 最终验证
```

各段刻意用不同排版密度（框架是四卡流程、主线是四段链、层是表、纠错是编号列表、
归属是时间线），避免读成同质卡片流。复习态下受控实验先给受控变量让人预测、
九条纠错先只给初始说法让人自己判断错在哪一步。

**共享数据源**：Day 4 和面试板都要引用 W3 / W5 / W6 三组实验数字，各写一份必然漂移，
因此抽出 `evidenceSets.ts` 作为 canonical 记录，两块板各渲染自己需要的字段。
抽象边界只到「实验事实」这一层——怎么用这些事实仍由各自组件决定。
验证里有一条专门断言两块板渲染出的指标文本完全一致。

### 本轮明确不做

- 不改动其它五个 tab 的任何内容。
- 不在面试板里重画 401 / 403 / 200 三条路径——W6 板已经有了，重复画只会让两处漂移。
  Day 4 里出现的三条路径是收束视图（只标停止点与状态码），不复制 Day 3 的八段跨层轨道。
- 不部署 Pages（发布是独立动作，按 `SHOWCASE-DEPLOY-PROTOCOL.md` 单独确认）。

## 第七轮：逐帧节奏全板校准（2026-08-12，用户反馈「动效太快」）

第五轮把逐帧时间轴推到全展板时，间隔沿用了各处手写的固定毫秒数（680–1800ms）。这些数字最初是配
W5 那些**短状态标签**定的；同一轮里解说升级成了完整句子（40–80 字），间隔却没跟着改，结果是
**读完之前就翻页**。W9 板落地时把同一个问题又复制了一遍（1100ms），用户在本地检查时指出，
随后要求把 week8 现有的自动播放一起校准。

### 判断：三类东西，不能一刀切

盘点后展板里只有 W5 的 8 个播放器是自动播放的（认证链、OAuth2、W3 pipeline、W6 轨道、面试板
本来就是 `autoPlay: false`）。这 8 个不是同一类：

| 类别 | 播放器 | 处理 |
|---|---|---|
| 一次性讲解，每帧说一件新事 | CPU 阻塞、线程池、pipeline、Worker、关停生命周期 | 改 `autoPlay: false`，与其余板一致；停留时间按解说长度给 |
| 闭环 + 每阶段是不同的说明文字 | 事件循环 tick | 保留循环与自动播放（停住就表达不出「回到调度」），停留时间按字数给 |
| 流量动画，文字是随帧变化的读数 | 流模型、背压 | 保留循环与自动播放，**不按字数拉长**——那会让「在流动」看不出来；只把固定间隔放慢（780→1500 / 680→1400） |

第三类是本轮最容易做错的地方：把「按字数给时间」当成通用规则套上去，背压的缓冲区涨落会变成
一格一格的幻灯片，闭环反馈的节奏感消失。判据是**文字是不是必须逐帧读完**：是散文就按长度给，
是读数就按动画节奏给。

### 新增共享工具

`framePlayer.tsx` 增加两样：

- `FramePlayerOptions.intervalAt?: (index) => number` —— 按帧给停留时间。不传则走原来的
  `setInterval` 路径，未改的板行为完全不变。变间隔用 `setTimeout` 逐帧重排。
- `dwellByText(text)` —— `min(7000, 1800 + 字数 × 60)`，约 11 字/秒的快读节奏；要细读就暂停。

`intervalAt` 存进 ref 而不是进依赖数组：调用方都会传内联箭头函数，直接进依赖会让效果每次渲染
重建定时器，逐帧推进再也触发不了。

### 验证

- `yarn typecheck` 与 `VITE_SHOWCASE_ONLY=1` 构建通过。
- 新增 W5 节奏检查脚本：8 个知识点 ×（自动播放预期 + 手动下一步可用）共 **16 项全过**，无 console error。
- 跨板回归：`autoPlay: false` 的四个板必须不动、W5 必须推进——5 项全过。第一次跑时我把
  认证链和 OAuth2 的预期写成了「应该推进」，差点把既有正确行为报成回归；预期必须按各板实际配置写。
- W9 板 72 项断言全过（含 4 项节奏断言）。
- 本轮无视觉改动，未重新采集截图；未发布 Pages。

## 第八轮：验收判据加严（2026-08-18，用户反馈「每块板都充斥巨量文字」）

### 起因

W9 板十三块 + W10 板四块落地之后，本人指出：每块板都是密集文字，没有配套视觉辅助，
提出「这个标准可以适当调整」。

核对原文之后，结论是**标准不该放宽，该补的是被执行时丢掉的半句**。
上面四条约束写的都是**成果判据**——防的是「加了个图标就宣布这块做完了」，
从来没有禁止视觉编码。但限定词「当作可视化成果」在执行时丢了，
变成了「能不用就不用」；再加上「图形只负责关系，结论保留文字」这一条，
十七块累积下来的结果是：**每块板都能通过「不读完整段也能判断」这条验收，
因为结论被写成了一句大标题**——版面本身不承担它。

### 改了什么

1. **验收判据加严一格**：
   - 旧：用户不读完整段落也能做出判断。
   - 新：**遮住这块板的标题与结论段，仍能答出它的验收句。** 答不出即视为未完成。
2. **给每条禁止配肯定面**（见「目标与约束」，四条）。
3. **「图形只负责关系」改成「图形负责结论本身」**。这一条是本轮返工的直接起因。

### 配套的复核仪式（人工，不做机器断言）

每块板落地后，在截图核对那一步加一道：**遮住 `h3` 与结论段（`.w*-note`），
自问一次该块的验收句。**

不写成脚本断言是有意的——「结论有没有被版面承载」是判断题，
硬做成代理指标（比如「非文字编码元素数 / 正文字数」比值）只会被凑数满足，
那正好是 W10 板 ⑥「三项自动检查漏掉什么」讲的同一种失效。
**这道闸的强制力等级是「人」，写下来是为了让它至少存在，不是为了让它自动化。**

### 落地范围

- W10 板已落地四块（⑥ ① ③ ②）按新标准返工，见
  [`week10-visualization-plan.md`](../../week10-observability/notes/week10-visualization-plan.md) §12.7。
- **W9 板十三块记 BACKLOG**，等主线走到 W10 D5-D 那次「按最新数据修正展板的过时结论」时一并处理——
  它 8/14 刚做过一轮截图核对与存量清扫，现在再返工一次成本高于收益。
- W10 尚未做的 ④ ⑤ 两块直接按新标准落地，不走「先做旧标准再返工」。

### 落地范围的补正（2026-08-20，本人指出波及面更广）

**本人的判断**：「不要过分追求动效」那句话的波及范围可能比当时认为的更广——
大段文字展示不是 W9/W10 两块板的局部问题，而是**所有板都可能需要重新校对**；
这两周只是集中爆发的地方，因为这两周的板内容密度最高、看得最明显。

**核对之后确认这个判断成立，证据有三条**：

1. **上面那份「落地范围」本身就只覆盖了两块板**：W10（当天返工）+ W9（记 BACKLOG 延后）。
   其余七个 tab 的板**从未进入过新判据的范围**——不是复核后判定不用改，是从来没被点名。
2. **git 上的冻结时间对得上**：`W3Board` / `W5Board` / `W6Board` / `W6Day4Board` /
   `AuthBoard` / `InterviewBoard` / `Dashboard` 七个组件的最后一次改动**都停在 2026-08-12**，
   早于 8/18 的判据加严。也就是说它们是在**「图形只负责关系、结论保留文字」**那条旧规则下建成的，
   而那条规则已经在 8/18 被改成了「**图形负责结论本身**」。**旧规则下的成品，按新判据当然是欠着的。**
3. **8/18 那次「存量清扫」清的不是这件事**：commit `d24038b`「把 W9 的排版结论推到全站八个 tab」
   只改了 `styles.css`（排版 token 与焦点层级），**没有碰任何一块板的内容形态**。
   排版统一了，不等于结论被版面承载了——这两件事在 8/18 当天被顺序处理，容易被记成同一件。

**因此「重新校对」的对象是八块板，不是 W9 那十三块**：
`AuthBoard` / `OAuth2Panel` / `W3Board` / `W5Board` / `W6Board` / `W6Day4Board` / `W9Board` / `W10Board`。
（W10 板是唯一全程按新判据建的，仍要过一遍——它 8/20 新增的 ⑤ 里有四格「未实测」，属数据时效那一维。）

> **范围修正（同日）**：首次列这份清单时把 `Dashboard.tsx` 算了进来，它属 **8080 管理后台**不是展板，已剔除；
> 同时补入漏掉的 `OAuth2Panel.tsx`。「学习笔记」tab 的 Markdown 正文按下一节「明确保留文字」排除。
> 「面试准备」板（`InterviewBoard.tsx`，末改 8/12）同属旧口径，但它是**材料索引**不是关系图，
> 是否纳入由核查线先判定，不默认排进上面八块。

**复检判据不新造**，就用已有的两样：

| # | 四条肯定面（见「目标与约束」） | 落到复检时问什么 |
|---|---|---|
| 1 | 同类对象 ≥ 3 个要相互比较 | 这块板里有没有让读者在脑子里建表的地方？ |
| 2 | 量可比且差距本身是结论 | 差距是结论的那几处，出图了吗？ |
| 3 | 信息有序或分层 | 用的是位置编码，还是「第一 / 第二」这类序号词？ |
| 4 | 正文超过一屏 | 有没有一个不读正文就能读到的锚（一眼读数 / 矩阵 / 尺）？ |

加上第八轮的复核仪式：**遮住 `h3` 与结论段，仍能答出这块板的验收句**。答不出即欠着。

**规模与排期：单开一条线，不跟学习主线走**（2026-08-20 本人决定）。
八块板 × 逐块四问属**代表页级别的内容返工**，装不进任何一天的学习时间盒；
更重要的是它**没有理由跟学习日程绑定**——展板欠着的是存量，不随今天学了什么而变化。
因此独立成线，执行文档见 [`showcase-audit-line.md`](./showcase-audit-line.md)：
按 15–20 分钟一块排队消化，**阶段一只判定不改**，判定全部做完再一次性决定改哪几块。
**不许在没有判定结论之前直接开改**——那会变成第二次「凭印象铺开」，和这次要修的失效同型。
（原计划让 W10 D5 块 G 做「抽样 3 块定性」，同日一并移出：D5 只保留数据时效那一维。）

**一条要留下的判断**（`LEARNING-PROTOCOL.md` §9 第 2 条，最多留一个思维模型）：
**只有禁止面的规则，会被执行成「能不用就不用」，而且波及范围比下规则的人预期的大。**
8/18 已经补了肯定面，这次补的是**范围**——补肯定面的时候，要同时问一句
「**这条旧规则已经产出了哪些成品，它们要不要跟着一起改**」，否则新规则只对新东西生效，
存量默默留在旧口径里，直到下一次集中爆发才被看见。

## 明确保留文字的页面与内容

- 前端笔记正文：Markdown 适合连续论述、代码、引用和可搜索的细节，图形化收益不足。
- 各实验的环境、样本量、命令和原始测量：图形不能替代可复核证据。
- 事实 / 推断 / 未测量的限定语：必须与图相邻保留，不能只藏在 tooltip。
- Node.js 错误传播的机制差异：适合短表格或文字边界，不应合并成一张看似统一的错误流。
- OAuth2 和认证的安全前提：图显示顺序，文字保留 `state`、凭据暴露面和“未接真实 provider”等限制。

## 依赖与风险

- 第二阶段依赖第一阶段 token 与状态语义保持稳定；若每页重新定义颜色，将失去跨页一致性。
- 运行时专题关系类型较多，风险是做成一张过载总图。应复用参与者底图，按专题突出局部关系。
- 证据矩阵风险是把未知项画成否定结论；文案必须区分“没有证明”和“证明不成立”。
- 手机端风险是用简单纵向堆叠破坏并行、分叉和反馈。需要语义变体，而不只是 CSS 缩放。
- 过早抽象共享组件可能增加维护成本；只有相同结构在两个以上页面稳定复用后才提取。
- 当前三个阶段改动仍在工作区中；新对话开始时必须先读 `git status --short`，不得覆盖或回退这些改动。

## 新对话恢复入口

新对话开始后按以下顺序恢复：

1. 阅读根目录 `AGENTS.md`、`LEARNING-PROTOCOL.md`、`LEARNING-STATE.md`。
2. 阅读本文，确认三个阶段均已完成，不再默认新增展示优化范围。
3. 运行 `git status --short`，保留现有未提交改动。
4. 只有收到新的展示需求时，才读取对应组件并在当前基线上做最小修改。
5. 启动或复用 Vite 开发服务器，先检查当前基线，再编辑。
6. 每完成一个代表视图就运行类型检查和小范围视觉验证；不部署，除非用户另行要求。

下一执行入口：**三阶段、第四轮审计、W6 Day 3 扩展、第五轮逐帧统一与第六轮（Day 4 板 + 面试板）均已收口**；
第五轮与**第六轮（Day 4 板 + 面试板）均已发布 Pages**，第六轮由本人初步核验线上更新完成。
展示前端暂无默认后续任务。挂起项两条，均需用户单独确认：

1. 认证主链是否改成真泳道（见第四轮「明确不做」）。
2. 知识点 4 的内存实测。当前展板只有相对示意，因为本日没有实测数据。补这份数据属于
   `AGENTS.md` 黑名单（W5「流与背压 demo」），AI 援助上限 L2，必须由仓库主人自己实现和运行；
   AI 只能给实验设计，以及在拿到真实数字后把它接进展板。实验设计要点：固定文件大小与并发数，
   分别测 `readFile` 整块与 `createReadStream` 流式两组，各记 `rss` / `heapUsed` / `external` /
   `arrayBuffers` 峰值与首字节时间，同一台机器同一 Node 版本连测三轮取区间而非单次值。
   拿到数据前，知识点 4 继续保持「相对示意，无绝对数值」，不得填入估算数字。

这两条的优先级已收进根目录 `BACKLOG.md`（分别为 P2-2 与 P2-1）；学习主线仍以根 `LEARNING-STATE.md` 为准。

---

## 第九轮收口 · 展板状态核查线（2026-08-21）

第八轮「落地范围的补正」立了一条独立线（[`showcase-audit-line.md`](./showcase-audit-line.md)）。
那条线现已按其 §8 关闭，这里只留收口记录，判定表与每轮实测留在执行文档里。

**阶段一**：八块板两维判完——视觉编码（遮住 `h3` 还能不能答出验收句）与阅读负担
（8/21 补入的主路径 / 重复量 / 文案可读性三问）。阅读负担这一维**做成了实测**：
把展板构建后跑起来，逐个量了八块板加面试板**全部 45 个专题**的正文区高度，
方法先拿已有的七个数字对照验证（六格差 ≤ 0.08 屏、「13 个专题中 11 个超过一屏」完全复现）。

**阶段二 · 八轮**：改了六轮、判掉两轮，另有两处由本人裁决。

| 对象 | 结果 |
|---|---|
| `W9` / `W10` 常驻外壳 | −64% / −56%，两块板的专题正文起点因此进了第一屏 |
| `W10` ⑤ 演练分档 | 5.91 → 4.07 屏 |
| `W9` `evidence` 契约验收 | 4.27 → 1.71 屏 |
| `W6Day4Board` | 5.12 → 2.77 屏 |
| `W10` ⑦ 红过才算数 | 4.35 → 3.00 屏 |
| 共性欠口②（量只写成数字） | `W3Board` + `W6Day4Board` 补行内长度编码 |
| 共性欠口①（互斥切换） | `AuthBoard` + `W6Board` Day 3 共用 `StopMatrix`，三条路径的停止点摆到同一根轴上 |
| 第 4 步四个小专题 · `W6Board` 证据矩阵 | **核实后判定不做**，零改动 |
| `OAuth2Panel` 凭据卡 | **本人裁决不改** |

**全程共同口径**：内容零删除（每轮用「全展开高度 + 逐块字数」双重复核）、
`yarn verify:board` 恒为 **421/421**、桌面与窄屏 × 深浅色每轮各核一次、**未部署**。

**留下的两条判断**（比改动本身更值得留）：

1. **按新规则清算存量时，先逐块判定再决定改哪块，判定本身就会把规模改小。**
   阶段一判完，规模从「大」回到了「P2 排得下」——而在判定之前，连这条线自己的开篇都默认它只会更大。
2. **代理指标只能决定「先看哪里」，不能决定「改什么」。** 这条线上栽过三次：
   字数比值、选择器命中数、以及「有个 `details` 没有 `open`」。
   后两次都直接生成了工作项，又都在动手前的核实阶段被判掉。
   由此补一条纪律：**凡是自己记下的欠项，动手前都要按验收句把整块板重看一遍**——记的时候看的是局部。

**不属于本线余额的两件事**：`W10` ⑤ 要不要做第二轮（它现在是全站最重，但剩下的重量在常驻主路径上，
是个新问题）；`W10 D5` 若要新开 tab（本人另开对话处理）。



---

## 第九轮：把结论交还给版面（2026-08-22，用户反馈「没有什么动效和图表」）

### 起因

W1–W2 缺口补齐、新增「服务端架构」板之后，本人看截图指出：
「没有什么动效和图表，像 express 四层分层数据怎么流转、mongodb 聚合这些不是很适合做成可视化吗」。

核对后确认成立，而且**违反的是第八轮已经定下的判据**——「遮住这块板的标题与结论段，
仍能答出它的验收句」「图形负责结论本身」。新板六块里有四块是表格加正文，
遮住标题就答不出。这与 8/20 那条「波及面更广」的补正是同一个机制：
判据改了，但新写的板仍按旧手感落地。

### 判定：哪些该改，哪些本来就该是表

| 内容 | 结论由什么承载 | 判定 |
|---|---|---|
| 一次请求穿过哪些层、每一跳携带什么 | 位置 + 方向 | 改流动图 |
| 中间件进入与离开为什么反序 | 嵌套几何 | 改同心环 |
| 七类错误汇聚到一个出口再扇出 | 汇聚 / 扇出的拓扑 | 改汇聚图 |
| 聚合每个 stage 改变了文档的什么 | 字段增删本身 | 改逐阶段形状变形 |
| 四层各自碰什么 / 不碰什么 | 二维分类 | **保持表格** |
| 四段逻辑归 HTTP 还是归业务 | 二维分类 | **保持表格** |

判据是「结论是关系还是分类」：关系（顺序、方向、包含、汇聚）用几何，
分类（这一格属于哪一类）用矩阵。把分类硬画成流程图同样是错的。

### 落地范围

**新板（`W2Board`）四块 + `W3Board` 聚合块**——见
[`w2-architecture-visualization-plan.md`](w2-architecture-visualization-plan.md) §7。

**存量两块**（本轮把同一形态倒回 8/12 的旧口径板）：

| 板 | 原形态 | 改成 |
|---|---|---|
| `AuthBoard` 端到端认证链 | 四条泳道只是一排静态表头；每段消息的收发双方写在文字里，七段消息画在同一个 x 位置 | **泳道序列图**：四条泳道是四列，每段消息画成一根跨列箭头，起点列 = 发出方、终点列 = 接收方，载荷骑在箭头上。于是「长期密码只在前三段」「受保护请求整段停在管道列内（两段自环）」不用读文字就看得出来 |
| `W6Board` 全栈轨道 | 八段拉成一条直线，看不出数据还要回到浏览器 | **U 形折返**：01–07 出站排一行，08 React render 落到第二行第一列，一条虚线把回程画出来；六个交接点各挂一枚令牌显示**这一段交出的值**（直接引用同页下方的交接契约表，不另写一份，05 query validator 在那份表里没有对应条目，就留空不补） |

`AuthBoard` 顶部的 `StopMatrix`（三条路径 × 七段的停止点）本来就是合格的非文字编码，未动。

### 新增的结构断言

图形承载结论之后「图塌了 = 结论没了」，因此几何本身要有断言：

```text
W4 泳道序列 七段消息 / 四条生命线 / 两段自环 / 两段反向
W4 泳道序列 箭头跨列与 from/to 一致（逐段比对 gridColumnStart-End，期望 1-3|2-4|1-3|1-5|4-5|3-5|4-5）
W4 手机档 文字路由仍在（窄屏画不出跨列箭头，退回文字版，信息不丢）
W6 轨道 八段 / 一段返回 / 08 换到第二行 / 08 回到第一列 / 交接值令牌在场
```

「箭头跨列与 from/to 一致」这条是本轮最值钱的一条：泳道图一旦列错，
读出来的结论会**反过来**（比如把「JWT 签发回客户端」读成「客户端签发给 Auth Service」），
而这种错在截图里很难一眼看出。执行时它立刻抓到我自己写错的一处——
反向箭头我按「一段」断言，实际是两段（签发 JWT、requireRole 查库），
断言红了才发现是断言写错、不是页面错。

`yarn verify:board` 从 581 升到 **596/596**。未部署。
