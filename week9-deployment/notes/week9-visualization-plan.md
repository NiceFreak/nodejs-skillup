# W9 部署链路可视化方法与执行记录

> 建立：2026-08-12（Asia/Shanghai）
> 上游事实：`day1-contract-freeze.md`、`day2-host-and-node-service.md`、`day2-result.md`、`day3-finish-d2-and-db.md`、`day4-http-reverse-proxy.md`、`week9-roadmap-d1-d4.md`、`LEARNING-STATE.md`
> 对标基线：`week8-fullstack/notes/visualization-optimization-roadmap.md`（三阶段 + 第四轮审计 + W6 Day3 扩展 + 第五轮逐帧统一 + 第六轮 Day4/面试板）
> 本轮范围：方法（§0–§11）+ **阶段 1 代表页已落地并验证**（§12）。不改 week9 笔记原文；**未发布 Pages**。
> 承载前提：`week8-fullstack/` 展示前端属 `AGENTS.md` 白名单（展示资产，AI 可搭建维护）；W9 的技术结论本身来自本人执行记录，展板只渲染既有事实，不产生新结论。

---

## 0. 这份方法要解决的问题

W9 的笔记已经很完整（2252 行，含 mermaid 拓扑图、端口表、执行记录、12 条认知修正），但它现在的形态有三个可理解性缺口：

1. **同一件事分散在多处**：端口表在 D1 §5.2、roadmap §1、D4 §5 各出现一次，状态还不同（D1 时 80 未落地、roadmap 里已落地）。读者要自己合并。
2. **时间过程被压成表格**：快失败 vs 慢失败、StartLimitBurst 的 60s 窗口计数、reboot 后自起——这些本质是"随时间推进的状态变化"，用表格只能给结论，给不出"停在第 3 次重启那一刻"的能力。
3. **实测与推演混排**：D1 §5.3 的两条失败路径写得和 D3 B4 的实测记录一样确定，但**502-B（proxy_pass 写错）和 500（运行中挂库）从未真正注入过**。纸面上有"根因/先查哪里"的段落标题保护，做成图之后极易被读成"四条路径都验证过"。

week8 已经把这三类问题的解法建构完成了（统一视觉 token、`framePlayer` 逐帧、证据边界三列 + 口径标注）。本方法的工作不是发明新语法，是**把 W9 的内容接到已经验证过的语法上**，并只在现有语法确实表达不了的地方做最小扩展。

---

## 1. 判据：什么算"可理解"

沿用 week8 的做法——验收句是"用户不读完整段落也能做出判断"，不是"页面变好看了"。W9 板要让人能当场答出下面六句，答不出就是没做到：

| # | 验收句 | 对应板块 |
|---|---|---|
| 1 | 公网请求最多能摸到哪一层，3000/27017 关掉公网是"没在用"还是"走内线" | ① 信任边界 |
| 2 | 两个 502 外部现象相同，**第一个排查动作**是什么，凭什么判据分叉 | ② 故障分叉 |
| 3 | 配置写错会不会无限重启，DB 挂了会不会被限速停住，为什么这两者结局不同 | ③ systemd |
| 4 | B2 的 200 **没有**证明什么（不证明 Nginx、不证明公网可达） | ④ 端到端验收链 |
| 5 | 反代后哪三类信息失真，为什么本应用决定不补传 | ⑤ 反代决策 |
| 6 | D1 冻结的哪条契约到今天仍然欠着 | ⑥ 契约销账 |

**可理解性是最基本要求**这条，在 W9 有一个 week8 没有的具体含义：W9 笔记的术语密度是全仓最高的（roadmap 自己写了 §8 附录做白话对照）。所以本方法把"术语 ↔ 白话"当作一等公民（见 §2.2），不当作 tooltip 装饰。

---

## 2. 对标 week8：复用什么、新增什么、不碰什么

### 2.1 直接复用，不重新发明

| 资产 | 位置 | W9 怎么用 |
|---|---|---|
| 视觉 token（flow / resource / success / controlled / failure / unknown） | `styles.css` `--viz-*` | 状态码与链路一律服从；**502/500 归 failure（异常），不是 controlled** |
| `useFramePlayer` + `FrameTransport` + `FrameNarration` | `framePlayer.tsx` | systemd 双路径、端到端链、故障分叉的停止点推进 |
| 展示 / 复习二态 | `Showcase.tsx` + 各板 `mode` prop | 展示态答"模型是什么"，复习态先给条件再揭示 |
| hash 深链 `mode/tab/topic` | `App.tsx` `parseHash` / `buildHash` | 新增 `tab=deploy` + 六个 topic；reveal 状态保持组件局部，不进 URL |
| tablist 键盘契约 | `tabs.ts` | 板内专题切换直接复用 |
| 证据边界三列（观察结果 / 能证明 / 不能证明） | W6Board、`evidenceSets.ts` 的 `proves` / `limits` | W9 每块板的结论区照抄这个结构 |
| 纠错三段式（❌ 初始说法 → ⚡ 错在哪 → ✅ 修正） | `W6Day4Board.tsx` `Corrections` | 12 条认知修正直接套 |
| Markdown 笔记直读源文件 | `MarkdownNotes.tsx` `?raw` | week9 五份笔记接进 notes tab，不维护副本 |
| 手机语义变体规则 | roadmap 3.1 | 三跳链路 / 端口表 / 逐帧轨道各自的手机形态 |

### 2.2 W9 需要的三处新增（附"为什么现有语法不够"）

**新增 1：信任边界的面（不是新颜色）**

现有六个 token 里没有"公网可达 vs 仅 loopback"。这是 W9 全周的底层心智，必须能一眼看出。

- **不新增颜色**——roadmap 明确"不把增加颜色当作可视化成果"，且颜色不能单独承载信息。
- 用**图形语法 + 文字标签**：公网面用实线包围框标「公网可达」，loopback 面用点线包围框标「仅本机」，ufw 画成包住公网面的闸门。
- 待做的 443 走 `unknown`（灰 / 虚线）+ 文字「D4-HTTPS 待做」，与已落地的 80 区分。

**新增 2：证据档位标签（W9 独有的必需品）**

week8 第五轮已有「按实测数值脚本化」/「模型示意」/「相对示意」三档。W9 需要把它做成**每个可视化对象都必须挂的显式属性**，因为 W9 的推演比例远高于 week8：

```text
measured   实测    有命令输出 / journal / PID / 时间戳可追溯
derived    推演    从已冻结契约或代码推出，未真正跑过一次
pending    待做    D4-HTTPS、时区修正、Java stretch
```

数据结构里每条事实带 `evidence: "measured" | "derived" | "pending"`，渲染时强制显示，不允许省略。**这是本次最容易做错的一条**，单独在 §7 展开。

**新增 3：术语 / 白话切换（建议做，排在阶段 3）**

roadmap §8 已经把 30+ 个术语配好了白话（信任边界 → 外面能摸到哪一层；反代 → 门卫转发；快失败 / 慢失败 → 崩溃循环 vs 等待依赖）。这份对照表本身就是 W9 笔记里可理解性含量最高的资产，但它现在是文末附录，读者不会在看拓扑图时翻回去。

- 形态：板级一个「白话」开关，把图内**标签文本**在 `term` / `plain` 之间切换；**不切换成两套图**，舞台结构不变。
- 数据：每个概念在数据层同时带 `term` 与 `plain` 两个字段，来源锁定 roadmap §8.1 / §8.2，不新造白话。
- 为什么不是装饰：它改变的是认知任务的入口难度，不是外观；且 §8.3 明确要求"守住已有好类比（门卫 / 管家 / 仓库 / 前台）"，切换器正好是这条纪律的执行面。
- 为什么排阶段 3：它跨所有板，应该在板结构稳定后统一加，否则每块板各接一次会漂移。

### 2.3 明确不碰

- **不动 `evidenceSets.ts`**。它现在是 Day4 板与面试板共享的三组实验（W3/W5/W6），两处都会遍历全量渲染。往里塞 W9 组会让那两块板凭空多出一组它们没有叙述的实验。W9 事实另建 `w9Facts.ts`。
- **不重画 W6 Day3 的八段跨层轨道**。W9 的链路看点是**跨进程 / 跨主机**（本地 → 公网 → Nginx → Node → Mongo），层内中间件顺序（validateToken → requireRole → controller）已经在 W6 板画过；W9 板在该处只做收束视图 + 交叉引用，不复制。
- **不引图表框架**、不加卡片 / 图标 / 阴影当成果、不为少量数字造柱状图。
- **不改 week9 笔记原文**。笔记是事实源，展板只渲染。若渲染时发现笔记内部有冲突（例如端口表三处状态不一致），做法是**在笔记里修正事实**并记录，而不是在展板里悄悄取一个版本。

---

## 3. 内容盘点：哪些适合图，哪些必须留文字

### 适合做图（关系 / 顺序 / 分叉 / 随时间变化）

| 内容 | 为什么图更好 |
|---|---|
| 三跳链路 + 端口表 + ufw | 关系与包含面，文字要读三段才能合并 |
| 四种失败结局的停止点 | 同一张图切换停止点，正是 week8 认证链 401/403/200 已验证的形态 |
| StartLimitBurst 的 60s 窗口计数 | 时间过程，表格给不出"停在第 3 次重启"的能力 |
| B2 / B3 / D4⑤ 三个验收环覆盖范围 | 三个环的差别是**覆盖段不同**，并排画一眼可见，分三段读则像重复记录 |
| 反代三类信息失真 × 是否消费 × 本次决定 | 三维判断，矩阵优于段落 |
| 契约 → 销账证据 → 仍欠 | 矩阵；且能直接暴露"HTTPS 仍欠" |
| 内存闸门（预测 450MB vs 实测 187.4MB） | 有对照价值的两个数，不是"少量数字硬做图" |

### 必须保留文字（照搬 week8 的同名约束）

- 每次实测的**命令、原始输出、journal 原文**——图不能替代可复核证据。
- **实测 / 推演 / 待做**的限定语必须与图相邻，不能只藏在 tooltip 或折叠区。
- **凭据纪律与安全推理**（bcrypt 12 轮、`.env` 600、明文不落盘、`history -c`、`--preserve-env`）：这些是纪律叙述，图形化收益不足。
- **AI 辅助记录与 L1/L2 边界**：属学习协作记录，不进展板可视化，随笔记原文在 notes tab 呈现即可。
- **Nginx 的四类职责与云厂商矩阵（day4 §10）**：概念论述，适合连续文字；notes tab 直接读原文。

---

## 4. 板块设计（六块）

统一填写模板：认知问题 / 形态 / 展示态 / 复习态 / 数据来源 / **口径边界**。

### ① 信任边界与端口（`topic=boundary`）

- **认知问题**：端口表分散三处且状态不同；"3000 ❌ 不是没在用，是 loopback 内线"这条注解要靠读。
- **形态**：三跳链路图，外套两层面——实线框「公网可达」（22 / 80 已落地，443 灰虚线待做）、点线框「仅本机」（3000 / 27017）；ufw 作为闸门包住公网面。端口表五行与链路联动：点任一行 → 对应跳高亮，并显示「监听地址 / 谁能发起 / 断了什么现象 / 不开的后果」。
- **展示态**：全图 + 五行全展开，443 明确标待做。
- **复习态**：先只给端口号与监听地址，问"哪些公网摸得到、3000 关掉会怎样、27017 为什么不开也能用"，答后揭示。
- **数据**：D1 §5.2 端口表 + §5.1 每跳表；roadmap §1（D4 后状态）；D4 §5 ufw 实际输出。
- **口径边界**：
  - ufw 只是**一层**。腾讯云控制台安全组是另一层（D4 §5「归因预备」已写）。图上必须标"两道闸门"，否则会被读成"ufw 放行 = 公网可达"。
  - 443 从未落地，任何形态都不能画成已完成。

### ② 故障分叉（`topic=failure`）—— 代表页，第一阶段先做这块

- **认知问题**：两个 502 外部现象完全相同，靠"第一个排查动作"区分。这是 W9 迁移价值最高的一条，现在是三段并列的 text 块，靠读者自己对齐。
- **形态**：**同一条链路，四态切换**（正是 week8 认证链 401/403/200 的已验证形态）：

  | 路径 | 停止点 | 外部现象 | 第一个排查动作 | 关键判据 | 证据档 |
  |---|---|---|---|---|---|
  | 正常 | 走完四跳 | 200 + JSON | — | — | measured |
  | 502-A 启动前挂库 | Node 未起，3000 无监听 | 502 | `systemctl status nodeapp` | **failed** + `ss` 看不到 3000 | derived（同形态近似实测，见下） |
  | 502-B 反代地址写错 | Nginx → Node 转发失败 | 502 | 同上 | **running** + `ss` 看得到 3000，Nginx 日志报 upstream | derived |
  | 500 运行中挂库 | 请求进得来，查库失败 | 500 | `journalctl -u nodeapp` | Node running，Mongo 侧异常 | derived |

  **状态码走到停止点之后才揭示**（照搬 week8 第五轮：从一开始就摆着的标签 → 变成推导结论）。
- **展示态**：四条路径可切，默认正常路径。
- **复习态**：先只给"外部现象 = 502"，让人说出下一步查什么、两种判据分别指向哪条；再揭示。
- **口径边界（本板最关键）**：
  - **只有 502-A 有同形态近似实测**——D3 B4 第二轮确实观察到 `failed` 停住且 3000 未监听，但**注入源是 JWT_SECRET 改短，不是 Mongo 挂**。板上必须写清"同形态近似，非本路径直接注入"。
  - **502-B 与 500 从未注入过**，是 D1 §5.3 的设计推演。一律标 `derived`。
  - 四条路径不能一律画成"已验证"。这是本次做成图之后最容易失真的一处。

### ③ systemd 失败模式（`topic=systemd`）—— 逐帧

- **认知问题**：快失败 vs 慢失败是面试常问点，且第一轮注入设计被 `Wants` 语义直接打掉——这两件事都是时间过程。
- **形态**：双路径逐帧时间轴（复用 `framePlayer`），并排同一时间轴 + 实时计数器「60s 窗口内重启次数」。
  - **快失败帧**（按 B4 实测脚本化）：`start` → 启动校验① `JwtSecretConfigurationError` → `exit(1)` → `RestartSec=10` 退避 → 重启计数 1…5（PID 4600 → 4659 → 4733 → 4839，均 status=1/FAILURE）→ ~42s 撞上 `StartLimitBurst` → `failed` 停住 → journal 三条决定性日志（`restart counter is at 5` / `Start request repeated too quickly` / `Failed to start`）。
  - **慢失败帧**：`connectDB` 等 `serverSelectionTimeoutMS=30s` → 60s 窗口内到不了 5 次 → 一直 `restarting`，DB 恢复后自动拉起。
- **附一段「注入设计盲区」三段式**：❌ 原设计（stop mongod → start nodeapp 观察失败）→ ⚡ 被 `Wants` 连带拉起否决（pid 3556 与 3557 相邻同秒）→ ✅ 改快失败注入。这条比结论本身更值得复习：它记录的是**实验设计本身会出错**。
- **展示态**：默认快失败，可切慢失败。
- **复习态**：先问"配置写错会不会无限重启 / DB 挂了会不会被停住"，再放帧。
- **口径边界**：
  - 快失败 = `measured`（PID / 时间 / journal 三类证据齐）。
  - 慢失败 = **`derived`**，从 `serverSelectionTimeoutMS=30s` + 限速窗口推出，**没有真正跑过一次**。标「模型示意」，与 week8 知识点 8（shutdown 顺序取自代码但非计时实测）同一处理。
  - `RestartSec=10s` 是配置值；D2 观察到的 `~11s` 退避是实测值。两者不能混写成一个数。

### ④ 端到端验收链（`topic=chain`）

- **认知问题**：B2 / B3 / D4⑤ 三次端到端读起来像三段重复记录，实际**覆盖段完全不同**。
- **形态**：一条链，三个验收环叠加显示各自起点与覆盖范围：

  ```text
  本地开发机 ──公网──> Nginx:80 ──loopback──> Node:3000 ──loopback──> Mongo:27017
  └────────────────── D4⑤ ─────────────────────────────────────────────┘
                                └──────── B2 / B3（从 127.0.0.1:3000 起）────┘
  ```

  链内逐帧走：`register 201` → 提权 `modified: 1` → `login 200 + accessToken` → `GET /reports/monthly-sales?months=6` → `200 + 真实聚合`。
- **展示态**：三环并列，点任一环显示"覆盖到哪 / 明确不覆盖哪"。
- **复习态**：先给某次验收的起点，问它**没有**证明什么。
- **数据**：B2（2581 单 / ≈155 万 / 8 月 314 单）；B3（`?months=1` → 8 月 314 单 / 191442.37，reboot 后）；D4⑤（2026-03 起 258 单 / 146988.82，经公网）。
- **口径边界**：
  - **三组数字口径不同**（months=6 / months=1 / 不同时间点），**不能并排当趋势读**。板上要显式写这条。
  - **时区观察点必须紧邻本板**：聚合 `$year/$month` 按 UTC、服务器 CST(UTC+8)，凌晨订单跨月归因（7 月 3 单）。不放这里，读者会把"7 月怎么冒出 3 单"当成数据错误。该项仍是 **D5 待决策**，标 `pending`。
  - B2 的 200 **不证明** Nginx 可用、**不证明**公网可达——这正是本板存在的理由，要写在环上而不是脚注。

### ⑤ 反代 header 决策（`topic=proxy`）

- **认知问题**：纯推理内容，笔记里是 §4.2 一段文字；但面试会问，且"XFF 与 trust proxy 必须成对引入"是易错点。
- **形态**：三行 × 三列矩阵——
  | 失真信息 | 理论补传 | 本应用是否消费 | 本次决定 |
  |---|---|---|---|
  | 客户端真实 IP → `127.0.0.1` | `X-Real-IP` / `X-Forwarded-For` | 否（logger 只记 method/url/状态码/耗时） | 不配 |
  | 原始协议 → 恒 `http` | `X-Forwarded-Proto` | 否 | 不配 |
  | 原始 Host → `$proxy_host` | `Host $host` | —（Nginx 默认行为需覆盖） | **配** |

  下挂一条判断规则：应用是否读 `req.ip` / `req.protocol` / `req.hostname` / `req.secure` → 读则 **XFF + `trust proxy` 成对引入**（分开做会拿到不可信的假 IP，且 `proxy_set_header` 的覆盖语义用于防客户端伪造）；不读则只配 `Host $host`。再给落盘的四行 server 块。
- **展示态**：全矩阵 + 结论 + 配置。
- **复习态**：先只给"应用代码读了什么"，让人自己推该配哪些头。
- **口径边界**：`Host $host` 已实测生效（`curl -I` 200 + `Server: nginx/1.18.0` + `X-Powered-By: Express`）；XFF / `trust proxy` 是**未采用方案**，不能画成已配置。

### ⑥ 契约销账 · 认知修正 · 资源闸门（`topic=evidence`）

三段收束，共一块板：

**a. 契约 → 销账证据矩阵**
D1 冻结的每条契约 → 哪天由什么证据销账 / 仍欠。含「启动即失败契约」（问题 9 选 A → B4 销账）、测试凭据轮换（D4① 闭环）、**HTTPS 仍欠**、时区修正仍欠（D5）。这块板要让"还欠什么"一眼可见。

**b. 认知修正 12 条**（❌ → ⚡ → ✅，复习态先只给初始说法）
沿用 Day4 板的 `kind` 分类（结论超出证据 / 调用顺序 / 概念混淆 / 证据不成立 / 边界归属），但 **W9 必须补一类：`experiential`（工具行为经验）**。

理由：`read -s` 网页终端读不到 stdin、`sudo` 默认 `env_reset`、ESM 解析锚点（`node -e` 锚 cwd / 文件脚本锚文件位置）、`needrestart` 提示——这些不是推理错误，是 `AGENTS.md` §4 明确定义的"必须真实遇过一次才知道"的经验知识。把它们和"结论超出证据"混成一类，等于告诉复习者"你本该推出来"，与协作规范直接冲突。

**c. 资源闸门 + 生产对照**
一条容量条（total 1931MB / available 1388MB / mongod 187.4 / nodeapp 83.9 / nginx 8.5，**Swap = 0 显式标注**）+ 预测对照（D3 §2.2 预测 WiredTiger ≈450MB「物理内存 50% 上限」→ 实测 187.4MB ≈40%，实证 cache 按需增长不预分配）+ 绿灯判定。旁边两列：「已做（真实生产也在做）」/「缺（W10 监控 / W11 CI-CD / 备份未排 / 多环境未排）」，并保留 day4 §10.2 的判断——**是成熟度差异，不是结构错误**。

- **口径边界**：`available 1388MB` 是**装 Nginx 之前**的实测；装后未重测，"仍 ~1350MB"是推算，标 `derived`。Nginx 的 8.5M 是安装时读数。

### 导航与笔记接入

- **学习演进导航**加一个节点：`W3 数据查询 → W4 身份边界 → W5 运行时 → W6 测试证据 → W9 上线运行`（复习态末尾仍是「产出 · 讲得出口」）。W9 是中性技术内容，**展示态也显示**（与只属复习态的面试板不同）。
- **notes tab** 接入 week9 五份笔记：roadmap 置首（浓缩地图最常翻），其后 day4 / day3 / day2 / day1 / plan，直接 `?raw` 读源文件。
  - **成本已核对**：`MarkdownNotes` 整体是 `lazy` 的，这些 raw 文本落在 notes chunk 而非入口 chunk，不影响首屏。但 week9 五份合计约 160KB 原文，notes chunk 会明显变大。落地时实测一次；若 gzip 后增量超出可接受范围，再改 `import.meta.glob` 按需分片——**不预先优化**。

---

## 5. 承载位置：week8 展板加一个 tab（推荐）

三个候选里只有一个成立：

| 方案 | 判断 |
|---|---|
| **A. `week8-fullstack` 展板新增 `tab=deploy`** | ✅ **推荐**。展板标题本就是"Node.js Skillup · 学习展板"，不是 week8 专属；W6 Day4 板与面试板已经是"把 week8 之外的内容接进来"的先例。token、`framePlayer`、hash 深链、手机变体、部署管线全部复用，跨周一致性天然成立 |
| B. 新建 week9 独立前端 | ❌ 要复制 token 与 `framePlayer`，两套必然漂移；且 Pages 只有 `/skillup-week8/` 一个子路径，发布链路要重做 |
| C. 只在 notes tab 放 markdown | ❌ 等于没做可视化。mermaid 在 notes 里能渲染，但给不出切换、逐帧和证据档位 |

**副作用与边界**：新 tab 是纯静态数据渲染，**不新增任何 `fetch` / API client**，`SHOWCASE_ONLY` 构建的"零后端依赖"不变量继续成立（`SHOWCASE-DEPLOY-PROTOCOL.md` §3）。发布是独立动作，需单独授权。

---

## 6. 数据层设计

```text
w9Facts.ts     canonical 事实：端口表 / 三次验收的实测数字 / RSS 与闸门 / 12 条认知修正 / 契约销账
               每条带 evidence: "measured" | "derived" | "pending" 与来源（笔记文件 + 章节）
w9Topics.ts    六块板各自的舞台数据（帧序列、路径定义、矩阵行），引用 w9Facts 而不复制数字
W9Board.tsx    板壳 + 六个专题的 topic 切换（复用 tabs.ts 键盘契约）
glossary.ts    术语 / 白话对照（阶段 3），来源锁定 roadmap §8，不新造
```

抽象边界与 `evidenceSets.ts` 同一条：**只抽"事实"这一层**，怎么用这些事实由各板决定；不抽图形 DSL。

一条硬约束：**任何数字只在 `w9Facts.ts` 出现一次**。W9 的数字在笔记里已经出现多次（314 单在 B2/B3 各一次、187.4MB 在 B5/roadmap/day4 各一次），复制到组件里必然漂移。落地时加一条断言：跨板渲染出的同名指标文本完全一致（week8 第六轮已有先例）。

---

## 7. 口径边界总表（本次最容易做错的地方）

按 week8 第五轮的做法，单独列出来，因为这是唯一会把"可视化"变成"制造假确定性"的风险面。

| 对象 | 档位 | 依据 | 做错会怎样 |
|---|---|---|---|
| 三跳链路 + 22/80 放行 + 内部/公网 200 | `measured` | D4 ⑤ curl/登录/报表 200；D4 §5 ufw 输出 | — |
| 443 / HTTPS | `pending` | 未落地 | 画成完成 = 整周验收失真 |
| 502-A（启动前挂库） | `derived`（同形态近似） | B4 观察到 failed + 3000 无监听，但注入源是 JWT_SECRET | 标成实测 = 谎报 |
| 502-B（proxy_pass 写错）/ 500（运行中挂库） | `derived` | D1 §5.3 设计推演，**从未注入** | 四路径一律"已验证"是最大风险 |
| 快失败链（计数 5 次 → failed） | `measured` | PID 4600→4839 + journal 三条 + ~42s | — |
| 慢失败链（一直 restarting） | `derived` | 由 30s 超时 + 限速窗口推出，未跑过 | 与快失败同档呈现 = 假实证 |
| B2 / B3 / D4⑤ 三组聚合数字 | `measured`，但**口径不同** | months=6 / months=1 / 不同时间点 | 并排当趋势读 |
| 时区跨月归因（7 月 3 单） | `measured` 现象，修正方案 `pending` | B3 观察 + D5 待决策 | 读成数据 bug |
| RSS 187.4 / 83.9 / 8.5MB、available 1388MB | `measured`（装 Nginx 前） | B5 /proc VmRSS + free -m | "装后仍 ~1350MB"是推算，需另标 |
| WiredTiger 预测 450MB | 已被实测推翻的**预测值** | D3 §2.2 → B5 实测 187.4MB | 当成事实展示 |
| `Host $host` 生效 | `measured` | curl 200 + X-Powered-By | — |
| XFF / `trust proxy` | **未采用方案** | D4 §4.2 决定不配 | 画进配置图 = 与服务器实际不符 |

---

## 8. 安全与发布边界

展板会发布到公开 GitHub Pages，所以在写第一行代码之前先划清：

**一律不进展板**（笔记本身已脱敏，展板不得倒退）：密码明文与哈希、`.env` 的任何键值、JWT_SECRET、SSH 密钥与登录细节、腾讯云实例 ID / 实例名 / 命令 ID。

**已在公开仓库、不新增暴露**：公网 IP `43.128.154.242`、端口 22/80、Nginx 1.18.0 / MongoDB 8.0.29 / Node 24 / Ubuntu 22.04。

关于是否在展板上遮蔽 IP，我的判断是**不遮蔽**，理由是：笔记原文已在公开仓库，且展板 notes tab 直接读源文件（遮蔽只能遮板、遮不了笔记），做半套只会制造"已处理"的错觉。真正有收益的动作在服务器侧而不是展板侧——`server_tokens off;` 关掉 Nginx 版本头（目前 `Server: nginx/1.18.0` 已经对外暴露）。这属于 D5 服务器动作，**本方法不扩范围，只记在这里供决策**。

若本人判断"不希望一台在跑的服务器的完整拓扑被做成公开可读的页面"，可选项是把 W9 板设为 `reviewOnly`（复用面试板已有的不变式：`Showcase.tsx` 过滤 + `parseHash` 深链进复习态 + `changeMode` 落回默认页）。这是 §11 的决策点之一。

---

## 9. 落地阶段与验收矩阵

沿用 week8 的纪律：**每个阶段完成先验证，再进下一阶段**；耗时验证主动终止，改用类型检查 / DOM 度量 / 代表性截图。

| 阶段 | 范围 | 完成标志 |
|---|---|---|
| 1 | `w9Facts.ts` + `w9Topics.ts` + 板壳 + **② 故障分叉（代表页）** | 一张图能同时表达四态停止点、第一个排查动作与三种证据档位 |
| 2 | ① 信任边界 + ③ systemd 逐帧 + ④ 端到端验收链 | 三跳边界、快慢失败对照与三个验收环的覆盖差异不再依赖段落说明 |
| 3 | ⑤ 反代决策 + ⑥ 契约销账/纠错/闸门 + notes 接入 + 导航节点 + **术语白话切换** | 六块齐；术语切换覆盖全部板内标签 |
| 4 | 手机语义变体 + 可访问性 + 完整验证矩阵 | 下表全过 |

**验证矩阵**（对生产静态产物做，不是开发服务器——week8 第六轮的教训）：

1. `yarn typecheck`
2. `VITE_SHOWCASE_ONLY=1 yarn build --base=/skillup-week8/`
3. 本地服务 Pages 目录、走真实子路径，Playwright 检查 `1440 × 1000` 与 `390 × 844`：
   - 六个 topic × 展示 / 复习 = 12 个状态：无页面级横向溢出、无 console error、**API 请求数 0**
   - 触控目标：`button` / `summary` 均 ≥ 24×24
   - 深色 / 浅色各核对一遍
4. 交互断言（至少）：
   - 故障分叉四态各自的停止点与状态码正确，且**状态码在走到停止点前不渲染**
   - 502-A / 502-B / 500 三处的 `derived` 标签实际渲染出来（不是数据里有、页面上没有）
   - systemd 快失败停在第 5 次重启帧时状态为 `failed`；慢失败路径带「模型示意」
   - 三个验收环的"不覆盖"文案存在
   - 443 在任何状态下都不显示为已完成
   - 跨板同名指标文本完全一致（RSS / 314 单 / 2581 单）
5. hash 导航不触发重新加载，断言前必须显式等待重渲染（week8 第六轮踩过的坑，直接继承）。

**发布**：不在本方法范围内。按 `SHOWCASE-DEPLOY-PROTOCOL.md`，"更新 / 构建 / 提交 / 推送 / 开 PR / 合并上线"是不同授权层级，需单独确认；且 Pages 从 `main` 发布，功能分支构建成功 ≠ 线上已更新。

---

## 10. 明确不做

- 不引入图表框架、不新增全局状态层、不抽图形 DSL。
- 不把增加卡片 / 图标 / 阴影 / 颜色当作可视化成果。
- 不动 `evidenceSets.ts`，不改 week8 现有六个 tab 的任何内容。
- 不重画 W6 Day3 的八段跨层轨道（W9 只做跨进程收束视图 + 交叉引用）。
- 不改 week9 笔记原文；发现事实冲突走"改笔记"而不是"展板取一个版本"。
- 不做 Java stretch 的可视化（未执行，无事实可渲染）。
- 不把 D4-HTTPS、时区修正、备份 / 多环境画成已完成或"即将完成"。
- 不部署（独立授权动作）。

---

## 11. 三个决策（2026-08-12 已定案）

| # | 决策 | 结论 |
|---|---|---|
| 1 | 承载位置 | **week8 展板加 `tab=deploy`**，不建独立前端 |
| 2 | 公开度 | **设为 `reviewOnly`**——W9 板只在复习状态出现，与面试板同一条不变式；**且暂不发布 Pages**，产物只留在本仓库 |
| 3 | 推进方式 | **先做 ② 故障分叉代表页**验证语法，成立后再推其余五块 |
| 2-b | 公开度（2026-08-14 升级） | **GitHub Pages 上传冻结**。决策 2 原本是「暂不发布」，现在升级为发布目标改到自建服务器 `8081`（带登录门禁），Pages `/skillup-week8/` 不再更新、线上旧产物暂不撤下。冻结的是上传，不是构建。理由与解冻条件见 `SHOWCASE-DEPLOY-PROTOCOL.md` §0 |

决策 2 的连带效果：§8 里「是否遮蔽 IP」这条不再需要单独回答——展板不进公开 Pages，服务器拓扑不会被公开索引。密码 / 密钥 / `.env` 值 / 实例 ID 一律不进展板这条纪律不受影响，继续执行。

服务器侧 `server_tokens off;`（关 Nginx 版本头）仍是一条独立建议，归 D5，不属展板范围。

---

## 12. 阶段 1 执行记录（2026-08-12）

### 落地内容

| 文件 | 作用 |
|---|---|
| `w9Facts.ts`（新） | canonical 事实：证据档位定义、四跳链路与信任面、D4 ⑤ 验收读数、启动顺序约束 |
| `w9Topics.ts`（新） | 四条路径的帧序列、判据、分叉规则、六块板的阶段进度 |
| `W9Board.tsx`（新） | 板壳 + 证据档位图例 + 故障分叉代表页 + 阶段进度 |
| `styles.css` | 追加 `w9-*` 段（含手机语义变体与 ≥1200px 字号） |
| `types.ts` / `App.tsx` / `Showcase.tsx` | 新增 `deploy` tab；`REVIEW_ONLY_TAB` 单值改为 `REVIEW_ONLY_TABS` 列表；学习演进加 W9「上线运行」节点（同样只在复习态） |

四条路径共用同一条链路，只有停止点不同；状态码走到停止点之后才揭示；每条路径强制挂证据档位并把「为什么是这一档」写在图旁边——四条里只有「正常」是 `measured`。

### 验证（对生产静态产物，走真实 `/skillup-week8/` 子路径，不是开发服务器）

- `yarn typecheck` 通过；`VITE_SHOWCASE_ONLY=1 yarn build --base=/skillup-week8/` 通过。
- Playwright 在 `1440 × 1000` 与 `390 × 844` 共 **66 项断言全过**，包括：深链 `tab=deploy` 自动进复习态、复习态 8 个 tab / 展示态 6 个、切回展示落回默认板、展示态学习演进不含 W9 节点、首帧不渲染状态码、四条路径末帧分别 200/502/502/500、每条路径渲染唯一停止点与证据档位、502-A 标为推演而非实测、复习门未揭示时不渲染判据表与分叉树、切路径回到第 1 帧、无页面级横向溢出、可点击目标 ≥24×24、无 console error、**后端请求数 0**。
- 桌面浅色 / 深色与手机浅色截图逐屏核对。

### 执行期修的四处

1. **切路径重置写成了 `useEffect([pathId, replay])`**——`replay` 每次渲染都是新的函数身份，效果因此每帧重跑，把 `index` 一直摁回 0，链路根本推不动。断言抓到（四条路径的状态码都揭示不出来）。改由选择动作本身触发，点击与方向键共用一个 `selectPath`。
2. **深色模式下停止点 chip 白字只有 3.2:1**（`#fff` 落在 `#0ca30c` / `#e66767` 上）。前景改用 `--surface`，明/暗 × 成功/失败四种组合都不低于 4.7:1。这条断言测不出来，是算对比度时发现的。
3. **判据表头用红 / 绿着色**，深色下红字在 `--grid` 上 4.32:1，低于 4.5。表头改用 `--text-primary`——场景名本身已经携带信息，颜色只是重复。
4. **停止点标记原本统一写「停在这里」**：同样红在 Node 那一格，「到不了这里」（Node 没起来）与「没被发到这里」（Node 活着但 Nginx 没发过来）是两件不同的事，通用文案会把它们抹平。改为按路径分别给 `stopLabel`。

### 阶段 1 修订：把「四张文字卡片」改成一张图（同日，看截图后）

第一版收口后本人指出「感觉没有太多可视化，文字很多」。复核属实，而且根因不是文字量，是**空间上没有编码信息**：

| 第一版的问题 | 实际后果 |
|---|---|
| 信任面做成了「每个节点各描一道边框」，方法稿 §2.2 写的是**包围框** | 读者看到四张并排卡片，看不到「3000 与 27017 在同一个面里」这件事 |
| 断点标在**节点**上 | 502 读成「Node 拒绝了请求」，而实际是 Nginx 到 Node 那条线连不通 |
| 节点之间只有静态 `→` | 请求「走过哪一段、断在哪一段」没有任何视觉表达 |
| 四个节点常驻各自的职责说明 | 光节点区就有四段散文，这是文字堆积的主要来源 |
| 四条路径要点四次才能比较 | 缺 week8 第五轮列出的第五条属性「一眼结论图」 |

修订内容：

1. **信任面改成真的包围框**：外部（虚线）/ 公网可达（实线，`ufw 只放行 22 / 80` 挂在下边框上）/ 仅本机 loopback（点线，把 Node 与 Mongo 一起圈住）。「公网请求最多到 Nginx」因此是看出来的，不是读出来的。
2. **连接线成为真的线**，带四种状态：未走（虚线）/ 已走（实线着色）/ 当前（加粗）/ **断开（点线 + ✕ 标记）**。
3. **断点画在线上**：502 断在 Nginx→Node，500 断在 Node→Mongo——断在哪条线本身就是「停得多深」这个结论。节点上只保留一句 `stopLabel`（到不了这里 / 没被发到这里 / 停在这里）。
4. **节点瘦身**：只留名称与监听地址；「这一跳负责什么」改为跟着当前帧走，一次只显示一条。
5. **新增一眼结论图**：四条路径各一行迷你链，断点位置直接对照——两条 502 断在同一条线上（所以外部现象一样），500 断得更深。它排在揭示之后，不破坏「先答再看」。
6. 舞台是 `role="img"` 并按当前帧生成等价文字描述，否则整张图对读屏是空的。

修订期又抓到一处：`overflow-x: auto` 会把 `overflow-y` 一并变成 `auto`，挂在边框上的面标签（`top / bottom: -8px`）因此被裁掉一半。截图核对发现，断言测不出来。补上下内边距解决。

修订后 66 项断言仍全过；桌面浅色 / 深色与手机浅色重新逐屏核对。

### 阶段 1 再修订：节奏（同日，本人本地拉代码检查后）

反馈是「动效太快」。查下来是两个选择都跟内容形态不匹配，而不是数字调大就行：

1. **不该自动播放。** hook 的 `autoPlay` 默认是 `true`，我沿用了默认值。但展板里和这块板同形态的
   ——「请求走链路 + 每步带文字说明」——认证链、W6 八段轨道、W3 pipeline、面试板**全部是
   `autoPlay: false`**；自动播放只用在 W5 那些短状态标签的动画上。改为 `autoPlay: false`，
   节奏交给读者。
2. **固定间隔配不上这块板的解说。** 原来是 1100ms/帧，而这里的逐帧解说是完整句子（最长约 75 字），
   读完之前就翻页了。`framePlayer` 增加可选的 `intervalAt(index)`，本板按解说长度给停留时间：
   `min(7000, 1800 + 字数 × 60)`，即约 11 字/秒的快读节奏；要细读就暂停。
   不传 `intervalAt` 的板继续走原来的 `setInterval` 路径，行为不变。

`intervalAt` 存进 ref 而不是进依赖数组——调用方几乎都会传内联箭头函数，直接进依赖会让效果每次渲染
重建定时器，逐帧推进再也触发不了（这正是本轮第一处缺陷的同一个坑）。

顺带补了一个跨板回归脚本：`autoPlay: false` 的四个板必须**不动**，W5 必须**推进**——
第一次跑的时候我把前者写成了「应该推进」，差点把既有正确行为报成回归。

验证：72 项断言全过（新增 4 项节奏断言：落地不自动播放、按播放后推进、6.5s 内不超过第 3 帧 ×2 视口）；
跨板回归 5 项全过。

### 构建体积

入口 chunk 301.91 → **317.75 kB**（gzip 113.30 → 119.54，+6.2 kB）；CSS 185.13 → **197.4 kB**（gzip 27.47 → 29.1，+1.6 kB）。`MarkdownNotes` chunk 未变（本阶段还没接 week9 笔记）。

### 给后续五块的约束（本轮修订反推出来的）

「用了 week8 的 token 和 framePlayer」不等于「做成了可视化」。后面五块每块至少要满足：**有一个空间关系是靠位置 / 包含 / 连接表达的，而不是靠一段说明文字**；并且尽量各带一张「一眼结论图」，让同类对象不必逐个点开才能比较。

---

## 12.5 ③ systemd 失败模式（2026-08-12，第 2 块）

### 为什么先做这块，而不是方法稿里排在前面的 ①

① 信任边界与故障分叉板重叠最多——信任面已经画在那块板上了。而 ③ 零重叠，且故障分叉板里
502-A 的根因已经写了「会按 RestartSec 退避重试，快失败还会撞上 StartLimitBurst 停住」，
那是一个悬着的引用，正好由这块板接住。

### 空间编码：同一个 60 秒窗口，两种重启密度

这块板的可视化不是「两段文字并排」，而是**同一根轴上两种疏密**：

- 窗口（`StartLimitIntervalSec = 60s`）是一条有宽度的带子，不是一个数字。
- 每次启动尝试是落在带子上的一个点，按帧逐个出现。
- **快失败**一次尝试几乎不占时间 → 5 个点全落在带子里 → 计数器打到 5/5 → 第 6 次被拒绝、`failed` 停住。
- **慢失败**一次就吃掉 `serverSelectionTimeoutMS=30s` → 带子里最多塞下 2 个点 → 计数永远到不了 5。

「为什么一个被停住、另一个不会」因此是从疏密看出来的。配套的一眼结论图把两条轴叠起来，
右侧直接给 5/5 与 2/5，不用切换两次才能对比。

### 口径边界

- **快失败 = 实测**：PID（4600/4659/4733/4839）、journal 四条原文、~42s 总时长都有记录。
  但每次重启的**精确时刻没记**，轴上的位置由 `RestartSec=10s` 与总时长反推——
  组件里显式写明「位置是推的，次数与结局是实测的」。
- **慢失败 = 推演**：由 30s 超时与 60s 窗口推出，没有真正跑过一次。
- journal 原文只在快失败下渲染；慢失败没有实测证据，就不摆证据区。

### 注入设计盲区单独成段

B4 第一轮的失败设计（`stop mongod → start nodeapp`）被 `Wants` 的连带拉起否决——
这条比结论本身更值得复习，因为它记录的是**实验设计本身会出错**。用 ❌ → ⚡ → ✅ 三段式呈现，
展示态与复习态都可见（它不是本题的答案）。

### 验证

- `yarn typecheck` 与 `VITE_SHOWCASE_ONLY=1 yarn build --base=/skillup-week8/` 通过。
- 断言从 72 增到 **112 项，全过**：新增 systemd 深链、走完前不揭示结局、快失败计数 1/5 → 5/5、
  结局为 failed、快失败标实测且渲染 4 条 journal、6 次尝试全渲染、切模式回第 1 帧、
  慢失败计数停在 2/5、结局是一直 restarting、标推演、不渲染 journal、
  复习态默认不渲染密度对照与结论、揭示后渲染两行、注入盲区三段式、
  systemd 专题无页面级横向溢出、触控目标 ≥24×24。
- 桌面浅色 / 深色与手机浅色截图逐屏核对。

### 本块修的两处（都是截图才看得见）

1. 窗口标题带与第 1 次尝试的标签都贴在 x=0，直接叠住了。改为固定的垂直分层：
   标题带 → 窗口带（标签交错两行）→ 轴 → 时刻。
2. `t=0` 的点与标签会向左溢出几像素，滚动容器只有 2px 内边距被裁掉（实测「pid 4600」显示成「id 4600」）；
   迷你图里 timeout 的橙点正好压在同为橙色的窗口带上糊成一片，加一圈页面底色描边分离。

另有一处是布局常识但第一次仍写错：`.w9-density-row` 是 `<button>`，放在普通 `div` 里不会撑满，
必须显式 `width: 100%`（放在 grid 容器里才会被 stretch）。

## 12.6 ① 信任边界与端口（2026-08-12，第 3 块）

### 空间编码换一种：嵌套的可达层

故障分叉板已经画过链路形态，这块如果再画一遍链路就是重复。改成**深度**：

```text
公网（虚线）
  └ 闸门 1 腾讯云安全组 → 闸门 2 ufw          ← 两道独立的闸门，串联
      └ 主机（实线）
          ├ 对外监听面：22 / 80 / 443（虚线待做）
          └ loopback 内线：3000 / 27017 / 53
```

「外面能摸到哪一层」于是读成**端口落在第几层**。配套的一眼结论图是可达性判定表：
公网可达 = 安全组放行 ∧ ufw 放行 ∧ 监听地址对外，任何一格拦住结论就是摸不到。

这块**不用逐帧播放器**——它不是时间过程，是静态结构 + 选中查看。刻意不让每块板长一个样。

### 三处诚实边界

1. **安全组那一列从未直接查过控制台。** day4 §5 只写了「归因预备（未触发但记录）」。80 从公网走通
   是实测，据此**反推**安全组放行了 22 与 80——反推不是观察；443 则完全未知。标 `measured` 会是谎报。
2. **53 端口不在 D1 契约表里**：Ubuntu 22.04 的 systemd-resolved stub，day2-result §1 的 `ss` 输出里有它。
   实际监听的端口比契约表多一个，这件事本身值得显示。
3. **443 用虚线灰**：它从来没落地过，不能和已放行的端口长得一样。

三条要留下的结论紧跟其后：❌ 不等于没在用（3000/27017 是 loopback 内线，三跳全靠它们）、
两道独立防线（绑 127.0.0.1 + ufw deny = 纵深防御）、ufw 不是全部（超时而非拒绝时该查安全组）。

### 验证

- typecheck 与 `VITE_SHOWCASE_ONLY=1` 构建通过；断言从 112 增到 **146 项全过**。
- 新增：默认专题落点、两道闸门、两区端口数、443 标待做、未选端口不渲染详情、
  选中 3000 显示 loopback 与 502 后果、复习态不渲染判定表、揭示后六行、
  安全组列显式标未知、表内明说「反推」、三条结论、无横向溢出、触控目标 ≥24×24。

### 本块抓到的一个类别性缺陷

`.w9-port-chip` 与 `.w9-reach-row` 是 `<button>`，重写了 `background` 却没写 `color`——
全局规则是 `button { color: #fff }`，于是未单独着色的子元素（这里是 `<code>`）继承了白字，
在浅色底上**直接消失**。端口号全都看不见了。

这类缺陷内容断言测不出来（文字确实在 DOM 里），只有读截图才发现。因此补了一条**计算色断言**：
板内任何可见文本的 `computed color` 都不得是 `rgb(255, 255, 255)`。这条以后能自动拦住同类问题。

## 12.7 ④ 端到端验收链（2026-08-12，第 4 块）

### 主图就是结论图：覆盖跨度

这块的重点不是「覆盖到哪」，是**没覆盖到哪**——「B2 的 200 没有证明什么」就是本块的全部问题。
所以主图直接做成覆盖跨度：同一条链切成四段（公网 / Nginx 反代 / Node 应用 / MongoDB），
三次验收各自占据不同的跨度，**没盖到的段用虚线空格显式标「没验证」**，不留白。

| 验收 | 起点 | 公网 | Nginx | Node | Mongo |
|---|---|---|---|---|---|
| B2 服务器内部端到端 | `127.0.0.1:3000` | 没验证 | 没验证 | 验证到 | 验证到 |
| B3 重启恢复 | 同上，reboot 之后 | 没验证 | 没验证 | 验证到 | 验证到 |
| D4 ⑤ 公网验收 | 本地开发机 | 验证到 | 验证到 | 验证到 | 验证到 |

三行一比就能看出：B2 的 200 落在链的右半段，左边两格是空的——不证明 Nginx 可用（那时还没装），
也不证明公网可达（走的是 loopback）。这句话原本要读三段记录再自己对齐。

### 两条必须紧邻的口径

1. **三组数字不能并排当趋势。** months 参数是 6 / 1 / 6，且 B2 与 D4 取数时间点不同。
   它们各自验证的是「这条链通不通」，不是同一把尺子量三次。
2. **时区观察点只挂在 B3 上，并标 `pending`。** B3 用 `months=1` 查却返回 2 个月（8 月 314 单 +
   7 月 3 单）：服务器 CST(UTC+8)，聚合的 `$year/$month` 按 UTC 分组，本地 8/1 凌晨的单被归到 7 月。
   不把这条放在 B3 旁边，「7 月怎么冒出 3 单」会被读成数据错误。D5 待决策是否用 `$dateToString` 修正。

### 复习门

先给起点与实测读数，问「这次**没有**覆盖到链上的哪几段、由此推不出什么」，再展开能证明 / 不能证明两栏。

### 验证

typecheck 与 `VITE_SHOWCASE_ONLY=1` 构建通过；断言从 146 增到 **172 项全过**。新增覆盖段数断言
（B2/B3 各 2 覆盖 2 未覆盖、D4 四段全覆盖）、趋势提示、复习门、B2 limits 明说 Nginx 与公网、
时区观察点只在 B3 且标待做，以及该专题的横向溢出与白字检查。

本块没有踩新坑；上一块补的计算色断言在这块直接复用，无新增缺陷。

## 12.8 ⑤ 反代 header 决策（2026-08-12，第 5 块）

### 空间编码：跨越边界的值改写

这是纯推理内容，既不是链路也不是时间过程。做法是把**同一个字段在 Nginx 两侧各写一次**，
中间一条竖线就是反代边界，跨越处挂「改写」标记：

```text
客户端真正发出              │ Nginx 反代边界 │            Node 实际看到
浏览器的公网 IP        ──改写──>            127.0.0.1（Nginx 自己）
http（将来是 https）   ──改写──>            恒为 http
43.128.154.242         ──改写──>            $proxy_host —— 即 127.0.0.1:3000
                                            ↳ 配上 Host $host 后 → 43.128.154.242（原样透传）
```

「失真」于是是字面可见的，不用先读一段解释再自己想象。

### 一个被自己抓到的语义错误

第一版把 Host 那行整行标成绿色（因为「配了」），但右侧显示的值仍是**默认行为** `$proxy_host`——
等于把「默认会变成什么」和「我们修没修」压进同一格，读者会以为 Node 现在看到的就是 `$proxy_host`。
改成两行：先橙色写默认失真，再绿色单起一行写配置之后的实际值。

### 三张决策卡与那条例外

每个字段一张卡：理论补传方案 / 应用是否消费（读代码逐处核对）/ 本次决定。
两条不配、一条配。**Host 是唯一的例外，且理由不同**：应用照样不读 `req.hostname`，
但透传原始 Host 是零成本的合理默认——Nginx 不覆盖就会把它改写掉。
卡上明写这句，防止读成「配不配只看应用消不消费」。

### 复习门后的两块答案

1. **判断规则**：应用是否读 `req.ip` / `req.protocol` / `req.hostname` / `req.secure`？
   读 → XFF 透传与 `app.set('trust proxy')` **必须成对引入**：只配 Nginx 不设 trust proxy，
   Express 不信任这些头、拿到的还是 `127.0.0.1`；只设 trust proxy 不在 Nginx 覆盖，
   客户端可以自己伪造 `X-Forwarded-For`。两边缺一边就是假 IP。
2. **落盘配置**：服务器上实际生效的那四行，并显式标注 `X-Real-IP` / `XFF` / `XFP` 与 trust proxy
   **都没有配**——未采用方案不能画成已配置。生效证据是 `curl` 的 200 + `X-Powered-By: Express`。

### 验证

typecheck 与 `VITE_SHOWCASE_ONLY=1` 构建通过；断言从 172 增到 **202 项全过**。
新增三条失真行、两掉一留、左右两侧的值都渲染、Host 行分两行写、三张决策卡的配/不配分布、
Host 卡的例外说明、复习门、成对引入与假 IP 的措辞、落盘配置里没有 `proxy_set_header X-`、
未采用标注，以及该专题的横向溢出与白字检查。

另修一处对齐：边界标签原本按 `left: 50%` 定位，而竖线在网格第 3 列、并不在容器正中，实际偏了。
改成用同一套网格把标签放进那一列。

## 12.9 ⑥ 契约销账与资源闸门（2026-08-12，第 6 块 · 六块齐）

收束块，三段合一，各用不同排版密度（照搬 week8 Day4 板「避免读成同质卡片流」的做法）。

### 段一：销账轨道——位置就是「哪天销的」

D1 冻结的 12 条契约按销账日排成四列：D2 / D3 / D4 / **仍欠**。仍欠那一列不是「第四天」，
用虚线边框和无填充与前三列区分开。8 条已销、4 条仍欠（HTTPS、证书续期、冷路径复核、时区口径），
「还欠什么」因此是看出来的，不用逐条读。

### 段二：内存尺——长度就是占多少

`1931 MB` 一条尺，mongod `187.4` / nodeapp `83.9` / available `1388` 按比例占段。
旁边把 **D3 §2.2 的预测（WiredTiger ≈450 MB）与 B5 实测（187.4 MB）并置**——
这不是「给少量数字硬做图」，是一次预测被实测推翻的对照，结论是 cache 按需增长不预分配。

口径两条：`available 1388 MB` 是**装 Nginx 之前**实测的，「装后仍约 1350 MB」是按 8.5 MB 推算、
没有重测；`Swap = 0` 是现状而非主动选择——真撞到上限时没有磁盘兜底。

### 段三：认知修正 12 条，并新增一类

沿用 Day4 的 ❌ → ⚡ → ✅ 三段式与分类，但**必须新增 `experiential`（工具行为经验）**。
12 条里有 4 条属于这一类：`read -s` 在网页终端读不到 stdin、`node -e` 的 import 锚 cwd、
文件脚本的 import 锚脚本位置、`sudo` 默认 `env_reset`。

这些不是推理错误，是 `AGENTS.md` §4 明确定义的「必须真实遇过一次才知道」的经验知识。
把它们和「结论超出证据」混成一类，等于告诉复习者「你本该推出来」，与协作规范直接冲突。
板上单独着色并写明这句。

复习态先只给初始说法，自己判断错在哪一步。

### 本块抓到的一类残留：纯文本里的 Markdown 语法

截图核对时发现页面上露出了字面的 `**`——我在普通字符串里写了 Markdown 加粗，它不会被渲染。
清查后发现 5 处 `**` 加若干反引号；而 week8 现有数据文件（`w6Day4.ts` / `w5Topics.ts`）里
**一个反引号都没有**，写的全是纯文本。这是我引入的不一致。

全部清掉，并补了一条断言：**逐专题扫渲染文本，不得出现 `**` 或反引号**（落盘的 nginx 配置块除外）。
这条以后能自动拦住同类残留。

### 验证

typecheck 与 `VITE_SHOWCASE_ONLY=1` 构建通过；断言从 202 增到 **250 项全过**。
新增销账四列与 8/4 分布、仍欠里点名 HTTPS、内存尺分段、预测与实测并置、Swap=0、
口径措辞、生产对照两列与六个归属、12 条修正、4 条 experiential、复习态默认收起、
经验类明说「不是推理错误」，以及六个专题各一条 Markdown 残留扫描。

## 12.10 notes tab 接入 week9 原文（收口项 a）

六份接进笔记 tab：浓缩地图、D4 反代、D3 数据库、D2 主机、D1 契约、以及本文（展板方法）。
按既有约定直读源文件，不在前端维护副本。

### 必须同时处理的一件事

**笔记 tab 在展示状态是可见的。** 直接把 W9 笔记塞进去，等于绕过「部署板设成 reviewOnly」
那个决定——那些笔记里就是公网 IP、端口、systemd 单元行为和排障判据。

所以给 `NoteSource` 加了 `reviewOnly`，展示状态过滤掉这六份。深链指向被过滤的笔记时
**给一句明确提示，而不是悄悄换成另一篇**——后者会让人以为链接坏了或内容变了。

### 两条必须说清的边界

1. **这只是展示过滤，不是内容边界。** `?raw` 是静态导入，六份笔记的正文**无论什么状态都打进了
   JS 产物**。任何人打开 devtools 或直接取那个 chunk 都能读到。静态站点上本来就不存在真正的
   内容边界——真正起作用的控制是既有的那条决定：**不发布 Pages**。当前没有暴露，是因为它没上线。
2. **体积**：`MarkdownNotes` chunk 222.85 → **352.33 kB**（gzip 102.30 → **197.51**，+95 kB）。
   它是懒加载的，只有点开笔记 tab 才拉。方法稿写的是「落地时实测一次，超出可接受范围再改
   `import.meta.glob` 按需分片，不预先优化」——数字放在这里，是否分片由本人决定。

### 顺带发现的一处既有不一致（已修，2026-08-13）

`interview-prep/` 的两份问答稿（`backend-qa-sheet.md` / `db-review-sheet.md`）是个人材料，
但它们原先**在展示状态也列在笔记 tab 里**；而「面试准备」板本身是 reviewOnly——
与「面试准备板收进复习状态，它是个人材料，不该出现在对外 demo 里」这条既有决定直接相抵。

PR #69 合并后按本人决定改掉：两份也标 `reviewOnly`。展示态的笔记 tab 现在只剩 7 份纯技术笔记
（W6 心智模型 / 项目说明 / 能力速查 / Hooks 面试 / 前端工具箱 / 存量项目 / 部署链路）。
被拦下时的提示按材料类型分两句——个人材料说「这是个人面试材料」，W9 说「这份笔记写的是一台在跑的服务器」，
不用一句通用文案糊过去。

### 验证

typecheck 与 `VITE_SHOWCASE_ONLY=1` 构建通过；断言从 250 增到 **264 项全过**。
新增：复习态列 15 份、六份 W9 都在列、展示态只列 9 份且导航里没有「W9」字样、
展示态深链被拦下时显示明确提示且不渲染正文、复习态能打开原文。

## 12.11 术语 / 白话切换（收口项 b，2026-08-13）

### 落地形态

板头一个开关，只切**标签文本**，舞台结构一个字不动——切成两套图会让两边慢慢漂移，
也违背 roadmap「不把增加交互当成果」的口径。词表 `w9Glossary.ts` 全部取自浓缩地图 §8，
**不新造白话**：18 条术语对照 + §8.3 的六条类比（门卫 / 管家 / 仓库 / 前台 / 救生索 / 两道闸门）。

底部加一个可展开的对照表，作为开关的「全都给我看」出口；它与开关状态联动——
拨到白话时对照表把白话那一侧提亮，两处不会各说各话。

### 范围刻意收窄

只给**确实还是术语**的那些标签挂切换：面标签、快失败 / 慢失败、`StartLimitBurst`、
契约 / 销账、内存闸门、纵深防御。板上大部分说明文字本来就是白话
（「门卫：公网只认它」「仓库：只有同机的 Node 能连上」），给它们再套一层切换
只会变成 roadmap 警告过的那种装饰。

切换后的术语带一条虚下划线，提示「这里本来是个术语」，避免读者以为板上从来只有白话。
开关是一次性状态，**不写进 hash**——与既有的 reveal 状态同一条口径，不污染分享链接。

### 断言顺带抓到的一处缺陷

计数器那一格原本有 `text-transform: uppercase`。它现在放的是 systemd 指令名，
被渲染成了 `STARTLIMITBURST`——照抄不到配置文件里。去掉 `text-transform`。
这类缺陷肉眼扫过去容易放过，是断言比对文本时才现形的。

### 验证

typecheck 与 `VITE_SHOWCASE_ONLY=1` 构建通过；断言 **290 项全过**。
新增：默认显示术语、拨到白话后模式名与计数器都换、**舞台结构不变**（时间轴与尝试点数不变）、
对照表 18 条且整体翻转、类比 6 条、拨回恢复、开关不写进 hash。

## 12.12 笔记按需分片（收口项 c，2026-08-13）

### 做法

15 份笔记的正文原先是静态 `?raw` 导入，全压在 `MarkdownNotes` 这一个 chunk 里——
点开笔记 tab 就得整包下载。改成每份一个动态 `import()`，Vite 各切一个 chunk，
只有真正打开某一份时才拉它。

### 效果比预期好

| | 分片前 | 分片后 |
|---|---|---|
| `MarkdownNotes` chunk | 352.33 kB（gzip **197.51**） | 164.81 kB（gzip **51.29**） |
| 单份笔记 | 全在上面那个包里 | 各自独立，最大的 day3 是 37.87 kB（gzip 27.83） |

比接入 week9 之前的基线（gzip 102.30）还小一半——week8 那批笔记也一起拆出去了。
现在点开笔记 tab 只付 51 kB，读哪篇再付哪篇。

### 与复习门天然对齐

复习态下笔记隔着「阅读前回忆」那道门，**未揭示就不会去拉正文**。
这不是额外做的优化，是分片之后自然成立的——省流量，也与「先回忆再看」的认知任务一致。

写这条断言时先写错了一次：在复习态测「切笔记应当新拉一个 chunk」，得到 0 次请求，
差点当成分片没生效。0 次才是对的。改到展示态测切换，另加两条专门测复习门前后的行为。

### 验证

typecheck 与 `VITE_SHOWCASE_ONLY=1` 构建通过；断言 **298 项全过**。
新增：打开笔记 tab 只拉当前这一篇、展示态切笔记时按需再拉一个、
复习态未揭示时不拉正文、揭示后才拉那一篇。

## 12.13 按 8/13 主线收口重建（D5 前，2026-08-13）

### 为什么必须重建，而不是「再加一块」

六块是 8/12 建的，事实截到 D4-HTTP。8/13 一天里主线走完了三件事（段 0 URL 面收敛、
8080 管理后台、D4-HTTPS 收口），**展板于是开始说谎**——不是过时，是谎报：

- 端口表里 443 写着 `bind: null` / `ufw: deny` / **「D4-HTTPS 待做」**；
- 契约销账板把 `https` 与 `cert-renew` 两条画在「仍欠」列；
- 「D4 ⑤ 公网验收」的不能证明栏写着「不证明 HTTPS——443 从未落地」；
- 反代块里那份落盘配置是**段 0 之前**的 `location / { proxy_pass }`，服务器上已经不存在了。

这四条都不是「少说了什么」，而是「说了不成立的话」。所以本轮先修谎报，再补新事实。

### 三类改动

| 类 | 内容 | 处理 |
|---|---|---|
| A · 谎报 | 443 端口行、两条契约销账、D4 验收 limits、生产对照 missing、80 落盘配置、安全组档位 | 改正 |
| B · 缺失 | 三个对外面、8080 端口行、段 0 URL 面收敛、两次新验收、TLS 排障两相位、两条认知修正、Q8 安全债 | 新增 |
| C · 措辞 | 反代 proto 决策的理由句（443 已上线但结论仍是「不配」） | 重写 |

### 三处刻意的克制

1. **链路图仍是四跳，不改成七个节点。** 三个面另用 `PUBLIC_FACES` 表达——
   把 80/443/8080 拆成三条并列链路，会让「公网请求最多到 Nginx」这条主结论被支线冲淡。
2. **Q8 安全债不混进 `CONTRACTS`。** 那张表是「D1 冻结的契约」逐条对账，塞一条不在冻结清单里的
   进去，销账时间轴的语义就坏了。改为挂在 URL 面那一节末尾，并在销账板留一句指路。
3. **段 0 不单开第七块。** 它与「信任边界与端口」是同一条最小暴露原则的两次应用
   （端口面 → URL 面），拆开会让「收窄的是面不是层」这个转折读不出来。

### 本轮新长出来的两条结论

这是重建比新增更值钱的地方——它们在 8/12 那版里不可能存在：

- **覆盖段这把尺子有边界。** D4-HTTP、D4-b、D4-HTTPS 三次的覆盖段**完全相同**（四段全覆盖），
  但入口面分别是 80 全量反代、80 削窄 + 8080、443 TLS。覆盖段回答「走到链上第几段」，
  回答不了「从哪扇门进来的」。验收链那块板因此新增一格「入口面」和一条边界说明。
- **安全组那一列从推演升格为实测。** 8/12 只能由公网 200 反推；8/13 两次真的动了控制台，
  并拿到「放行前 timed out → 放行后 refused」的差分。这是全板唯一一次**档位升级**，
  升级本身比结论更值得记——它示范了「反推」与「观察」的差别可以被一次实验消掉。

### 验证

typecheck 通过；`VITE_SHOWCASE_ONLY=1` 生产构建通过（day4b 原文单独切出 29.98 kB / gzip 23.70）；
静态入口零后端依赖 grep 无匹配；产物标题与 `/skillup-week8/` 子路径引用正确。
桌面 1440 与移动 390 两个视口逐块截图核对，六块无横向溢出、无控制台报错。

### 顺带修的一处日期错误

`week9-plan` 8/11 记的「收口顺延到 8/15」最终没有发生（D3/D4 都按期压了回来），
而 8/15 是周六、day4b 又把它写成「周五 8/15」——自相矛盾。D5 实为 **8/14（周五）**，
本轮已在计划、状态、roadmap 与 day3 / day4b 中统一更正并留下更正说明。

## 12.14 六块扩到九块：拆一块、加两块（2026-08-13 晚）

### 触发：一个提问

重建完六块之后被问了一句「D4 的学习和 roadmap 不需要新增展板吗」。核查下来，是需要的——
而且暴露出 §12.13 里那条「段 0 不单开第七块」的克制**用错了判据**。

当时的理由是「段 0 与端口面同属一条最小暴露原则」。但本板的规则从来是
**每块只回答一个问题**，不是「每块讲一条原则」：

- 端口面问的是「**外面能摸到哪一层**」——网络层的事；
- 段 0 问的是「**进了门之后，谁该被拦在哪一层**」——授权层的事，是 W4 的知识落到部署层。

原则相同 ≠ 问题相同。代价也量得出来：合并后的「信任边界」板桌面高 4443 px，
比第二长的板还高 17%，已经是全板最挤的一页。

### 更要紧的：day4b 有两类内容一条都没进展板

grep 过一遍，这些词在整个前端零命中：**止损 / 连败 / account key / 构建位置 / 溯源 / 制品 / 速率限制**。
「回滚」与「备份」只出现在生产对照的**缺失**栏里——也就是说，展板把回滚呈现成「还没做」，
而 8/13 那天其实是**先设计好两级回滚才敢动那台机器的**。这是又一处口径失真，
只不过方向相反：前一轮是把做过的说成没做完，这一轮是把做过的整个略去。

### 结果：9 块

| # | 块 | 问题 | 来源 |
|---|---|---|---|
| ① | 信任边界与端口 | 外面能摸到哪一层 | 保留（URL 面移出后回到只答网络层） |
| ② | URL 面与授权层 | 进了门谁被拦在哪 | **从 ① 拆出** |
| ③ | 证书与信任 | HTTPS 通了证明了什么 | **新增** |
| ④ | 故障分叉 | 两个 502 差在哪 | 保留 |
| ⑤ | systemd 失败模式 | 崩溃循环怎么被停住 | 保留 |
| ⑥ | 改一台在跑的机器 | 改砸了怎么退回去 | **新增** |
| ⑦ | 端到端验收链 | 某次 200 没证明什么 | 保留 |
| ⑧ | 反代 header 决策 | 反代后该传什么头 | 保留 |
| ⑨ | 契约销账与闸门 | 还欠什么 | 保留 |

顺序是一条阅读弧线：三层结构（网络 → 授权 → 加密）→ 两类故障 → 改动纪律 → 验收方法 → 一处细节 → 收束。

### ③ 证书与信任

此前 D1 契约的头号交付物「域名 HTTPS 可访问、证书有效」竟然没有自己的板——
H1 只作为验收链里某一次验收的附属面板存在。可是「200 只证明活着、`SSL_VERIFY:0` 才证明被信任」
问的是加密层，不是验收方法。

空间编码用**真嵌套的签名关系**：系统根证书库 → LE 中间证书 → 我的证书 → SAN 与域名匹配，
四环层层包住，「被信任」于是是一条看得见的指回路径。第二段是 90 天时间轴——
「为什么这件事必须自动化」从刻度疏密看出来。另含 `certonly` vs `--nginx` 的选择
（后者会改写你已有的 shop，让「我配的」和「跑着的」不是同一份）与 LE 速率限制。

**第一版渲染成了四张平铺卡片**——`TRUST_CHAIN.map()` 产出的是兄弟节点，
CSS 里那条 `.w9-trust-ring .w9-trust-ring` 后代选择器从来没匹配上。截图才看见：
嵌套关系整个丢了，退化成一个列表，正好违反本板「至少有一个空间关系靠位置/包含/连接表达」的硬要求。
改成递归组件真嵌套后成立。**教训：写「嵌套」的时候要确认 DOM 真的嵌套了，CSS 选择器不会替你造结构。**

### ⑥ 改一台在跑的机器

全板唯一一块讲**过程纪律**而不是系统结构的，也是最像真实工作的一块。

空间编码是「**改动生效的深度**」：两级回滚落在两个深度上，而那个反直觉的结论——
`nginx -t` 没过反而是更安全的一级，因为配置压根没被加载——正是从深度看出来的。

另含止损线（连败 3 次 × 间隔 ≥5 分钟：前者留抖动余量，后者避开 LE 速率墙）、
六步回退清单、以及清单里最容易被顺手删掉的一条：保留 `/etc/letsencrypt`，
里面的 ACME account key 删了重来会撞账号注册限制。

末尾挂 `shop.bak` 这个**活着的破口**：它停在段 0 之前，拿它回滚会把 URL 面收敛一起退掉，
而且不会有任何报错。留下的那条可迁移结论是——「有备份」和「备份是当前形态」是两件事。

### 顺带修的一类缺陷（第二次出现）

§12.9 记过「纯文本里的 Markdown 语法」。这一轮又在新写的字符串里漏出四处 `**强调**`
和几处反引号——它们在 `<p>` 里原样显示。**同一类缺陷第二次出现，说明它不是手误而是习惯**：
写事实字符串时会不自觉地按 Markdown 排版。已全部改成靠语序表达强调。

### 验证

typecheck 与 `VITE_SHOWCASE_ONLY=1` 生产构建通过；
**九块 × 桌面 1440 / 移动 390 两个视口全覆盖断言**：无横向溢出、无控制台报错、
每块面板高度均 ≥400 px（防止某块只渲染出空壳而截图时没注意）。
执行期抓到一处溢出：90 天轴末刻度在 `left:100%` 上，文字框再往右伸把整页顶宽到 1524 px，
改成向左展开后消除。

## 12.15 D5 收口后的阶段 1：先修谎报，再补新事实（2026-08-14）

### 触发与顺序

W9 全周 8/14 收口，新增 `day5-rebuild-closeout.md`（336 行）与 `server-permission-cheatsheet.md`，
`day5-demo-script.md` 与 `shop-ssl.conf` 也变了。但本轮**没有先加新块**——
和 §12.13 那次同一判断：展板此刻在说谎，修谎报比补内容紧急。

### 六处谎报（都在页面上，不只是数据里）

| # | 展板当时说 | 8/14 事实 |
|---|---|---|
| 1 | Q8 是「这个选择欠下的账」，且「Express 那五条路由至今谁都能调」 | 8/14 已还：统一守卫 + 三档 401/403/200 + jest 全过 + 线上 401 复现 |
| 2 | 时区「D5（8/14）待决策」，且挂在契约仍欠列 | 明确不修——UTC 保留为已知口径 |
| 3 | shop.bak 是「这套纪律当前唯一的破口」，161 字节 | 已刷新为 424 字节的当前白名单形态，破口闭合 |
| 4 | 生产对照把「应用层鉴权补齐」列在缺失栏 | 已做；缺失栏换成 8080 明文过渡期与配置不在 git |
| 5 | 「四个对外面」「四个面各自一份 server 块」 | 五个面 / 四份 server 块——第五个面是 443 里的一个 location |
| 6 | 契约仍欠 2 条（冷路径复核、时区口径） | 两条都在 D5 收掉，仍欠 0 条 |

第 5 条不是数量更新，是**一个新区别**：8/13 之前「对外面数」与「server 块数」一直相等，
所以从没有必要分开。8/14 之后不相等了，`PublicFace` 因此加了 `blockKind` 这一格——
数据结构的形状跟着事实变，而不是把新面硬塞进旧形状里。

第 6 条的连带效果最值得记：**「还欠什么」这块板的答案换了一批人**。
D1 那 12 条全部有了结论之后，如果只对账契约表，板上会得出「没有欠账了」这个假结论。
所以新增 `OPEN_ITEMS`：8080 明文过渡期、shop-ssl 改动不在 git、`users.http` 样例失效、
时区偏差本身。并且**主动接受**与**还欠着**分成两类着色——
把它们混成一张「待办」会让前者显得像疏忽，也会让后者显得像已经想清楚了。

### 并入既有块的五处新事实

① ufw 五段 8/14 复核不变（重启冲不掉这层）｜② Q8 已还 + 新增双层防线图｜
③ `certbot.timer` LAST 04:14:01｜⑦ 新增两次验收｜⑩ admin 迁 443 是「服务不变、门换了」的最纯样本。

其中两处产生了新结论：

- **③ 是全板第二次档位升级**（第一次是安全组那一列）。8/13 手里只有 `enabled` 与 `NEXT`，
  说「它每天会跑两次」是从配置推的；8/14 看到 `LAST`，才第一次有了观察。
  同时必须写清它**仍然不证明**续签成功——timer 跑的是检查，剩余天数大于 30 就跳过。
- **⑦ 长出了覆盖跨度图的一种新形状**。此前所有验收要么覆盖右半段（B2/B3 从 3000 起），
  要么四段全覆盖。D5-A 冷启动是第一次**覆盖左边三段、最右边一段空着**——
  它打的是根路径 200 与两个 404，全都不查库。数据面在重启后正常，
  是当天晚些时候浏览器登录 + 报表锚点 258 才补上的，那是另一次验收。
  不把这两次分开写，「四面全通」会被读成「一切正常」。

### 双层防线图：第一版把机制画错了

Q8 还清之后，同一个 `GET /users` 会走出两条路：公网走到 Nginx 被 404 挡下；
服务器内直连 3000 被应用层 401 挡下。空间编码是**两条路径各自停在哪一层**。

第一版把两条都画成「发起 → Nginx → Express」，只是给内线那条画了实线。
截图才看出问题：那等于说内线也经过了门卫——而这块图存在的全部理由，
正是要否掉「两个数字是同一道防线的两种说法」。改成内线的 Nginx 节点变灰、
入线段标「绕过」之后才成立。**断言测不出这类错**，它测的是元素在不在，不是画的对不对；
所以补了四条结构断言（内线必须有 `.bypass`、内线的 Nginx 必须 `.dim`、
公网那条必须有 `.cut`）把这个形状钉住。

### 一处 CSS 与数据脱节

`SETTLE_DAYS` 加了 `D5`，销账轨道就有了五列，而 `.w9-settle-cols` 还写着
`repeat(4, ...)`——「仍欠」那一列掉到第二行，**横坐标 = 哪天销的**这层位置编码当场失效。
typecheck 与内容断言都不会响。补了一条几何断言：五列的 `top` 必须只有一个值。

### 第三次的 Markdown 残留（这次是存量）

§12.9 与 §12.14 各记过一次「纯文本里写了 Markdown 语法」。本轮的断言写得比前几轮严
（扫整块板、只排除 `pre` 与 `code`），于是抓出**5 处存量反引号**：
`HTTPS_READINGS.renew`、契约 `cert-renew` 的销账证据、以及三处认知修正的文案。
它们是 8/13 重建时写进去的，此前的断言没覆盖到。全部改成纯文本。

同一类缺陷第三次出现，结论比前两次更硬：**它不是手误，是写事实字符串时的默认排版习惯**，
只能靠断言拦，不能靠注意。

### notes tab 接入五份

`day5-rebuild-closeout` / `day5-demo-script` / `server-permission-cheatsheet` /
`day4c-showcase-gate-deploy`（此前一直漏着，⑩ 板的源笔记没进过笔记 tab）/ `week9-plan`，
全部 `reviewOnly`。按需分片继续成立，五份各切一个 chunk，`MarkdownNotes` 主 chunk 未变
（166.86 kB / gzip 52.03）。

### 验证

`yarn typecheck` 通过；`VITE_SHOWCASE_ONLY=1` 生产构建通过；
对生产静态产物跑断言 **112 项全过**（1440 × 1000 与 390 × 844 两个视口，
含十个专题各自的白字扫描、Markdown 残留扫描、横向溢出与触控目标）；
桌面浅色 / 深色与手机浅色逐块截图核对。零后端请求断言通过。

**顺带核出的一件事（不在本轮范围，但影响 Pages）**：D4-c 的登录门禁让 showcase 入口
不再零后端——`AppShowcase.tsx` 引入了 `api.ts` 的 token，登录走后端 `/auth`。
8081 由 Nginx 反代 `/auth` 所以门禁是通的；Pages 没有后端，门禁将无人能登入。
也就是说 **Pages 现在即使解冻也发不出可用版本**，除非先决定门禁在纯静态目标上怎么处理。
已记进 `SHOWCASE-DEPLOY-PROTOCOL.md` §0。

### 断言脚本仍未入库

历轮那 250 → 298 → 112 项断言从来只活在会话里，每轮重建一次。本轮正因为重建时写得更严，
才抓到 5 处存量残留——这既说明重建有价值，也说明**每轮的严格程度不可比**。
是否把它落成仓库里的脚本，留给本人决定。

## 12.16 阶段 2：⑪ 发布变更单（2026-08-14，第 11 块）

### 与 ⑥ 的分工，以及为什么它不该并进去

⑥「改一台在跑的机器」问的是**改砸了怎么退回去**——失败之后；
⑪ 问的是**凭什么敢按下回车**——动手之前。同一次发布的两个相位。

判据仍是那条「每块只回答一个问题」。而且两块的空间编码根本不同：
⑥ 是「改动生效的深度」（两级回滚落在两个深度上），⑪ 是一张覆盖矩阵。
并成一块会让两个都退化成列表。

### 空间编码：六项验证 × 四层覆盖

主图是矩阵，行是六项验证（按深度排：构建期 → 本地预演 → 线上公网 → 服务器内），
列是这次发布真正在担心的四件事（新入口 / 应用层守卫 / 旧面回归 / 公网兜底）。

**列的分布本身就是结论**，写成六段文字就看不出来了：

- 新入口被验了 **3 次**——构建期、本地预演、线上各一次，一次比一次贵。
  它是这次唯一真正新增的东西，也是唯一可能完全不工作的东西。
- 公网兜底只验了 **1 次**。它是段 0 那条安全边界，而动 443 的配置最容易顺手碰坏它。
- 第 ④ 项**一次落在两列上**：443 报表不带 token，既证明 API 面没被新 location 挤坏，
  又证明 Q8 真的上线了。一个请求验两层——而这只有在期望写成 401 而不是 200 时才成立。

底部那一行列计数是一眼结论图：3 / 2 / 2 / 1。

### 详情区把「期望 → 期望来源 → 实测」按这个顺序钉死

这块板的全部主张就在这个顺序里：先写期望，再去对结果。所以详情区的三行不是随手排的，
而且**期望来源**必须占一行——没有来源的期望等于事后解释。断言直接查 `dt` 的文本序列。

第一次写这条断言时偷了懒，拿整页文本比 `期望` 与 `实测` 谁的下标小，结果失败了：
证据档位图例里就有「实测」这个词，位置比正文靠前。改成查 `dt` 序列才是在测结构。

### 产物二份制排在变更单本体之后

它不是事先想到的——是写变更单逐项列改动时才发现的：原方案要把带 base 的产物覆盖到
服务器现有的 `dist`，而那正是 8080 在 serve 的那一份，覆盖之后 8080 首页会去引用
`/admin/assets/...`，整站资源 404、过渡期入口当场废掉。

所以排版顺序是「先看那张单子，再看它当场拦下了什么」。这条是变更单最有说服力的一次回报：
四要素里「改动清单」那一栏的收益，不是回滚时知道改了什么，是**写的时候就发现了冲突**。

### 手机上抓到的一处

覆盖矩阵在 390px 上按项展开、每格自带层名，这套照抄验收链那块板已经验证过的做法。
但底部的列计数忘了给 `data-col`——手机上表头一收起来，一眼结论就变成了
**四个没有主语的数字**（3 次 / 2 次 / 2 次 / 1 次）。截图才看见。补上 `data-col` 与 `::before`。

同一类问题在同一块板里出现两次（格子做了、脚注忘了），说明「表头收起时每个单元格都要自带标签」
这条应该在写移动变体时**逐个元素过一遍**，而不是只处理主体。

### 顺带补的一处范围声明

认知修正表现在有 17 条，而浓缩地图 §5 已经有 32 条。板上那 17 条是**执行期踩出来的**，
D5 能力检验里口述时暴露的 8 处不在里面——它们性质不同：不是做的时候踩到，
是讲的时候才发现自己没真懂。不写清楚，17 会被读成全部。
补了一句范围声明并指向浓缩地图；那 8 条的正式归宿是 ⑫。

### 验证

`yarn typecheck` 与 `yarn build:showcase` 通过；`yarn verify:board` **133 项全过**
（十一块 × 两视口的体检 + ⑪ 的九条内容断言）；桌面浅色 / 深色与手机截图逐块核对。

## 12.17 断言脚本落进仓库（2026-08-14）

`scripts/verify-w9-board.mjs` + `yarn verify:board`。促成它的是 §12.15 那件事：
那轮把 Markdown 残留扫描写严了一点，立刻抓出 5 处**存量**反引号——它们是 8/13 写进去的，
前几轮的断言压根没覆盖到。也就是说每轮重写一次，严格程度就重新掷一次骰子。

脚本自带静态服务（`node:http`，不依赖 http-server），playwright 不进 devDependencies——
它只在这一个脚本里用得到，装进依赖树会让每个人的 install 都变重。

**一处必须记下来的解析细节**：裸标识符 `import("playwright")` 只会沿 `node_modules` 往上找，
**解析不到全局安装的包**。第一版就是这么写的，本地明明装了却报找不到。
正确做法是显式再试一遍全局目录——node 在 `<prefix>/bin/node`，全局包就在 `<prefix>/lib/node_modules`
（nvm、homebrew、apt 装的 node 都是这个布局）。错误提示里也把试过的路径逐条列出来，
不然下次还得重新查一遍。

## 13. 下一步入口

```text
十一块已落地，事实截到 D5（8/14）收口。Pages 上传已冻结，发布目标改为服务器 8081：
① 信任边界与端口（五个对外面 / 两道闸门 / timed out→refused 差分）
② URL 面与授权层（白名单 / 404 形态 / 两层威胁模型 / Q8 已还 + 双层防线图）
③ 证书与信任（信任链四环 / H1 三处收紧 / 90 天轴 / timer LAST 档位升级）
④ 故障分叉（+ TLS 排障两相位）        ⑤ systemd 失败模式
⑥ 改一台在跑的机器（两级回滚 / 止损线 / 六步清单 / shop.bak 破口已闭合）
⑪ 发布变更单（四要素 / 六项验证 × 四层覆盖矩阵 / 产物二份制 / 三个执行期踩点）
⑦ 端到端验收链（7 次 + 入口面 + 冷启动那次的空右段）
⑧ 反代 header 决策（四份落盘配置，含 443 的 location /admin/）
⑨ 契约销账（12 条全销）与资源闸门 + 契约表之外的四笔
⑩ 服务边界 vs 暴露边界（一进程五入口 / 两种切分各选过一次）

  → notes tab 已接入 W9 十二份原文（含 D5 收口、demo 讲稿、权限速查表，全部 reviewOnly）
  → 前端已按 admin / showcase 双入口解耦：模块边界仍成立，但**门禁让 showcase 不再零后端**
  → 发布是独立授权动作；当前决策为 Pages 冻结、仅服务器 8081（见 SHOWCASE-DEPLOY-PROTOCOL §0）

仍未上板的一块（记 BACKLOG，非阻断；原稿把它编成 ⑩，而 ⑩ 已被「服务边界 vs 暴露边界」占用）：
  ⑭ 产物与溯源 —— 「服务器上跑的是哪个 commit，产物里有什么」
     素材已齐。产物二份制那条 8/14 已经落到 ⑪ 上（它是那次发布的一部分），
     这里剩下的是真正的溯源问题：服务器上跑的是哪个 commit、shop-ssl 的改动不在 git。
     暂缓理由：主体是 week8 前端自身的发布链路，离 W9 部署主线较远。

按 8/14 定案的方案，⑪ 已落地（见 §12.16），剩下两块（每块做完先验证再进下一块）：
  ⑬ 以谁的身份碰谁的东西 —— 身份 × 对象矩阵，12 条坑族挂到对应格子上，全部 experiential。
  ⑫ 讲得出来才算会 —— C 模块 8 处当场修正钉在它所属的那一层上（DNS→…→Mongo），
     一眼结论图是七层轴上错误的分布密度。排最后，因为它与 ⑨ 的边界要先划清。
```

每块的共同要求（第一轮修订反推出来的）：**至少有一个空间关系是靠位置 / 包含 / 连接表达的，
而不是靠一段说明文字**；并且尽量各带一张一眼结论图。

W9 主线本身不受影响：D4-HTTPS 与 D5 仍按 `week9-plan.md` 推进；本方法只在既有事实上做展示，不产生新的技术结论，也不占用主线的验收口径。
