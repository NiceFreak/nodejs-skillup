# DEBT.md — 欠债记账

规则见 [`AGENTS.md`](./AGENTS.md) 的「欠债记账」与「重建梯子」两节。记账触发条件：

- AI 对黑名单知识点给出 **L2（定向提示 / 伪代码骨架）** 援助；
- AI 对任何知识点误给了 **L3/L4（局部 / 完整实现）** 援助（白名单项除外）；
- 重建练习中**卡档**（某档两次未通过）。

每条债在状态恢复后安排该知识点的延迟重建（通常次日或周验收前）；还债 = 按重建梯子当前档位重建通过 + 至少两项掌握证据（见 `AGENTS.md` 的「欠债记账」），完成后把状态改为「已还」。

| 日期 | 知识点 | 援助级别 | 场景（一句话） | 重建安排 | 状态 |
|---|---|---|---|---|---|
| 2026-07-13 | 自然月报表边界：滚动窗口 vs 自然月契约、`$gte/$lt` 半开区间、起点移动 `months - 1` | L2 骨架 | 问题由本人发现，方案由 AI 在沟通中给出；首次脱离提示时把 `$lt/$lte` 误判为性能问题 | 第一档重建通过（2026-07-16 D4）；2026-07-27 能独立把需求改为“本月及前两个月”，推导三个月自然月范围、半开区间和跨年/月末边界测试。未约定的空月补零不计入证据 | 已还 |
| 2026-07-14 | 注册竖切的文件职责与依赖方向 | L2 骨架 | 本人先给出文件方案，AI 在 review 后明确最小文件集合、复用边界及 D2/D3 范围 | 第一档重建通过（2026-07-17 D5）；2026-07-27 能对齐当前真实代码复述重复邮箱链：Repository 将 `11000` 翻译为 `EmailConflictError`，Service / Controller 透传，Express 5 交给全局 error handler 映射 409 | 已还 |
| 2026-07-15 | JWT 签发链路：配置校验归位、Service 签发与响应边界 | L2 定向提示 | 本人完成首版签发后，AI review 定位 JWT secret 校验误放数据库连接、Service 重复校验及 Controller 未迁移契约 | 第一档重建通过（2026-07-17 D5）；2026-07-27 能把可配置有效期拆为启动配置校验与 Service 签发必改项，并识别 `expiresAt` 响应和过期错误码属于可选 API 契约 | 已还 |
| 2026-07-16 | 最小 RBAC：数据库角色来源、授权中间件、admin-only 报表接线 | L2 定向提示 | 本人已完成 role 与 requireRole 首版，AI review 后给出接入财务报表 API 的最小范围与验证路径 | 第一档重建通过（2026-07-17 D5）；2026-07-27 能基于 `validateToken → requireRole → controller` 真实链路，预测新增 `analyst` 对 Schema、现有授权中间件契约、路由和 401/403 的影响 | 已还 |
| 2026-07-21 | CPU 阻塞实验的 timer 测量基准：注册时间、实际等待与迟到量 | L2 定向提示 | 本人完成 20ms / 2000ms 对照实验后，AI review 定位计时基准早于预测日志和 timer 注册，导致“实际等待”混入注册前开销 | 2026-07-27 第一档重建通过：能区分 timer 注册、CPU 起止与 callback 开始，定义实际等待和迟到量；并预测 delay 100ms→300ms 时迟到量约 1900ms→1700ms，说明实验的证明边界 | 已还 |
| 2026-07-22 | Threadpool 排队实验的测量口径与证据边界：任务提交 / worker 执行 / callback 开始、事实 / 推断 / 未测量 | L2 定向 review | 本人完成 `pbkdf2-test.js` 与判断表后，AI 多轮定位 Poll 队列误述、提交与执行混淆、elapsed 口径及过度归因，并给出三层收口结构；核心修正与实测由本人完成 | 2026-07-27 第一档重建通过：能把 callback 时间分布和总耗时列为事实，把 4+4 与 pool 排队列为受控推断，并明确 worker 开始、纯计算耗时和精确排队时间未测量；能设计只比较完成分布而不冒充内部计时的观测 | 已还 |
| 2026-07-23 | `pipeline()` 输出端失败注入：路径解析、统一错误出口与 stream 销毁证据 | L2 定向提示 | 本人实现成功 / 失败路径后，AI review 精确定位 `FAIL_OUTPUT` 实际指向可新建的 `src/src` 文件，导致预期 `EISDIR` 未发生；核心修复与复跑由本人完成 | 第一档重建通过（2026-07-24 D5，只看本人一页笔记从空白重建成功 / 输出端失败路径）；掌握证据：能独立推导不存在路径为何会被创建、复述错误交付链与证据边界，并预测任意 UTF-8 转换需求对 Transform / 验证契约的影响 | 已还 |
| 2026-07-23 | 当前项目异步 `bcrypt.hash/compare` 的执行归属：CPU 密集不等于占用 JS 主线程 | L2 定向纠错 | AI 在展示审查中指出 D1 笔记把异步 bcrypt 误归为主线程 CPU；本地 bcrypt 6.0.0 README 与 `Napi::AsyncWorker` 源码表明异步 API 使用 threadpool | 2026-07-27 第一档重建通过：能复述 JS 主线程、native binding 与 libuv threadpool 的登录链路，预测 pool 饱和对登录与 heartbeat 的不同影响，并设计分档独立运行、并发测登录与 `/ping`、对比 pool size 的观测方案 | 已还 |
| 2026-08-26 | Jenkins `Run.getLog()` 返回类型：无参版本返回 `String`（整个日志，Groovy 按字符迭代）；`getLog(int)` 返回 `List<String>`（按行、tail 语义） | L2 定向提示 | V9 反向证明暴露 validate-logs 永远绿：注入假私钥头未被扫到；诊断输出 `size=31663`（字符数）且前 5 元素为 `S/t/a/r/t`（逐字符）；AI 对照 Jenkins 源码定位根因并指出 `getLog(int)` 修复方向，修复由本人实现 | 第一档重建通过（**2026-08-27 W11 D4**）：不看笔记，从 V9 证据推导出「无参版按字符迭代 → 匹配对象单字符 → 假私钥头在日志里但永远失配」与「`getLog(int)` 按行迭代 → 匹配对象整行 → 修复生效」的完整机制；掌握证据两项：全量扫描方案的分层代价预测与取舍、15000 行构造样本的 tail 窗口漏报验证设计（首答两处偏差见 `day4-rollback-drill.md` §4） | 已还 |
| 2026-08-27 | 类 2 最小样本：close 竞争构造与收尾逻辑（close 注册位置、探测时机、sync 收尾兜底） | L2 定向提示 | 本人实现 `reproduce-close-race.js` v1→v8；AI review 定位：探测在 close 完成回调后 → 判据 100% 假阳性；close 在 cb 内 → 竞争窗口不成立（应移到 `listen()` 调用后与回调竞速）；sync 模式 catch/close 回调/短兜底缺「cb 未触发 → probe pending」收尾 | 重建安排：**W12 D2（2026-09-01）第一档执行 → 卡档**——不看解释复述「探测时机为何必须在 close 前发起」「三种 close 时序（inCallback/afterListen/sync）各自的竞争语义与实测结果」「EADDRINUSE 注入为何须绑同地址」。（2026-08-28 D5 拍板顺延；2026-08-31 D1 全天被五周计划评审占用，再顺延一天，仍为 D2 第一入口；从零盲重建）。**卡档记录（2026-09-01 W12 D2）**：题 1 方向对但机制偏差（本案是单次连接探测与 close 竞速，非快照对比；探测是发起非完成）；题 2 整体偏离——通用 Node 语义推演而非本案脚本构造，sync「阻塞当前线程」为 Node 事实错误（`server.close()` 不阻塞），未给实测结果（真实实测：三模式 × 100 均未复现，listening 回调微任务恒先于 setImmediate close → falseActive=0、sync 取消 listen 回调 → A=0；假 active 靠完整 server.js + EADDRINUSE 注入复现），未触及 sync 收尾兜底（catch / close 回调 / 短兜底 cb 未触发 → probe pending）；题 3 方向对但缺本案关键经验事实（IPv6 通配 `*:3002` 不挡 IPv4 `127.0.0.1:3002`）。AI 已按重建规则以 L1 讲解真实机制，详见 `day2-freeze-and-baseline.md` §5。**再重建：另排（D2 下午机动或 D3 前），仍第一档，连续两次通过才升档**。**D3（2026-09-02）再重建 → 再次卡档**：题 1（探测时机/动作/信号）✅ 完整——单次连接探测须在 close 前发起、`net.connect` 观察 connected/refused/timeout 三态；题 3（同地址注入）✅ 完整——地址族 + IP 形态 + 端口、IPv6 通配 `*:3002` 不挡 IPv4 `127.0.0.1:3002`（bindv6only=0）、需 `listen(3002, '127.0.0.1')` 精确抢占；题 2 部分正确——inCallback 无竞争窗口 ✅、sync 取消 listen 回调（A=0）✅，但 afterListen 机制推导有 Node 事件循环事实错误（误称 bind 完成回调与 setImmediate close 位于同一 check 阶段；实际 bind 完成由 poll 阶段观察、listening 经 nextTick 微任务派发、恒先于 check 阶段 setImmediate close，falseActive=0 是阶段顺序保证的确定性结果），且**仍未触及 sync 收尾兜底**（catch `ERR_SERVER_NOT_RUNNING` / close 回调 / 短兜底 cb 未触发 → probe 置 timeout、无条件 finish）。AI 已按重建规则以 L1 讲解，详见 `day3-bub-main-chain.md` §8。**再再重建：另排（D4/D5 机动），仍第一档**。**D4（2026-09-03）第三次重建 → 通过（连续通过第 1 次）**：完整第一档三题全部通过——afterListen 阶段推导（poll 观察 bind 完成 → listening 经 nextTick 派发 → 恒先于 check 阶段 setImmediate close，`falseActive=0` 为阶段顺序保证的确定性结果）与 sync 三层收尾兜底（catch `ERR_SERVER_NOT_RUNNING` / close 回调置 closeDone / `SYNC_CLOSE_TIMEOUT` 50ms 无条件 finish）补齐前两次卡档缺口；题 1 一处次要瑕疵（close 完成后稳态 connect 为 `ECONNREFUSED`、`TIME_WAIT` 属连接层）不改判。**D5（2026-09-04）第四次重建（完整第一档）→ 通过（连续通过第 2 次）**：三题全过——题 1 探测时机/动作/三信号完整（timeout 语义精确化点不判错）；题 2 inCallback 无竞争窗口 / afterListen poll→nextTick→check 确定性阶段推导 / sync A=0 + 三层收尾兜底全部补齐（D2/D3 两次卡档缺口）；题 3 补正后通过（本案 macOS 实测：IPv6 通配 `*:3002` 不挡 IPv4 `127.0.0.1:3002`，须 `listen(3002,'127.0.0.1')` 精确预占，不以 Linux 一般推断替代平台实测）。掌握证据两项核对通过：证据 A（runOne 五状态数据流 + finish 单次守卫 + probeResult pending→终态路径 + 三模式目的与取舍）+ 证据 B（PROBE_TIMEOUT 200→20ms / SYNC_CLOSE_TIMEOUT 50→5ms 对三模式统计与结论的影响预测）。证据 A 两处表述已指正不阻断（`listeningAtCallback`「必为 true」是实测结论非代码保证；inCallback 的 listen 失败走 `server.on('error')` 立即 resolve，非靠 3s 兜底）。连续 D4+D5 两次通过 + 两项掌握证据 → **已还（2026-09-04 W12 D5）** |
