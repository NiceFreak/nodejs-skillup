$kind: http-request
description: "Expected: 400,name 是 Model 里的必填字段。"
url: "{{baseUrl}}/users"
method: POST
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "email": "missing-name-{{$timestamp}}@example.com",
      "age": 20
    }
order: 3000
