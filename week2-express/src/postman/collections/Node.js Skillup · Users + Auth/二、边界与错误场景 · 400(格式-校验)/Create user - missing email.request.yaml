$kind: http-request
description: "Expected: 400,email 是 Model 里的必填字段。"
url: "{{baseUrl}}/users"
method: POST
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "name": "Missing Email User",
      "age": 20
    }
order: 2000
