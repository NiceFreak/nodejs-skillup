好,选 A——独立输出练习。规则说清楚:**我给需求,你从空白独立设计整条管道并写出来。写之前我不给骨架、不给阶段提示。** 卡住了你可以问我方向,但别一上来就问,先自己撑一会儿,那个"撑"的过程才是练习本身。

---

**新需求:月度销售趋势报表**

统计**最近 6 个月**、每个月的销售情况,输出每个月的:
- 该月**订单总数**
- 该月**销售总额**
- 该月**平均订单金额**

只统计 `completed` 状态的订单,**按月份先后顺序排列**(从早到晚)。

---

**这个需求和前两个的关键不同(你要自己应对的新东西):**

前两个是"按客户分组",分组键是现成的字段(`userId`)。**这次是"按月份分组"——但你的订单里没有"月份"这个字段,只有 `createdAt`(一个完整的日期时间戳)。** 所以你要解决一个新问题:**怎么从 `createdAt` 这个日期里,提取出"年-月"来作为分组依据?**

这是本次的核心挑战,也是你没做过的。提示到此为止——**怎么提取月份、用什么运算符,你自己查、自己想。** 方向:聚合里有专门处理日期的运算符,能从一个 Date 里取出年、月、日。

**你要自己做的完整决策(和前两次一样,但这次没人帮你列骨架):**
1. `$match` 筛什么(status + 时间范围"最近6个月")
2. **`$group` 的 `_id` 怎么定**——这是难点,要按"年+月"分组,想清楚 `_id` 写成什么
3. 三个统计字段怎么算(这个你熟)
4. `$sort` 按什么排、升序还是降序(注意需求要"从早到晚")

**先自己写,撑住,别急着问。** 真卡死了(尤其日期提取那块查不到方向),再来问我要方向——但先自己试。

写完把**完整管道 + 你的设计思路**发我。我会像前两次一样 review,但这次重点看:**没有引导的情况下,你能不能独立把结构立起来。**

（数据方面:你库里现在的订单 `createdAt` 跨度够不够 6 个月?你 seed 的数据从 1 月到 7 月都有,够用。不够的话跑出来月份少几个,不影响验证逻辑对错。）

开始吧。

你独立把骨架立起来了,`$match`/统计字段/`$project`/`$sort` 的结构都在——这说明前两天的框架你是记住了的。但这一版有几个**核心逻辑没对上需求**,而且暴露了一个关键点:你把上一个报表的模板套过来了,但没真正针对"按月份"这个新需求改造。逐个看,我指方向不给实现。

**🔴 1. 最核心的问题:`$group` 的 `_id` 完全错了,而且你回避了本次的真正挑战。**

你写的 `_id: "$orderId"`——两个问题:
- **没有 `orderId` 这个字段**(你订单的主键是 `_id`,没有 `orderId`)
- 更关键:**需求是"按月份分组",不是按订单分组。** 你 `_id` 应该是"这条订单属于哪个年月",但你写成了订单标识。如果按订单分组,每个订单自成一组,根本没有"月度"统计。

**你把本次唯一的新挑战跳过了**——我提示的"从 `createdAt` 提取年-月作为分组键",你没做,直接套了个字段名上去。这是这道题的核心,不能绕。

回到那个必须自己解决的问题:**怎么从 `createdAt`(一个完整日期)里提取出"年"和"月",用它们作分组键?** 方向再给明确一点:聚合有专门的**日期运算符**,`$year`、`$month` 能从一个 Date 里取出年份数字和月份数字。你要用它们构造 `_id`。想想 `_id` 怎么写——是只按 `$month` 分组,还是要 `$year` + `$month` 一起?(提示:如果只按月份 1-12 分,那 2026年1月 和 2025年1月 会被合并成一组,对不对?)

**2. `$project` 里的字段全是从旧模板抄来的,和月度需求对不上。**
- `userId: "$_id"` —— 这是客户报表的逻辑,月度报表哪来的 userId?你现在 `_id` 该是年月,不是用户
- `createdAt: "$month"` —— `"$month"` 这个字段不存在(你没造过);而且你想输出的应该是"这是哪个月",得来自你 `$group` 的 `_id`

`$project` 要输出的是:年月标识 + 三个统计值。你把它改成贴合月度报表的字段。**先想清楚 `$group` 的 `_id` 长什么样,`$project` 才知道从哪取月份。** 这两步是连着的。

**🔴 3. `$sort` 排错了维度。** 需求要"按月份先后从早到晚",你写的是 `totalSpending: -1`(按销售额降序)——那是上个报表的排法。月度趋势要按**时间顺序**排,不是按金额。你要按 `_id`(年月)升序排。想想怎么写。

**4. 参数问题(小):** 你函数签名 `(status, date)` 里 `date` 是"最近6个月的起点",这个 OK,和之前一致,service 层算好传进来。这个没问题。

---

**这次 review 的重点结论(对照你昨晚说的"输出/掌握"):**

你**结构记住了**(四个阶段的形状、`$project`/`$sort` 的写法都对)——这证明框架层面你掌握了。但你**把上一题的模板整体平移**,没有针对新需求(按月分组)做核心改造,尤其**回避了"日期提取"这个唯一的新难点**。

这恰恰是有价值的发现:**"能套用模板" ≠ "能应对新变化"。** 真正的掌握体现在遇到"按月份"这种没做过的分组时,你能想到"我需要从日期提取月份、需要 `$year`/`$month`"。这一步你现在卡住了——**这就是你该精准补的地方**,不是整个聚合不会,是"日期处理"这个具体点没接触过。

**所以别气馁,这个卡壳是好事**——它精确定位了你的空白:日期运算符 + "按派生字段分组"的思路。

**你现在去做:**
1. 查 `$year` / `$month`(或者 `$dateToString` 也能做,更简洁,可选)怎么用
2. 重新设计 `$group` 的 `_id`——按"年+月"分组
3. `$project` 从新的 `_id` 里取出月份输出
4. `$sort` 改成按 `_id`(年月)升序

**核心自己想:`_id` 怎么用 `$year`/`$month` 构造。** 这是这道题的题眼。写不出来再来问,但先自己试。改完发我。

大进步——你自己找到了 `$month` 并用它构造 `_id`,这道题的题眼你攻下来了。剩下几个问题,有🔴也有需要你想清楚的判断,逐个看。

**先肯定核心突破:** `_id: { month: { $month: "$createdAt" } }` ——你独立想到了"从 createdAt 提取月份作分组键",而且用了对象形式 `{ month: ... }`。这一步是这道题最难的,你没要实现就自己做出来了。这说明"日期提取"这个空白你补上了。

**但有个🔴设计缺陷,是我上一条特意提示、你还没处理的:只按 `$month` 分组,跨年会出错。**

你 `_id` 只有 `month`(1-12)。想这个场景:如果数据里有 **2025年6月** 和 **2026年6月** 的订单,它们的 `$month` 都是 `6`,会被**合并成同一组**——但它们是不同的月份!你的报表会把跨年的同月订单错误地并到一起。

我上一条提示过这个:"如果只按月份 1-12 分,那 2026年1月 和 2025年1月 会被合并成一组,对不对?" 你这版还是只用了 month。**正确做法:`_id` 要同时包含 `$year` 和 `$month`**,这样 "2025-06" 和 "2026-06" 才是两组。你自己改——`_id` 里加一个 `year: { $year: "$createdAt" }`。

（你现在数据都在 2026 年,所以跑出来"看起来对"——但这是数据碰巧没跨年掩盖了 bug。这正是 Day1 教训的重演:"数据碰巧对" ≠ "逻辑对"。生产数据一跨年就错。）

**🔴 2. `$project` 里 `month: "_id.month'"` ——有个语法错,你自己看:**

```js
month: "_id.month'"
```
两个问题:
- **缺了 `$` 前缀**:引用字段值要写 `"$_id.month"`,你漏了 `$`,现在它成了字符串字面量 `"_id.month"`(一个普通字符串,不是取值)
- **末尾多了个单引号 `'`**:`"_id.month'"` 结尾那个 `'` 是手误

正确应该是 `"$_id.month"`(带 `$`、无多余引号)。等你 `_id` 改成含 year+month 后,这里要取的可能是 `"$_id.year"` 和 `"$_id.month"` 两个。

**🔴 3. `userId: "$_id"` 又是从旧模板抄来的,月度报表没有 userId。**

这个字段和月度趋势毫无关系,是上一题客户报表的残留。删掉。你 `$project` 该输出的是:年、月、三个统计值。想清楚要哪些字段,别留旧模板的痕迹。

**🔴 4. `$project` 和 `$sort` 的顺序 + 字段引用冲突——一个隐蔽的坑。**

你 `$project` 里 `_id: 0`(排除了 _id),但 `$sort` 又写 `"_id.month": 1`(要按 _id.month 排)。**问题:`$project` 在 `$sort` 之前执行,它已经把 `_id` 排除了,等数据流到 `$sort` 时,`_id` 已经不存在了,`$sort` 按一个不存在的字段排,不会报错但排序无效。**

两个解法思路(你选):
- 要么 `$sort` 放在 `$project` **之前**(趁 `_id` 还在,先排序再投影)
- 要么 `$project` 里把年月保留成新字段(如 `year`/`month`),`$sort` 改成按这些新字段排

想想哪种更合理。(提示:回忆 Day1 你想通的"`$sort` 排的是它执行时刻文档里存在的字段"——`$project` 改变了文档形状,顺序很关键。)

---

**这次 review 的结论:**

**核心能力验证了:** 你独立攻破了"日期提取"这个新难点(`$month` 的发现和使用),这是这道题真正考你的地方,你做到了。剩下的问题分两类:
- **旧模板残留**(userId、只有month没year)——你惯性套用了上一题,需要针对新需求清理
- **细节错误**(`$` 前缀、多引号、project/sort 顺序)——这些是你已经会、但手快抄漏的

**最该记住的一点:跨年要用 year+month 分组。** 这个你数据没跨年所以没暴露,但它是"按时间周期分组"的经典正确姿势,面试和生产都会考。

**你去改:**
1. 🔴 `_id` 加 `year`(跨年正确性)
2. 🔴 `$project` 的 `month` 改成 `"$_id.month"`(+year),修 `$` 和多引号
3. 🔴 删掉 `userId`
4. 🔴 理顺 `$project` 和 `$sort` 的顺序/字段(别按已被排除的 `_id` 排)

改完发我。这几个理顺,你就独立完成了第三个聚合场景——而且是自己从空白立起来、自己攻破新难点的,含金量比前两个跟做的高。

方向对了很多——`$sort` 挪到 `$project` 前面(趁 `_id` 还在先排序),这个顺序你理顺了。但 `_id` 的结构写错了,导致 year/month 没真正分开,还有 `$project` 的字段写法有问题。逐个看。

**🔴 1. `_id` 结构写错了——`$year` 和 `$month` 被塞进了同一个 `month` 对象里,这不合法也不对。**

看你写的:
```js
_id: {
    month: {
        $year: "$createdAt",
        $month: "$createdAt"
    }
}
```

问题:你把 `$year` 和 `$month` **两个运算符放进了同一个 `{}` 里**,还都挂在 `month` 这个键下。一个对象里放两个聚合运算符,MongoDB 不知道该怎么算(而且语义上,`month` 键里既算年又算月,自相矛盾)。

**你要的是 `_id` 有两个并列的子字段:`year` 和 `month`,各自用各自的运算符。** 结构应该是:
```
_id: {
    year:  { $year:  "$createdAt" },
    month: { $month: "$createdAt" }
}
```
`year` 和 `month` 是 `_id` 下**并列的两个键**,一个取年、一个取月。你现在是嵌套错了层级 + 合并了运算符。自己改成并列结构。

**🔴 2. `$project` 里字段名用了 `"_id.year"` 这种带点的 key——写法不对。**

```js
"_id.year": "$_id.year",
"_id.month": "$_id.month'"
```

两个问题:
- **输出字段名不该叫 `"_id.year"`**。你已经 `_id: 0` 排除了 _id,又想输出 `_id.year` 当字段名,自相矛盾,而且带点的 key 在这里语义混乱。**给它们起干净的顶层名字**,比如 `year` 和 `month`(直接平铺出来,前端好用),值才是 `"$_id.year"` / `"$_id.month"`。
- **`"$_id.month'"` 又有那个多余的单引号 `'`**——你上一版就有这个手误,这次没改掉。去掉末尾的 `'`。

改成类似:
```
year:  "$_id.year",
month: "$_id.month"
```
左边是你要输出的干净字段名,右边是从 `_id` 取值(带 `$`、无多余引号)。

---

**其他都对了:**
- `$sort` 按 `_id.year` + `_id.month` 升序,而且放在 `$project` 前面 ✓ ——你理顺了"趁 _id 还在先排序"这个顺序,这是上一版的🔴,你改对了
- `userId` 删掉了 ✓
- year+month 一起分组的**意识**有了(只是结构写错),跨年正确性的点你接受了 ✓

---

**你去改两处:**
1. 🔴 `_id` 改成 `year` 和 `month` **并列**的结构,各用各的运算符
2. 🔴 `$project` 的输出字段起干净顶层名(`year`/`month`),值用 `"$_id.year"`/`"$_id.month"`,去掉那个多余单引号

改完发我。这两处理顺,管道就对了,可以跑。跑出来应该是按月份从早到晚排列、每月一条统计。到时候你会看到 `_id` 里 year/month 分开的效果。

这版对了。逐项确认:

- `_id` 改成 `year`/`month` 并列结构,各用各的运算符 ✓
- `$sort` 按 year+month 升序、放在 `$project` 前 ✓
- `$project` 输出干净的 `year`/`month` 顶层字段,值带 `$` 取自 `_id`,多余单引号去掉了 ✓
- 跨年正确性(year+month 一起分组)解决了 ✓

管道逻辑正确,可以跑了。**你先实际跑一下**(挂个临时路由或写个脚本调用,service 层传入"6个月前"的 date + `"completed"`),确认输出是按月份从早到晚、每月一条统计。跑出来贴我看一眼——顺便验证 Decimal128(totalSpending/avgOrderValue)有没有需要在 service 转换。

---

**先别急着往下,这次独立练习值得复盘一下——这是今天最有价值的部分,呼应你昨晚说的"输出/掌握"。**

**你这次真实的表现,如实说:**

**独立做到的:**
- 四阶段管道结构完全自己立起来(说明框架真的内化了)
- **自己发现并使用了 `$month`/`$year`**——这是本题唯一的新难点,没要实现就攻破了
- `$sort` 放 `$project` 前的顺序判断(第二版自己改对)

**卡住/反复的地方(这些就是你的"精准空白"):**
1. **惯性套用上一题模板**——userId 残留、一开始按订单分组。这是最该警惕的:你倾向"改上一个能跑的",而不是"针对新需求从头想"
2. **跨年分组**——提示了一次才补 year,说明"按时间周期分组要包含年"这个点还没成为直觉
3. **一串细节反复**(`$` 前缀、多引号、`_id` 嵌套层级)——这些是"知道但手快出错",多写会好

**这次练习证明了什么(对你昨晚的困惑很重要):**

你昨晚担心"重建难、掌握没那么好"。这次独立练习给了个**真实、客观的答案**:你的"重建"能力**比你以为的强**——框架你立得起来,新难点(日期提取)你自己攻得下来。你卡的不是"不会聚合",而是两类具体的东西:**惯性套模板的思维习惯**(要靠有意识地"从需求出发"来纠正),和**细节手误**(要靠多写来消除)。

**这两类都不是"能力不足",是"习惯"和"熟练度"。** 而它们恰恰只能靠"多输出"来改——你昨晚说要把天平往输出压,这次就是一次成功的输出练习,而且它精确暴露了该练什么。**这比你再读十篇聚合教程有用得多。**

**一句话:你不是掌握得不好,你是需要更多这种"从空白独立做"的输出,来把'会套模板'升级成'会应对新变化'、把'知道'升级成'手熟'。** 今天这次就是很好的一次。

---

先把这个管道跑起来验证结果,贴我看。然后我们决定:是接着把它竖切成正式 endpoint(像 Day2 那样),还是今天上午到此、下午做别的。你先跑。

返回空数组 `[]`——不是你聚合逻辑错,是**时间边界算错了**,`$match` 把所有订单都滤掉了。这行有两个 bug,叠在一起:

```js
$gte: new Date().setMonth(new Date().getMonth - 6)
```

**🔴 bug 1:`getMonth` 后面漏了 `()`,它是方法不是属性。**
`new Date().getMonth` ——你没调用它,拿到的是**函数本身**,不是月份数字。函数参与减法 `函数 - 6` = `NaN`。应该是 `new Date().getMonth() - 6`(带括号调用)。

**🔴 bug 2:`setMonth` 返回的不是 Date,是数字(时间戳)。**
`setMonth()` 这个方法很坑:它**修改原 Date 对象,但返回值是毫秒时间戳(number),不是 Date 对象**。所以你 `$gte` 拿到的是个数字,不是 Date。而且叠加 bug1 的 `NaN`,`setMonth(NaN)` 让整个日期变成 `Invalid Date`。

**结果:** `$gte` 是个无效值,匹配不到任何订单,返回 `[]`。

**怎么算"6个月前"——给你两个干净的方向,自己选:**

**方向A(和你之前一致,推荐):** 你 Day1 算"30天前"用的是 `new Date(Date.now() - 30*24*60*60*1000)`。但"6个月"不能简单换算成"180天"——每月天数不同。不过对趋势报表,近似成"180天前"通常够用,简单。

**方向B(精确按月):** 用 Date 构造:先拿当前日期,把月份减 6。正确写法是**分两步,别链式**:
```
const d = new Date();
d.setMonth(d.getMonth() - 6);   // 先改,d 本身被修改成6个月前
// 然后用 d(它现在是 Date 对象)
```
关键:`setMonth` 要**单独一行调用**(它就地修改 `d`),之后用 `d` 这个变量——别用 `setMonth` 的返回值(那是数字)。

**你选一种改。** 记住核心教训:`$gte` 必须拿到一个**合法的 Date 对象**,不是数字、不是 Invalid Date。这和你 Day1 seed 时"createdAt 必须是 Date 不是字符串"是同一类坑——**日期类型要对**。

---

**另外提醒:这个 `reports.js` 里的时间计算,最终应该在 service 层做**(和你 Day2 一样,service 持有"6个月"这个业务规则、算好 date 传给 repository)。你现在临时脚本里写死在 `$match` 里是为了快速验证,可以。但正式竖切时,`getMonthlySalesTrend(status, date)` 的 `date` 由 service 算好传入——你函数签名已经这么设计了,对的。

改时间边界那行,重跑。这次应该能出数据——按月份从早到晚,每月一条。跑出来贴我。

好,状态一般就做 A——竖切是你 Day2 走过一遍的熟路,判断为主、不烧脑,适合今天。

规则不变:核心你写,我 review。竖切三层你已经有 Day2 的模板,但**别直接套 Day2 的模板不动脑**(你今天刚在月度聚合上踩过"惯性套模板"的坑),这次每层想一下"月度报表和客户报表哪里不同"。

**你要竖切的三层 + 各自的判断点:**

**1. repository**:`getMonthlySalesTrend(status, date)` 你已经写好了(上面那个),直接用。这层完成。

**2. service**:新写一个,比如 `getMonthlySalesTrendReport`。要做两件事,注意和 Day2 的不同:
- **算时间边界**:这次是"6个月前"不是"30天前"。用你刚调试对的那个正确写法(`setMonth` 单独一行改、或近似 180 天),别再踩 `setMonth` 返回数字的坑。这个"6个月"是业务规则,service 持有。
- **Decimal128 转换**:`totalSpending`/`avgOrderValue` 转 number。你 Day2 用的是 `...rest` 展开 + 只覆盖两个转换字段那个写法,很干净,可以复用那个思路。

**3. controller + route**:
- route:`GET /reports/monthly-sales`(挂你 Day2 建的那个独立 `reportsRouter`)
- controller:调 service,`res.json()` 出去
- **参数问题想一下**:月度报表要不要也参数化?比如 `?months=6`(查几个月)、`?status=completed`。你 Day2 给客户报表做了 days/status 参数化 + 校验中间件。月度报表**要不要同样处理**?还是先写死 6 个月和 completed?
  - 判断:如果这个报表将来可能查"最近3个月""最近12个月",参数化有价值;如果就固定看半年,写死更简单。**你自己定,说得出理由。** 参数化的话,你得复用/新写 months 的校验(类似 validateDays)。

**建议(你定):** 今天状态一般,**status 复用你已有的 validateStatus 中间件**(现成的),**months 可以先写死 6 或简单参数化**——别为了参数化把简单事搞复杂。核心是把竖切跑通,参数化是锦上添花。

---

**自底向上写**(你 Day2 验证过这个顺序顺):repository 已好 → 写 service → 接 controller/route。

先写 **service 层**(算6个月边界 + Decimal128 转换),发我 review。重点我会看:时间边界这次算对没有(别再踩 setMonth 坑)、Decimal128 转换、以及你有没有无脑套 Day2 模板 vs 针对月度报表想过。

Decimal128 转换那块你复用得很干净(`...rest` + 只覆盖两个字段),这部分对。但有两个问题,一个是🔴逻辑错、一个是设计不一致,你自己看:

**🔴 1. 时间边界算的是"30天",不是"6个月"——需求没对上。**

```js
const setDate = new Date(Date.now() - 30 * 24 * 60 * 60 * 1000)
```
这是你 Day2 客户报表的"30天"公式,你直接抄过来了。但月度趋势报表要的是**最近6个月**。你又踩了刚才提醒的"惯性套模板"——把上一题的时间边界原样搬来,没针对月度需求改。

**改成 6 个月。** 两个方向(你刚在 reports.js 里调试对过其中一个):
- 近似:`6 * 30 * 24 * 60 * 60 * 1000`(180天,趋势报表够用,简单)
- 精确:`const setDate = new Date(); setDate.setMonth(setDate.getMonth() - 6);`(单独一行改,别用返回值——你刚踩过这个坑)

选一个,但结果必须是"6个月前",不是30天。

**🔴 2. 参数签名不一致:你收 `{ status }`,但没收 months,时间还写死。**

你解构 `{ status }` 只拿了 status,时间边界写死在函数里。这里有个设计矛盾要你想清楚:
- 如果时间就固定"6个月"、不让调用方改 → 那 `setDate` 写死可以,但要写对(6个月)
- 如果想参数化(`?months=6`)→ 那签名该是 `{ status, months }`,用传入的 months 算边界

你现在是"写死时间"但"参数化 status"——**半参数化,不统一**。回到你 Day2 的决策风格:要么都固定、要么都可配。**你自己定:月度报表的时间跨度要不要可配?** 我建议这个报表**先写死6个月**(月度趋势通常就是固定看半年/一年,不常动态调),那就把 status 也可以考虑写死 completed——或者保持 status 参数化(因为"看不同状态的月度趋势"有点意义)。**关键是想清楚并说出理由,别无意识地半套模板。**

---

**这次又是"惯性套 Day2 模板"——第二次了(上午聚合一次,现在 service 一次)。**

不是批评,是帮你看清一个**真实的模式**:你竖切/写 service 时,倾向"复制上一个能跑的,改一点点"。好处是快、结构稳;**风险是"改一点点"时容易漏改关键的业务差异**(这次漏了"6个月 vs 30天")。

**给你一个对治的小习惯:** 每次套用旧模板后,**问自己一句"这个新需求和上一个,业务上哪里不同?"** ——月度报表 vs 客户报表,不同就在"时间跨度(6月 vs 30天)"和"分组维度(月份 vs 客户)"。把不同点列出来,逐个确认改到位了。这样既享受模板的快,又不漏业务差异。

---

**你改:**
1. 🔴 时间边界改成 6 个月(别用 30 天公式)
2. 🔴 定清楚 months 要不要参数化,让签名和时间处理一致(建议:时间写死6个月,status 保留参数化,说得出理由)

改完发我。然后接 controller/route,竖切就完成了。

你选了参数化 months,方向可以,但**参数的语义和计算对不上**——这是个🔴,而且会让结果错得很隐蔽。

**问题:参数叫 `months`,但你按"天"在算。**

```js
const setDate = new Date(Date.now() - months * 24 * 60 * 60 * 1000)
```

`24 * 60 * 60 * 1000` 是**一天**的毫秒数。所以 `months * 一天` 的意思是"`months` 天前"。如果调用方传 `months=6`,期望是"6个月前",但你算出来的是"6**天**前"。**参数名说的是月,计算做的是天,完全对不上。**

后果:传 `months=6`,实际只往前查了6天,大部分订单被滤掉,报表几乎空的——而且不报错,你得跑出来看数据不对才发现。

**改法,两个方向(你选):**

**方向A:名副其实按月算。** 参数叫 months,就真按月减。用 `setMonth`(单独一行,别用返回值):
```
const setDate = new Date();
setDate.setMonth(setDate.getMonth() - months);
```
传 6 就是 6 个月前,语义对上。

**方向B:近似换算。** 如果你想保留"用毫秒公式"的简单写法,那把一个月近似成30天:
```
months * 30 * 24 * 60 * 60 * 1000
```
但注意这是近似(每月天数不同),而且**参数名建议想清楚**——如果按天近似,不如把参数就叫清楚。

**我建议方向A**:参数叫 months 就真按月算,`setMonth` 语义最准,月度报表本来就该按自然月对齐。你刚在 reports.js 调试时用过 `setMonth`,熟。

---

**这次的问题比"套模板"更细一层,值得点一句:**

你上一版是"忘了改 30天→6个月",这一版你**记得要改成月了(参数名改成 months 了),但计算公式只改了一半**——变量名从 30 换成了 `months`,可底层那个"一天的毫秒数"没动。**这是"改了个寂寞"的典型:表面改了(参数名),核心没改(计算逻辑还是按天)。**

对治还是那句:改完问自己"这个值的**单位/语义**对吗?" ——`months * 一天的毫秒` 一眼就能看出单位不匹配(月 × 天)。养成"检查单位一致性"的习惯,这类 bug 就能自己抓出来。

---

**你改:** 时间计算改成真按月(方向A的 `setMonth`,推荐),让它和参数名 `months` 语义一致。

改完发我,顺便说一下:months 参数化了,那**校验中间件**你打算怎么处理?(months 该是正整数,类似 validateDays——你要复用还是新写?)controller/route 接的时候要考虑。先改 service 时间计算,再说校验。
复用, 把validateDays 改为更通用的名字, 比如 validateDate

service 的时间计算这次对了——`setMonth` 单独一行、用变量本身不用返回值,语义和 `months` 对上了。这部分 OK。

**但"复用校验"这个决策方向对,命名想岔了,得帮你拨正:**

你想把 `validateDays` 改成通用的复用——**思路对**(days 和 months 校验逻辑确实一样:都是"正整数、缺省给默认")。但你起的名字 `validateDate` **不准确,会误导**:

- `validateDays` / `validateMonths` 校验的是一个**数量**(30、6 这种正整数)
- `validateDate` 听起来是校验一个**日期**(2026-07-08 那种)

这俩是不同的东西。你要复用的是"校验一个**正整数数量**"的逻辑,不是"校验日期"。所以名字该往"正整数"或"正数量"方向起,而不是 `validateDate`。

**几个更准的候选(你选):**
- `validatePositiveInt` —— 直白,说清它校验"正整数"
- `validateCount` —— 语义"数量"
- `validatePositiveNumber`

**但这里有个更麻烦的问题,你得先想清楚,不然复用会出 bug:**

`validateDays` 里**写死了默认值 30**:
```js
if (days === undefined || days === null) {
    return { valid: true, value: 30 };   // ← 默认 30
}
```
但 months 的默认值该是 **6**,不是 30。如果你直接复用这个函数,months 缺省时会被填成 30(30个月!),错得离谱。

**所以"复用"不能简单改名——你得把"默认值"变成参数传进去**,而不是写死。想一下函数签名怎么改:让调用方**告诉它默认值是多少**。类似:
```
validatePositiveInt(value, defaultValue)
```
days 校验时传默认 30,months 校验时传默认 6。这样一个函数才能真正复用给两个场景。

**这是个很好的"抽象"练习**——把"变化的部分"(默认值)提取成参数,"不变的部分"(正整数校验逻辑)保留在函数体。这正是"提取可复用函数"的核心思维:找出哪些是共性(留在函数里)、哪些是差异(变成参数)。

**你去做:**
1. 把 `validateDays` 改造成通用的 `validatePositiveInt`(或你选的名),**默认值改成参数**
2. 两个中间件(days 的、months 的)分别调用它,传各自的默认值(30 / 6)
3. 想一下:两个校验中间件是复用一个、还是各写一个薄中间件调用同一个校验函数?(提示:中间件从 `req.query` 取的字段名不同——一个取 `days`、一个取 `months`——所以中间件可能要分开,但底层校验函数共用)

**这里有个设计判断留给你:** 中间件层要不要也通用化?还是"校验函数通用、中间件各写一个"?想想哪种更清晰。改完发我,我看你抽象得干不干净。

我先分开写吧

可以,分开写是稳妥的选择——而且在你现在的场景下,可能是**更对的**选择,不只是省事。说清楚为什么,让你这个决定是想过的、不是图省事:

**"校验函数通用 + 中间件各写一个"这个组合是合理的**,因为两层的"变与不变"不同:
- **校验逻辑**(正整数、缺省给默认)——days 和 months **完全一样**,该抽成一个通用函数复用
- **中间件**——两者要从 `req.query` 取**不同字段**(`days` vs `months`)、给**不同默认值**(30 vs 6)、存到**不同地方**(`req.days` vs `req.months`)。差异点不少,硬合成一个反而要传一堆参数、变复杂

所以"底层校验函数共用、上层中间件各写一个薄的"是清晰的分工:**共性下沉到函数,差异留在各自的中间件**。你分开写中间件,对。

**但别忘了核心目的还是复用校验逻辑**——两个中间件里,别各自把"正整数校验"重写一遍,都调用那个通用的 `validatePositiveInt(值, 默认值)`。中间件只负责"从 query 取对的字段、传对的默认值、存对的地方",校验本身交给共用函数。这样才达到了复用,只是复用在函数层、不在中间件层。

**所以你要写的:**

1. **通用校验函数**(utils/validators.js):`validatePositiveInt(value, defaultValue)` —— 把 `validateDays` 的默认值 30 改成参数 `defaultValue`,逻辑不变
2. **days 中间件**:取 `req.query.days`,调 `validatePositiveInt(days, 30)`,存 `req.days`
3. **months 中间件**:取 `req.query.months`,调 `validatePositiveInt(months, 6)`,存 `req.months`

（days 那个中间件你 Day2 已经有了,改造一下让它调用新的通用函数、传默认值 30;months 中间件新写一个,照着 days 的样子。）

**一个小提醒:** 改造 `validateDays` → `validatePositiveInt` 后,**Day2 那个 days 中间件的调用也要跟着更新**(它原来调 `validateDays`,现在要调 `validatePositiveInt(days, 30)`)。别改了函数名忘了改调用方——这类"改了定义忘了改引用"的坑你之前踩过(Week2 的 instanceof 注册)。改完确认 days 的报表还能跑。

先写通用函数 + 两个中间件,发我。我看校验函数抽得干不干净(默认值参数化对没对)、两个中间件有没有正确复用它。

"月份不生效"的原因就在 controller,你自己看这行——**你调错了 service 函数:**

```js
export async function getMonthlySalesTrendReportController(req, res) {
    const status = req.status;
    const months = req.months;
    const reportData = await getCustomerSpendingReport({ status, months });  // ← 调的是客户报表!
    return res.json(reportData);
}
```

你在**月度报表的 controller** 里,调用的却是 **`getCustomerSpendingReport`**(客户消费报表),不是 `getMonthlySalesTrendReport`(月度报表)。

**后果链**:`getCustomerSpendingReport` 的签名是 `{ status, days }`,你传的是 `{ status, months }`。它拿不到 `days`(得到 `undefined`),于是:
```js
const date = new Date(Date.now() - undefined * 24*60*60*1000);  // undefined 运算 = NaN → Invalid Date
```
时间变成 Invalid Date,`$match` 行为异常。而且它跑的是**客户分组聚合**(按 userId),不是月度聚合——所以你看到的结果既不是月度、月份也"不生效",因为**根本没走月度那条管道**。

**这又是"惯性套模板"的延续,而且是最典型的一次:** 你复制了 Day2 客户报表的 controller,改了函数名(`...MonthlySalesTrend...Controller`)、改了参数(`months`),但**忘了改最关键的那行——实际调用的 service 函数**。表面全改了,核心调用没改。和你上午 service 那次"参数名改了、计算公式没改"是同一个模式:**改了外壳,漏了里子。**

**改:** controller 里调 `getMonthlySalesTrendReport({ status, months })`。

---

**其余部分 review,都对:**

- **`validatePositiveInt` 抽象干净** ✓ 默认值参数化了,逻辑不变,共性下沉成功。这个抽象做得好——找准了"变的是默认值、不变的是校验逻辑"
- **months 中间件**正确复用了通用函数、传默认值 6、存 `req.months` ✓
- **route** 挂载对,`validateMonthsParam` + `validateStatusParam` + controller ✓
- **service `getMonthlySalesTrendReport`** 本身写对了(时间按月算、Decimal128 转换)✓

**确认一件事:** 你 Day2 那个 `validateDaysParam` 中间件,改造成调用 `validatePositiveInt(days, 30)` 了吗?你没贴它。如果它还在调用旧的 `validateDays`(已经不存在了),客户报表会报错。**去确认 days 中间件也更新成调用 `validatePositiveInt(req.query.days, 30)`**,别改了函数名漏改调用方(上午提醒过的坑)。

---

**改这一个🔴(controller 调对函数)+ 确认 days 中间件更新了,月度报表就通了。**

改完 Postman 跑 `GET /reports/monthly-sales?status=completed&months=6`,应该返回按月份从早到晚、每月一条、Decimal128 转成 number 的结果。跑出来贴我,这个竖切就验收。

**顺带记一笔今天反复出现的模式(值得进笔记):** 今天你套用旧模板出错了**三次**(聚合的 userId 残留、service 的时间公式、controller 的函数调用),都是"改了外壳、漏了核心"。这是个真实的个人 pattern,对治方法就是我说的:**套模板后,逐一核对"这个新需求和旧的,业务上哪几处不同",确保每处都改到位。** 认识到这个 pattern,比改对这一个 bug 更有价值。

好,试 A——集成测试。这是新东西,先讲**心智模型和它的独特难点**,再搭环境。别急着写,先理解"集成测试和单元测试到底哪里不同"。

**核心区别:单元测试测"纯函数",集成测试测"整条链路 + 真实数据库"。**

你昨天的 `validateStatus` 单元测试——输入一个值、检查返回,不碰数据库、不起服务器,纯粹。集成测试不一样:测 `GET /reports/monthly-sales`,要**真的发一个 HTTP 请求**,走完 route→中间件→controller→service→repository→**真实数据库**,再检查返回的 JSON。这带来三个单元测试没有的难点:

**难点1:要有数据库,而且不能用你的开发库。**
测试要连数据库(不然聚合查什么)。但**绝不能连你正在用的 `week2` 库**——测试会增删数据,跑一次就把你的开发数据搞乱了。所以要么用**独立的测试库**(比如 `week2_test`),要么用内存数据库。测完还要清理。这是集成测试最麻烦的地方:**测试数据的准备和隔离**。

**难点2:测试要自己准备"已知数据",才能断言结果。**
你单元测试时,输入是你手给的。但集成测试查数据库,**数据库里得先有你知道的数据**,你才能断言"应该返回2条、u2排第一"。所以每个测试前要**先塞入固定的测试数据**(seed),测完清掉。这样结果才可预测。

**难点3:异步 + 生命周期钩子。**
连库、塞数据、发请求、清库——都是异步的,而且有顺序:**测试前**连库+塞数据,**测试后**清库+断开。Jest 用 `beforeAll`/`afterAll`/`beforeEach`/`afterEach` 这些"钩子"管理这个生命周期。这是新概念。

**Supertest 在这里的角色:** 它负责"发 HTTP 请求"这一步——把你的 `app` 递给它,它在内存里模拟请求、拿响应,你不用真开端口。

---

**先做一个关键决策(影响整个测试怎么搭):测试数据库用什么?**

三个方案,取舍不同:

**方案A:独立测试库(`week2_test`)** —— 连一个真实的、但专门给测试用的 MongoDB 库。简单直接,你现在的 MongoDB Community 就能开个新库。缺点:测试依赖本地 MongoDB 运行、测完要清数据。

**方案B:mongodb-memory-server** —— 一个专门的库,在内存里临时起一个真 MongoDB,测完自动销毁。好处:完全隔离、不污染任何东西、CI 里也好用。缺点:要装个新依赖、第一次下载 MongoDB 二进制稍慢。**这是业界测试 Mongoose 的主流做法。**

**方案C:直接用开发库** —— 绝对不要,会毁你的开发数据。排除。

**我建议方案B(mongodb-memory-server)**,因为:它是标准做法、完全隔离(你不用担心污染 week2)、以后接 CI 时无缝(CI 环境没有你本地的 MongoDB,内存库自带)。你计划表里 W6 要"CI 跑绿",用内存库现在就为那时铺好路。

但方案B要装依赖、配置稍多。**如果你今天状态一般想轻一点,方案A(独立测试库)更快上手**,概念少。

**你选哪个?** B 更正规、为 CI 铺路,但配置多一点;A 更快,但隔离性和 CI 友好度差些。考虑到你今天状态一般,我倾向**先用 A 快速跑通第一个集成测试**(理解集成测试的骨架最重要),等 Day4/W6 再换成 B 接 CI。但你定。

选完我给你搭环境(配置类,我直接给),然后测试用例你写。

好,选 B(mongodb-memory-server)——正规做法,为 W6 的 CI 铺路。配置类我直接给,测试用例你写。

**第一步:装依赖**

在 `week2-express/` 根目录:
```bash
npm install --save-dev mongodb-memory-server
```
（`jest` 和 `supertest` 你昨天装过了,不用重装。）

第一次装,`mongodb-memory-server` 会下载一个 MongoDB 二进制文件(几十MB),稍慢,正常。装完它会缓存,以后就快了。

**第二步:一个关键的前置改造——你的 app.js 要能被测试导入**

这是集成测试的一个常见坑,先讲清楚。看你 app.js 现在的结构:
```js
const app = express();
// ...中间件、路由...
startServer();   // ← 直接启动了服务器 + 连了数据库
```

**问题:** 集成测试要 `import { app }` 拿到你的 Express app 交给 Supertest。但你 app.js 一被 import,`startServer()` 就会执行——它会**连你的真实数据库、占用端口**。测试环境不想要这个(测试要连内存库,不是你的 week2)。

**所以要把 app.js 拆成两部分:**
- **app 的定义**(express() + 中间件 + 路由 + 错误处理)→ 要能被 import,**不含启动逻辑**
- **启动逻辑**(connectDB + listen + 优雅关闭)→ 单独放,只在真正跑服务器时执行

**改造方向(你来做):**
1. 在 app.js 里,把 `app` **导出**:`export { app }`(或 `export default app`)
2. 把 `startServer()` 的调用和 graceful shutdown 挪到一个**单独的入口文件**(比如 `server.js`),或者用一个条件包住,让它**只在直接运行时执行、被 import 时不执行**

最干净的做法是**拆文件**:
- `app.js` 只负责定义并导出 app(不调 startServer)
- 新建 `server.js`:`import { app }` + connectDB + listen + 优雅关闭,这才是 `npm run dev` 的入口

然后 `package.json` 的 dev script 指向 `server.js`。

**这个改造有额外好处**(不只是为测试):它把"应用定义"和"应用启动"分离了——这是 Express 项目的**最佳实践**,让 app 可测试、可复用。你 Week2 没拆是因为没测试需求,现在有了,正好拆。这也是个真实的工程演进。

---

**你先做第二步的改造**(拆 app.js / server.js),这是纯结构调整,不难但要想清楚哪些留在 app.js、哪些挪去 server.js:

- **留 app.js**:`express()`、所有 `app.use(中间件)`、路由挂载、错误处理中间件、`export { app }`
- **挪 server.js**:`import { app }`、`connectDB`、`app.listen`、`gracefulShutdown`、`process.on(SIGINT/SIGTERM)`

改完确认 `npm run dev` 还能正常启动(现在入口是 server.js)。

先做这个拆分,发我看结构对不对。**这一步是集成测试能跑的前提**,拆干净了,后面写测试就顺了。拆完我们再写测试文件(连内存库 + 塞数据 + Supertest 发请求)。

好,app/server 拆分搞定,`npm run dev` 还能起就说明拆对了。这个"应用定义与启动分离"是个真实的最佳实践,你以后所有 Node 服务都可以这么组织。

现在写集成测试文件。先讲**测试的整体骨架和生命周期**,具体断言你写。

**测试文件的结构(四个部分):**

```js
import { test, expect, describe, beforeAll, afterAll, beforeEach } from '@jest/globals';
import request from 'supertest';
import mongoose from 'mongoose';
import { MongoMemoryServer } from 'mongodb-memory-server';
import { app } from './app.js';        // ← 你导出的 app
import Order from './models/orders.js';
// 可能还要 import User（如果测 $lookup 的报表需要 user 数据）

let mongoServer;

// 【生命周期1】所有测试开始前：起内存库 + 连接
beforeAll(async () => {
    mongoServer = await MongoMemoryServer.create();
    const uri = mongoServer.getUri();
    await mongoose.connect(uri);
});

// 【生命周期2】所有测试结束后：断开 + 关掉内存库
afterAll(async () => {
    await mongoose.disconnect();
    await mongoServer.stop();
});

// 【生命周期3】每个测试前：清空 + 塞入已知测试数据
beforeEach(async () => {
    await Order.deleteMany({});
    // 塞入你设计的固定测试数据（见下）
    await Order.insertMany([ /* 已知数据 */ ]);
});

describe('GET /reports/monthly-sales', () => {
    test('返回按月分组的销售统计', async () => {
        // 用 supertest 发请求 + 断言（你写）
    });
});
```

**四个生命周期钩子的作用(理解这个是集成测试的核心):**

- **`beforeAll`**:所有测试**跑之前一次**——起内存 MongoDB、连上。整个测试文件共用一个内存库。
- **`afterAll`**:所有测试**跑完一次**——断开连接、销毁内存库。清理干净,不留痕迹。
- **`beforeEach`**:**每个 test 之前**都跑——清空 orders、重新塞入固定数据。**为什么每个测试前都重塞?** 保证每个测试都从**相同的已知状态**开始,测试之间不互相污染(上一个测试改了数据,不影响下一个)。这是集成测试可靠的关键。

**你要做的关键决策1:测试数据怎么设计?**

你要塞入**已知的、可预测的**订单数据,这样才能断言结果。建议**复用你之前 seed 的那批数据**(你 Day1 精心设计的那14条,跨月份、跨 status)——但注意:内存库是空的,连 user 都没有。

**这里有个判断你要做:** 测 `monthly-sales` 报表需要 user 数据吗?
- 月度报表的聚合是 `$match→$group(按月)→$sort→$project`,**没有 `$lookup`**,不关联 user
- 所以测月度报表,**只需要塞 order 数据,不需要 user**

（如果你后面要测 `customer-spending` 那个带 `$lookup` 的报表,那才需要连 user 一起塞。先测简单的月度报表。）

**你要做的关键决策2:断言什么?**

用 Supertest 发请求的写法:
```js
const res = await request(app)
    .get('/reports/monthly-sales?status=completed&months=6');

expect(res.status).toBe(200);          // 断言状态码
expect(res.body).toBe(???);            // 断言返回内容
```

`res.body` 是返回的 JSON。你要断言什么?想想——你塞的已知数据,经过月度聚合,**应该**返回几条、每条什么样?(你 Day1 那批数据里 completed 的分布你算过。)但注意:**内存库的"当前时间"就是测试运行的此刻**,`months=6` 是"最近6个月",你塞的数据日期要落在这个窗口内才会被统计。这里有个坑:**你 seed 数据的固定日期(2026-01到07)相对"测试运行时刻"是不是还在6个月内?** 想清楚,否则可能查出来是空的。

---

**先做两件事,别急着写全:**

1. **搭好骨架**:把上面的结构框架建起来(beforeAll/afterAll/beforeEach + 一个空 test),`npm test` 跑,确认**内存库能起、能连**(哪怕 test 是空的、只放个 `expect(1).toBe(1)`)。先验证环境通。
2. 环境通了,再设计测试数据 + 写断言。

**先搭骨架、跑通"内存库能连"这一步。** 有报错(尤其 mongodb-memory-server 下载/启动、或 import app 相关)贴我。这一步通了,集成测试就成功一大半。

对,你多半见过——**这套 `describe`/`test`/`beforeAll`/`afterAll` 是 Jest(或 Vitest/Mocha)的通用结构,不是 Supertest 的**。你之前写的很可能是**前端测试**,同样用这套骨架,只是"被测对象"和"断言工具"不同。帮你把这层关系理清,你就知道自己其实有底子:

**分清两层:**

1. **测试框架层(Jest)** —— 提供 `describe`/`test`/`expect`/`beforeAll` 这些**通用脚手架**。不管你测前端组件、后端接口、还是纯函数,这层都一样。**你熟的就是这层。**

2. **"被测对象 + 断言工具"层** —— 这层随场景变:
   - 你以前(前端):被测对象是 **React 组件**,工具多半是 **React Testing Library**(`render`、`screen.getByText`、`fireEvent`),断言"渲染出了什么、点击后变成什么"
   - 现在(后端):被测对象是 **Express app/接口**,工具是 **Supertest**(`request(app).get(...)`),断言"HTTP 状态码、返回的 JSON"

**所以你不是在学一套全新东西,是在熟悉的 Jest 骨架里,换了一个"被测对象"和"发请求的工具"。** 你以前 `render(<Component/>)` + `getByText`,现在 `request(app).get(url)` + `expect(res.body)`——**结构同构,只是主角从组件换成了接口。**

**几个对应关系,帮你迁移已有直觉:**

| 你熟的前端测试 | 现在的后端集成测试 |
|---|---|
| `render(<Component/>)` | `request(app).get('/...')` |
| `screen.getByText(...)` | `res.body` / `res.status` |
| `fireEvent.click(...)` | `.post().send({...})`(发数据) |
| mock API 返回 | 塞真实数据进内存库 |
| `beforeEach` 重置组件状态 | `beforeEach` 重置数据库数据 |

**一个关键的不同(前端测试通常没有的):数据库生命周期。**

前端测试里,你 mock 掉数据、组件是纯的,`beforeEach` 顶多重置一些状态。但后端集成测试多了**真实数据库的起停和数据准备**——`beforeAll` 起内存库、`beforeEach` 塞数据、`afterAll` 销毁。**这是你以前前端测试没怎么碰的部分,也是今天唯一真正"新"的东西。** 其余的 Jest 骨架你都有底子。

所以:**别把这当全新领域**。你会 Jest 骨架(前端练过),今天要新增的只是"连内存库 + 塞数据 + 用 Supertest 发请求"这三样。心理上轻装上阵。

---

回到动手——先搭骨架、跑通"内存库能连"。就用我上一条给的结构,`beforeAll` 起内存库、放个空 `test('sanity', () => expect(1).toBe(1))`,`npm test` 看能不能连上内存库。

**你先试着自己搭**(既然 Jest 骨架你熟),卡在 mongodb-memory-server 或 import app 的地方再问。搭好跑通贴我,或有报错贴我。

好消息藏在报错里——**内存库连上了!** 看这行:
```
console.log  uri:  mongodb://127.0.0.1:62498/
```
`beforeAll` 成功起了内存 MongoDB 并拿到 uri(那个随机端口 62498 就是内存库)。**环境通了,这是今天最关键的一步。**

**报错是个小结构错,你自己看:**
```
Tests cannot be nested. Test "sanity" cannot run because it is nested within "返回按月分组的销售统计".
```

你把 `test('sanity', ...)` 写在了另一个 `test(...)` **里面**:
```js
test('返回按月分组的销售统计', async () => {
    test('sanity', () => expect(1).toBe(1))   // ← test 套在 test 里,不允许
});
```

**规则:`test` 不能嵌套在 `test` 里。** 层级应该是 `describe`(分组)→ 里面放多个**平级**的 `test`。你现在是 test 套 test,非法。

**改法:** sanity 应该和那个业务 test **平级**,都直接放在 `describe` 下:
```js
describe('GET /reports/monthly-sales', () => {
    test('sanity check', () => {
        expect(1).toBe(1);
    });

    // 业务 test 先留空或注释掉,等 sanity 过了再写
});
```

或者你既然内存库已经连通了(sanity 的目的达到了),可以**直接删掉 sanity,开始写真正的业务测试**——sanity 只是用来验证环境的,现在环境证明通了,它完成使命了。

---

**所以你现在两个选择:**

**A. 先留个平级的 sanity 确认结构对**,`npm test` 看它单独绿了,再写业务测试。稳妥。

**B. 直接删 sanity,写业务测试**(内存库已证明能连)。

**建议 B**——环境已经通了,直接进正题。写那个真正的测试:

```js
describe('GET /reports/monthly-sales', () => {
    test('返回按月分组的销售统计', async () => {
        const res = await request(app)
            .get('/reports/monthly-sales?status=completed&months=6');
        
        expect(res.status).toBe(200);
        // ↓ 断言 res.body,你来写
    });
});
```

**但写断言前,先解决那个我提醒过的关键坑——测试数据。**

你 `beforeEach` 里塞了数据吗?塞的是什么?这决定你能断言什么。而且那个**时间窗口的坑**要想清楚:

- 你的测试查 `months=6`(最近6个月),今天是 2026-07-08,6个月前约 2026-01-08
- 你塞的测试数据 `createdAt` 必须落在这个窗口内,才会被统计
- **如果你塞的是固定日期(2026年1-7月),得确认它们相对"测试运行的此刻"还在6个月内**——1月的可能刚好在边界外

**建议:测试数据的日期用"相对现在"的算法**,别用写死的绝对日期。比如塞几条 `createdAt: new Date()`(现在)、几条 `new Date(设成2个月前)`——这样不管哪天跑测试,数据都稳定落在窗口内。**测试要可重复,不能依赖"今天恰好是哪天"。** 这是集成测试设计测试数据的一个重要原则。

**你先处理 `beforeEach` 的测试数据**(塞几条已知的、日期用相对算法、包含 completed 和其他 status),想清楚"这些数据经过月度聚合应该输出什么",然后写断言。

数据 + 断言写好发我。卡在"该断言什么"就先把你塞的数据贴我,我帮你一起推该期望什么结果。

骨架和思路都对了,但测试数据有几个🔴会让测试**跑不对或跑不稳**,得先修——这些正是集成测试数据设计的关键坑。

**🔴 1. `totalAmount` 写成了 `{ "$numberDecimal": "99" }`——这是序列化后的形状,塞进库会出问题。**

还记得 Day1-2 的教训吗:`{ $numberDecimal: "99" }` 是 Decimal128 **经过 JSON.stringify 之后**的样子,不是你在代码里构造 Decimal128 的方式。你 seed.js 里当时是直接写数字 `totalAmount: 99`,靠 Mongoose 自动 cast 成 Decimal128。现在你把序列化后的形状原样塞回去,Mongoose 不认这个 `{$numberDecimal}` 对象,大概率报错或存成错误结构。

**改:** `totalAmount` 直接写数字,`totalAmount: 99`、`totalAmount: 1500`,和你 seed.js 当初一样,让 Mongoose 自动转。

**🔴 2. 固定绝对日期 + 时间窗口,会让测试"看哪天跑"而变得不稳定。**

这是我上一条重点提醒的坑,你部分踩了。你的数据日期是写死的 `2026-01-01`、`2026-03-06`……而查询是 `months=6`(最近6个月)。问题:

- 今天 2026-07-08,6个月前 ≈ 2026-01-08
- 你那条 `2026-01-01` 的订单,**在 6 个月窗口外**(1月1日早于1月8日),不会被统计
- 更糟:**这个测试如果哪天(比如8月)再跑,窗口移动,更多数据会掉出去,断言 `toHaveLength(3)` 就崩了**

**测试必须可重复——不能依赖"今天恰好是几号"。** 解决办法:**所有测试数据的日期用"相对现在"算**,别用绝对日期。比如:
```js
const now = new Date();
const monthsAgo = (n) => { const d = new Date(); d.setMonth(d.getMonth() - n); return d; };
```
然后数据里 `createdAt: monthsAgo(1)`、`monthsAgo(2)`、`monthsAgo(5)` ……这样不管哪天跑,数据都稳定落在"最近6个月"内。你已经用了一条 `new Date()`(现在),对——把其余的也改成相对算法。

**🔴 3. `_id` 你手写了固定值,而且有一条可能重复。**
你手动指定了 `_id`(那串 ObjectId 字符串)。手动指定本身可以,但:(a)你得确保它们格式合法且不重复——你其中 `"6a4c60d9f9dedb5d69170d23"` 和别的很像,看仔细别撞;(b)其实**测试数据不需要手动指定 `_id`**,让 Mongoose 自动生成更省事(你月度报表也不关心具体 _id)。建议**删掉所有 `_id` 字段**,让它自动生成,少一个出错点。

**🔴 4. `items` 是 required,你一条都没塞——可能插入失败。**
你 order schema 里 `items` 是 `required: true`。但你测试数据里没有 `items`。`insertMany` 时可能因为缺 required 字段而报验证错误。要么给每条加个 `items: []`(如果空数组能过 required——你得确认),要么临时你测的是月度聚合、不关心 items……但 schema 要求它必填,你就得给。**确认一下**:塞的时候如果报 ValidationError,就是这个原因,给每条补 `items`。

---

**关于断言 `toHaveLength(3)`——先把数据修对,再回头核对这个数字对不对。**

你现在的断言是"返回3条"。但这取决于你的数据修正后、落在6个月窗口内的 completed 订单,按月分组后有几个不同月份。**你数据改成相对日期后,要重新数**:有几条 completed、分别落在几个不同的月份(同月的会合并成一条)。比如你若塞了 completed 在"1个月前、2个月前、5个月前、现在"——那是4个不同月份 → 应该 `toHaveLength(4)`,不是3。**改完数据自己重新算这个数**,别沿用现在的3。

**顺带一个更强的断言建议**(可选):光断言长度(`toHaveLength`)比较弱——返回3条但内容全错也能过。可以再断言**内容**,比如某个月的 `orderCount`、`totalSpending` 等于你预期的值。这样测试更有保护力。但先让长度断言跑对,再加强。

---

**你要改的:**
1. 🔴 `totalAmount` 改成直接写数字(`1500` 不是 `{$numberDecimal}`)
2. 🔴 日期全改成"相对现在"算法(`monthsAgo(n)`),别用绝对日期
3. 🔴 删掉手写的 `_id`(让自动生成)
4. 🔴 确认 `items` required —— 缺了就每条补 `items: []`
5. 改完**重新数**该返回几条,修正 `toHaveLength` 的数字

**另外你 import 了 User 但月度报表用不到**(没 `$lookup`),可以删掉 User 的 import 和相关,保持干净。

改完 `npm test` 跑。这次可能还会有坑(尤其 items required、日期窗口),有报错贴我,我们一起看。这是集成测试最麻烦的"数据准备"环节,趟过这关就顺了。

数据修得干净了(数字金额、相对日期、去掉 _id)。但断言的数字要重新核对——你可能又没数准,而且有个**边界坑**会咬你。先自己跟我数一遍。

**筛选条件:completed + 最近6个月。逐条过你的 completed 数据:**

| createdAt | status | 月份(相对) |
|---|---|---|
| monthsAgo(1) | completed | 1个月前 |
| monthsAgo(2) | completed | 2个月前 |
| monthsAgo(4) | completed | 4个月前 |
| monthsAgo(6) | completed | **6个月前 ← 边界!** |

(canceled 的 monthsAgo(3)、pending 的 monthsAgo(5) 被 status 滤掉,不算。)

**这4条 completed 分别落在 4 个不同月份**(1、2、4、6个月前),按月分组 → **应该是 4 组,不是 3**。你断言 `toHaveLength(3)` 大概率会失败。

**但这里有个更麻烦的边界坑,`monthsAgo(6)` 那条:**

你的报表 service 算的时间边界是 `setMonth(getMonth() - 6)`,查询是 `createdAt >= 6个月前`。你数据里 `monthsAgo(6)` 也是同样算法算的"6个月前"。**问题:这两个"6个月前"是同一时刻吗?**

不一定——`beforeEach` 里算 `monthsAgo(6)` 的时刻,和 service 里 `setMonth(-6)` 执行的时刻,**差了几毫秒到几秒**(测试运行有先后)。如果 service 算的边界比数据的时刻**晚哪怕一毫秒**,那条 `monthsAgo(6)` 就 `< 边界`,被滤掉 → 只剩 3 条 → 你 `toHaveLength(3)` 反而"歪打正着"过了,但这是**脆弱的巧合**,换台机器/换个运行时机就可能变 4。

**这就是"卡在边界上的测试数据"的大忌**:结果依赖毫秒级时序,不稳定。**测试数据要避开边界,落在窗口内"安全地带"。**

**改法:把数据日期都放在"明确在窗口内"的位置,别贴边界。** 比如全用 `monthsAgo(1)` 到 `monthsAgo(5)` 之间,别用 `monthsAgo(6)`(正好贴边)。重新设计一组"月份明确、不贴边、你能一眼数清有几组"的数据。

**建议你重新设计成这样的思路**(具体值你定):
- 几条 completed,落在**明确不同的月份、都在6个月内、不贴6个月边界**(比如 1、2、3、5 个月前)
- 掺 1-2 条非 completed(验证 status 过滤)
- 然后**你能确定说出**:completed 的落在 N 个不同月份 → 断言 `toHaveLength(N)`

**改完关键是:你要能自信说出"应该返回几条",而不是猜。** 数据设计的目标就是"结果可预测、不依赖时序巧合"。

---

**另外提醒:先跑一次看真实结果,别光改断言数字。**

你可以先把断言改成宽松的、或者加个 `console.log(res.body)`,**先看接口真实返回了什么**(几条、每条什么),再根据真实输出核对你的预期对不对。集成测试第一次跑,"先看实际返回"比"猜一个数字断言"更能帮你理解。

**你去做:**
1. 重新设计测试数据——月份明确、不贴6个月边界、你能数清有几个不同月份
2. 先 `console.log(res.body)` 跑一次看真实返回
3. 根据真实返回 + 你的预期,定 `toHaveLength(正确的数)`
4. (可选加强)再断言某条的 orderCount / totalSpending

改完跑,贴我 `res.body` 的真实输出 + 你的断言。有报错(尤其 items required 那个我上次提的,你这次没加 items,先看会不会报)也贴。

测试全绿了,而且你的集成测试**真正跑通了整条链路**——`res.body` 打印出来是真实的月度聚合结果,说明请求走完了 route→中间件→controller→service→repository→内存库,全链路验证成功。这是集成测试的核心价值实现了。

**先回答你的问题:有没有更好的断言方式?——有,而且你问得正是时候。**

你现在(或之前)的 `toHaveLength(3)` 是**弱断言**——它只检查"返回几条",不检查"内容对不对"。返回3条但金额全错、月份全错,它也会绿。**弱断言给你虚假的安全感。** 更好的断言分几个层次:

**层次1:精确断言整个响应体(最强,推荐用于结果确定的场景)**
你现在结果完全可预测,可以直接断言整个数组等于期望值:
```js
expect(res.body).toEqual([
    { orderCount: 1, year: 2026, month: 2, totalSpending: 777, avgOrderValue: 777 },
    { orderCount: 2, year: 2026, month: 3, totalSpending: 1221, avgOrderValue: 610.5 },
    // ...其余
]);
```
`toEqual` 深度比较整个结构。**这是最严格的**——任何字段错了都会红灯。但缺点见下(时间敏感)。

**层次2:断言关键字段,不锁定全部(更灵活)**
只挑你真正关心的验证,比如"3月那条应该有2单、总额1221":
```js
const march = res.body.find(r => r.month === 3);
expect(march.orderCount).toBe(2);
expect(march.totalSpending).toBe(1221);
expect(march.avgOrderValue).toBe(610.5);
```
用 `find` 定位到具体月份,断言它的统计值。**比长度断言强得多**——它验证了聚合算得对(2单、求和、平均)。

**层次3:结构断言(用于结果不完全固定时)**
```js
expect(res.body).toEqual(
    expect.arrayContaining([
        expect.objectContaining({ month: 3, orderCount: 2 })
    ])
);
```
`objectContaining`/`arrayContaining` 只检查"包含某些字段",不要求完全相等。灵活,适合"我只在乎某条对不对,不在乎别的"。

---

**但你的场景有个特殊问题,直接影响该用哪种断言——你的数据是"相对日期",月份会随运行时间漂移。**

看你这次结果:月份是 2、3、4、5、6。但这些是**基于"今天是7月"算出来的**(monthsAgo(1)=6月、monthsAgo(2)=5月……)。**如果下个月(8月)再跑这个测试,月份全变了**(monthsAgo(1)=7月……),你要是用层次1精确断言写死 `month: 2`,下月就红了。

**这就是"相对日期数据"和"精确断言"的矛盾:** 数据为了稳定用了相对日期(不贴边界),但精确断言又依赖具体月份值。怎么破?

**两个方向(你选):**

**方向A:断言"聚合逻辑对",不断言"具体月份值"。** 你真正想验证的是"按月分组对不对、统计算得对不对",不是"月份正好是2"。所以断言那些**不随时间漂移的东西**:
- 返回的**条数**(几个不同月份)
- **总条数、总金额**跨所有月份加起来对不对
- 某个已知特征,比如"有一条 orderCount=2"(那个双单月份),用 `find(r => r.orderCount === 2)` 定位再断言它金额=1221

这样不管哪月跑,只要聚合逻辑对,断言就成立。**推荐这个**,因为它测的是"逻辑正确性",不受时间影响。

**方向B:数据用绝对固定日期 + 精确断言,但要保证日期永远在窗口内。** 问题是"永远在窗口内"和"绝对日期"很难两全(时间往前走,绝对日期迟早出窗口)。所以除非你 mock 掉"当前时间"(用 Jest 的 fake timers 把"现在"固定死),否则方向B难稳定。**mock 时间是更进阶的做法**,今天可以先不上。

**我的建议:方向A。** 断言"逻辑不变量"而非"漂移的具体值"。比如:
```js
// 断言:有5个月份分组
expect(res.body).toHaveLength(5);
// 断言:那个有2单的月份,总额是1221、均值610.5(验证求和与平均逻辑)
const twoOrderMonth = res.body.find(r => r.orderCount === 2);
expect(twoOrderMonth.totalSpending).toBe(1221);
expect(twoOrderMonth.avgOrderValue).toBe(610.5);
// 断言:所有月份都是 completed 统计(canceled/pending 被排除),completed 共5单
const totalOrders = res.body.reduce((sum, r) => sum + r.orderCount, 0);
expect(totalOrders).toBe(5);
```
这三个断言分别验证了:分组数、求和/平均逻辑、status 过滤——**都不依赖"今天几月",换月跑照样绿。**

---

**一个测试思维的总结(值得记):**

**好的断言测"不变量"(invariant),不测"偶然值"。** 月份具体是2还是7,是偶然的(取决于哪天跑);但"2单的那个月总额=1221""completed 共5单""canceled被排除",这些是逻辑保证的、永远成立的——测这些。**问自己:这个断言,换个时间/换台机器跑,还该成立吗?该成立的才是好断言。** 你之前 `toHaveLength(3)` 会随时间变(不好),`orderCount===2 的月份总额1221`(好)。

**你去做:** 把断言从单纯 `toHaveLength` 升级成方向A那种"验证逻辑不变量"的组合(分组数 + 某月的统计值 + 总单数/status过滤)。写好跑一遍确认绿,这个集成测试就真正有保护力了。

顺带:你这次没加 `items` 也没报 required 错——说明要么 items 的 required 对空/缺省的校验较松,要么 insertMany 绕过了。能跑就先不管,记一笔"items required 在 insertMany 时未拦截"待查即可。

改断言,发我看。

这个红灯**不是坏事——它可能抓到了一个真实的边界问题,值得查清楚**。别急着把 5 改成 6,先搞明白为什么是 6。

**期望5、实际6,差1条。你的 completed 订单本来就是4条**(monthsAgo 的 1、2、4、6),等等——我用的"5"是基于你上一次那组数据(不同的),你这次数据又调整过。先回到你**当前**的数据数一遍:

你这次 `beforeEach` 塞的 completed 有哪几条?你贴的上一版数据里 completed 是:monthsAgo(1)、monthsAgo(2)、monthsAgo(4)、monthsAgo(6) = **4条**。但实际 totalOrders 返回 **6**。

**6 > 4,这说明有非 completed 的订单被算进来了,或者数据比你以为的多。** 这里有两种可能,你要查是哪个:

**可能1(最该警惕):`beforeEach` 数据没清干净/累积了。**
`beforeEach` 每个测试前跑一次 `deleteMany({})` 再 insert。但如果你**有多个 test**,或者之前跑残留的数据没清……不过你 `deleteMany({})` 应该清了。**更可能的是:你现在数据数组里 completed 的条数,和你以为的不一样。** 回去数你当前测试文件里 status 是 completed 的到底几条。

**可能2:status 过滤没生效,canceled/pending 也被算进来了。**
如果返回的 totalOrders=6 = 你所有订单数(不管 status),那说明 `$match` 的 status 过滤在测试环境没起作用——那是个真 bug。

**怎么查——把 res.body 打出来看:**

你上一次运行(用 console.log)时看到的是 5 个月份分组、orderCount 分别是 1,2,1,1,1 = 6。**注意那次就是6!** 有个月份 `orderCount: 2`。所以 totalOrders 一直是 6,不是 5——**是我给你的示例数字 5 错了,不是你的代码错了。**

回看你上次的真实输出:
```
month 2: orderCount 1
month 3: orderCount 2   ← 这里是2
month 4: orderCount 1
month 5: orderCount 1
month 6: orderCount 1
```
1+2+1+1+1 = **6**。你的 completed 订单确实是6条(有一个月2单)。**我之前写断言示例时随口说了个5,那个5是我错了**,你实际 completed 数据求和就是6。

**所以:期望值该是6,不是5。是我给的示例数字错了,你的代码和数据是对的。**

**改:** `expect(totalOrders).toBe(6)`。

---

**但这件事本身是个很好的教训,借机说透:**

**你差点要"把代码改成迎合一个错误的断言"——幸好没有。** 这正是我之前讲的"红灯时先问'谁对'":这次是**断言的期望值错了(我给的5是拍脑袋的),代码是对的**。如果你不假思索把某处改了去迎合"5",反而会改错。

**正确的做法(你其实做对了——你来问我):** 红灯时,回到"事实"核对——你上一次的真实 `res.body` 明明白白是6条 completed(1+2+1+1+1),那期望就该是6。**以实际数据的真相为准,不以我随口给的示例数字为准。** 我给的断言示例是"结构模板",里面的具体数字(5)你必须用你自己数据的真实情况替换——你没替换、直接用了我的5,所以红了。

**这也提醒你一个用别人代码/示例的原则:** 别人给的断言示例里的**具体数值**,永远要用你自己的真实数据核对替换,不能照抄。结构可以抄,数字必须自己算。

---

**你改 `toBe(6)`,再跑,应该全绿。**

改完之后,你这个集成测试就**真正完成了**——它现在验证了:HTTP 200、按月分组的条数、某月的求和/平均逻辑、completed 总单数(status 过滤)。全链路 + 逻辑不变量,有保护力。

跑绿了告诉我,我们收口——今天你从零搭起了集成测试(内存库、生命周期、Supertest、逻辑不变量断言),这块本是 W6 的核心,你 Day3 就啃下来了。

全绿了,集成测试正式完成。

这个是你今天真正啃下来的硬骨头——从零搭起一套集成测试:内存库(mongodb-memory-server)、生命周期钩子(beforeAll/afterAll/beforeEach)、Supertest 发请求、还有最关键的**"断言逻辑不变量而非漂移值"**的设计思维。而且这套东西本来是 W6 的核心内容,你 Day3 就提前拿下了。

**本周验收物到今天全部集齐:**
- ✅ 3个聚合场景(客户消费、`$lookup`关联、月度趋势——月度那个还是你独立设计的)
- ✅ populate / N+1(含实测验证)
- ✅ explain 查询优化(Day1)
- ✅ 单元测试(validateStatus)+ 集成测试(monthly-sales 接口)——平铺任务超额完成
- ⬜ 周复盘(周五)
- 笔记你每天在记

**本周实际上到今天(Day3)就已经把 Day5 该有的东西做完了。** Day4-5 会非常宽松。

---

今天状态你说一般,但产出很足:上午独立聚合练习、下午竖切 + 从零搭集成测试。而且今天最有价值的不只是代码,是几个**关于你自己的观察**:三次"套模板漏核心"的 pattern、"红灯先问谁对"(那个5 vs 6,你差点改错但守住了)、以及"断言测不变量"的思维。这些比代码本身更值得记。

**收工前建议(轻的):**
- **改个文件名**:你测试文件叫 `test.js` 放在 src 根目录,建议挪到规范位置(比如 `__tests__/monthly-sales.test.js`,和你单元测试的 `__tests__/` 一致),顺手删掉里面调试用的 `console.log`
- **commit** 固化今天产出(月度报表竖切、集成测试、validatePositiveInt 抽象)
- 让 Claude Code 整理今天的笔记(月度聚合 `$year`/`$month`、集成测试骨架、断言不变量、"套模板漏核心"pattern)

放这里:

```
week2-express/src/__tests__/monthly-sales.test.js
```

**理由——两个测试的"层级"不同,该放在各自对应的位置:**

- **单元测试** `validators.test.js` 测的是 `utils/validators.js` 这**一个文件**。所以它放在 `utils/__tests__/` 下,紧挨着被测文件——"测谁,就放谁旁边"。这是单元测试的惯例。

- **集成测试** `monthly-sales.test.js` 测的是**整条链路**(route→controller→service→repository→db),它不属于任何单一模块,横跨了 `routes`/`controller`/`services`/`repositories` 好几层。放在某一个模块的 `__tests__` 下都不合适(它不只测那一个)。所以放在 **`src/__tests__/`**(src 层级的测试目录),表示"这是应用级别的集成测试",不隶属某个具体模块。

**一个通用原则(记下来):测试文件的位置,反映它测试的"范围"。**
- 测一个函数/模块 → 放该模块旁边的 `__tests__`(单元)
- 测整个应用的某条链路/接口 → 放顶层 `src/__tests__`(集成)

**有些项目更进一步,直接用目录名区分**,比如:
```
src/
├── __tests__/
│   ├── unit/          # 单元测试
│   └── integration/   # 集成测试
```
你现在测试还少,不用搞这么细。先把集成测试放 `src/__tests__/monthly-sales.test.js`,和单元测试的 `utils/__tests__/validators.test.js` 分开,就够清晰了。以后测试多了,再考虑 unit/integration 分目录。

**移动时注意一件事:import 路径要跟着改。** 你测试文件从 `src/test.js` 挪到 `src/__tests__/monthly-sales.test.js`,深了一层,里面的相对 import 要相应调整:
- `import app from './app.js'` → `'../app.js'`
- `import Order from './models/orders.js'` → `'../models/orders.js'`

挪完 `npm test` 跑一下确认还是绿的(路径没改错)。绿了就收工。