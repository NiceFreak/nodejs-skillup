# Week 3 周复盘 · 聚合与测试(7/6–7/10)

> 用途:本周第 1 篇周复盘,**中文稿先写、再译英文版**(和"每周英文技术总结"合流,W6 直接汇总)。
> 写法要求(计划里定的):15–30 min,只写**最关键的 1–2 点** + **一个还没吃透的问题**。
> 核心标准:**写"我对什么的理解变了",不是"我做了什么"。** 流水账不算复盘。
>
> ⚠️ 下面是提纲脚手架,`【】`里是给你的提示,写的时候删掉。中文稿写完把 §5 英文版补上。

---

## 1. 本周做了什么(2–3 行,事实,别展开)

【一句话概括交付物就行,这段不是重点。参考:3 个聚合报表(客户消费 / `$lookup` 关联 / 月度趋势,最后一个独立设计)+ 四层竖切 + 单元/集成测试。】

-
-

---

## 2. 最关键的 1–2 点(复盘的核心)

【挑「概念上真正拨正了认知」的,深写。下面是候选,选 1–2 个,每个回答:*原来我以为…… → 现在我理解…… → 判据/例子是……*】

**候选池(选 1–2):**
- **聚合逻辑该落在哪一层** —— 管道代码归 repository,业务意图(days/status/6个月)归 service。为什么这条分层不显然?
- **按「派生键」分组** —— 用 `$year`/`$month` 从 Date 提取年月;为什么只用 month 会跨年出错?
- **断言逻辑不变量,而非偶然值** —— 相对日期数据 + 断言漂移值的矛盾;判据是"换时间/换机器跑还该成立吗"。
- **`explain` 把"慢"变成可对比证据** —— `COLLSCAN → IXSCAN`、复合索引的 ESR 顺序。

### 关键点 A:【标题】
- **原来我以为**:
- **现在我理解**:
- **判据 / 一个具体例子**:

### 关键点 B(可选):【标题】
- **原来我以为**:
- **现在我理解**:
- **判据 / 一个具体例子**:

---

## 3. 关于我自己的一个观察(强烈建议写这段)

【这周最有价值的产出——不是代码,是对自己工作方式的观察。素材现成,见 Day3 笔记:】

- **「套模板漏核心」pattern**:一天内复现 3 次(聚合残留 `userId` / service 时间公式没改单位 / controller 调错 service 函数),都是"改了外壳、漏了里子"。
- **对治方法**:套模板后逐一核对"新旧需求业务上哪几处不同"。
- 【补一句:你打算怎么在下周的编码里刻意练这个对治动作?】

---

## 4. 一个还没吃透的问题(计划要求)

【选一个真实的、下周想深挖的。候选:】

- `$lookup` **子管道(sub-pipeline)**:关联时只取 `name`/`email`,而不是把整个 user 文档搬进内存——代价差多少?怎么写?
- **populate vs `$lookup` 的切换边界**:什么数据规模/场景下该用哪个?
- `items` **required 为何在 `insertMany` 时没被拦截**?(Day3 留的待查)

我的问题是:
【写清楚"我卡在哪、我猜是什么、下周想怎么验证"】

---

## 5. English version(中文稿定稿后再写)

> 译写提示:不用逐句翻译。用英文重写 §2–§4 的核心,当作 W6 技术总结的素材。保持"understanding shift"的写法:*I used to think… Now I understand… because…*

### What I built (2–3 lines)
-

### The key insight(s)
-

### A pattern I noticed about myself
-

### One thing I haven't fully grasped
-
