# Week 4 计划 · 认证与鉴权（7/13–7/17）

> 本周有效学习时间为 D2–D5 四天。主线优先级：自建账号认证闭环 > RBAC 与关键测试 > OAuth2 流程 > W3 遗留实验。代码继续竖切进 `week2-express/src/`，本目录只保存计划和学习笔记。

## 进度校准（7/14 D2 结束时更新）

D2 超额完成：注册竖切之外，提前吃掉了原属 D3 的 Login 凭据验证（bcrypt compare、统一 401、历史无 hash 用户拒绝、HTTP 接线均已真实验证）。据此调整：

1. **D3 从「登录 + JWT + 中间件」减负为「JWT 专注日」**——登录凭据部分已完成，D3 只做 JWT 签发、验证中间件和受保护接口，原先「范围较满」的风险解除。
2. **释放的时间定向用于还债**：两条未还欠债（自然月报表边界、注册调用链）各安排一个 15–20 分钟第一档重建单元，分插 D3 / D4，不等到周五验收前挤在一起。
3. **D4 增加一项已排队的安全评估**：Login 计时枚举风险（错误密码约 314ms vs 不存在用户约 2ms），评估 dummy hash / 限流，允许只产出结论不落代码。
4. 周三取舍门槛维持不变——门槛三项里「登录能签发 JWT」仍是 D3 的核心交付，不因登录提前完成而放松。

## 响应格式统一（7/14 契约已定，待落地）

现状盘点：成功侧三种形状并存（users/reports 裸文档、DELETE `{ message }`、auth `{ message, data }`），错误侧两种（`{ error }` 与登录/注册校验中间件的 `{ code, message }`），auth 模块内部 400/401 也不一致。

**契约 v2（2026-07-14 本人决策：错误与成功对齐、成功带 code；业务数据字段定名 `payload`，避免与 axios 的 `res.data` 叠出 `res.data.data`）**：

```text
成功：{ code, message, payload }
  code    = 与 HTTP 状态码一致（200 / 201）
  message = 简短语义化中文
  payload = 业务数据；列表为数组，单条为对象；DELETE 成功 payload 为 null
错误：{ code, message }
  code    = 与 HTTP 状态码一致（400 / 401 / 404 / 409 / 500）
  message = 具体中文原因；401 保持统一文案「邮箱或密码错误」
  不带 payload 字段
硬约束：body.code 永远等于 HTTP 状态码
```

注：auth 两接口当前已上线的 `data` 字段属存量，随 D4 迁移一并改名为 `payload`；Postman 断言在迁移落地前保持匹配现状，迁移后同步。

**实现方向（7/14 本人决策）**：错误信封统一收口到全局 error handler，校验中间件不再各自拼响应（类比前端把 API 解析封装进同一个拦截器：格式知识只存在于一处）。三个默认值一并确认：`body.code` 恒等于 HTTP 状态码；DELETE 成功 `payload: null`；错误响应不带 `payload`。

**排期**：契约已定，D3 起 JWT 中间件的新错误分支直接按新格式出生；存量迁移（裸文档接口改信封是破坏性变更，需一批完成，当前消费方仅 Postman 与集成测试）放 D4 与 401/403 测试同步做，D4 排满则降级 W6。实现属黑名单，由本人完成；迁移后 Postman 断言（含黄金路径/409 文件夹读 `data._id` 的脚本）由 AI 同步。

## 本周验收闭环

```text
注册
→ 密码哈希入库
→ 登录校验密码
→ JWT 签发
→ 鉴权中间件验证 JWT
→ 受保护接口
→ 最小 RBAC（401 / 403）
→ 认证关键路径测试
```

掌握标准：能脱离代码讲清密码与 token 分别解决什么问题、成功与失败路径如何流经各层，并在周验收前从空白重建核心链路。

## 每日 Checklist

- [x] **D1（周一 7/13）· Week 3 Demo + 月度边界修复**：完成 Demo；修复 `months=6` 返回 7 个自然月份的设计缺陷；同步集成测试。鉴权未开始。
- [x] **D2（周二 7/14）· 密码哈希 + 注册竖切**：完成注册契约、密码模型、历史用户兼容设计与注册最小链路；真实验证 201 / 400 / 409，数据库无明文、响应与普通查询不泄露 hash。**超额：提前完成 D3 的 Login 凭据验证**（compare、统一 401、历史无 hash 用户拒绝，真实验证 200 / 401×3 / 400）。（见 `day2-password-hash-register-login.md`）
- [ ] **D3（周三 7/15）· JWT 专注日：签发 + 鉴权中间件 + 受保护接口**：先定义再实现——明确 JWT 与 compare 的边界、最小 payload、过期时间和 secret 来源；登录成功后签发 token 并更新响应契约；中间件解析验证 Bearer token（过期/篡改分支明确）；至少保护一个真实接口。**附加**：15–20 分钟第一档重建——自然月报表边界（还债 ①）。
- [ ] **D4（周四 7/16）· RBAC + 安全失败路径 + 测试**：区分 401/403；角色来源可信；覆盖无 token、非法/过期 token、权限不足等关键路径中的 1–2 条；评估 Login 计时枚举风险（dummy hash / 限流，允许只产出结论）。**附加**：15–20 分钟第一档重建——注册调用链（还债 ②）。
- [ ] **D5（周五 7/17）· OAuth2 流程 + Demo + 周复盘**：完成主线验收与 demo（从注册走到受保护接口）；画清授权码流程及 state/redirect URI/code/token 的职责；确认两条欠债均已按第一档通过并在 `DEBT.md` 改状态；写第 2 篇周复盘。

## 周三取舍门槛

周三结束时检查：

- [ ] 登录能签发 JWT。
- [ ] 鉴权中间件能验证 JWT。
- [ ] 至少一个真实接口已受保护。

任一项未完成时：D5 的 OAuth2 降级为**流程说明与安全分析**，不实现完整第三方登录代码。RBAC 只保留一个最小角色规则，不扩展权限模型。

## 每日止步条件

### D2

- 数据库不出现明文密码。
- 注册响应不包含 password / passwordHash。
- 重复邮箱仍保持明确的冲突语义。
- 本人能解释为什么验证密码要 compare，而不是重新 hash 后比较字符串。

### D3

- 登录成功返回带过期时间的 token，失败路径维持 D2 已验证的统一 401。
- token 有明确 payload、签名密钥来源和过期时间；payload 不含敏感字段。
- 签发与验证分开设计，不混写；中间件只负责认证，不把具体业务权限硬编码进去。
- 至少一个真实接口无有效 token 时不可访问。
- 重建单元 ①（自然月边界）完成并记录通过与否。

### D4

- 无身份返回 401；身份有效但权限不足返回 403。
- 客户端不能通过请求体自行提升角色。
- 至少 1–2 个认证关键测试通过。
- Login 计时枚举风险有明确结论：修复，或写下暂不修复的理由与触发条件。
- 重建单元 ②（注册调用链）完成并记录通过与否。

### D5

- 主线 demo 可从注册走到受保护接口。
- OAuth2 掌握以能讲清流程和威胁点为最低标准。
- 周复盘明确一个已掌握点和一个仍需验证的问题。

## 明确不抢占主线的事项

- `match-index-explain.js` 修复：主线达成当日止步条件后，最多投入 30 分钟。
- W3 选择性、复合索引顺序、`$lookup` 子管道实验：本周不排期。
- DTO / Decimal128 重构：本周不做。

## AI 协作方式

默认使用 `AGENTS.md` 的 L1/L2：原理、提示、伪代码、review。本周主线（JWT 签发验证、OAuth2 授权码交换、RBAC 鉴权判断、密码哈希策略）全部属于黑名单，援助上限 L2，任何情况下不升 L3/L4；给到 L2 即在 `DEBT.md` 记账，并在本周验收前按重建梯子安排延迟重建。库的纯 API 细节（`jwt.sign/verify` 参数顺序、`bcrypt` 参数等）属白名单，可直接问。
