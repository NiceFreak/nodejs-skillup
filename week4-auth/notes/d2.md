当前基线正常：`2` 个测试套件、`6` 个测试全部通过。

上午唯一主线：

```text
注册契约
→ 密码哈希原理
→ 注册分层设计
→ 自己实现注册竖切
```

不做 JWT、登录、RBAC、OAuth2，也不处理 W3 遗留。

时间建议：

1. **30 分钟：定义注册契约**

先在 `week4-auth/notes/` 新建 D2 笔记，回答：

```text
接口路径是什么？ 新增 register 路由
允许哪些输入字段？ 输入密码,用户名,邮箱(以上三项必填),手机号(选填),都需要校验格式,密码包含大小写,数字,符号,但先不校验强度.用户名不出现符号.邮箱校验格式(主要验证 @, 都是 mock 邮箱,不做强校验),手机号验证11 位数字.手机号如果无助于学习可以不引入,请帮我判断
成功响应返回什么？200
重复邮箱返回什么？409
非法输入返回什么？400
明文密码在哪些层短暂出现？提交的 options?我们目前还没有用到
数据库最终保存什么？加密后的密码
```

我建议注册使用独立的 `POST /auth/register`，不要直接把现有 `POST /users` 等同于注册。前者包含密码哈希和安全语义，后者目前只是普通 CRUD。

2. **30 分钟：讲清密码模型**

先独立回答：

- 为什么不能存明文密码？泄露风险,撞库风险,降低用户信任度,内部窃取等
- 为什么不使用可逆加密？会被黑客或内部工程师破解
- salt 解决什么问题？彻底的密码混淆
- 登录时为什么用 `compare`，而不是重新 hash 后比较字符串？保证一致性
- 哪一层负责调用 hash？哪一层只负责保存结果？ service 做哈希, 但是这是我看了下一段提示得知的,我理解为 Controller 做,repository 负责保存

3. **剩余上午：注册竖切**

按现有分层逐层设计：

```text
route
→ controller：读取 HTTP 输入
→ auth service：执行密码哈希和注册业务
→ user repository：保存用户
→ MongoDB：只出现 passwordHash
```

上午止步条件：

- 注册成功返回 `201`。
- 数据库没有明文密码。
- 响应不包含 `password` 或 `passwordHash`。
- 重复邮箱继续返回 `409`。
- 你能解释每层为什么这样分。

现在先不要写代码。请先把下面这份契约用你自己的话回答出来：

```text
POST /auth/register

输入：见前面
成功：200
重复邮箱：409
非法输入：400
数据库保存：加密后的密码与其它数据
响应禁止出现：不太确定这个概念,是指状态码吗?
```

你发出来后，我先做契约 review，再进入密码哈希原理。

## 我的第一版回答
接口路径是什么？ 新增 register 路由
允许哪些输入字段？ 输入密码,用户名,邮箱(以上三项必填),手机号(选填),都需要校验格式,密码包含大小写,数字,符号,但先不校验强度.用户名不出现符号.邮箱校验格式(主要验证 @, 都是 mock 邮箱,不做强校验),手机号验证11 位数字.手机号如果无助于学习可以不引入,请帮我判断
成功响应返回什么？200
重复邮箱返回什么？409
非法输入返回什么？400
明文密码在哪些层短暂出现？提交的 options?我们目前还没有用到
数据库最终保存什么？加密后的密码
- 为什么不能存明文密码？泄露风险,撞库风险,降低用户信任度,内部窃取等
- 为什么不使用可逆加密？会被黑客或内部工程师破解
- salt 解决什么问题？彻底的密码混淆
- 登录时为什么用 `compare`，而不是重新 hash 后比较字符串？保证一致性
- 哪一层负责调用 hash？哪一层只负责保存结果？ service 做哈希, 但是这是我看了下一段提示得知的,我理解为 Controller 做,repository 负责保存
POST /auth/register

输入：见前面
成功：200
重复邮箱：409
非法输入：400
数据库保存：加密后的密码与其它数据
响应禁止出现：不太确定这个概念,是指状态码吗?
## Review 后纠正
创建成功使用 201 created,这是已实现的内容, 我回看了代码,200 是请求成功,必须区分
数据库保存的是 密码的哈希值, 提高安全性
明文密码会通过 api 请求进到 service 中,但是哈希加密是一个与中间件无关的模块吗? compare 去使用哈希和加盐, 是为了将数据库中的哈希密码匹配为用户之后登录提交的明文密码?
注册响应禁止出现任何与明文密码,哈希密码和用户不需要感知的内容,在当前模型下,返回登录名,邮箱就已经足够,或许可以加上注册时间
## 最终契约
1. 明文密码流转体系: post请求参数携带 -> body parser -> 校验中间件 -> Controller 处理 req -> service 调用 bcrypt 处理 hash 和 salt -> hash 密码入库.
2. compare 需要用到 hash,从中读取 salt,用于匹配密码
3. salt 不需要保密, 一般来说, 由于 hash 算法是固定的,加盐是为了增加扰动,如相同密码加随机盐,产生不同的 hash 密码,通常来说 salt 不用保密
4. 谁读取 req.body？Controller
谁调用 hash？service
谁保存 passwordHash？ repository
谁决定返回哪些安全字段？Controller
5. 最终契约
路径：POST /auth/register
输入：{ email: string, required, password: string, required, name: string, required }
成功状态码：201
成功响应字段：{ message: "created", data: { email: string, name: string } }
重复邮箱：409 conflict 具体原因
非法输入：400 具体原因, 空字符串,纯空格禁用
name：非空、trim 后非空、最低长度 1
email：required + 邮箱格式
password：非空、禁止纯空格、最低至少 15 位, 不允许 123456
数据库保存：email（明文）、passwordHash（bcrypt 哈希值）、_id, name
数据库禁止保存：明文密码
响应禁止出现：passwordHash, password
## 仍不确定的问题
compare: 
1. 用户本次提交的“明文密码”字符串, 
2. 数据库中存储的完整的 passwordHash 字符串, 
3. 输出布尔值

建议不要“先允许简单密码”，也不要做大小写/数字/符号组合正则。

当前 NIST 对无 MFA 的单因素密码建议最低 15 个字符，并明确不建议额外的字符组合规则；同时建议拦截常见或已泄露密码。[NIST SP 800-63B](https://pages.nist.gov/800-63-4/sp800-63b.html)、[OWASP Authentication Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Authentication_Cheat_Sheet.html)

D2 先采用不增加复杂度的部分：

```text
最低 15 个字符
不强制大小写/数字/符号组合
不允许纯空白
不允许 123456（长度规则本身已排除）
常见密码 blocklist、限流放到 D4
```

这只是把 `6` 换成 `15`，不会增加实现难度，也避免后面推翻契约。

**下一步：注册分层设计**

先不要写代码。根据现有项目，你需要处理一个真实兼容问题：

- 现有 2000 个 seed 用户没有密码。
- 现有 `POST /users` 也不接收密码。
- 如果直接把 `passwordHash` 设置为必填，之后运行 seed 或创建普通用户会失败。
- 如果不隐藏 `passwordHash`，现有 `GET /users` 可能把它返回出去。

请在 `d2.md` 继续回答四个设计问题：

```text
1. passwordHash 在 Schema 中暂时 required 还是 optional？
   如何兼容现有 seed 用户和 POST /users？

2. 如何保证普通 GET /users 默认查不到 passwordHash？

3. 注册密码规则放在哪一层？
   HTTP 校验中间件还是 auth service？理由是什么？

4. 注册是否复用现有 repository.createUser？
   现有 repository 已经处理了哪些数据库错误？
```

然后由你画出文件变化，不需要写实现：

```text
需要新增哪些文件：
需要修改哪些文件：
每个文件只负责什么：
哪些现有函数可以复用：
```

一个关键提示：Repository 已经能保存任意 `userData`，并把 Mongoose ValidationError 和 E11000 翻译成业务错误。先判断它是否真的需要新增一套“注册 Repository”，不要因为功能名变成 auth 就复制数据库逻辑。

这一步仍是 L1 原理与设计问题，不记债。

1. passwordHash 在 Schema 中暂时 required 还是 optional？
   如何兼容现有 seed 用户和 POST /users？
   暂时 optional, passwordHash 在 Schema 中要定义, 实现兼容.
   password 只存在于请求和 Service 内存中，绝不能进入持久化模型

2. 如何保证普通 GET /users 默认查不到 passwordHash？
    使用投影, 关闭其返回, select: false 默认排除其在查询时返回

3. 注册密码规则放在哪一层？
   HTTP 校验中间件还是 auth service？理由是什么？
   注册密码规则放在 service 调用的 auth. HTTP 校验中间件 应该只做基本的格式校验, 更具体的业务逻辑放在 service

4. 注册是否复用现有 repository.createUser？
   现有 repository 已经处理了哪些数据库错误？
   可复用, repository已经能够把保存转换为领域错误

需要新增哪些文件：
1. `routes/auth.js`：注册相关路由；命名与现有 `routes/users.js`、`routes/reports.js` 一致。
2. `controller/auth.js`：读取注册请求数据并返回安全响应；目录沿用现有单数 `controller/`。
3. `services/authService.js`：处理注册业务规则和密码哈希；D2 不包含登录用的 `compare`。
4. `middlewares/validateRegisterBodyMiddleware.js`：注册请求的 HTTP 输入校验；名称直接说明它校验的对象和中间件身份。

需要修改哪些文件：
1. `models/users.js`：只新增 optional 的 `passwordHash`，并设置查询默认不返回；不定义明文 `password`。
2. `app.js`：挂载 auth router。
3. `package.json` / lockfile：安装密码哈希依赖后由包管理器更新。

当前不需要修改：
1. `services/users.js`：普通用户 CRUD 不反向调用 auth service。
2. `repositories/users.js`：D2 复用现有 `createUser`；按邮箱查询留到 D3 登录。

每个文件只负责什么：middleware 校验 HTTP 输入，route 组合请求链路，controller 处理 HTTP 输入输出，auth service 处理注册业务与密码哈希，repository 操作数据库，`app.js` 负责应用装配。

可复用：`validateHasRequestBodyMiddleware`、repository 的 `createUser`、现有领域错误和全局错误中间件。

## AI 辅助记录

- 辅助范围：注册竖切的最小文件集合、命名、各层依赖方向，以及 D2 不引入 `compare/findByEmail` 的范围边界。
- 援助级别与场景：L2 骨架；本人先给出方案，AI 在 review 后整理并修正文件级结构。
- 本人理解验证：待实现过程中复述每层职责，并预测加入登录时需要新增的数据库能力。
- 延迟重建：本周验收前完成第一档 15–20 分钟注册调用链重建，见 `DEBT.md`。
- Postman Auth 注册文件夹、变量串联和基础 smoke 断言由 AI 维护，按最新规范归入 API 展示资产白名单，不构成学习债务；自动化测试的场景和核心断言仍由本人完成。

## 注册竖切验证结果

2026-07-14 完成真实服务验证：

```text
无请求体：400
密码不足 15 位：400
首次注册：201
重复邮箱：409
现有测试：2 个测试套件、6 个测试通过
```

数据库检查确认：

- 新用户文档包含 bcrypt 格式的 `passwordHash`。
- 数据库没有保存明文 `password`。
- 注册响应只返回 `name/email`，不包含 `password/passwordHash`。
- 通过应用 `GET /users` 查询时，Mongoose 的 `select: false` 默认排除了 `passwordHash`。
- 直接使用 mongosh 查询集合仍能看到 `passwordHash`；`select: false` 是 ODM 查询默认投影，不是数据库访问控制。

验证使用的临时注册用户已删除，临时服务已正常关闭。

一个关键边界：
select: false 只影响之后的数据库查询。new User(...).save() 返回的当前文档对象仍可能带有 passwordHash，所以注册响应仍必须主动构造安全字段，不能直接返回 Repository 的完整结果。
