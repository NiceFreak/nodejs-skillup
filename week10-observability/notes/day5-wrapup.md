# W10 Day 5（8/21）：收口日（runbook · 延迟自测 · 能力检验 · 展板校正 · 状态收口）

> 建立：2026-08-20（Asia/Shanghai，D4 收口后起草）
> 上游：[`week10-plan.md`](./week10-plan.md) §4 D5 + §7 交付物 ⑤⑥ ｜ 契约：[`day1-observability-contract.md`](./day1-observability-contract.md)（8/17 冻结）
> 前置：[`day4-fault-drills.md`](./day4-fault-drills.md) §11 —— D5 的三个硬依赖（每类的「首查命令 / 判定分叉 / 预防」已落纸 · P3 盲区结论已落纸 · 基线绿）**8/20 15:30 全部达成**
> 形态：**本周最后一天，也是唯一一天「产出物要被别人（延迟后的自己）照着用」**。
> 与 D2–D4 的差别：前三天的产出是**改动**，今天的产出是**可被复用的判断**——所以今天的验收不看「我做了什么」，看「不看笔记还能不能走通」。

---

## 1. 今日唯一主线与验收句

**主线**：把 D2–D4 三天的事实收成两样东西 —— 一份**别人能照着用的 runbook**，和一次**证明它能被照着用的自测**。

```text
A runbook 成篇（症状 → 首查命令 → 判定分叉 → 修复 → 预防）
→ B 延迟自测（不看 D4 笔记，只按 runbook 走通两类）
→ C 能力检验口述（日志链路 / 失败路径分叉判据 / 改需求预演）
→ D 展板校正（先按最新数据核对已有结论、修正过时的那几格，再谈补内容）
→ E 状态收口（周计划勾选 + LEARNING-STATE + 复盘 + 项目叙述 + 口语稿 + commit）
```

**今日验收句**（取自 `week10-plan.md` §7 交付物 ⑤，不临时改）：

> **runbook 的延迟自测：不看笔记，按它走通两类故障。**

**验收句的可证伪形态**（今天用它销账，缺一即未收口）：

| # | 判据 | 怎么算过 |
|---|---|---|
| 1 | runbook 有结构不是流水账 | `runbook.md` 每一类都有**五列齐全**（症状 / 首查命令 / 判定分叉 / 修复 / 预防）；「判定分叉」列必须写成**可判真假的条件**（如「`/health` 200 → 走 X；非 200 → 走 Y」），写成「检查一下应用层」的不算 |
| 2 | 盲区在册 | D4 挖出的**三个盲区**（`df -BG` 取整 / check-app scope / nodeapp 假 active）在 runbook 里各有一条「**这一类监控不会替你报，只能靠人发现**」，且写明**发现它的替代信号** |
| 3 | 自测是盲的 | B 模块全程**不打开 `day4-fault-drills.md`**；中途一旦翻笔记，就地记「第几步翻的、翻的是什么」——**这条比走通更重要**，它是 runbook 缺口的唯一直接证据 |
| 4 | 自测走通两类 | 两类各自从**症状**走到**恢复基线**，每类留至少一条命令输出 |
| 5 | 口述三问当场答 | C 的三问口述一遍并**记录当场修正**（W9 D5 形态：修正本身计入掌握证据，不是失分） |
| 6 | 收口不留悬案 | 周计划 D5 勾选 · `LEARNING-STATE.md` 指向 W11 · D4 排过来的**类 2 根因**要么定论、要么显式转 backlog（不许沉默消失） |

> 判据 3 是今天真正的秤。runbook 写得好不好，不由写的人评价——由**一个不知道昨天发生过什么的人**评价，
> 而今天这个人只能由你自己扮演：**不看 D4 笔记**。翻一次笔记 = runbook 在那一处是空的。

---

## 2. 变更单（动手前冻结，四要素）

### 2.1 改动清单 —— 今天就这几项，别的都不动

> 「归属」按 `AGENTS.md` §2 / `week10-plan.md` §6：**白** = AI 可直接给语法与格式；**黑** = 本人决策与推理，AI 上限 L2。
> 「性质」区分**留下的改动**与**当天必须还原的临时改动**。

| # | 改动 | 位置 | 性质 | 归属 |
|---|---|---|---|---|
| 1 | 新建 runbook 正文 | `week10-observability/notes/runbook.md` | **留下** | **黑**（分叉结构与内容本人写；AI 只出空表头与 review） |
| 2 | B 延迟自测的两类注入与还原 | 服务器（形态待 §3 P1 拍板） | **临时**（当类还原） | 白（命令，D4 §5.3 原文）/ **黑**（走哪两类、按 runbook 定位） |
| 3 | 类 2 根因：读 `server.js` 的 listen 错误处理分支 | `week2-express/src/server.js`（**只读**） | 不改代码 | **黑**（W6 错误边界，本人作答） |
| 4 | 本文件 §6–§10 现场填写 | `notes/day5-wrapup.md` | **留下** | **黑**（AI 不代填） |
| 5 | `week10-plan.md` §4 D5 勾选 + 结果一句话 | `notes/week10-plan.md` | **留下** | 白 |
| 6 | `LEARNING-STATE.md` 收口（当前周 → W11 入口） | 仓库根 | **留下** | 白 |
| 7 | 第二轮 W10 复盘 + 5–10 分钟项目叙述 | 本文件 §9 / §10 | **留下** | **黑**（叙述由本人给，AI 只挑毛病） |
| 8 | `day5-english-speaking.md` | `notes/` | **留下** | 白（按 `DAILY-SPEAKING-PROTOCOL.md`） |
| 9 | 展板校正的**结论记录**；需翻档的格子改数据层 + 断言 | `week8-fullstack/`（见 §3 P3/§5.4） | **留下** | 白（展示资产）/ **黑**（哪一格与最新数据不一致） |
| 10 | 必要时 `DEBT.md` | 仓库根 | **留下** | 白 |
| 11 | **check-disk 判据改造**（P3-② 拍板「今天改」，2026-08-21） | 服务器 check-disk 脚本（改前备份） | **留下** | **黑**（判据与验证推理）/ 白（shell 语法） |

**明确不在清单里**（动了就是超纲）：不改 `app.js` / `server.js` 任何一行（③ 只读）；**四个 check 脚本中除 #11 check-disk 判据改造外，其余三个不改任何阈值**；不动 `journald.conf`；不动 8080 下线；不碰 Java / Python；不做 Prometheus / webhook。

### 2.2 执行顺序（建议排布，B 的形态待 §3 P1 拍板后定）

```text
块 A 基线复位（起点必须干净）→ 块 B 答 §3 五题（P1 决定 C/D 两块怎么走）
→ 块 C runbook 成篇（先排结构再写字）→ 块 D 延迟自测（盲测，两类）
→ 块 E 类 2 根因（先写预测，再读 server.js 对照）
→ 块 F 能力检验口述 → 块 G 展板校正 → 块 H 全量回归 + 残留清扫 → 块 I 状态收口 + commit
```

**顺序上不许换的两条**：

1. **C 必须早于 D**。runbook 写完之前做自测，测的是记忆不是文档；那样即使全部走通，判据 3 也拿不到证据。
2. **D 必须早于 E**（P1 定案选 a、且类 2 入选自测时尤其硬）。块 E 要读 `server.js`，一旦先读了，P4 的「**先预测后读码**」当场作废，类 2 的自测也不再是盲的。

### 2.3 B 模块前置四件事核验表（**P1 已定案选 a = 实做注入，本表今天必生效**；注入前逐格打勾，空一格即不许注入）

> 四列口径同 D4 §2.3：「止步」「回滚」来自 8/17 契约 §5.4 原文，今天照做不重议；
> 「还原点」「基线时刻」现场填。**选哪两类见 P1，不在这里预设。**

| # | 故障类型 | 档 | ① 还原点 | ② 基线（本类注入前那一次三层基线的时刻与结果） | ③ 止步条件（契约原文） | ④ 回滚命令（契约原文） | 四格齐？ |
|---|---|---|---|---|---|---|---|
| 1 | 反代配置错误 | A | 注入前 `cp /etc/nginx/sites-available/shop-ssl …shop-ssl.d5bak`（记字节数与 proxy_pass 行） | 块 A（14:11）三层全绿：五面 200（ssl=0）+ /health 200 + 三服务 active + 四 timer active | `nginx -t` 非零立即止**不 reload**；reload 后五面异常非 502/504 | 恢复 `shop-ssl.d5bak` → `nginx -t` → reload | ☐ |
| 3 | 磁盘逼近满 | B | 注入前字节级 `df -B1 /` 实测 avail（fallocate 值按此用换算率重算，禁抄 26.4G） | 块 A（14:11）：avail 31G（字节级待现场实测） | **df 可用 < 3.5G** 立即 `rm -f /tmp/disk-fill.bin`；`fallocate` 返回 `No space left` | `rm -f /tmp/disk-fill.bin && df -h /` | ☐ |

> **今天的注入量不许照抄 D4 的数**：类 3 若入选，`fallocate` 值按**当天现场 `df -B1 /`** 用 D4 §10.3 的换算率
> （声明 1G → 字节级 +1,073,745,920）重算。抄昨天的 26.4G 就是 D4 已经踩过一次的「抄过期数」。

### 2.4 回滚（动手前写好，三层，沿用 D4 §2.4）

| 层 | 触发场景 | 动作 | 验证 |
|---|---|---|---|
| **L1 · 单类回滚** | 某一类自测结束或触发本类止步条件 | 执行 §2.3 第 ④ 列该类的回滚命令 | 该类注入面恢复 + 本类残留清零 |
| **L2 · 服务层回滚** | L1 执行完但该面仍不正常 | `sudo nginx -t && sudo systemctl reload nginx`；`sudo systemctl restart nodeapp`；必要时从 `.bak.20260818` / `.d4bak` 恢复 site 文件 | 三层基线复测全绿 |
| **L3 · 全量复位** | 两类互相污染或分不清现象来源 | 停止全部自测；恢复在机备份；`rm` 全部 `/tmp/` 演练产物；兜底 `sudo reboot`（W9 D5 A 已实测冷启动自起） | 重启后三层基线 + 四 timer 全绿 |

**残留清单（收工时逐条核零，块 H）**：`/tmp/disk-fill.bin` 不存在 · `ss -tlnp | grep :3000` 只有 nodeapp 自身（backlog 511）· `shop-ssl` 与 `.d4bak` 的 `diff` 为空（**备份保留在机**）· **除 check-disk（#11 刻意留下）外**其余三个 check 脚本与 `.bak` 的 `diff` 为空 · systemd unit 无临时 `Environment=` 行。
> #11 例外：check-disk 与其改前备份 `check-disk.sh.bak.20260821` 的 diff **非空是刻意保留**（P3-② 拍板的今天改造），不是残留；块 H 核零时单独确认该备份存在即可。

**刻意保留、不算残留**：`logger -t DRILL` 标记行与自测造成的 check FAIL 行（`journalctl -t DRILL` 可过滤，**不 vacuum**）。

### 2.5 止步条件（止损线，触发即停，不讨价还价）

1. **今天是本周最后一天**：**收口（E）优先于自测完备（B）**。到收工前 60 分钟仍未进入块 I，则**停止一切注入**，B 按已完成的部分收口并写明缺口，剩余转 W11。
2. **基线不绿不许注入**（同 D4）。块 A 三层基线任一项非绿 → 先修，修不好今天不做 B 的实做形态，改纸面走查。
3. **磁盘可用 < 3.5G** → 立即 `rm -f /tmp/disk-fill.bin`，不等观察做完（同分区有 MongoDB 数据）。
4. **`nginx -t` 非零** → 绝不 reload。
5. **任何指向 `/etc/letsencrypt/live/` 的写操作** → 立即停（本周红线，不撤销 / 不重签 / 不停 80）。
6. **周末无人值守**：块 H 未通过（残留未核零或基线非全绿）**不许收工**——这台是唯一生产机，今晚之后是两天无人值守。

---

## 3. 动手前必须先答的问题（`week10-plan.md` §4 D5 没覆盖，落到执行层才暴露）

> 规则同 D2 §3 / D3 §3 / D4 §3：**AI 只出题与追问，答案本人写。**
> 这五题没有一题是命令语法题——今天所有命令 D4 §5 已经给全了；这五题问的是「今天的收口凭什么算数」。

### P1（延迟自测的形态）B 模块的「隔至少一天、不看笔记」，今天怎么才成立？

> **已拍板（2026-08-20 本人定）：选 a —— 8/21 当天实做两类，不顺延。**
> 口径随之明确：「隔天」按**隔一天（8/20 → 8/21）**执行，不读成「至少隔一整天」。
> 下面三条出路保留原文留痕；**a 的代价（间隔只有 1 天、走通了也可能是记住了而不是查到了）不因为拍板而消失**——
> 它转成两条执行纪律：① 判据 3「翻笔记就地记账」是今天最重要的一格；
> ② §7 每类的 ③④ 两项要显式回答「这一步我是**查 runbook 得到的**还是**本来就记得**」，记得的那几步同样是 runbook 的缺口。
> **2026-08-21 块 B 拍板**：实做 **类 1 + 类 3**，判据 = 两个 D4 完整闭环，间隔 1 天最容易「记住而不是查到」，是测 runbook 最严的组合；类 2 不实做注入，块 E 保持纸面（预测 → 读码 → 对照）。

**冲突事实（起草时报告，属 `AGENTS.md` §1「review 允许且鼓励」）**：

- `week10-plan.md` §7 交付物 ⑤ 写的是「**隔天**不看笔记按它走通两类」；D4 是 **8/20**，D5 是 **8/21**，间隔 **1 天**。「隔天」这个词能读成「隔一天（8/21 满足）」也能读成「至少隔一整天（8/22 才满足）」——**两种读法今天做的事不一样**。
- 实做形态意味着**再往唯一生产机注入一次故障**，且是周五、之后两天无人值守（§2.5 止步 6）。
- 类 2 若入选，D4 的收口本来就把它排到了 D5「从现象推理应用吞错」（D4 §6.3 ⑥）。

**三条出路（列代价，不替选）**：

| 出路 | 做法 | 代价 / 收益 |
|---|---|---|
| **a. 今天实做两类** | 按 runbook 盲测，真注入真恢复 | 收益：拿到唯一直接证据（判据 3+4 全销账）。代价：间隔只有 1 天，「记忆污染」风险高——走通了也可能是记住了而不是查到了；且周五注入 |
| **b. 今天纸面走查 + 顺延实做** | 今天只按 runbook 口述定位链（不注入），实做排到 W11 第一天（8/24，隔 4 天） | 收益：间隔足够、生产机零风险。代价：W10 交付物 ⑤ 今天**不完整**，要在周计划和状态文件里显式写「延期项」，不能装作做完了 |
| **c. 拆开：一类实做 + 一类纸面** | 例如类 2（D4 未走完，记忆最不完整）实做，另一类纸面 | 收益：折中。代价：两类证据强度不同，收口时必须分别写明哪类是实测哪类是走查 |

**要答的**：选哪条？**判据是什么**（不是「时间够不够」，是「哪条能真的证伪 runbook」）？若选 a/c，实做哪两类、为什么是它们？

### P2（runbook 的分叉结构）症状怎么映射到首查项？

> **2026-08-21 块 B 拍板（三问）**：
> ① 第一刀 = `curl 127.0.0.1:3000/health`，200 vs 非 200 劈反代层/应用层（D4 三类实测：类 1 注入态 200→反代层；类 2 000→应用层；类 3 200→资源层）。
> ② 失灵边界 = 类 3 资源型故障（/health 200 但磁盘/内存逼近线，探针不碰 DB）；补位信号 = 四 check 输出。
> ③ 五面全挂 → 共享下游（nodeapp / Nginx 进程与监听）；单面挂 → 该面专属 server block。
> 分叉骨架：`/health`（200→反代/资源层；非 200→应用层）→ 五面 curl（全挂→共享下游；单面挂→专属 block）。写进 runbook「通用第一刀」。

D4 三类各自的首查项已经有实测答案（`/health` 一步劈开反代层 / 应用层；类 3 用 `df -h /`）。runbook 要回答的是更上一层的问题：

1. 一个人**只看到症状**（还不知道是哪一类）时，第一刀切在哪？为什么是它而不是别的？
2. 这一刀**什么时候不成立**？（D4 已经有一个现成的反例：类 1 注入态 `/health` 是 **200**，而对外 443 根路径是 **502**——同一个信号在两类里含义不同）
3. 「五面全挂」和「只有一个面挂」，首查项一样吗？不一样的话，差在哪一层？

**要答的**：写出 runbook 的**分叉骨架**（哪些判定点、每个判定点的真假两支各通向什么）。这是黑名单项，AI 只 review 不代写。

### P3（三个盲区在 runbook 里怎么落）写成「已知不会报」还是「待修」？

> **2026-08-21 块 B 拍板（三问）**：
> ① 盲区①替代信号 = **两步链**：journalctl 见 OK 行 avail 逼近阈值（触发怀疑，入口）→ `df -B1 /` 字节级确认真实余量（终点；`df -BG` 的 4G 是取整产物，不可当终点）。盲区②替代信号 = 公网 curl 或 error.log upstream 模式。盲区③替代信号 = `ss -tlnp` 无 3000 + health 000。
> ② check-disk 判据**今天改**：改脚本（`df -B1` 字节级比较，分区用 `/` 非 `/data`）→ 注入前手动验证（与 `df -B1` 对比判定一致）→ 块 D 注入后复验（确认报红）；走变更单四要素（改前备份 `check-disk.sh.bak.20260821`，改坏一条命令还原；性质=**留下**，块 H 残留清单针对演练临时改动，不误判今天的刻意改动）；不入 W11。
> ③ 盲区② = 补监控 → W11 CI 部署验证；盲区③ = 补应用错误处理 → 代码 backlog。

D4 挖出三个独立盲区：① `check-disk` 的 `df -BG` 取整（avail∈[3.5,4.0)GiB 静默绿）；② `check-app` 对反代语义错失明（443root=502 时四项全绿）；③ nodeapp「假 active」（无监听 + health 000 但 active）。

**要答的（一问一格，别混）**：

1. 每一条在 runbook 里的**替代信号**是什么？（即「监控不报的时候，人靠什么先发现」）
2. `check-disk` 的判据**今天改不改**？改 = 当天验证成本 + 周五改生产脚本；不改 = runbook 里留一条已知盲区 + 转 W11/backlog。**只能选一个，并写明代价**。
3. ②③ 两条属于「补监控」还是「补应用错误处理」？归属不同，去向也不同（W11 CI 流水线 vs 代码 backlog）。

### P4（类 2 根因）从「nc 占 3000 + nodeapp active + health 000」能推出什么？

> **2026-08-21 块 B 预测①（AI review 修正版）**：`listen` 失败 = Server 实例 emit `'error'` 事件（非 unhandledRejection——`listen()` 返回实例不是 Promise）；**无 error listener → Node 当未捕获异常抛出 → 崩溃退出非零 → systemd failed**；有 listener 但捕获后不 `process.exit(1)` → active 但无监听（D4 现象）。块 E 读 server.js 对照这个分叉。

**顺序不许颠倒**：先写预测，再读 `server.js` 对照。

1. 预测：`listen` 失败时，Node 会发生什么？为什么进程还活着、systemd 还认为 active？
2. 读 `server.js` 后：实际的错误处理分支长什么样？和预测差在哪？
3. 修复建议（**只写建议，今天不改代码**）：要让这种情况变成 systemd 可见的 failed，最小改动是什么？它会不会误伤正常关停路径？

> 这题属黑名单（W6 错误边界与失败路径），**AI 上限 L2**：只追问、只 review，不给实现，也不替你读出结论。

### P5（收口判据）W10 这一周，做到什么算收口？

> **2026-08-21 块 B 拍板（第 ③ 问；① ② 留块 D/F 后结算）**：runbook 成篇下限 = §1 判据 1、2 销账（五列齐全可判真假 + 三盲区在册有替代信号）+ 通用第一刀分叉骨架在 + 速查表三张键在。① 七项交付物② 掌握验收四条，块 D/F 完成后回填 §10。

1. `week10-plan.md` §7 的七项交付物，今天逐项是「达成 / 部分达成 / 未做」的哪一种？**部分达成的必须写清缺口和去向**。
2. 掌握验收四条（§7 表下）今天能销几条？销不掉的那条，延迟重建排在哪天？
3. 「runbook 成篇」的下限是什么？（写到什么程度可以停手，不然今天会无限打磨——`AGENTS.md` §8 的收口纪律同样适用于文档）

---

## 4. 今日时间盒

| 块 | 时长 | 内容 | 产出 / 收工判据 |
|---|---|---|---|
| A | 20 min | **基线复位**：三层基线（五面 curl + `/health` + 三服务 + 四 timer）+ `df -h /` / `free -m` / `journalctl --disk-usage` 采值 | 三层全绿落纸；**不绿不许进 D** |
| B | 25 min | 答 §3 的 P1–P5（P1 先答，它决定 D 怎么走），回填 §2.3 | 五题有答案；P1 定案写死在 §2.3 |
| C | 60 min | **A runbook 成篇**：先排分叉骨架（P2 的答案）再填字；三类五列 + 盲区表 + 五面/四服务速查表 | `runbook.md` 存在且判据 1、2 销账 |
| D | 50 min | **B 延迟自测**（**P1 定案 = 实做注入**）：两类真注入真恢复，**全程不开 D4 笔记**；翻笔记就地记账；每类结束即复位基线 | §7 两份记录填满，每类至少一条命令输出；翻笔记次数与位置有记录；两类各自恢复基线 |
| E | 25 min | **类 2 根因**（P4）：先写预测 → 读 `server.js` → 对照 → 修复建议 | §8 有预测、有对照、有「改不改、为什么」 |
| F | 25 min | **C 能力检验口述**：① 一条日志从产生到可被检索经过哪些层 ② 两条失败路径的分叉判据 ③ 改需求预演（日志量涨十倍 / 再加一个公网面） | §9.1 三问各一段 + **当场修正逐条记录** |
| G | 15 min | **D 展板校正（只做数据时效那一维）**：`yarn verify:board` 全过 + W10 七块对最新数据核一遍（重点 ⑤ 那四格、① 那格）；表达形态那一维**整条移交独立线**，今天只确认线已立、指针已留 | §9.2 ① 有结论、不一致格当场修正；② 三个复选框打勾即可 |
| H | 15 min | **全量回归 + 残留清扫**：三层基线复测 + §2.4 残留逐条核零 | 全绿 + 残留清零有证据（`diff` 空 / `ls` 不存在 / `pgrep` 无匹配） |
| I | 35 min | **E 状态收口**：§10 收口段、周计划勾选、`LEARNING-STATE.md` 指向 W11、W10 复盘 + 项目叙述、口语稿、必要时 `DEBT.md`、commit | commit；W10 收口 |

合计约 **4 小时 30 分**（块 G 由 25 min 缩到 15 min——表达形态那一维已移交 [`showcase-audit-line.md`](../../week8-fullstack/notes/showcase-audit-line.md)）。

**时间不够时砍的顺序**：G 展板校正（非阻断，可转独立线的下一个空档）→ D 的第二类（保一类，写明只做了一类）→ F 的第③问。
**A、C、H、I 一步都不许砍**：A 是今天所有证据的起点，C 是本周唯一对外交付物，H 是周末无人值守的判据，I 是「这一周有没有收口」本身。

---

## 5. 执行细节（白名单部分，只放语法与工具行为）

> 本节只放**换个发行版 / 换个反代 / 换个前端框架就不成立**的纯工具细节。
> **runbook 的分叉结构、定位顺序、根因推理、盲区归属一律由 §3 的答案决定，不在这里预设。**

### 5.1 三层基线复位（块 A / 每类自测结束 / 块 H 各跑一次）

命令原文见 [`day4-fault-drills.md`](./day4-fault-drills.md) §5.2（五面 curl + `/health` + `systemctl is-active` 三层），今天照跑不改。

### 5.2 自测取证与演练标记

```bash
# 演练标记：注入前后各打一条，收工时可一次性过滤出今天的所有人为痕迹
logger -t DRILL "D5 self-test start: <类名>"
journalctl -t DRILL --since today --no-pager

# 检查当下怎么说（手工触发一次，不等 timer）
sudo systemctl start check-app.service
journalctl -u check-app.service -n 5 --no-pager
```

> 经验知识（`AGENTS.md` §4）：`Type=oneshot` 正常跑完是 `Deactivated successfully`，失败是 `Failed with result exit-code`；
> `systemctl is-active` 对 oneshot **看不出绿红**——这条在 D4 已经用到过，runbook 的「首查命令」列写 check 时要连这句一起写。

### 5.3 注入与还原骨架

引自契约 §5.4，原文见 D4 §5.3。**类 3 的 `fallocate` 值按当天 `df -B1 /` 用换算率重算**（声明 1G → 字节级 +1,073,745,920，D4 §10.3 实测定论）。

### 5.4 展板校正与构建（块 G，白名单展示资产）

```bash
# 仓库根 → 前端目录
yarn typecheck
yarn verify:board                 # D4 收口时 421/421；今天应保持全过
VITE_SHOWCASE_ONLY=1 yarn build:showcase
```

> **发布边界**（`SHOWCASE-DEPLOY-PROTOCOL.md` §0）：GitHub Pages 上传**冻结**，未经本人当次会话明确解冻不得复制 / 提交 / 推送任何 Pages 产物。
> 当前发布目标是服务器 **8081**，且**上线是独立授权动作**——8/20 的 421/421 是本地构建，服务器 8081 仍是旧版。
> **今天是否部署由本人决定，不阻断收口**（§7 已列入「明确不做」的默认档）。

### 5.5 收尾 git 检查

```bash
git status --short
git diff --stat
git log --oneline -5
```

> 收口前按 `LEARNING-PROTOCOL.md` §6 第 6 条核一遍：没有误提交凭据、调试输出、服务器真实密钥路径。
> 笔记里贴命令输出时，凭据与密钥路径一律 redact（本周红线，D2 已执行同样口径）。

---

## 6. runbook 骨架（**结构由本人在 P2 定，本节只放空表头**）

> 落点：`week10-observability/notes/runbook.md`（`week10-plan.md` §7 交付物 ⑤）。
> 五列来自周计划原文（症状 → 首查命令 → 判定分叉 → 修复 → 预防），**不是 AI 新造的结构**；
> 每类的内容、判定分叉的写法、盲区的替代信号，全部由 §3 P2/P3 的答案决定，**AI 不代填**。

**建议的文件骨架**（顺序与取舍本人可改）：

```text
0. 适用范围与前提（这份 runbook 假设的是哪台机、哪五个面、哪四项检查）
1. 通用第一刀（P2 的答案：只看到症状时先跑什么、为什么）
2. 分类条目（每类一节，五列齐全）
   2.1 类 1 反代配置错误
   2.2 类 2 端口占用 / 应用假 active
   2.3 类 3 磁盘逼近满
3. 监控盲区表（三条，各含「监控为什么不报」+「人靠什么先发现」）
4. 速查表（五个公网面 / 四个服务 / 四项检查 各自的一行判据与命令）
5. 演练痕迹与真事故如何区分（`journalctl -t DRILL`）
6. 局限（这份 runbook 覆盖不到什么：OOM、多机、证书真过期……）
```

**每类的空表头**：

| 症状（对外看到什么） | 首查命令（先跑哪一条，为什么先跑它） | 判定分叉（真 → 走哪 / 假 → 走哪） | 修复 | 预防 |
|---|---|---|---|---|
| 待填 | 待填 | 待填 | 待填 | 待填 |

---

## 7. B 延迟自测记录（当天现场填，每类一份）

> **填写纪律**：全程不开 `day4-fault-drills.md`。翻了就在「翻笔记记账」里写清楚**第几步翻的、翻的是什么、runbook 缺了哪一句**——
> 这不是失分，这是今天唯一能直接证伪 runbook 的数据。

### 7.1 自测类 A：____（P1 定）

- **① 起点基线**（时刻 + 三层结果）：
- **② 注入**（P1 定案 = 实做；写下真实执行的命令与时刻）：
- **③ 只看症状时我第一步跑了什么，为什么**（并标：这一步是**查 runbook 得到的**还是**本来就记得**——间隔只有 1 天，「记得」的那几步同样是 runbook 的缺口）：
- **④ runbook 指向哪一支，实际走到哪一支**（一致 / 不一致，不一致要归因；同样标「查到的 / 记得的」）：
- **⑤ 根因**（显式标 **事实 / 推断 / 未验证**）：
- **⑥ 修复 + 恢复基线证据**：
- **⑦ 翻笔记记账**（次数 / 位置 / runbook 缺的那一句）：
- **⑧ runbook 回填**（这次自测直接改了 runbook 的哪几行）：

### 7.2 自测类 B：____（P1 定）

（同 7.1 八项）

---

## 8. 类 2 根因定论（P4，先预测后读码）

- **① 预测**（读 `server.js` **之前**写，2026-08-21 块 B 修正版）：`listen` 失败 = Server 实例 emit `'error'` 事件（非 unhandledRejection）；**无 error listener → Node 当未捕获异常抛出 → 崩溃退出非零 → systemd failed**；有 listener 但捕获后不 `process.exit(1)` → active 但无监听（D4 现象）。
- **② 读到的实际分支**（只读，不改，98 行全文）：42 行 `server = app.listen(PORT, HOST, () => { logger.info(...) })`——带成功回调，**无任何 `.on('error')`**；31 行 `await connectDB()` 在 listen 之前；46–52 行外层 `try/catch` 捕获同步错误并 `process.exit(1)`（但 `listen` 异步错误不进此 catch）；57–98 行 gracefulShutdown（SIGINT/SIGTERM，引用外层 `let server`）。
- **③ 预测 vs 实际的偏差与归因**：预测「无 listener → 崩溃 failed」被 D4 证伪（实测 active 假活，未崩溃）。**关键事实**：D4 注入态 journald 打印了 **「服务运行端口: 127.0.0.1:3000」**——这是 42–44 行 listen 的**成功回调**被触发，而 `ss` 无 3000 监听、health 000。**归因修正**：不是「被隐式错误处理器捕获」（全文无 `uncaughtException`/`unhandledRejection` 钩子，此归因无据），而是 **`listen` 成功回调已触发但底层 socket 未实际绑定——机制未在 D4/D5 验证**，显式标 **未验证**。推断方向（均未验证）：error 事件被延迟到下一事件循环、成功回调先触发（非预期）；或某些条件下 EADDRINUSE 不阻止成功回调（取决于 backlog/系统行为）。
- **④ 修复建议**（最小改动 + 影响；**今天不改代码，此建议留 W11 前定案**）：listen 后加 `server.on('error', ...)`，`EADDRINUSE` 时 `logger.error(...)` + `process.exit(1)`，让 systemd 感知 failed。**注意两点**：① 必须复用第 6 行外层 `let server`（`gracefulShutdown` 73 行引用它），不能 `const server =` 新建遮蔽变量；② 因机制未验证，需在 W11 以最小可复现样本（仅保留 listen + 错误处理）先确认「成功回调触发 vs 底层绑定」的实际关系，再定最终修复，避免盲目加 `process.exit(1)` 产生副作用。
- **⑤ 去向**：**runbook §3 盲区③** 更新为「成功回调触发但底层未绑定（机制未验证）」+ 替代信号保持 `ss` 无监听 + health 000；**记 `BACKLOG.md`**：类 2 根因复现（W11 最小样本）+ 修复建议（error 监听 + exit(1)，复用外层 server 变量）；**W11** 复现机制后补 runbook 修复列。

---

## 9. C 能力检验与 D 展板校正（当天现场填）

### 9.1 三问口述（沿用 W9 D5 C 模块形态：**当场修正计入掌握证据**）

1. **一条日志从产生到能被查到，经过哪些层**（谁生成 id、谁传、谁落盘、谁轮转、用什么查）：
   - 口述要点：Nginx 生成 `$request_id` → `proxy_set_header X-Request-Id` 传 Node → pino-http 读头注入日志 → stdout → systemd 接管 → journald 落盘 → `journalctl` 按服务/requestId/时间查；Nginx 侧独立落盘 access/error.log（obs 格式含 `$request_id`）→ logrotate 轮转；演练痕迹用 `journalctl -t DRILL` 过滤。
   - 当场修正：① 字段名 = **`requestId`**（以线上 NDJSON 实测为准，非 req_id）；② Nginx 为唯一生成方，未传时记兜底值（如 `UNDEFINED`）**不生成新 id**；③ 三时间戳口径 = Node/journald UTC(Z) / Nginx access +08:00 / Nginx error 本地无标记，不是两分；④ `Storage=persistent` 与 `MaxFileSec=1month` 未验证/未拍板，标**待确认**。
2. **两条失败路径的分叉判据**（「五面全挂」vs「只有一个面挂」先查什么不一样；`/health` 200 vs 非 200 各指向哪一层）：
   - 口述要点：第一刀 `/health` 本地直连绕开反代——**200** → 反代层/资源层（类 1/类 3）；**非 200（含 000）** → 应用层/进程层（类 2 假 active）→ `ss -tlnp | grep :3000` + `journalctl -u nodeapp`。第二刀在反代层内：**五面全挂** → 共享下游（本机无全局 upstream，四 block 独立 proxy_pass；首查 nodeapp 监听 + error.log upstream 模式）；**单面挂** → 该面专属 server block（如 443 根挂查 `shop-ssl` 13 行）。
   - 当场修正：① ````/health``` 200 只证明 Node **内存态当前能响应**（探针不碰 DB/不写盘），不代表应用整体健康——类 3 磁盘满时 200 但很快写盘失败（原说「应用正常」过宽）；② 删掉「Nginx 全局 upstream 被改错」嫌疑——本机**无全局 upstream 实体**（W9 定论：四 block 各自独立 proxy_pass），五面全挂首查应为 nodeapp 监听 / Nginx 进程，不查全局配置。
3. **改需求预演**（日志量涨十倍先压垮哪一层；再加一个公网面要动哪几处）：
   - 口述要点：① 日志量涨十倍 → 先压垮 journald（rate limit 丢日志 = 可观测性失效，先于磁盘爆）→ 磁盘空间（SystemMaxUse=500M 快速填满 + journald 删旧日志的 I/O 抢占 MongoDB）→ Nginx access.log 膨胀（logrotate 跟不上）；pino 异步写 stdout 阻塞风险较低。② 加公网面 → Nginx 新 server block（listen + proxy_pass + log 格式）→ **ufw allow 新端口（双栈）** → Node 路由（或复用现有）→ 日志/X-Request-Id 传递 → HTTPS 需证书（同域可复用 sslip.io 单证书）→ check 探针评估。
   - 当场修正：① **ufw 已启用**（W9 D2 实测 + H2 冻结：22/80/443/8080 双栈 ALLOW default deny）——不是「未启用」；加面必须 `sudo ufw allow <port>`。② 新 block 用「proxy_pass → 127.0.0.1:3000」，**不提全局 upstream**（本机无此实体）。③ journald `RateLimitBurst` 默认约 10000/30s，本机是否生效**待验证**，不当铁律。④ 强化：丢日志**先于**磁盘爆；新面「对外可达性探针」= **盲区②补监控的自然挂载点**（W11 CI）。

### 9.2 展板校正（两个正交维度，今天只做第一维 + 第二维的定性）

**这一步做的是什么**（2026-08-20 本人定的表述，替换此前的「核谎」）：展板是**某一时刻事实的快照**，
后面几天的实测会让其中几格过时——过时不是谎报，是数据变新了、板没跟上。
所以动作是**逐格拿最新数据核对已有结论，不一致的当场修正**；「补新内容」是另一件事，排在校正之后。

> 这个口径同时改掉一个判断顺序：不必先论证某一格「是不是在说谎」再决定改不改，
> 而是直接问「**这一格的结论和最新一次实测还对得上吗**」——对不上就翻档，对得上就留着。

**2026-08-20 本人指出的第二维（范围比原计划大得多）**：「不要过分追求动效」那句话的波及面可能更广，
导致**大段文字展示**；这两周只是集中爆发的地方，**所有板都可能需要重新校对**。
核对后这个判断成立，证据三条（详见 [`visualization-optimization-roadmap.md`](../../week8-fullstack/notes/visualization-optimization-roadmap.md) 第八轮「落地范围的补正」）：
8/18 的判据加严只落到 W10（返工）+ W9（延后），**其余七块板从未被点名**；
它们的组件最后一次改动**都停在 2026-08-12**，是在旧规则「图形只负责关系、结论保留文字」下建成的；
8/18 的存量清扫 `d24038b` 只推了 `styles.css` 的排版，**没碰内容形态**。

**于是块 G 拆成两件互不替代的事，而第二件已在 2026-08-20 移出本日**：

| 维度 | 问的是什么 | 对象 | 归属 |
|---|---|---|---|
| **① 数据时效** | 这一格的结论和**最新一次实测**还对得上吗？ | W10 板七块（重点 ⑤ 那四格「未实测」、① 那格「已拍板」） | **今天做完**：不一致的当场翻档 + `yarn verify:board` 保持全过 |
| **② 表达形态** | 遮住 `h3` 与结论段，还能答出这块板的验收句吗？（+ 四条肯定面） | **八块板**（见下） | **今天不做**：整条移交 [`showcase-audit-line.md`](../../week8-fullstack/notes/showcase-audit-line.md) |

> **为什么 ② 整条移出去**（2026-08-20 本人决定）：**展板核查单开一条线，不必和学习同步开展**。
> 它按 15–20 分钟一块排队消化，节奏由余量决定；收口日本来就满，塞进来的结果只会是「凭印象铺开改」，
> 而那恰好和这条线要修的失效同型。**本日连「抽样 3 块定性」也不做**——那是那条线阶段一的第一个单元。
> 两个维度的触发方式本来就不同：① 是**事件驱动**（新实测一到就修，跟着学习走），② 是**排队驱动**。
> **P2-4 的档位今天不动**，等那条线阶段一的判定出来再定。

**① 数据时效（今天做完）**

- `yarn verify:board` 结果：**421/421 全过**（两次：3:48 基线 / 3:55 翻档后复验，均「通过 421 项，失败 0 项」；playwright 因全局目录缺失改为本地 devDependency 1.62.1 装好）
- 与最新数据不一致的格子与处置（当场翻档 / 记 backlog）：**四处当场翻档**（`w10Facts.ts`）：① `BLIND_SPOTS.rounding.fixCandidate` →「已修（#11 改字节级判据）」、`goesTo` →「已修→runbook 保留为教训」；② `BLIND_SPOTS.fakeactive.fixCandidate` → 修复方向（error 监听 + exit(1) + 复用外层 server）、`goesTo` →「D5 已读码定论→W11 复现」，**grade 保持 pending**（机制未验证，不夸大）；③ `DRILLS.port.rootCause.unverified` →「机制未验证：回调触发但未绑定；W11 最小样本复现」；④ `THRESHOLD_RULERS.disk.caveat` 追加「8/21 已修：字节级判据，该红就红实证已得」。**刻意不改**：`CHECK_VERDICTS.port` 四格保持 `untested`（D5 未实做类 2 注入，不能因根因定论就写红）。
- 是否部署 8081：**默认不部署**（§11 边界：上线是独立授权动作；421/421 为本地构建，服务器 8081 仍旧版）

> **2026-08-21 追记（另一次对话，不改当天的判断）**：这一步当天只做「校正」，明确把「补新内容」排在后面。
> 那件后续的事已经做完——**D5 自己的内容新建成展板第八块 ⑧「不看笔记还能不能走通」**（三页：第一刀 / 盲测两类 /
> 两条收尾的强度），直接按新标准落地，`verify:board` 441 → **490/490**，仍**未部署**。
> 笔记列表也补进了 `runbook.md` 与本文件两份（`reviewOnly`）。执行记录见
> [`week10-visualization-plan.md`](./week10-visualization-plan.md) §12.11。

**② 表达形态（今天不做，只确认线已立起来）**

- [ ] 确认 [`showcase-audit-line.md`](../../week8-fullstack/notes/showcase-audit-line.md) 存在且范围表（八块板）无误
- [ ] `LEARNING-STATE.md` 留一行指针（当前进展 0/8），**不复制那条线的内容**
- [ ] `BACKLOG.md` P2-4 指向该线；**档位今天不动**

> 那条线的对象是**八块板**：Auth / OAuth2 / W3 / W5 / W6 / W6Day4 / W9 / W10。
> （「面试准备」板同属旧口径，但它是材料索引不是关系图，是否纳入由那条线先判定；
> 「学习笔记」tab 的 Markdown 正文与 8080 admin 的 `Dashboard.tsx` 明确不在范围。）

**原口径不一致的处置（起草时报告的第二处）**：`week10-plan.md` §4 D5-D 指的是 W9 板十三块，
`LEARNING-STATE.md` 指的是 W10 板七块——**这条随拆维自动解掉**：
① 的对象是 W10 七块（今天做），② 的对象是八块板全体（移交独立线）。

---

## 10. 收口：实际发生了什么（2026-08-21 当天填）

> 形态同 D2 §11 / D3 §9 / D4 §10：期望 vs 实测逐项对照 + 偏差归因 + 执行期新增事实。

### 10.1 §1 六条验收判据逐条销账

| # | 判据 | 结果 | 证据 / 缺在哪 |
|---|---|---|---|
| 1 | runbook 五列齐全、分叉可判真假 | ✅ 达成 | runbook.md §2 类 1/2/3 五列齐全；判定分叉均为「真→X / 假→Y」可执行条件（/health 200 vs 非 200、五面全挂 vs 单面挂） |
| 2 | 三个盲区在册且有替代信号 | ✅ 达成 | runbook §3 三条齐，各含「为什么不报」+「替代信号」；①已修（#11）②补监控（W11）③修复方向已定、机制未验证（W11） |
| 3 | 自测是盲的（翻笔记有记账） | ✅ 达成 | 类 1/类 3 全程 **0 次翻笔记**；runbook 已知缺口（注入量算法）靠本人原理推算，卡住即记账的纪律未触发 |
| 4 | 走通两类 | ✅ 达成 | 类 3（注入 27.3G 触止步→止损→26.4G 重注入→字节级 FAIL→恢复）+ 类 1（diff 双证据→502→200 恢复） |
| 5 | 三问口述 + 当场修正 | ✅ 达成 | §9.1 三问全答 + 当场修正（requestId 字段、Nginx 兜底值、三时间戳口径、ufw 已启用、无全局 upstream 等） |
| 6 | 收口不留悬案 | ✅ 达成 | 类 2 根因从「待读码」→「已读码定论（机制未验证）」；盲区②→W11、假 active→W11 复现、展板已翻档四处、无沉默悬案 |

### 10.2 W10 七项交付物结账（P5 第 1 问的答案落这里）

| # | 交付物 | 达成 / 部分 / 未做 | 缺口与去向 |
|---|---|---|---|
| ① | 观测契约 | ✅ 达成 | day1 契约 15/15 拍板；D5 未有推翻项 |
| ② | 日志改造上线 | ✅ 达成 | D2 完成：requestId 串两条流 + 脱敏 + 五面回归全绿 |
| ③ | 四项检查 + timer | ✅ 达成 | D3 四项各弄红一次；**D5 #11 补 boot 字节级判据**（check-disk 已修） |
| ④ | 排障记录 3–5 类 | ✅ 达成 | D4 三类五段式 + 命令输出；D5 盲测新增两类证据 |
| ⑤ | runbook | ✅ 达成 | runbook.md 成篇 + 延迟自测两类走通（判据 1–4 销账） |
| ⑥ | 周复盘 + 项目叙述 | ✅ 达成 | 本节 §10.3/§10.4 |
| ⑦ | 英语并行线 | **部分达成** | 本周四篇口语稿已生成（day1–day4）；**day5 待生成**（收口协议第 8 条，见 §13 下一条） |

### 10.3 第二轮 W10 复盘（本人写，AI 只挑毛病）

- 这一周最值钱的一条发现：**「假输入能红 ≠ 真条件该红」是活证据**——D3 的弄红全是假输入（改阈值、假证书），D4 三类真注入当场证伪了「检查脚本可信」的前提：磁盘真到 3.84GiB 报绿（取整盲区）、反代真 502 四项全绿（scope 盲区）、端口真冲突 nodeapp 假 active。检查脚本只能证明「判据算得对」，不能证明「真故障会红」，必须真注入一次才能销账。
- 最贵的一次弯路与它的可迁移教训：**类 3 注入量用「记忆 + 十进制混淆」算，27.3G 触止步**。教训 = 注入量的单位与换算率（fallocate 声明 G = GiB）必须先验再算，不能拿可读格式的数字做减法（D4 的 0.7G 偏差同源）。可迁移：操作链任务里「量」的每次取值都要从当天基线重算，抄过期数就是假事实。
- 下一次做同类任务会改的一个做法：**演练注入量算法写进 runbook 的「准备」节**（今天刻意留缺口当盲测物料，结果卡住证明「排障手册」与「演练物料」职责要分开——runbook 面向真事故，注入量应放演练准备清单，不让读者在排障时猜）。

### 10.4 5–10 分钟项目叙述（背景 → 问题 → 做法 → 结果 → **边界**）

（本人口述后落纸；「边界」段是重点——W9 收口时已证明它是最容易被略过、也是面试最容易追问的一段。）

- **背景**：一台 2C/2G/swap=0 的 Ubuntu 生产机承载 5 个公网面（80/443/8080/8081/内部 3000），服务无任何观测能力——出问题只能靠"看报错"。
- **问题**：想让"出了问题怎么查"从经验变成方法：一条请求从 Nginx 到 Node 能被一个 id 串起来、四项检查能在故障前报红、三类典型故障能按 runbook 修。
- **做法**：D2 日志改造（requestId + 脱敏 + 三时间戳口径）；D3 四项检查 + timer + 各弄红一次；D4 三类真注入（磁盘满/反代错/端口占）挖出三个盲区；D5 runbook 成篇 + 延迟自测两类 + 修掉 check-disk 取整盲区。
- **结果**：延迟自测两类不看笔记走通（0 次翻笔记）；check-disk 字节级判据封堵盲区（该红就红实证）；五面基线全绿 + 残留核零；verify:board 421/421。
- **边界**：① 盲区②（反代可达性）与类 2 假 active 机制未完全闭合，已转 W11；② OOM/证书真过期未演练（写不出回滚或触碰现网红线）；③ 单机可观测性 ≠ 多机集中收集（集中=统一格式+落点+可检索，未上 ELK/Loki）；④ 展板表达形态核查单开一条线（今天的 D 只做数据时效一维）。

### 10.5 块 H 回归与残留核零

```text
# 15:58 三层基线最终回归
80 200 443api 200 443adm 200 8080 200 8081 200
health 200
nginx/mongod/nodeapp all active；四 timer all active

# 残留清单逐条核零（§2.4）
/tmp/disk-fill.bin → ls: No such file or directory
ss -tlnp | grep :3000 → LISTEN 0 511 127.0.0.1:3000（backlog=511 为 nodeapp 自身）
shop-ssl vs d5bak DIFF_EMPTY（D5 还原完成）
shop-ssl vs d4bak DIFF_EMPTY（D4 备份保持）
/opt/check-disk.sh.bak.20260821 EXISTS（#11 刻意保留，非残留）
nodeapp.service 无 Environment= 行（NO_ENV_LINE）

# check-disk 手工触发最终态
15:58:55 Deactivated successfully（字节级判据绿态，与 15:18:41 FAIL 红态形成对照）
```

结论：**全绿 + 残留核零**，周末无人值守判据（止步 6）满足。

---

## 11. 今日明确不做

- **不改 `app.js` / `server.js` 一行**——类 2 根因是**只读**任务（§2.1 ③）。修复建议写进 §8 ④，改动本身排 W11 或 backlog。
- **除 #11 check-disk 判据改造（P3-② 已拍板今天改，走变更单四要素）外**，其余三个 check 脚本（app / mem / cert）不动任何阈值或判据。
- **不做 OOM、不做证书真过期演练**（本周红线不变：不撤销 / 不重签 / 不改 `/etc/letsencrypt/live/`、不停 80）。
- **不建 Prometheus / Grafana、不接 webhook 告警**（D1 已降 stretch）。
- **不动 8080 下线**（发布变更，非观测主题）。
- **不碰展板的表达形态那一维**——八块板的判定与返工**整条不在今天**（含「抽样定性」）。它已单开一条线：[`showcase-audit-line.md`](../../week8-fullstack/notes/showcase-audit-line.md)，排队不排期，今天只确认线已立、指针已留。
- **不解冻 GitHub Pages、默认不部署 8081**——展板本地构建可以跑，发布是独立授权动作（§5.4）。
- **不碰 Java / Python**（8/17 拍板：Java 并入 W11，Python 留 W12）。
- **不在收口日顺手修「看着不顺眼」的东西**——今天的改动清单只有 §2.1 那十项。

---

## 12. AI 协作边界（本日形态）

| 事项 | 归属 | AI 上限 |
|---|---|---|
| 基线 / 注入 / 回滚 / 只读定位命令的**语法与工具行为** | 白 | L3/L4（D4 §5 已给全，今天照用） |
| 本文件与 `runbook.md` 的**结构、空表头、时间盒、核验表格式** | 白 | L3/L4 |
| 展板构建与断言脚本、`yarn verify:board` 的执行与修断言 | 白（展示资产） | L3/L4，最小范围 |
| 周计划勾选、`LEARNING-STATE.md`、口语稿、commit message | 白 | L3/L4 |
| **runbook 的分叉结构**（症状 → 首查项的映射） | **黑** | **L2**：只出题与 review，不代写 |
| **三个盲区的替代信号与归属判断**（P3） | **黑** | **L2** |
| **类 2 根因推理与修复建议**（P4，W6 错误边界） | **黑** | **L2**：只追问，不给实现、不替读结论 |
| **延迟自测的形态取舍**（P1）与**收口判据**（P5） | **黑** | **L2**：只列代价与冲突，不替选 |
| **三问口述与项目叙述** | **黑** | **L2**：只挑毛病，不代拟讲稿 |

**执行纪律**（W9 D5 修正结论，D4 沿用）：手敲不是目的、证据才是——
**亲手最小集 = runbook 的分叉判断 + 自测时的首查决定 + 根因推理**；批量验证与命令拼写可由 AI 出、本人核输出。
**特别地：块 D 自测期间，AI 不得提示定位方向**——那一刻 AI 的任何提示都等同于翻笔记，会直接毁掉判据 3。
如果 AI 在块 D 期间给出了定位方向，按 `AGENTS.md` §5 记 `DEBT.md`，并把该类自测判为不通过。

---

## 13. 收尾清单（块 I）

- [ ] `runbook.md` 存在，§1 判据 1、2 销账（五列齐全 + 三个盲区在册）
- [ ] §7 两份自测记录填满（或按 P1 的形态写明只做了几类、缺口在哪），每份至少一条命令输出
- [ ] §8 类 2 根因：预测 → 实际 → 偏差 → 建议 → 去向，五项齐
- [ ] §9.1 三问口述记录 + 当场修正逐条落纸
- [ ] §9.2 展板校正：① `yarn verify:board` 结果 + 不一致格处置（今天做完）；② 只确认 [`showcase-audit-line.md`](../../week8-fullstack/notes/showcase-audit-line.md) 已立 + `LEARNING-STATE.md` 留一行指针（0/8）+ `BACKLOG.md` P2-4 指向它
- [ ] §10.1 六条判据、§10.2 七项交付物逐条结账，**部分达成的写清去向**
- [ ] §2.4 残留清单逐条核零，三层基线最终回归全绿（贴输出到 §10.5）
- [ ] `week10-plan.md` §4 的 D5 勾选 + 结果一句话
- [ ] `LEARNING-STATE.md`：当前周 → **W11（8/24–8/28，CI 流水线与自动化发布）**，含 W10 未完项的接手位置
- [ ] 必要时 `DEBT.md`（今日预期不触发：白名单格式 + 黑名单止步 L2；块 D 被提示则必须记）
- [ ] `day5-english-speaking.md`（按 `DAILY-SPEAKING-PROTOCOL.md`，**主题取自当天真实发生的事**）
- [ ] `git status --short` / `git diff --stat` 核一遍，无凭据与调试输出
- [ ] commit

---

## 14. 下周入口（W11：CI 流水线与自动化发布，8/24–8/28）

`week10-plan.md` §9 已经写好交棒接口，今天收口时按这三条留好，不额外做适配：

1. **四项检查 + 五面基线 curl** = W11 流水线的**部署后验证步骤**——今天 runbook 的「速查表」那一节就是它的现成输入。
2. **runbook 的「预防」列** = W11**回滚策略**的输入。写「预防」时想着这个用途，别只写「以后小心点」。
3. **W10 未完项的显式移交**：类 2 修复（应用错误处理）、`check-disk` 判据改造、展板 W9 十三块返工、8080 明文下线——
   今天在 `LEARNING-STATE.md` 里各留一行去向（W11 / backlog / 不做），**不许沉默消失**。
   另：W11 还接手 W9 未做的 Java stretch（最小 jar + systemd + Nginx location，8/17 拍板）。

---

## 15. AI 辅助记录

- 2026-08-20（D4 收口后起草）：AI 起草本文件——变更单结构、时间盒排布、§6 runbook **空骨架与空表头**（五列取自 `week10-plan.md` 原文，非新造）、§7–§10 的空模板、§5 的白名单命令与工具行为（均为 D4 §5 已给内容的引用，未新增）。
- **§3 P1–P5 全部留空待本人作答**；runbook 的分叉结构、盲区替代信号、类 2 根因、自测形态取舍、收口判据 AI 不代填（`week10-plan.md` §6 黑名单，上限 L2）。
- **起草时报告的两处不一致**（属 `AGENTS.md` §1 硬线「review 允许且鼓励」，AI 未替选、未改任何已冻结文件）：
  1. **延迟自测的间隔口径**：交付物 ⑤ 写「隔天不看笔记」，而 D4=8/20、D5=8/21 只隔 1 天 → 落成 §3 P1 的三条出路。
     **2026-08-20 本人拍板选 a（当天实做两类，不顺延）**；AI 未替选，只把「间隔 1 天」的代价转成两条执行纪律（判据 3 记账 + §7 ③④ 标「查到的 / 记得的」）与一条顺序硬约束（§2.2 第 2 条：D 必须早于 E，否则 P4 先预测后读码作废）。**实做哪两类仍待本人在块 B 作答。**
  2. **展板校正的对象**：`week10-plan.md` §4 D5-D 说的是 W9 板「十三块」，`LEARNING-STATE.md` 8/20 版说的是 W10 板「七块」（W9 十三块已转 `BACKLOG.md`）→ 落成 §9.2 的口径表，待本人拍一句。
- 2026-08-20（本人反馈后修订）：D 模块的表述由「核谎 / 有没有说谎」改为「**按最新数据修正过时结论**」（§9.2 开头写明理由），本文件、`week10-plan.md` D5 条目与 `LEARNING-STATE.md` 三处同步；**只改表述与判断顺序，不改这一步要做的事和它的非阻断档位**。
- 2026-08-20（本人指出波及面更广后扩范围）：本人判断「不要过分追求动效」的波及范围更大、大段文字问题这两周才集中爆发，
  **所有板都可能要重新校对**。AI 去核对后确认该判断成立并给出三条证据（8/18 落地范围只覆盖 W9/W10；七块板组件冻结在 8/12 的旧规则下；
  8/18 存量清扫只动 `styles.css` 未碰内容形态），落成 roadmap 第八轮「落地范围的补正」+ `BACKLOG.md` P2-4 范围扩到九块板（**档位不动，待定性结论**）
  + 本文件 §9.2 拆成两维（今天只做数据时效 + 表达形态抽样定性）。**范围与档位的最终决定权在本人**；AI 做的是核对、举证与排期建议。
- 2026-08-20（本人决定单开一条线）：**展板核查不与学习同步开展**——表达形态那一维（含原定的「抽样 3 块定性」）**整条移出 D5**，
  落成独立执行文档 [`showcase-audit-line.md`](../../week8-fullstack/notes/showcase-audit-line.md)（排队不排期 / 阶段一只判定不改 / 明确不作为并行线，
  8/17 拍板的并行线仍只有英语 + 项目叙述）。本日块 G 由 25 min 缩到 15 min，只留数据时效那一维。
  同时更正范围：核查对象由「九块板」改为**八块板**——首次列表把 8080 admin 的 `Dashboard.tsx` 误算作展板、漏了 `OAuth2Panel.tsx`，已在 roadmap 与 `BACKLOG.md` 同步更正。
- 未对黑名单知识点提供 L2 骨架，**未触发 `DEBT.md` 记账**。
