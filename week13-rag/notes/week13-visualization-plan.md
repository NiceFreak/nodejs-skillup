# W13 visualization plan — RAG 分享展板与历史素材

> 建立：2026-09-09；更新：2026-09-11（Asia/Shanghai）。状态：D5 六专题已实现，本地交互与视觉检查通过；本人演练验收待做。
> 本次依用户明确授权，D5 以演练、RAG 成果展示和框架映射为主线。下方 §0–§2 保留 D3 素材快照，
> 其中未来时态只表示 9/9 当时状态；现行设计与事实边界以 §3 起为准。仅本地构建，不提交或部署。

## 0. 素材边界（事实快照）

- 对象：RAG（Retrieval-Augmented Generation）固定链路的前端输入工程。
- 本周已完成：corpus snapshot `rules-c0a4b85`（7 文件 76,243 bytes；raw corpus-only 18,697
  estimated tokens）；eval `w13-eval-v1`（dev/holdout 各 10 题，未运行模型）；Prompt `rag-prompt-v0`
  与 response schema；serialization 单一规范与 fixture；确定性 parser / citation registry /
  Evidence Context 实现（572 blocks，真实整串 sha256 `8a02c665…`，双跑一致，覆盖审计为 0）。
- 未完成边界：serialized 输入 token 计量、context budget、全语料上下文 baseline、BM25/dense、
  首次 holdout 均未执行；判据 #1–#7 逐字确认未完成。

## 1. 可迁移思维模式（每条：事实 → 可迁移点 → 边界）

### 1.1 契约先于实现
- 事实：D1 先冻结 corpus/manifest；D2 先冻结 eval 题意与判分；D3 先冻结 serialization 规范与
  fixture，之后才允许实现 parser。每个阶段门禁不过不叠加下一阶段。
- 可迁移点：任何含不可逆成本或影响后续对照的输入（语料、题库、配置、接口契约）先冻结版本与 hash，
  再进入实现；冻结后变更走新版本而非静默修改。
- 边界：冻结不等于正确；语义边界（如 source block 分得是否合理）仍由后续 eval 暴露。

### 1.2 按输出边界分层设判据
- 事实：serialization 是纯函数链 spans → model_content → source wrapper → Evidence Context；
  D3 判据 #1–#7 分别放在正文/hash、wrapper、组装与稳定、职责边界四类输出上。
- 可迁移点：对确定性管线，在每个中间输出的字节边界放一个可自动化断言，比在末端放一个总检查更容易定位哪层坏了。
- 边界：判据只覆盖「能写成字节相等或结构不变量」的属性；语义判断（如 atomic claim 是否成立）留在人工。

### 1.3 hash 是盲的，需要字节级 golden 补盲
- 事实：content_sha256 只覆盖 model_content，wrapper/块序/分隔符错误 hash 全绿；
  因此 fixture A/B/C 用期望字节补 hash 抓不到的组装层错误。
- 可迁移点：指纹只能证明「同一对象没变」，不能证明「对象之间的关系正确」；关键结构错误要用
  期望字节（golden）或结构性不变量兜底。
- 边界：golden 只在「期望本身正确」时有效；期望来自契约语义，不是实现自证。

### 1.4 确定性层与概率层分离
- 事实：corpus/parser/registry/eval 结构全部脚本化与双跑一致；模型只出现在 generation 层。
- 可迁移点：系统含模型或其它不可控层时，先把可控层做成确定性、可重跑、可冻结的，失败归因
  （retrieval miss / context assembly / prompt / generation）才可能成立。
- 边界：当前只验证了确定性层可重复；generation 质量与端到端结果尚未验证。

### 1.5 脚本验收 > 声称，但脚本必须能触发目标机制
- 事实：本周实现用 pytest + CLI 双跑 + 审计落盘代替口头验收；同时仓库历史有脚本假绿反例
  （W11 validate-logs 对注入的假私钥头不报警；dry-run 不触发 pre-receive 检查）。
- 可迁移点：验收输出应来自可复算命令且与断言语义分离；断言必须可证伪、要测的目标机制必须真的被触发。
- 边界：脚本本身可能写错断言；判什么、阈值多少仍由人冻结，不由实现方自证。

### 1.6 身份与指纹分离
- 事实：source_id（`corpus/source#Lstart-Lend`）是位置身份；content_sha256 是内容指纹；
  两者职责不同：位置变化不影响指纹，内容变化不改变身份。
- 可迁移点：实体索引、缓存 key、trace/verifier schema 设计时应显式区分「身份」与「内容完整性」两类校验。
- 边界：指纹用于完整性校验，不替代身份；引用回源以 source_id 为准。

### 1.7 回归基准与冻结纪律
- 事实：真实全语料整串 sha256 首次产出后待冻结为 regression 基准；holdout 首次运行只能在
  serialization/Prompt/BM25/dense/eval/实现与评分全部冻结后，且首跑结果不用于调参。
- 可迁移点：评估系统与 harness 的结果处理分「冻结前」与「冻结后」两种状态；冻结后只按预先登记的
  regression 节点复跑，防止结果反向调参造成虚高。
- 边界：首跑成功只证明当次链路可运行，不证明质量或掌握；dense 与 holdout 未完成时如实写为部分完成。

### 1.8 评估隔离
- 事实：dev/holdout 物理分离并共享 schema；reference answer / expected branch / evidence
  requirement 不进模型输入；Prompt 只承载规则，Query 只含 query。
- 可迁移点：任何带金标的实验先隔离评估输入；对 W14 Agent 同样适用于 verifier 语义与工具权限边界。
- 边界：物理隔离只是结构条件，不等于题目质量或判分正确。

## 2. 给展板的可能数据面（不预写形态）

- 语料：7 文件 / 76,243 bytes / 18,697 estimated tokens（raw）。
- serialization 产物：572 blocks（kind 计数见 `evidence/serialization/criteria-report-rules-c0a4b85.md`），
  Evidence Context 89,854 chars，整串 sha256 `8a02c665…`，双跑一致。
- 管线层次：query → retrieval（未实现）→ context assembly（已实现）→ generation（未实现）→ citation/abstention（未实现）。

> 以上数字均为已实测/已估算值；若后续计量改变，以最新证据为准，展板文案由数据计算不手写。

## 3. D5 展示设计契约（2026-09-11，先于各轮数据层与 JSX）

本次将原跨周的 AI 工程入口拆为两个能力入口：`Python / Bub 基础` 与 `RAG 实践`；保留 `AI 工程总览` 兼容旧深链。
W13 入口以整体路线图作为首个专题，随后按功能分类。新增实现理由专题承接代码解读，证据页增加本地确定性重算。
顶层入口保持单行横向滚动，避免增加入口后在窄屏硬换行；普通鼠标滚轮会转换为横向滚动，触控板和触摸保持原生横滑；内部周次只用于路径和学习记录，用户可见导航使用能力名称。
形态由能力关系、可点击回溯和对照数据独立推导；未改业务后端或 RAG 生成主链，没有模型调用。
用户已授权以展示学习成果与当前 RAG 功能为主创建必要展板。追加要求为整体路线图、脱离笔记可读的功能文案、
SVG/图标/流程承担概念关系；五页结构已先向主agent说明并交付本人过目；随后追加实现理由与本地重算入口。

| ① 单一问题 | ② 10 秒结论 | ③ 对象与数据形状 | ④ 结论编码 | ⑤ 视觉舞台 | ⑥ 文字层级 | ⑦ 视觉记忆点 | ⑧ 图标策略 | ⑨ 动效策略 | ⑩ 验收证据 |
|---|---|---|---|---|---|---|---|---|---|
| rag-roadmap：当前 RAG 能力做到哪里，后续能力如何承接？ | 可追溯输入已经建立，检索与回答已试验但质量未过，状态编排仍是后续能力 | 输入准备、三种检索、全量输入、回答与拒答、质量评估、未来状态编排；实施与质量状态分列 | SVG 能力拓扑按依赖连接，实线完成、双线试验未过质量、虚线计划中；文字状态作为第二编码 | 首屏完整能力图与状态图例；按能力依赖而非日期布置 | 常驻能力名称、状态、下一连接；细节证据下沉；内部编号不作首层术语 | 已完成输入通向两条试验路径，未来编排虚线围住可复用节点 | 文档、检索、回答、校验、状态图共用一套inline SVG图标，每图标同时带名称 | 静态；状态是事实分类而非播放过程 | 五个能力节点与分支/状态可读；桌面完整锚；手机压缩全站导航；各主题默认/展开、深浅主题共48状态，截图人工方式复核与本人复述分开 |
| rag-flow：当前固定 RAG 如何从冻结语料走到回答？ | 同一来源可走 full-context 或 retrieval，后半段共用组装、生成和引用校验 | 7份项目规则文档、572个可追溯原文片段、两条输入分支、汇合节点、回答/拒答两个输出 | 分支拓扑：语料分流至全量/检索，汇入 Evidence Context；query 单独进入检索和生成；registry 指向引用校验 | 首屏用文档与片段图标解释输入形状；完整分叉、汇合与输出，内部snapshot/hash下沉 | 常驻节点/箭头/边界；折叠确定性层与生成层说明；来源进详情 | 两支输入汇入同一条生成链 | 文档/检索/回答/校验使用共用SVG图标并配名称，箭头只表方向 | 静态，拓扑不虚构播放顺序 | 桌面1440×1000、手机390×844默认/展开；节点方向断言；遮标题可读出两个输入分支和共同后半链 |
| rag-implementation：现有代码为什么分成这些职责？ | 来源身份贯穿流程；检索、生成与评估分别承担职责 | 资料解析/来源身份、检索排名、上下文、生成记录、分项评估；每步输入输出和边界理由 | 三职责泳道配带方向连接；底部来源身份链贯穿Document/Hit/Context/citation | 首屏职责与理由成对；原文可回查、同分稳定、字节保持、输入一致、pending不通过各附在对应转换上 | 常驻输入输出和短理由；具体函数/限制进入折叠与代码解读文档 | 身份跨可替换组件后回到独立评估 | 共用文档/检索/回答/校验SVG对象图标 | 静态；只表达已存在职责，不把未来迁移画成运行事实 | guide与源码交叉核对；泳道与手机标明owner保持等价；六页默认/展开深浅主题共48状态；本人解释验收待演练 |
| rag-evidence：一次历史回答如何回到冻结来源？ | 回答的 citation 可展开定位到冻结原文；按预期拒答与 false abstention 用分支对齐区分 | 固定BM25 dev三例：answered、expected abstained、expected answered/actual abstained；claims、citations、source lines | SVG图标回溯轨道：回答 → citation 按钮 → 原文；预期/实际分支两列对齐 | 首屏先显示历史回放标签、案例选择与分支结果，默认一个claim及其引用；点击展开冻结文本 | 常驻问题、分支和一个claim；其余claim与原文展开；原始JSON路径下沉 | citation连接到具有行号的原文 | 回答/引用/原文SVG配名称，原生按钮和展开标记用于回溯操作 | 静态；交互只改变选择，遵守reduced motion | 三例切换、引用逐项回源、键盘点击与焦点、默认/展开截图；明确BM25语义pending，分支一致不升级为质量通过 |
| rag-eval：当前检索与输入规模证据支持哪些结论？ | 同一8题下可比检索覆盖；输入token下降不等于质量提升 | 3方法×3个top_k的9有效配置；8适用题；同10题FC/BM25输入token；质量判定层级 | SVG同基线条形比较检索通过数（桌面图固定280px并保持标签可读，手机自适应大字）；共用长度基线比较两条输入总量；分母标签常驻 | 首屏9条紧凑分组条形与8题分母、输入token两条对照 | 常驻指标定义/分母/未达门禁；折叠单次运行、人工pending和R1运行后诊断边界 | 九条检索结果止于8题刻度前；长短输入条不连接质量排名 | 不用图标装饰数值 | 静态，避免把离散top_k假装连续质量曲线 | 原始JSON计算、排除BM25缺陷版；数字与条长断言；双视口、深浅色、默认/展开截图 |
| rag-framework：本周经验如何映射到LangChain与LangGraph？ | 已用LangChain Document/BM25 与 dense 的 Embeddings/向量库；契约由本项目拥有；LangGraph是后续状态编排实践 | 现有组件、职责、框架接口、实施状态四类字段 | 对齐映照：自定义确定性输入 → 已用LangChain检索 → 自定义生成/评分；下方虚线映射待做LangGraph节点/条件边/状态 | 首屏完整实施边界，实线已做/虚线未来，避免正文列表冒充映射 | 常驻组件职责与状态；折叠LCEL未用、dense 已接 Embeddings/向量库但排序与缓存身份由本项目定义、模型client复用W12 | 实线当前链与虚线未来状态图分层 | 不引入新图标库 | 静态；未来路径不播放 | 已实施接口与代码核对；虚线/文字双编码；遮标题可区分已做和待做；双视口及reduced motion；2026-09-11 事实同步后复测 |

### 3.1 事实与数据边界

- 数据生成器只读 16 个精确 allowlist 输入：dev items、三个BM25 retrieval结果、三个dense结果、三个hybrid结果、
  full-context v1+JSON 与 BM25 e2e、serialization registry、人工判定 worksheet §11 及两份被引用的冻结公开原文。
  人工诊断字段保留 worksheet 来源，不伪装成原始 JSON 判定。禁止通配扫描 evidence。
- BM25检索5/6/7 of 8、dense3/4/5、hybrid4/5/7；3×3=9有效配置，缺陷版BM25 top10不纳入。
- full-context机械8/10，本人人工记录4/10属于运行后R1诊断；BM25 e2e机械8/10、人工语义pending。
  citation_precision按当前机械字段表示citation可解析比例，不能称语义支持率。
- BM25 context 1,332–2,020 chars；prompt_tokens 1,131–1,535。full-context同配置44,704–44,725。
  10题总输入447,159与12,723；图中比例由JSON算出，仅输入token下降，不作总费用或质量提升结论。
- LangChain实际使用Document、BM25Retriever.from_documents和get_scores后自排序；2026-09-11 起 dense 的 embedding
  与向量存储也接入 LangChain（E5Embeddings + InMemoryVectorStore，向量来自冻结 `.npy` 缓存，排序与存储键由本项目定义）。
  generation复用W12 client，未使用LCEL，LangGraph未实施。D5 离线测试 81 passed（原 71 + 新增 10）；
  接线等价性由 `verify-dense-langchain-equiv.py` 验证，不等于回答质量验证。
- 回放取2026-09-10既有dev输出，只读展示；本轮不发起真实模型调用，也不读取受保护eval或holdout证据。

### 3.2 首屏与独立阅读要求

- 整体路线图首项入口为 `rag-roadmap`；另五项继续按功能分类，不按每日笔记顺序。
- 文案首层直接解释项目规则问答、检索、引用与拒答；snapshot、hash、worksheet编号和内部stage标记下沉。
- RAG页将全站主题收进“其它学习专题”折叠；手机保持本页结论和RAG专题切换常驻，不让全站九项菜单占用首屏。
- SVG承担能力连接、方向、对照长度与职责归属；图标只识别稳定技术对象，不能代替关系本身。

## 4. 交付验证（2026-09-11）

- `yarn typecheck`、`yarn build:showcase`、`yarn verify:rag-data` 通过。生成数据核对 16 个精确来源、
  3 个 dev 回放案例、9 个检索配置；构建保留既有大 chunk 提示，不影响本地运行。
- `yarn rehearse:rag --check`：6 专题 × 2 视口 × 2 主题 × 默认/展开 = **48 状态通过**。
  页面横向溢出、低于 24px 的可见按钮/summary、reduced-motion 下动画、浏览器 page error 均为 0。
  每次导航后回到页面顶部，再量完整结论锚下界；不以保留的滚动位置或锚起点替代首屏检查。
- 键盘展开 citation、模型可见 source block、三例预期/实际分支、九组数值、本人诊断 4/10、
  路线图 → 实现理由 → 代码导读的主动回忆门 → 返回专题均通过真实浏览器操作。
- 证据页真实点击固定本地重算：检索顺序 / 证据上下文 hash / 上下文字符数各 **10/10**；未调用模型。
  5 种端点拒绝边界、合成运行失败显示、普通静态页面按钮禁用均通过。
- 最终 `yarn verify:board`：**1535 项通过，0 项失败**，其中 AI 工程专项 398 项通过。
  回归暴露了笔记跳转后旧页滚动回调覆盖新页章节的问题；已按产生回调的笔记 ID 校验后再更新位置。
  跨笔记检查等待目标文档的实际章节显示，再验证章节与专题返回参数；修复后全站回归和 RAG 48 状态均重新通过。

### 4.1 视口、层级与字数

下表高度是 `.ae-stage` 的默认/全部展开高度；屏数分别除以桌面 1000px、手机 844px。
结论锚下界是从窗口顶部量起的桌面坐标，**包含全站顶栏**；六页均不超过 1000px。
深浅主题的高度和下界相同。手机保留能力依赖、单次调用顺序和比较分母；SVG 与泳道使用纵向语义变体。

| 专题 | 桌面默认 / 展开 px（屏） | 手机默认 / 展开 px（屏） | 桌面完整锚下界 | 主路径中文解释字数 | SVG 标签字数（桌面 / 手机）；其它标签字数（桌面 / 手机） |
|---|---|---|---|---|---|
| 整体路线 | 806 / 1067（0.81 / 1.07） | 1144 / 1705（1.36 / 2.02） | 937px | 128 | 209 / 147；20 / 20 |
| 链路 | 760 / 978（0.76 / 0.98） | 919 / 1393（1.09 / 1.65） | 892px | 102 | 0 / 0；105 / 105 |
| 实现理由 | 888 / 1134（0.89 / 1.13） | 1135 / 1793（1.34 / 2.12） | 961px | 148 | 0 / 0；130 / 108 |
| 证据回放 | 998 / 2048（1.00 / 2.05） | 1161 / 3073（1.38 / 3.64） | 992px | 138 | 0 / 0；78 / 78 |
| 检索对照 | 917 / 1921（0.92 / 1.92） | 1453 / 3872（1.72 / 4.59） | 988px | 187 | 13 / 13；59 / 59 |
| 框架衔接 | 886 / 1151（0.89 / 1.15） | 1291 / 1757（1.53 / 2.08） | 995px | 170 | 0 / 0；75 / 75 |

字数来自实际可见的文本节点：解释性 `p/small` 单列，排除折叠详情、代码、导航、标签以及作为原始样例的
query/claim；SVG 文本与其它短标签分别统计，隐藏的另一视口 SVG 不重复计数。图形中的 HTML 节点名称按标签计；
SVG 标签为 0 不表示没有 SVG 图标或方向连接。全部默认解释正文低于 220 字。
证据页全部展开保留原始回答、原文行及模型可见块，检索页全部展开保留 16 份来源 hash；这些可追溯材料进入
第二层详情，未把完整证据铺在默认主路径。手机检索默认变长来自同基线九条检索对照与独立输入规模上下排列，
分母和评估含义先出现，保留可比字号。

### 4.2 视觉判断与截图

实现方 AI 和独立 reviewer 已查看本轮桌面、手机、深浅主题与证据展开截图，并按图中对象、关系、方向与边界
做人工方式复核。首屏缺失的检索末组、首条 citation、泳道方向箭头已修正后重拍，未复用旧图声称通过。
下表是 **AI 对图形是否承载结论的复核**，不是本人已经完成 10 秒复述或遮标题验收。

| 专题 | 图形应独立承载的 10 秒结论 | AI 视觉复核 | 本人 10 秒 / 遮标题验收 |
|---|---|---|---|
| 整体路线 | 资料支撑检索与回答，评估检查两者；状态编排仍计划中 | 通过：依赖连接、双线试验状态与虚线未来分明，节点可进入对应页 | 待演练 |
| 链路 | 全量与检索两支汇入共同的上下文、生成与回答链 | 通过：汇合明确指向 Context → Generation → 输出，手机顺序等价 | 待演练 |
| 实现理由 | 来源身份跨过检索、生成，最终交给独立评估 | 通过：泳道标职责，方向箭头和每步输入输出、理由对应 | 待演练 |
| 证据回放 | 点击回答引用可回到同一版本原文；重算输入与回放答案分开 | 通过：首条 claim 和引用可点击；预期与实际分支并排，原文在操作后展开 | 待演练 |
| 检索对照 | 同 8 题比较检索，同 10 题比较输入；不能推成质量排名 | 通过：九条同基线结果、输入两条、机械 8/10 与本人诊断 4/10 首屏齐备 | 待演练 |
| 框架衔接 | LangChain 已承担 Document/BM25 与 dense 向量检索，LangGraph 状态编排还未实施 | 通过：当前职责实线、未来映射虚线，组件所有权明确；dense 已并入 LangChain 一侧，排序与缓存身份仍标在本项目 | 待演练 |

截图与机器度量保存在本轮临时证据目录（不入库）；交付消息给出可访问位置。命名：
`<临时证据目录>/<topic>-<desktop|mobile>-<light|dark>-<default|expanded>.png`，共 48 张；
另有 `rag-evidence-desktop-live-verify.png` 与 `rag-evidence-mobile-live-verify.png`，
以及 `metrics.json`。`topic` 对应 §3 的六个路由名。
默认态、展开态与真实重算结果分别保留，不以脚本通过替代截图判断，也不以展示可运行替代本人学习掌握。

### 4.3 本地重算交互契约（追加需求）

证据页在本地演练上下文提供“重算检索与模型输入”：固定比较10条开发评测的检索顺序、证据上下文 hash 和上下文字符数。
页面展示三项各自一致数，并明确“本次检索与证据上下文和历史记录一致；不重新生成答案”。
这次核对 Evidence Context，不重新组装完整 messages 或核对 system Prompt。静态发布入口保留历史回放，提示本地演练可重新核对。
端点仅由本地演练脚本提供，不接受题目、命令、模型或文件参数，不调用模型；请求失败时提供终端备用入口提示。
交互验收包括loading/error/success可读、aria-live、真实点击重算、逐题结果折叠，以及原有引用回源和模型可见片段。
本地脚本与端点由主agent实现/验证，展板组件由视觉agent实现。生成和评估主链不因展示UI改变。
本地入口为前端目录的 `yarn rehearse:rag`，自动打开已构建资产及隔离演练浏览器，初始专题 `rag-roadmap`。
正常应用鉴权未改，其它浏览器仍走登录。代码、本地构建与演练入口已交付；本次没有提交、推送或部署。

### 4.4 事实同步：dense 接入 LangChain（2026-09-11 同日追加）

D5 当日完成 LangChain dense 接线后，框架页陈述的「dense 未接框架」已过期（`TECHNICAL-WRITING-PROTOCOL.md` §2.4）。
本次只改文字，不改图形、分页、主路径或数据来源：`ragTopics.ts` 的 anchor/boundary/source，`RagVisual.tsx` 的
所有权路径中段与折叠说明。未把 dense e2e 结果加入展板数据——那需要扩充 16 项 allowlist 与数据结构，属另一次变更。

| 证据 | 结果 |
|---|---|
| 构建 | `yarn typecheck`、`yarn verify:rag-data`（16 sources / 3 replays / 9 configs）、`yarn build:showcase` 通过 |
| 全站回归 | `yarn verify:board`：**1535 项通过，0 失败**，AI 工程专项 398 项 |
| 48 状态 | `yarn rehearse:rag --check`：checked 48、pageErrors 0、overflow 0、键盘与重算交互通过 |
| 复测度量 | 框架衔接：桌面 863 / 1129px、手机 1271 / 1737px、完整锚下界 995px、主路径 150 字、其它标签 68 字；溢出 0 |
| 度量变化 | 相对 §4.1 原值：桌面展开 1142→1129、手机 1188/1718→1271/1737、主路径 114→150、其它标签 65→68；默认态首屏仍在 1.5 屏内、锚下界未变 |
| 截图 | 48 张重拍于临时目录 `/tmp/rag-sync-20260911`（不入库，命名同 §4.2） |
| 边界 | 这是文案事实同步，不是视觉改版；本人 10 秒复述与遮标题验收仍为待演练，机器检查不替代人工视觉判断 |

### 4.5 事实同步：BM25 人工语义判定完成（2026-09-11 同日追加）

BM25 与 dense 端到端的人工语义判定完成后，展板上「BM25 人工判定待补」的表述已过期
（`TECHNICAL-WRITING-PROTOCOL.md` §2.4）。本次只改文字，不改图形、分页、主路径与数据来源，
也**没有**把 3/10 写进展板数据——那需要扩充 16 项 allowlist 与数据结构，属另一次变更。

| 位置 | 改法 |
|---|---|
| `ragTopics.ts`（`rag-evidence` 的 boundary） | 「BM25 人工语义判定待补」→「BM25 端到端为授权链路示例，人工语义判定已完成且未通过」 |
| `RagVisual.tsx`（输入规模折叠说明） | 「BM25 的人工语义判定仍待补。」→「BM25 的人工语义判定已完成且未通过（该轮为授权链路示例）。」 |
| `RagVisual.tsx`（分数范围行 `<small>`） | 「BM25 人工判定待补」→「BM25 人工判定已完成 · 未通过」 |

| 证据 | 结果 |
|---|---|
| 构建 | `yarn typecheck`（exit 0）、`yarn verify:rag-data`（16 sources / 3 dev replays / 9 retrieval configurations）、`yarn build:showcase`（exit 0，built in 2.86s） |
| 全站回归 | `yarn verify:board`：**1535 项通过、0 失败**，AI 工程专项 398 项（exit 0） |
| 48 状态 | `yarn rehearse:rag --check`：checked 48、pageErrors 0、overflow 0、smallControls 0、animations 0；键盘引用 / 分支回放 / 本地重算 10/10 / 5 种端点拒绝 / 运行失败显示 / 静态页禁用 / 导览往返全部 passed（exit 0） |
| 复测度量 | 框架衔接桌面默认：`stageHeight 863`、锚下界 `995`、主路径 `150` 字、其它标签 `68` 字 —— 与 §4.4 一致；本次改动不涉及该页布局 |
| 截图 | 48 张重拍于 `/tmp/rag-sync3-20260911`（不入库，命名同 §4.2） |
| 边界 | 仍是文案事实同步，不是视觉改版；展板不显示 3/10 这个数字；本人 10 秒复述与遮标题验收仍为待演练 |

### 4.6 数据扩充：三条端到端的机械与人工判定（2026-09-11 同日追加）

上一节只做到"不再声称人工判定待补"。本节把判定结果本身放进展板：**allowlist 由 16 项扩到 19 项**
（新增 `evidence/dense-langchain-e2e/dev-dense-langchain-e2e-top10.json` 与两份语义判定工作表），
`ragDemoData.json` 增加 `dense` 区块，`bm25`/`dense` 各带 `diagnostic`（来自工作表 §11 的本人结论）。

| 对象 | 展板现在显示 |
|---|---|
| 机械通过 | 全部资料 `8/10` · BM25 `8/10` · Dense `7/10` |
| 本人诊断 | 全部资料 `4/10`（R1 运行后诊断）· BM25 `3/10` · Dense `3/10`（D5 判定 · 授权链路示例，均不通过） |
| 折叠详情 | Dense 端到端的输入合计、context 区间、"其中 1 条响应结构失败"；并注明 BM25/Dense 两轮为授权链路示例、不作质量验收 |

**生成器约束**：`humanDiagnostic()` 校验收口表行数与 item 集合必须与 dev 题集一致，行数不足或 ID 不匹配即拒绝生成；
工作表内容变化会改 sha256，`verify:rag-data` 随之要求重新生成。

| 证据 | 结果 |
|---|---|
| 数据 | `yarn generate:rag`：19 exact inputs / 3 dev replays / 9 retrieval configurations；`yarn verify:rag-data` 通过 |
| 构建 | `yarn typecheck`（exit 0）、`yarn build:showcase`（exit 0，built in 3.04s） |
| 全站回归 | `yarn verify:board`：**1535 项通过、0 失败**，AI 工程专项 398 项（exit 0） |
| 48 状态 | `yarn rehearse:rag --check`：checked 48、pageErrors 0、overflow 0、smallControls 0、animations 0；交互项全部 passed（exit 0） |
| 度量变化（检索对照页） | 桌面默认 `926 → 917`、桌面展开 `1761 → 1900`；手机默认 `1419 → 1412`、手机展开 `3461 → 3805`；完整锚下界 `997 → 988`（≤1000）；主路径字数 `119 → 124` |
| 截图 | 48 张重拍于 `/tmp/rag-allowlist-20260911`（不入库，命名同 §4.2） |
| 边界 | 默认态屏数未变（桌面 0.92、手机 1.67，沿用 §4.1 已记录的手机变长理由）；「本人 10 秒复述与遮标题验收」仍为待演练，机器检查不替代人工视觉判断 |
### 4.7 路线图首屏判据距离与门禁短句（2026-09-11 同日追加）

本次回应一个呈现问题：路线图首屏原来只有「质量未通过」一类的状态词，四个节点又分属三个不同判据对象，
读者容易把它读成同一个能力的三次失败。本次只补呈现密度与解释结构，**不改状态词强度、不改判据阈值、
不改契约条款、不改图形与分页**：未通过仍是未通过。

| 位置 | 改法 |
|---|---|
| `RagVisual.tsx` 能力节点 | 新增可选 `statusDetail` 行：`可追溯资料`「确定性验收 · 全部通过」、`选择证据`「最高 7/8 · 门禁 8/8」、`有依据的回答`「机械 8/10 · 本人 3–4/10」、`评估结果`「三层检查已运行」；桌面四节点状态行与判据行同高对齐 |
| `RagVisual.tsx` 图例下方 | 常驻短句：质量门禁＝端到端五条条件同时成立、10 题最多错 1 题；检索要求适用题全部命中 |
| `RagVisual.tsx`「能力范围与状态说明」折叠 | 补失败分层（六条失败中五条含引用位置、块粒度或要求覆盖问题，其中一条还含 claim 内容错误；另有一条为错误拒答）与本页不衡量的范围（掌握程度、生产可用性、语料规模代表性） |
| `rag.css` | `.rag-capability .rag-capability-status-detail` 11px/600；`.rag-visual .rag-gate-note` 12px 次要色 |

节点上的数字全部由 `ragDemoData.json` 计算：检索取九配置中的最高通过数，人工判定取三条端到端的最小/最大值，
未手写进文案。

| 证据 | 结果 |
|---|---|
| 数据 | `yarn verify:rag-data`：19 exact inputs / 3 dev replays / 9 retrieval configurations（数据层未改） |
| 构建 | `yarn typecheck`（exit 0）、`yarn build:showcase`（exit 0，built in 3.50s；含随后笔记同步的最终重建） |
| 48 状态 | `yarn rehearse:rag --check`：checked 48、pageErrors 0、overflow 0、smallControls 0、animations 0；键盘引用 / 分支回放 / 本地重算 10/10 / 5 种端点拒绝 / 运行失败显示 / 静态页禁用 / 导览往返全部 passed（exit 0）；最终重建后重跑一次仍为 exit 0，两次度量一致 |
| 全站回归 | `yarn verify:board`：**1535 项通过、0 失败**（exit 0，最终重建后重跑） |
| 度量变化（整体路线） | 桌面 780/991 → **806/1067px**；手机 1064/1505 → **1108/1686px**；完整锚下界 911 → **937px**（≤1000）；主路径字数 62 → **93**（≤220）；SVG 标签 175/124 → **199/139**；桌面与手机溢出均为 0 |
| 文字越界自测 | 一次性脚本按 SVG 用户单位比较每个节点内文本的 `getBBox()` 与所属节点矩形：桌面与手机各 5 个节点均无右界或下界越界（最紧一处为手机`选择证据`，文本右界 139 < 节点右界 172） |
| 截图 | 51 个文件重拍于 `/tmp/rag-doc-distance-20260911`（48 张状态图 + 2 张实时重算图 + `metrics.json`，不入库，命名同 §4.2）；最终重建后整批重拍，不复用旧图 |
| 边界 | 未新增或删除任何 allowlist 来源；未改状态词强度与判据；「本人 10 秒复述与遮标题验收」仍为待演练，机器检查不替代人工视觉判断 |

同轮的文案事实同步（不属展板）：`day5-demo-script.md` 的 D4 失败计数由 4 条改为与 D4 §6.19 一致的 5 条、
§13:00 过期的「BM25 人工语义评估与 dense 接线仍待补」改为已完成的实际结果、§0:00 起段补判据与判定权；
`day5-demo-qa.md` 表头与 `scripts/demo-replay.py` 摘要中「BM25 人工语义待判」的过期表述一并订正。
### 4.8 展板文案中性化与可读化（2026-09-11 同日追加）

本次回应两类读不懂的文案：**学习过程代号与内部缩写**（`D4` / `D5` / `R1` / `W12` / `W13`、`registry`、
`requirement span`、`identity 门控`、`-01` / `-02` 缺陷版本号）与**过度精简的数字短语**
（「最高 7/8 · 门禁 8/8」「机械 8/10 · 本人 3–4/10」）。改动只涉及呈现与用词：
**未改判据、阈值、状态词强度、数据层数值或图形结构**。

| 位置 | 改法 |
|---|---|
| 路线图节点判据行 | 「最高 7/8 · 门禁 8/8」→「命中 7/8 题 · 要求全中」；「机械 8/10 · 本人 3–4/10」→「机械检查 8/10 题 · 人工判定 3–4/10」；「确定性验收 · 全部通过」→「输入可重算 · 10/10 一致」；「三层检查已运行」→「检索 / 机械 / 原文检查已运行」 |
| 路线图图例下常驻句 | 「质量门禁：五条条件同时成立」→ 逐条写出五条（运行有效、不做错误拒答、引用全部可解析、10 题至少过 9 题、五类题型各至少过 1 题），并保留「检索要在 8 道适用题上全部命中」 |
| 检索对照页分数区 | 「机械通过」→「机械检查通过」；「本人诊断」→「本人逐题判定」；单位补「题」；说明句去掉 `D5 判定` / `授权链路示例` / `运行后澄清`，改为「经本人批准，只验证链路能跑通」 |
| 检索对照页折叠 | `Retrieval-only` / `requirement spans` / `-01`·`-02` / `D4 §6.14` / `R1` / `D5` / `citation_precision` 换成中文表述与日期；「机械 7/10」→「程序检查通过 7/10 题」 |
| 链路页 | `Source block` / `Query` / `Schema` / `Registry` / `claim` / `citation` 首次出现处补中文；`Parser`、`context assembly` 改中文 |
| 实现理由页折叠 | `W12 client` / `W12 DeepSeekClient` → 「上一阶段的模型客户端」；`registry_index` → 「片段在语料中的原始序号」；`evaluator` / `verdict` / `pending` → 中文 |
| 框架页 | `W13 的输入输出契约` → 「本阶段的输入输出契约」；「按分数 / registry_index 自排序」→「按分数与片段原始序号自排序」；`identity 门控` → 「按模型与参数组合校验缓存身份」 |
| 证据页 | 「历史 dev」→「历史开发评测」；`snapshot：` → 「语料版本：」；「来自 eval 定义」→「评测时预先定义的」；「该轮为授权链路示例」→「该轮经本人批准，只验证链路能跑通」 |
| `ragTopics.ts` | 来源引用去掉小节号、补「2026-09-10 运行记录」；`requirement span 交集` → 中文；`registry 校验 citation` → 「引用注册表校验引用能否解析」；`固定 dev 链路` → 「固定开发链路」 |
| `scripts/generate-rag-demo.mjs` | 判定状态文案 `R1 运行后诊断` / `D5 判定 · 授权链路示例` → `判定标准在运行之后才明确 · 诊断结论` / `2026-09-11 人工判定 · 链路验证运行` |

保留不改的标识：证据文件名（`dev-*.json`）、API 与字段名（`prompt_tokens`、`citation_precision`）、
代码标识符所在段落，以及冻结模型响应原文中的字样——它们是可追溯锚点，改写会使证据无法定位。

| 证据 | 结果 |
|---|---|
| 数据 | `yarn generate:rag` 重新生成（19 exact inputs / 3 dev replays / 9 retrieval configurations）；`yarn verify:rag-data` 通过 |
| 构建 | `yarn typecheck`（exit 0）、`yarn build:showcase`（exit 0）；本轮共三次重建，最后一次（含文档更新）2.99s |
| 48 状态 | `yarn rehearse:rag --check`：checked 48、pageErrors 0、overflow 0、smallControls 0、animations 0；键盘引用 / 分支回放 / 本地重算 10/10 / 5 种端点拒绝 / 运行失败显示 / 静态页禁用 / 导览往返全部 passed（exit 0）；多次重跑度量一致 |
| 全站回归 | `yarn verify:board`：**1535 项通过、0 失败**（exit 0） |
| SVG 文字越界自测 | 一次性脚本按 SVG 用户单位比较节点内文本 `getBBox()` 与节点矩形：桌面与手机各 5 个节点均未越界。首轮手机「组织回答」判据行超界 2px，缩短为「机械 8/10 题 · 人工 3–4/10」后文本右界 338 < 节点内边界 342 |
| 度量变化 | §4.1 表已按最终值更新；六页最大主路径字数 **187**（≤220），最大桌面完整锚下界 **995**（≤1000），桌面与手机溢出均为 0 |
| 截图 | 51 个文件重拍于 `/tmp/rag-copy-final`（48 状态图 + 2 张实时重算图 + `metrics.json`，不入库） |
| 边界 | 未改判据、阈值、状态词强度与数据层数值；未新增或删除 allowlist 来源；「本人 10 秒复述与遮标题验收」仍为待演练 |

同轮文档改动：重写 [`../src/w13rag/README.md`](../src/w13rag/README.md) 为全包导读（11 个功能模块、依赖方向、
两条数据流、检索/生成/评估细节与边界），新增 [`../scripts/README.md`](../scripts/README.md)（15 个入口的用途、
输入输出与安全边界），并在 `notes/rag-implementation-guide.md` 同步入口与 dense 端到端事实。
