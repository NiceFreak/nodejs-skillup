# W13 visualization plan — 内容素材草稿

> 建立：2026-09-09（Asia/Shanghai）。状态：**设计方案草案，待本人 review；尚未进入实现**。
> §0-§2 是当周事实、证据与可迁移点的素材记录；§3-§14 是按 `SHOWCASE-VISUAL-PROTOCOL.md` §2 产出的
> 十列设计契约与形态推导，由 `TECHNICAL-WRITING-PROTOCOL.md` 约束句子表达。
> 设计契约由实现方先写、经本人过目后才能写数据层或 JSX（视觉规范 §2）；§14 列出必须由本人裁定的项。
> 当前主线未完成（serialized 输入计量 / baseline / BM25 / dense / holdout 未跑），展板整理不占主线。

## 0. 素材边界（事实快照）

- 对象：RAG（Retrieval-Augmented Generation）固定链路的前端输入工程。
- 本周已完成：corpus snapshot `rules-c0a4b85`（7 文件 76,243 bytes；raw corpus-only 18,697
  estimated tokens）；eval `w13-eval-v1`（dev/holdout 各 10 题，未运行模型）；Prompt `rag-prompt-v0`
  与 response schema；serialization 单一规范与 fixture；确定性 parser / citation registry /
  Evidence Context 实现（572 blocks，真实整串 sha256 `8a02c665…`，双跑一致，覆盖审计为 0）。
- 未完成边界：serialized 输入 token 计量、context budget、全语料上下文 baseline、BM25/dense、
  首次 holdout 均未执行。
- 本行原写「判据 #1–#7 逐字确认未完成」，与同日 `LEARNING-STATE.md` 冲突；实际状态为判据 #1–#7 已由本人
  逐条确认、整串基准已冻结（`evidence/serialization/frozen-rules-c0a4b85.sha256`），仍未完成的是 review
  工作表语义点 A1–A8 的批注回填，因此 serialization 实现里程碑（L1）未正式闭合。

## 1. 可迁移思维模式（每条：事实 → 可迁移点 → 边界）

### 1.1 契约先于实现
- 事实：D1 先冻结 corpus/manifest；D2 先冻结 eval 题意与判分；D3 先冻结 serialization 规范与
  fixture，之后才允许实现 parser。每个阶段门禁不过不叠加下一阶段。
- 可迁移点：任何含不可逆成本或影响后续对照的输入（语料、题库、配置、接口契约）先冻结版本与 hash，
  再进入实现；冻结后变更走新版本而非静默修改。
- 边界：冻结不等于正确；语义边界（如 source block 分得是否合理）仍由后续 eval 暴露。

### 1.2 按输出边界分层设判据
- 事实：serialization 是纯函数链 spans → model_content → source wrapper → Evidence Context；
  D3 判据 #1–#7 分别放在正文/hash、wrapper、组装与稳定、职责边界四类输出上。
- 可迁移点：对确定性管线，在每个中间输出的字节边界放一个可自动化断言，比在末端放一个总检查更容易定位哪层坏了。
- 边界：判据只覆盖「能写成字节相等或结构不变量」的属性；语义判断（如 atomic claim 是否成立）留在人工。

### 1.3 hash 是盲的，需要字节级 golden 补盲
- 事实：content_sha256 只覆盖 model_content，wrapper/块序/分隔符错误 hash 全绿；
  因此 fixture A/B/C 用期望字节补 hash 抓不到的组装层错误。
- 可迁移点：指纹只能证明「同一对象没变」，不能证明「对象之间的关系正确」；关键结构错误要用
  期望字节（golden）或结构性不变量兜底。
- 边界：golden 只在「期望本身正确」时有效；期望来自契约语义，不是实现自证。

### 1.4 确定性层与概率层分离
- 事实：corpus/parser/registry/eval 结构全部脚本化与双跑一致；模型只出现在 generation 层。
- 可迁移点：系统含模型或其它不可控层时，先把可控层做成确定性、可重跑、可冻结的，失败归因
  （retrieval miss / context assembly / prompt / generation）才可能成立。
- 边界：当前只验证了确定性层可重复；generation 质量与端到端结果尚未验证。

### 1.5 脚本验收 > 声称，但脚本必须能触发目标机制
- 事实：本周实现用 pytest + CLI 双跑 + 审计落盘代替口头验收；同时仓库历史有脚本假绿反例
  （W11 validate-logs 对注入的假私钥头不报警；dry-run 不触发 pre-receive 检查）。
- 可迁移点：验收输出应来自可复算命令且与断言语义分离；断言必须可证伪、要测的目标机制必须真的被触发。
- 边界：脚本本身可能写错断言；判什么、阈值多少仍由人冻结，不由实现方自证。

### 1.6 身份与指纹分离
- 事实：source_id（`corpus/source#Lstart-Lend`）是位置身份；content_sha256 是内容指纹；
  两者职责不同：位置变化不影响指纹，内容变化不改变身份。
- 可迁移点：实体索引、缓存 key、trace/verifier schema 设计时应显式区分「身份」与「内容完整性」两类校验。
- 边界：指纹用于完整性校验，不替代身份；引用回源以 source_id 为准。

### 1.7 回归基准与冻结纪律
- 事实：真实全语料整串 sha256 首次产出后待冻结为 regression 基准；holdout 首次运行只能在
  serialization/Prompt/BM25/dense/eval/实现与评分全部冻结后，且首跑结果不用于调参。
- 可迁移点：评估系统与 harness 的结果处理分「冻结前」与「冻结后」两种状态；冻结后只按预先登记的
  regression 节点复跑，防止结果反向调参造成虚高。
- 边界：首跑成功只证明当次链路可运行，不证明质量或掌握；dense 与 holdout 未完成时如实写为部分完成。

### 1.8 评估隔离
- 事实：dev/holdout 物理分离并共享 schema；reference answer / expected branch / evidence
  requirement 不进模型输入；Prompt 只承载规则，Query 只含 query。
- 可迁移点：任何带金标的实验先隔离评估输入；对 W14 Agent 同样适用于 verifier 语义与工具权限边界。
- 边界：物理隔离只是结构条件，不等于题目质量或判分正确。

## 2. 给展板的可能数据面（不预写形态）

- 语料：7 文件 / 76,243 bytes / 18,697 estimated tokens（raw）。
- serialization 产物：572 blocks（kind 计数见 `evidence/serialization/criteria-report-rules-c0a4b85.md`），
  Evidence Context 89,854 chars，整串 sha256 `8a02c665…`，双跑一致。
- 管线层次：query → retrieval（未实现）→ context assembly（已实现）→ generation（未实现）→ citation/abstention（未实现）。

> 以上数字均为已实测/已估算值；若后续计量改变，以最新证据为准，展板文案由数据计算不手写。

---

## 3. 展板范围、读者与开工边界

### 3.1 完成对象

一块覆盖 W13 RAG 输入工程的展板内容：把 corpus 冻结、source block 切分、Evidence Context 组装、eval 契约
和验证判据这五个模块，做成扫描 10 秒能读出结论、图形承载结论、正文退到第二层的视觉页面；同时如实呈现
retrieval、generation 与首次 holdout 在 2026-09-09 尚未运行。

### 3.2 读者与使用场景

- 主要读者是本人，用于延迟复习与 W14 harness 前的机制回忆。
- 次要场景是对外演示 AI Engineer 阶段的工程方法；演示状态不改变事实强度，只改变可见 tab 集合。

### 3.3 开工门槛

- 本方案只是设计契约，不构成开工许可。实现排在 W13 主线（A1–A8 回填 → 输入计量 → baseline → BM25）
  达到当日止步条件之后，按 `LEARNING-STATE.md` 的「周末条件项」执行。
- 实现前必须经本人 review 本方案（视觉规范 §2：编码表由实现方先写，本人过目后才能实现）。
- 实现期若改变核心图形、分页或主路径，先回写本方案，不用执行记录事后替代设计契约。

### 3.4 与既有展板的关系

- W12 方案已定：W13-W16 产出在 `ai-engineer` tab 内加组，不新增顶层 tab
  （`week8-fullstack/notes/w12-ai-visualization-plan.md` §2 范围门禁）。本方案沿用该门禁。
- 因此本方案所说的 tab = `ai-engineer` 板内的**功能模块导航**（`AeGroup` 级），不是顶层 `ShowcaseTab`。
- 形态一律按 §5 从内容关系推导。既有 `framePlayer` / `charts` / 泳道 CSS 只在 §11 作为实现复用候选出现，
  不作为形态依据（视觉规范 §1 护栏、§3 形态推导纪律）。

### 3.5 硬边界（违反即阻断验收）

| # | 边界 | 原因 |
|---|---|---|
| H1 | 展板任何状态都不显示 eval 受保护 split 的题面、query、预期结论或证据要求 | `AGENTS.md` §1.3 受保护内容；该 split 只能出现计数、结构与冻结状态 |
| H2 | 未运行的 metric 不渲染为 `0`，渲染为「未测量」状态格 | `0` 会被读成实测低分，属事实强度漂移 |
| H3 | bytes、chars、token 三个单位在同一图内必须各自标注，不互相换算 | D1 术语表：byte / 字符 / token 不能直接换算；token 计量尚未对 serialized 输入执行 |
| H4 | 每个数字标注事实等级（已实测 / 已估算 / 待运行验证），估算值标 `estimate` | `TECHNICAL-WRITING-PROTOCOL.md` §2 |
| H5 | 不呈现任何模型回答样例为「运行结果」；契约演示用的合成响应必须标注排除在正式题集之外 | 9/9 尚未调用模型 |
| H6 | 计数、比例、状态由数据层计算，不手写进文案 | 视觉规范 §2.1 |

## 4. tab 划分（按功能与模块，不按天）

按天划分会把同一个机制拆到相邻两天（例：source block 语义在 D2 冻结、parser 在 D3-D4 实现、判据在 D4 执行），
读者要跨 tab 才能读完一条机制。按功能划分让每个 tab 回答一个可独立成立的问题。

| tab | 名称 | 这一 tab 只回答什么 | 覆盖的模块与产物 | 对应原始阶段（不作导航依据） |
|---|---|---|---|---|
| T0 | 链路总览 | 五个阶段中哪些已确定性可重跑，哪些没运行过 | 管线分层与实现状态 | D1-D4 全部 |
| T1 | 输入冻结 | 靠什么证明「现在用的和当初冻的是同一份语料」 | `corpus/rules-c0a4b85`、manifest、normalization、raw token estimate | D1 |
| T2 | 切分与引用 | Markdown 怎样被确定性切成可引用 block，ID 指向什么 | source block 规则、`parser.py`、citation registry、source identifier | D2 + D4 |
| T3 | 组装与容量 | 正文进入模型前被加了多少东西，膨胀来自哪一层 | `serialize.py`、Evidence Context、context budget 三项 | D3 + D4 |
| T4 | 评测与判分 | 一题什么条件算通过，什么条件直接否决整个 split | `w13-eval-v1`、判分契约、metrics 与阈值、split 隔离 | D2 |
| T5 | 门禁与证据 | 现在已经被证明的是什么，没被证明的是什么 | 判据 #1–#7、9 条测试、覆盖审计、冻结基准、未完成项 | D3-D4 |

每个 tab 底部各挂一道复习题（只考一个设计点，答案默认折叠），题面见 §6 各块第 ⑥ 列。

## 5. 形态推导表

推导顺序固定为：内容核心关系 → 认知任务 → 形态；本表只用内容本身作输入。动效分类按视觉规范 §3.3：
① 语义过程动效（内容随时间变化，必须可暂停/单步/重放）；② 方位过渡；③ 纯装饰（禁止）。

| 块 | 内容核心关系（一句话） | 认知任务 | 推导出的形态 | 动效分类与理由 |
|---|---|---|---|---|
| T0 | 五个阶段构成固定顺序的管线；每段分属确定性层或概率层；实现状态分四档，断点位置本身是结论 | 顺序、归属、停止位置 | 双带管线拓扑：上带确定性层、下带概率层，阶段节点按顺序连接，未实现处物理断开 | ②方位过渡。不做过程动画：retrieval 与 generation 未实现，播放一次「数据流过全链路」会呈现未发生的事实 |
| T1 | 一次完整性校验按固定顺序发生：读取 → normalization → 计算 sha256 → 与 manifest 逐文件比对 | 顺序、逐项对照、数量差异 | 文件条（共用基线长度编码）+ 右挂逐文件校验格 + 四步校验序列 | ①语义过程动效：校验是真实按序执行的过程，逐帧一步，末帧停在 7/7 通过 |
| T2 | parser 逐行扫描源文档，标题栈随行推进 push/pop，块边界在特定行落下，thematic break 是不可跨越的硬边界 | 顺序、层级包含、边界 | 行序舞台：左侧源行、右侧标题栈阶梯、行间落下的块边界刻度；下方块类型与语境深度分布 | ①语义过程动效：确定性扫描本身随行推进，每帧只推进一行并只引入一个状态变化 |
| T3 | 同一段正文经两层加工被三次加宽：复制必要语境 → 包 wrapper → 与相邻块间加空行 | 形状变化、数量差异、包含 | 变形阶梯：同一段正文的四个形态并排，配共用基线的增量瀑布图 | ①语义过程动效：组装是确定性变换链，逐帧一次变换，末帧字节与 registry 实测一致 |
| T4 | 判分是一条顺序检查链，任一条件失败即该 item 失败；其中一个条件失败会直接否决整个 split | 分叉、停止位置、二维分类 | 判分路径图（answered/abstained 两条链 + 否决出口标在链上位置）+ 5×2 覆盖矩阵 | ①语义过程动效：单题判分按序推进，逐帧一个条件；样例为合成响应并标注非运行结果 |
| T5 | 七条判据分别落在四类输出层上；hash 只覆盖 model_content，wrapper 与块序是它的盲区，由 golden 字节补上 | 二维分类、覆盖空格 | 覆盖矩阵：判据 × 输出层，空格即盲区；旁挂门禁阶梯标出当前停在哪一级 | ②方位过渡。覆盖关系是静态事实；破坏性验证（改 parser → verify FAIL）在 9/9 未执行，只作折叠条目并标「未执行」，不演成过程 |

颜色一律作第二编码：状态另用填充图案与文字标签，线型区分实线（已可重跑）与虚线（未运行），
分布图另带数值标签。灰度打印或未读图例时仍可读出全部差异。

## 6. 逐块十列设计契约

列名按视觉规范 §2：① 单一问题 ② 10 秒结论 ③ 对象与数据形状 ④ 结论编码 ⑤ 视觉舞台 ⑥ 文字层级
⑦ 视觉记忆点 ⑧ 图标策略 ⑨ 动效策略 ⑩ 验收证据。

### 6.1 T0 · 链路总览：确定性层与概率层的实现状态

| 列 | 内容 |
|---|---|
| ① 单一问题 | W13 的 RAG 链路里，哪些阶段已经是确定性可重跑的，哪些还没有运行过？ |
| ② 10 秒结论 | 五个阶段中，corpus 冻结与 context assembly 已实现并双跑逐字节一致；retrieval、generation、citation 判分三段截至 2026-09-09 未运行，因此不存在端到端质量结论 |
| ③ 对象与数据形状 | 5 个阶段节点，每个带：所属层（确定性 / 概率）、实现状态（已冻结 / 已实现并自测 / 未实现 / 未运行）、产物路径、失败归因能否成立。状态四档由数据层枚举，计数由数据计算 |
| ④ 结论编码 | 位置（上带 = 确定性层，下带 = 概率层）+ 连接（阶段顺序）+ 线型（实线 = 已可重跑，虚线 = 未运行）+ 断点位置（未实现处物理断开）。颜色只作第二编码 |
| ⑤ 视觉舞台 | 首屏：一条横贯的双带管线占据舞台，断点在 retrieval 与 generation 两处；管线正上方一句主结论；右侧四档状态计数（由数据算出）。板头与导航之外不放连续正文 |
| ⑥ 文字层级 | 常驻：阶段名、状态标签、断点原因一句；折叠：每阶段的产物路径与复算命令；notes：D1/D3 原文。复习题「retrieval 未实现时，为什么不能把一次回答错误归因为 retrieval miss？」 |
| ⑦ 视觉记忆点 | 实线在确定性带上贯通、进入概率带即断开——断口位置就是当前能力边界 |
| ⑧ 图标策略 | 状态用三个语义符号（锁 = 已冻结、对勾 = 已实现并自测、空心圆 = 未运行）+ 文字名称；无装饰图标 |
| ⑨ 动效策略 | ②方位过渡：点击阶段跳到对应功能 tab 并高亮该模块，过渡短、不循环。不做过程动画，理由见 §5 |
| ⑩ 验收证据 | 验收句：哪两段没有运行过？1440×1000 与 390×844 各一张截图；结构断言：未运行阶段的 DOM 不含任何数值型分数节点；`data-anchor` 标在断点处 |

### 6.2 T1 · 输入冻结：语料快照与逐文件完整性

| 列 | 内容 |
|---|---|
| ① 单一问题 | 冻结一份语料之后，靠什么证明「现在读到的和当初冻结的是同一份」？ |
| ② 10 秒结论 | manifest 为 7 份文件逐个记录 bytes、sha256 与 git blob，校验是逐文件比对而不是整体判断；raw corpus-only 的 18,697 tokens 是离线 tokenizer 估算值，不是 provider usage |
| ③ 对象与数据形状 | 7 文件 × (bytes、chars、行数、sha256 前 8 位、通过状态)；一次校验 4 步；1 个 token 估算值 + 分类 `estimate` + tokenizer 版本 |
| ④ 结论编码 | 长度（共用基线的文件体量条，按 bytes）+ 顺序（校验四步）+ 逐格状态（7 个校验格，形状 + 文字双编码） |
| ⑤ 视觉舞台 | 首屏：7 行文件条左对齐共用基线，每行右端挂一枚校验格；上方一句主结论；四步校验条位于文件区正上方并与当前帧联动 |
| ⑥ 文字层级 | 常驻：结论句、单位标注（bytes 与 chars 并列，token 单列并标 estimate）；折叠：normalization `repository-content-v1` 的含义与「不移动既有正文行号」边界、被拒绝的 tokenizer 版本（5.16.1/0.23.2 因中文与空格回环失败）；notes：D1 原文。复习题「manifest 记录的 sha256 变了，能不能推断出正文被改？」 |
| ⑦ 视觉记忆点 | 7 行文件条右端各挂一枚指纹格——完整性是逐文件的，不是一个总数 |
| ⑧ 图标策略 | 校验格用通过 / 未比对两种形状；文件类型不加图标 |
| ⑨ 动效策略 | ①语义过程动效：四帧 = 读取 → normalization → 计算 sha256 → 与 manifest 比对；每帧只引入一步，末帧停在 7/7 通过。可暂停、单步、重放；`prefers-reduced-motion` 下停在末帧并保留全部信息 |
| ⑩ 验收证据 | 验收句：完整性校验的比对粒度是什么？断言：7 个文件条的长度顺序与 manifest bytes 排序一致；bytes/chars/token 三个单位各自带标注；截图两视口 |

### 6.3 T2 · 切分与引用：parser 扫描与身份 / 指纹分离

| 列 | 内容 |
|---|---|
| ① 单一问题 | 一份 Markdown 怎样被确定性地切成可引用的 source block，引用 ID 指向的又是什么？ |
| ② 10 秒结论 | 572 个 block 由不调用模型的 parser 按已冻结规则重算；`source_id` 只标核心 `source_span` 的行范围，必要标题与表头进 `context_spans` 而不扩大 ID 行范围；`content_sha256` 只验证完整性，不作身份 |
| ③ 对象与数据形状 | 一段真实源文档的连续行（含标题、表格、`---`）；标题栈层级；6 类 block 计数（list_item 349 / paragraph 144 / table_row 43 / code 23 / quote_para 10 / quote_list 3）；语境深度 4 档（1 层 15 / 2 层 351 / 3 层 193 / 4 层 13）；7 份文档各自 block 数 |
| ④ 结论编码 | 位置（行序）+ 包含（标题栈阶梯的嵌套层级）+ 边界（块边界刻度落在行间）+ 长度（类型分布与深度分布共用基线）。core 行与 context 行用线型与行首标记区分，不只靠颜色 |
| ⑤ 视觉舞台 | 首屏：左栏真实源行（带行号）、右栏标题栈阶梯、两栏之间落下块边界刻度；上方主结论一句；分布图在第二屏区域，首屏不占位 |
| ⑥ 文字层级 | 常驻：结论句 + 「ID 只标核心行范围」一句边界；折叠：六条切分规则原文（列表按顶层项、表格按数据行附表头、fence 只向前合并、blockquote 递归、`---` 硬边界）、review 工作表 A1–A8 的开放问题；notes：D2 原文。复习题「附加标题进入 model_content，为什么不进入 source_id 的行范围？」 |
| ⑦ 视觉记忆点 | 标题栈的阶梯随行升降，块边界刻度在 `---` 处永不跨越 |
| ⑧ 图标策略 | 块类型用固定形状标记 + 名称（首次出现同时给名称）；标题栈层级不用图标，用缩进与层号 |
| ⑨ 动效策略 | ①语义过程动效：每帧推进一行，只引入一个状态变化（标题入栈 / 出栈、块边界落下、遇 `---` 停止合并）。帧数据来自真实文档片段与 registry 实测结果。可暂停、单步、重放；reduced-motion 下渲染全部边界的静态终态 |
| ⑩ 验收证据 | 验收句：ID 与 hash 各自负责什么？断言：块类型计数之和 === 572 且由数据计算；标题栈层数与 registry 中该 entry 的 `context_spans` 数一致；截图两视口 |

### 6.4 T3 · 组装与容量：Evidence Context 的三层膨胀

| 列 | 内容 |
|---|---|
| ① 单一问题 | 同一段正文进入模型前被加了多少内容，这些增量分别来自哪一层？ |
| ② 10 秒结论 | 语料原文 34,354 chars 经复制必要语境成为 52,997 chars 的 `model_content` 合计，再经每块一对 `<source>` 标签与块间空行成为 89,854 chars 的 Evidence Context；两层增量分别来自「语境复制」与「wrapper 与分隔」，对应的 token 计量与 context budget 截至 2026-09-09 未执行 |
| ③ 对象与数据形状 | 4 级阶梯量（chars）：原文 → 核心 span 正文 → `model_content` 合计 → Evidence Context；每级增量的来源标签；一个真实 entry 的 4 个形态字节；容量三项（reserved output 4096 tokens、safety margin 100,000 tokens、context budget 未冻结） |
| ④ 结论编码 | 共用基线的长度（增量瀑布）+ 分段填充（增量来源分层）+ 形状变形（同一段正文四次加宽）+ 包含（`model_content` ⊂ serialized block ⊂ Evidence Context） |
| ⑤ 视觉舞台 | 首屏：同一段真实正文的四个形态左对齐并排，宽度即字符数；右侧瀑布图给四级量与两段增量；主结论一句在正上方；容量三项在瀑布图下方一行，budget 一格显示「未冻结」 |
| ⑥ 文字层级 | 常驻：结论句 + 单位标注（chars，不是 token）+ 「token 计量未执行」一句；折叠：wrapper 精确字节规则、块间恰一个空行与首尾无空行、hash 只覆盖 `model_content`；notes：D3 §6.2.0 原文。复习题「块间空行属于哪一层，为什么它不进入 `content_sha256`？」 |
| ⑦ 视觉记忆点 | 同一段正文被三次加宽——每一次加宽都能指到一条冻结规则 |
| ⑧ 图标策略 | 无新图标；wrapper 用真实标签文本 `<source id="…">` 显示，不用符号替代 |
| ⑨ 动效策略 | ①语义过程动效：四帧 = 核心 span → 前置必要语境 → 包 wrapper → 与相邻块之间加空行；每帧只做一次变换，末帧字节与 registry 中该 entry 实测一致。可暂停、单步、重放；reduced-motion 下直接呈现四形态并排 |
| ⑩ 验收证据 | 验收句：89,854 与 52,997 的差来自哪一层？断言：四级量全部由 registry 与 Evidence Context 产物计算，无硬编码；同图内 bytes 与 chars 不混用；budget 格渲染为「未冻结」而非数值；截图两视口 |

### 6.5 T4 · 评测与判分：通过条件与否决条件

| 列 | 内容 |
|---|---|
| ① 单一问题 | 一道 eval 题在什么条件下算通过，什么条件会直接否决整个 split？ |
| ② 10 秒结论 | dev 10 题覆盖五类行为各 2 题（8 题预期 answered、2 题预期 abstained，共 18 条 evidence requirements）；answered 要同时满足 8 条 item 条件，任一失败即该题失败；预期 abstained 的题返回 answered 直接否决该 split，不被其它题分数抵消 |
| ③ 对象与数据形状 | 5 类行为 × 2 个 split 的覆盖矩阵；answered 8 条 / abstained 5 条判分条件的顺序链；6 个 metric（2 个门禁、4 个诊断）及阈值（item_pass_rate ≥ 0.9、citation_precision = 1.0）；每个 metric 的当前状态为「未测量」 |
| ④ 结论编码 | 矩阵（行为类型 × split）+ 分叉（answered / abstained 两条判分链）+ 停止位置（否决出口标在链上的具体位置）+ 覆盖格状态（dev 可展开、受保护 split 上锁） |
| ⑤ 视觉舞台 | 首屏：5×2 覆盖矩阵在左、判分链在右，否决出口用醒目的停止标记落在链上；主结论一句在两者上方 |
| ⑥ 文字层级 | 常驻：结论句 + 「所有 metric 未测量」一句 + 门禁 / 诊断的区分；折叠：8 条 answered 条件与 5 条 abstained 条件原文、`corpus_absence` 是评测者预先冻结的判分依据；notes：判分契约原文。复习题「citation_precision 阈值为 1.0，为什么 claim 零 citation 时它不能记为通过？」 |
| ⑦ 视觉记忆点 | 五行两列的覆盖矩阵，右列整列上锁——覆盖相同、内容不可见 |
| ⑧ 图标策略 | 锁形标记表示受保护 split（带 accessible name「内容受保护，不展示」）；停止标记表示否决出口；其余用文字 |
| ⑨ 动效策略 | ①语义过程动效：单题判分逐帧推进一个条件并显示通过 / 失败。样例响应为合成数据，帧内常驻标注「契约演示，非模型运行结果，不属于 dev 或受保护 split」。可暂停、单步、重放 |
| ⑩ 验收证据 | 验收句：哪一个条件失败会否决整个 split？断言：受保护 split 的列在 DOM 中只有计数与状态、无题面字段；所有 metric 渲染为「未测量」而非 0；截图两视口 |

### 6.6 T5 · 门禁与证据：判据覆盖与盲区

| 列 | 内容 |
|---|---|
| ① 单一问题 | 现在被证明的是什么，没有被证明的是什么？ |
| ② 10 秒结论 | 7 条判据与 9 条测试（5 条 fixture 字节回归 + 4 条真实语料不变式）覆盖确定性组装层：7 份文档 uncovered 与 duplicated 均为 0，两次构建逐字节一致，整串 sha256 已冻结为回归基准；这些只证明确定性层可重复，不证明语义切分合理、retrieval 有效或模型回答正确 |
| ③ 对象与数据形状 | 7 条判据 × 4 类输出层（正文与 hash / wrapper / 组装与稳定 / 职责边界）的覆盖矩阵；每格标覆盖它的测试或审计；`content_sha256` 覆盖范围与其盲区（wrapper、块序、块间分隔）；门禁阶梯 5 级及当前档位 |
| ④ 结论编码 | 矩阵（判据 × 输出层）+ 覆盖空格（hash 盲区留空并标出由 golden 字节补上）+ 停止位置（门禁阶梯当前停在哪一级） |
| ⑤ 视觉舞台 | 首屏：覆盖矩阵占主位，空格清晰可见；右侧门禁阶梯竖排 5 级，当前档位标记落在第 3 级与第 4 级之间；主结论一句在矩阵上方 |
| ⑥ 文字层级 | 常驻：结论句 + 「只证明确定性层可重复」这一边界；折叠：判据 #1–#7 逐字原文、A1–A8 review 工作表状态、破坏性验证方案（标「未执行」）；notes：D3 §6.1 原文。复习题「fixture 字节回归能抓到哪一类 `content_sha256` 抓不到的错误？」 |
| ⑦ 视觉记忆点 | 覆盖矩阵里的那一格空白——hash 全绿不等于组装正确 |
| ⑧ 图标策略 | 覆盖格用形状 + 文字（已覆盖 / 未覆盖）；冻结基准用锁形标记；无其它图标 |
| ⑨ 动效策略 | ②方位过渡：矩阵格与门禁阶梯之间点击互相高亮，短过渡不循环。不做过程动画，理由见 §5 |
| ⑩ 验收证据 | 验收句：hash 的盲区是什么，被什么补上？断言：矩阵格状态由数据层计算；破坏性验证条目带「未执行」状态字段；截图两视口 |

## 7. 数据层规格

### 7.1 由脚本计算、不手写进文案的量

数据层在构建期由一个只读脚本从既有产物导出为 TS 数据模块；展板不在运行时读文件系统。

| 量 | 来源产物 | 当前值（2026-09-09 复算，供设计估算，实现以脚本输出为准） |
|---|---|---|
| 文件数 / bytes | `corpus/rules-c0a4b85/manifest.json` | 7 / 76,243 bytes |
| 文件 chars / 行数 | snapshot 文档 | 34,354 chars / 1,346 行（其中非空行 936） |
| block 总数与类型分布 | `evidence/serialization/registry-rules-c0a4b85.json` | 572；list_item 349 / paragraph 144 / table_row 43 / code 23 / quote_para 10 / quote_list 3 |
| 每文档 block 数 | 同上 | AGENTS 104 / DAILY-LEARNING-REPORT 71 / DAILY-SPEAKING 32 / LEARNING 100 / SHOWCASE-DEPLOY 78 / SHOWCASE-VISUAL 100 / TECHNICAL-WRITING 87 |
| `context_spans` 角色与深度分布 | 同上 | heading 1,305 + table_header 43 = 1,348；深度 1 层 15 / 2 层 351 / 3 层 193 / 4 层 13 |
| `model_content` 合计 chars | 同上 | 52,997 |
| Evidence Context chars | `evidence-context-rules-c0a4b85.txt` | 89,854 |
| wrapper 与块间分隔增量 | 上两项之差，由脚本计算 | 36,857 chars |
| 覆盖审计（uncovered / duplicated） | `criteria-report-rules-c0a4b85.md` | 7 份文档均为 0 / 0 |
| 整串 sha256 与冻结基准 | `frozen-rules-c0a4b85.sha256` | `8a02c665…`，两次构建一致 |
| raw corpus-only token | `evidence/token-count-rules-c0a4b85.json` | 18,697，分类 `estimate` |
| dev 题目结构 | `eval/dev/items.json` | 10 题；五类各 2；8 answered / 2 abstained；18 条 evidence requirements（16 source_span + 2 corpus_absence） |
| 受保护 split 结构 | `eval/manifest.json` 与判分契约 | 只取题数与覆盖类别；不读取、不导出任何题面字段 |

> 本表的核心 span 正文与语境复制的分项（约 32,151 与 20,826 chars）为设计期复算的量级参考，与
> `model_content` 合计 52,997 存在 20 chars 差额，来自 10 行 hard break 保留的行尾两空格。实现时由脚本
> 直接给出分项，不沿用本表数值。

### 7.2 状态字段与事实等级

每条事实条目至少带：`value`、`unit`（bytes / chars / tokens / count）、`evidence`
（`已实测` / `已估算` / `待运行验证`）、`sourcePath`、`status`
（`frozen` / `implemented` / `not_implemented` / `not_run`）。

- 未运行项的 `value` 为 `null`，渲染层遇 `null` 渲染「未测量」，不做数值回退（对应硬边界 H2）。
- `status` 取值随主线推进变化，实现时以当时 `LEARNING-STATE.md` 为准；本方案记录的是截至 2026-09-09 的取值。
- 结论句不写死计数，模板从数据插值（对应硬边界 H6）。

### 7.3 受保护内容在数据层的处理

导出脚本对受保护 split 只允许写入 `count`、`behaviorTypes`、`frozen` 三个字段，且脚本本身不读取该目录下的
题面内容；结构信息取自 eval manifest 与判分契约。审查点：导出产物中不得出现该 split 的 query 或
`expected_rule_conclusion` 字符串。

## 8. 动效与交互规格

### 8.1 分类落位

| 位置 | 分类 | 帧数 | 每帧引入的唯一新事 |
|---|---|---|---|
| T1 完整性校验 | ①语义过程 | 4 | 读取 / normalization / 计算 sha256 / 逐文件比对 |
| T2 parser 扫描 | ①语义过程 | 每帧一行（片段约 12-16 行） | 标题入栈、标题出栈、块边界落下、遇 `---` 停止合并，四种事件之一 |
| T3 单块组装 | ①语义过程 | 4 | 核心 span / 前置必要语境 / 包 wrapper / 加块间空行 |
| T4 单题判分 | ①语义过程 | 与该分支条件数一致（answered 8 / abstained 5） | 推进一个条件并给出通过或失败 |
| T0 阶段跳转、T5 矩阵与阶梯联动 | ②方位过渡 | — | 高亮位置变化，短时、不循环、关闭后不丢信息 |
| 全站 | ③纯装饰 | 0 | 不使用 |

### 8.2 播放控制与可访问性

- 每个语义过程动效必须提供暂停、单步前进、单步后退、重放，控件有 accessible name 与键盘可达。
- `prefers-reduced-motion` 下：不自动播放，渲染信息完整的终态，单步控件保留。
- 帧解说使用 live region 播报当前帧引入的变化；帧内所有状态另有文字标签，不依赖颜色或位移。
- 帧停留时长按解说文本长度计算，不使用固定节奏。

### 8.3 交互与深链

- 六个功能 tab 与当前专题写入 URL；刷新与分享不丢位置。
- 折叠区摘要必须写明里面是什么（例：「判据 #1–#7 逐字原文」），不写「更多」「详情」。

## 9. 图表清单与编码

| 图 | 位置 | 数据 | 形态与编码 | 第二编码 |
|---|---|---|---|---|
| C1 语料体量 | T1 | 7 文件 bytes（并列标注 chars） | 共用基线水平条 | 数值标签 + 单位标注 |
| C2 膨胀瀑布 | T3 | 4 级 chars 与 2 段增量 | 共用基线瀑布，增量分段填充 | 每段填充图案 + 增量来源文字 |
| C3 块类型分布 | T2 | 6 类计数 | 共用基线水平条，按计数排序 | 类型名 + 数值 |
| C4 语境深度分布 | T2 | 4 档计数 | 位置编码（深度为序）+ 长度 | 层数标签 |
| C5 每文档 block 数 | T2（折叠） | 7 项 | 小倍数条形，与 C1 同序以便对照 | 与 C1 相同的文件顺序 |
| C6 覆盖矩阵 | T5 | 7 判据 × 4 输出层 | 矩阵 + 覆盖空格 | 形状 + 文字（已覆盖 / 未覆盖） |
| C7 eval 覆盖矩阵 | T4 | 5 行为 × 2 split | 矩阵，受保护列上锁 | 锁形 + 文字说明 |
| C8 metric 状态条 | T4 | 6 metric × (阈值, 当前状态) | 阈值刻度 + 状态格 | 「未测量」文字，不画 0 值柱 |

C1 与 C5 使用同一文件顺序，使读者可以直接对照「文件大小」与「切出的 block 数」是否成比例；这一对照本身
是 T2 的一条结论（切分粒度由结构决定，不由文件体量决定），因此两图不合并。

## 10. 可访问性与响应式

- 桌面 `1440×1000`、手机 `390×844` 两套语义布局；手机端先出主结论、必要边界与播放控件，再进入图形。
- 矩阵在手机端改为按行分组的列表结构，保持行列语义等价，不做横向滚动的整表压缩。
- 宽内容（真实字节样例、Evidence Context 片段）放在自身 `overflow-x: auto` 容器内，页面不横向滚动。
- 图标按钮、播放器、tab 切换满足键盘、焦点、accessible name 与触控目标要求。
- 固定高度舞台不得裁切文本；帧解说区高度按最长帧文本预留。

## 11. 实现接线点（形态定稿之后的工程决策）

本节是实现选择，不构成形态依据。

- 新增数据模块 `w13RagTopics.ts` 与组件 `W13RagBoard.tsx`（`week8-fullstack/src/frontend/src/`，白名单展示资产）。
- 在 `aiEngineerTopics.ts` 的 `AeGroup` 增加 W13 的六个功能模块组；`AiEngineerBoard.tsx` 分发到新组件。
  不改 `types.ts` 的 `ShowcaseTab`（沿用 §3.4 的范围门禁）。
- 语义过程动效可复用既有 `framePlayer.tsx`（`useFramePlayer` / `FrameTransport` / `FrameNarration`
  已含暂停、单步、reduced-motion 与解说 live region）；若某块的帧模型不契合，另写而不迁就组件。
- 共用基线条形可复用 `charts.tsx` 的 `HBarChart`；矩阵形态若与 `StopMatrix` 的行列语义不一致则新写。
- 数据导出脚本置于 `week13-rag/scripts/`，只读 corpus 与 evidence 产物，输出 TS 数据模块；不调用模型，
  不读取受保护 split 内容。
- 仓库无统一图标源，图标以内联 SVG 实现，跨块同一对象使用同一形状（视觉规范 §3.2）。

## 12. 验收证据（按视觉规范 §7）

| 证据 | 本方案的最低要求 |
|---|---|
| 构建 | `yarn typecheck`、`yarn build:showcase`、`yarn verify:board`、`yarn audit:visual` |
| 视口 | 桌面 `1440×1000`、手机 `390×844` 各六块 |
| 状态 | 默认态；再核全部展开态与复习门展开态 |
| 度量 | 每块记录默认态屏数（目标 ≤ 1.5 屏）、全部展开态屏数、主路径中文正文字数（目标 ≤ 220 字，超 300 字须说明哪些限定语不能下沉） |
| 结构断言 | 六块各自的 ⑩ 列断言 + 三条全局断言：① 受保护 split 的题面字段不出现在产物中；② 未运行 metric 不渲染数值；③ 每块存在一个 `data-anchor` 且不落在图例、计数或导航上 |
| 人工视觉验收 | 逐块记录：遮住标题与结论段后能否从图回答该块 ① 列问题；10 秒后能否说出对象、关系与结论；首屏是否先出现视觉舞台；视觉记忆点是否来自技术关系。逐项记通过或卡点，不写「看过」 |
| 截图 | 桌面与手机截图路径写入本方案 §13 的交付记录区 |
| 边界 | 记录事实等级、未运行项、颜色第二编码、reduced-motion 行为与手机端语义等价 |

机器断言全绿而人工视觉验收未通过时，该块判为未完成。

## 13. 明确不做

- 不新增顶层 tab，不返工既有 W2/W5/W9/W10/W11/W12 板。
- 不展示受保护 split 的任何题面内容。
- 不预画 retrieval（BM25 / dense）、generation 与 holdout 结果；这三段在主线跑出证据前只在 T0 显示为断点。
- 不做「一次 query 流过全链路」的整体动画（§5 已说明理由）。
- 不把 W13 的可迁移思维模式（§1 的八条）单独做成一块板；它们作为各功能 tab 的折叠层结论出现，避免与
  技术对象脱钩成为独立方法论页。
- 不引入图标库或图表库依赖。

## 14. 待本人确认项（AI 不代填）

以下属于本人拥有的语义或取舍判断，本方案只给出候选与理由，实现前需要逐条确认：

1. §4 的六个功能 tab 划分是否成立，特别是 T0 是否单独成块，还是并入 T5 的门禁阶梯。
2. §6 各块 ② 列的 10 秒结论措辞：事实强度与边界是否准确，尤其 T5「只证明确定性层可重复」的边界。
3. T2 与 T3 的真实样例选哪一个 entry（候选：`rules/SHOWCASE-VISUAL-PROTOCOL.md#L50-L50`，含 heading 两层
   与 table_header，可同时展示语境复制与 identifier 不扩大两条结论）。
4. T4 的合成响应样例内容——它不属于 dev 与受保护 split，但仍是判分语义的展示，语义由本人冻结。
5. 各块 ⑦ 列的视觉记忆点是否来自技术关系，是否需要替换。
6. §12 的度量护栏是否按块放宽：T2 的 parser 规则较多，默认态可能超过 1.5 屏，需要本人决定是拆页还是记录
   不可下沉的理由。
7. 展板是否在展示状态可见。语料是本仓库公开规则文档、不含凭据与可定位端点，本方案倾向展示状态可见；
   最终由本人裁定。

## 15. 交付记录区（实现后回填）

| 项 | 内容 |
|---|---|
| 实现日期 | 待回填 |
| 构建输出 | 待回填 |
| 度量（每块屏数 / 字数） | 待回填 |
| 人工视觉验收逐项结论 | 待回填 |
| 截图路径 | 待回填 |
| 遗留锦上添花项与代价 | 待回填 |
