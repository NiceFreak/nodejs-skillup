{
  "schemaVersion": 1,
  "checklistId": "technical-v2-benchmark-freeze-checklist-01",
  "status": "policy_for_future_versions",
  "purpose": "防止 benchmark 在运行结果之后才被定义。",
  "requiredBeforeFreeze": [
    {
      "id": "item_set_sha",
      "description": "题集文件 SHA-256 已记录并由 owner 确认。"
    },
    {
      "id": "runtime_conditions_sha",
      "description": "corpus snapshot、registry、Prompt、retrieval 配置、schema 与运行器版本的 SHA-256 已记录。"
    },
    {
      "id": "criteria_source_recallability",
      "description": "每个 criterion 绑定的 source block 必须在冻结 retrieval 配置的 top-k 中可召回；否则视为设计缺口，需在冻结前处理。"
    },
    {
      "id": "passing_threshold",
      "description": "owner 在 freeze 前确认 passingThreshold 记录对象；运行后不得补设。对象必须包含 value、denominator、source.type、source.reference 或 source.rationale、source.confirmedBy、source.confirmedAt；overall pass 按声明的 overall_pass_rate_denominator 与 item_pass_rule 计算，N/A 层不进入适用层分母。"
    },
    {
      "id": "pass_criteria",
      "description": "item pass 条件、primary criteria、applicable layers、overall pass 聚合规则和 N/A 处理已固定。"
    }
  ],
  "passingThresholdRecord": {
    "requiredFields": [
      "value",
      "denominator",
      "source.type",
      "source.confirmedBy",
      "source.confirmedAt"
    ],
    "sourceTypeEnum": ["reference", "requirement", "estimate"],
    "sourceFieldRule": "source.type=reference 时必须有 source.reference；source.type=requirement 或 estimate 时必须有 source.rationale。",
    "confirmedAtRule": "source.confirmedAt 必须早于 freezeAt。",
    "ownerConfirmationRequired": true,
    "currentValue": {
      "value": 0.9,
      "denominator": "all_in_scope_items",
      "source": {
        "type": "reference",
        "reference": "w13-eval-v1 scoring contract item_pass_rate >= 0.9",
        "rationale": "借鉴历史契约的数值作为新独立题集的起始阈值；不表示新题集与旧题集难度等价，冻结前仍可基于新题集难度重新评估。",
        "confirmedBy": "owner",
        "confirmedAt": "2026-09-12"
      }
    },
    "currentStatus": "owner_confirmed_starting_reference",
    "reassessmentAllowedBeforeFreeze": true
  },
  "freezeGate": "五项全部存在且 owner 已确认，才允许 freezeAuthorized=true。",
  "runGate": "freezeAuthorized=true、holdoutRunAuthorized=true、runCountAfterFreeze=0 且命令行显式确认，才允许唯一一次 run。",
  "postRunRule": "运行结果只读；不得用结果补设阈值、修改题集或改变通过判据。",
  "currentHoldout01": {
    "status": "not_retroactively_repaired",
    "benchmarkPass": null,
    "reason": "threshold_not_pre_frozen"
  }
}
