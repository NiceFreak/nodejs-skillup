# 复盘：异地触发链路的工作量为什么是六小时

> 建立：2026-08-26（Asia/Shanghai，W11 D3 附加项第二轮收尾当晚）
> 对象：`ops/showcase-deploy` 触发分支 + Jenkins `showcase-deploy` job + 手机侧 skill `trigger-showcase-deploy`
> 上游：[`change-order-showcase-remote-trigger.md`](./change-order-showcase-remote-trigger.md)（变更单四要素、9 条可证伪验证、§9 执行结果回填）、
> [`deploy-showcase-script.md`](./deploy-showcase-script.md) §7（异地触发的三条决定性事实）
> 本篇记录估算偏差与方法修正，不重复记录链路本身怎么工作——那在变更单里。

---

## 1. 可追溯的时间区间

| 锚点 | 时刻（UTC） | 证据 |
|---|---|---|
| 首版入库（skill + 触发分支骨架 + 变更单） | 11:21:36 | `44f0b9d` |
| 设计假设被推翻一：云容器没有工具链 | 11:40:58 | `252b5db` |
| 设计缺陷被推翻二：建分支脚本会删掉 gitignored 的 `.env` | 11:51:50 | `19cbe09` |
| 设计假设被推翻三：`jenkins-deploy-key` 复用不了 | 12:09:08 | `e72aadd` |
| Jenkins 侧首次真实发布成功 | 14:01:58–14:05:28 | 构建 6，回执 `20260826T135858Z-b150b4f` |
| 手机侧首次真实触发写入 | 15:00:05 | `a43b67f`（该信号在 45 分钟内未被轮询捡起） |
| 开发机解除休眠 | 约 15:12 | 本人操作，无自动记录 |
| 移除 `PathRestriction` 后发布成功（手动 Build Now） | 16:12:51–16:16:14 | 构建 7，回执 `20260826T145956Z-b150b4f`，`deployedSha: db0c394` |
| 轮询恢复：`trigger.json` 写入到构建启动 | 16:19:50 → 16:21:19（**1 分 29 秒**） | `4eb8f5a` → 构建 8，回执 `20260826T161939Z-db0c394`，`status: skipped`。无人操作 Jenkins |

可追溯区间为 **4 小时 55 分**（11:21:36 → 16:16:14）。三条决定性事实的实测与方案取舍发生在首个提交之前，没有提交时刻可锚；本人记的总量约 6 小时，两者不冲突。

---

## 2. 估算偏差一：按箭头数估，按接触面数付

**原判断**：方案是四个箭头（手机 → GitHub → Jenkins → 服务器），实现量不大。

**实际现象**：信息流从起草到跑通一次没改过，但变更单 §9.2 记下 A–K 共 11 条执行期偏差，其中零条是流水线阶段划分错误。

**关键证据**：11 条偏差分别落在 Jenkins 插件管理（A）、Yarn 与 `.gitignore` 的交互（B）、Groovy command expression 解析（C）、Groovy 字符串转义层级（D）、workspace 文件跨构建残留（E）、`git push --dry-run` 的协议行为（F）、GitHub deploy key 与分支保护的优先级（G、H）、vite 输出的 ANSI 控制字符（I）、git plugin 的轮询扩展弃用（J、K）。

**偏差类型**：估算模型错误，不是执行不力。方案的复杂度由箭头数决定，工作量由**接触面数**决定——本链路实际要动 7 个系统：Jenkins 插件、Groovy 脚本沙箱、git plugin 扩展、GitHub deploy key、GitHub 分支保护、服务器 `authorized_keys`、macOS 电源管理。每个系统的默认行为只在真跑时暴露。

**修正**：首次打通一条新链路，按「接触面数 × 45 分钟」估。本链路 7 个接触面 ≈ 5 小时，与实测 4 小时 55 分吻合。

**边界**：该系数来自单次样本，尚未在第二条链路上复验。

---

## 3. 估算偏差二：验证是主体工作量，但没有进预算

**原判断**：变更单的 9 条可证伪验证是实现完成后的确认动作。

**实际现象**：pipeline 首版编写耗时远小于验证耗时；9 条验证中有 3 条的第一版判据测不出它要测的东西。

**关键证据**：

| 条目 | 判据缺陷 | 暴露方式 |
|---|---|---|
| §9.2-F | `git push --dry-run` 只协商不发送数据，不触发 GitHub pre-receive 检查 | 改真实 push 后才暴露 main 无保护 |
| §9.2-G | 规则未勾「Include administrators」时，write 权限 deploy key 按管理员级凭据绕过 | 真实 push 输出 `Bypassed rule violations` |
| §9.2-J → K | 「10 分钟无新构建」是单向判据，同时兼容「过滤生效」与「轮询未运行」两种情况 | 手机侧真实触发后 45 分钟零构建 |

**偏差类型**：预算遗漏 + 判据设计缺陷。三条同型：验证方法本身测不出目标命题。

**修正**：见 §5 第 1 条。

---

## 4. 返工的三种性质

11 条偏差按性质分三类，处理方式不同。

| 类别 | 条目 | 说明 |
|---|---|---|
| **推翻了会长期误导的结论** | `252b5db`、`e72aadd`、§9.2-K | 不返工的代价是留下一条「记录为已验证、实际不可用」的链路。K 条尤其明确：验证 6 当时记为 ✅，而真实状态是轮询未运行 |
| **首次集成的实价** | §9.2-A、B、C、D、E、I | 工具默认行为与本项目组合方式的首次相遇，无法靠更仔细的设计提前消除 |
| **判断失误** | §9.2-J 的处理、`19cbe09` 前的脚本第一版 | J 条当天已记下 `The extension that requires a workspace for polling is deprecated`，处理写的是「观察，若后续自触发再改」，只设了假阳性一侧的观察判据 |

第三类可追溯的代价约 1 小时（15:00 触发写入到 16:16 发布成功）。

---

## 5. 两条写进变更单模板的修正

### 5.1 可证伪验证必须双向

一条验证若只写「某现象不出现」，它同时兼容「机制正确抑制了该现象」与「机制根本没运行」。这类判据不算通过。

以陷阱 1 为例，正确形态是两句成对：

1. 一次 `trigger.json` 改动**能**在 ≤5 分钟内起构建；
2. 一次回执 push **不**起新构建（起一次 `NOT_BUILT` 且不写回执，属于预期内）。

缺第 1 句时第 2 句不承载信息。

### 5.2 构建日志里的弃用警告不能用「观察」结案

处理方式二选一：当场设一个**会失败**的判据并执行，或记入 `DEBT.md`。写「若后续出现 X 再改」等于把判据推迟到某个特定失败模式上，其他失败模式不在观察范围内。

---

## 6. 后续步骤

### 6.1 触发链路（变更单 §9.5）

- [x] **轮询恢复验证**（2026-08-26 达成）：写入 `20260826T161939Z-db0c394`（16:19:50Z，`4eb8f5a`），构建 8 于 16:21:19Z 启动，**延迟 1 分 29 秒**，无人操作 Jenkins；回执 `status: skipped`，`deployedSha: null`，`checks` 五项为空、`evidence` 为空（`main` = `db0c394` = `LAST_SUCCESS.deployedSha`，`force: false`）。同时覆盖 §5.1 的第 1 句与变更单验证 7 的手机侧复现。
- [ ] **陷阱 1 重验**：上一条的回执 push（`4cbcdb0`）之后观察 10 分钟，期望恰好一次 `NOT_BUILT`（`skip=duplicate`）且不写回执。
  **判据必须看 Jenkins 构建列表，不能只看 receipts**：`NOT_BUILT` 分支本就不写回执，因此「没有新回执」同时兼容「起了一次 NOT_BUILT」与「一次构建都没起」——与 §9.2-J 同型的单向判据。本人需确认构建 9 存在且结果为 `NOT_BUILT`、日志含 `skip=duplicate`。
- [ ] **验证 8 手机侧**：`force: true` 触发一次，期望 `status: succeeded`。
- [ ] **验证 9**：五面回归 80 / 443 / 8080 / 8081 全 200，由本人浏览器执行（手机侧会话连不到 8081）。
- [ ] **D1 收尾**：把「触发信号即发布授权」的口径写进 `SHOWCASE-DEPLOY-PROTOCOL.md` §4.5。

### 6.2 展板

`week11-visualization-plan.md` §19.0 的上板条件①为「§4.2 的 9 条验证跑完并回填」。§6.1 四条完成后条件①成立，⑧「远程触发的信任边界」可按 §19.1 的编码表开工，实现侧为 `measured` 而非整页 `pending`。

§19.5 记的数据冲突在开工时仍需处理：sudoers 白名单 D3 取值为 8 条，`showcase-land` 落地后为 9 条，同板出现时必须标注各自时点。

### 6.3 待复验

§2 的「接触面数 × 45 分钟」系数为单次样本，下一条新链路开工时先记预估、收尾时对照实际，两点之后再决定是否写进模板。
