# W11 Day 5（8/28）：收口日（对照说明 · 能力检验 · 延迟重建 · 展板 ①⑦ · 8081 发布 · 状态收口）

> 建立：2026-08-27（Asia/Shanghai，D4 收口后起草）
> 上游：[`week11-plan.md`](./week11-plan.md) §3 最低交付边界（六条）与 §4 D5 的 A–E；[`day4-rollback-drill.md`](./day4-rollback-drill.md) §6 明日入口与 §8 收尾清单；[`DEBT.md`](../../DEBT.md) 2026-08-27 条目；[`week11-visualization-plan.md`](./week11-visualization-plan.md) §5 ①⑦ 编码表与 §10 排期第 4 批
> 形态参考：W10 [`day5-wrapup.md`](../../week10-observability/notes/day5-wrapup.md)（收口日五模块 + 结账 + 复盘）、本周 [`day4-rollback-drill.md`](./day4-rollback-drill.md) §0 / §2 / §3（缺口表 + 四要素变更单 + 待答题）
> 状态：**起草完成，P1–P6 待本人作答**。§0 列出起草期发现的八处覆盖缺口，各自指向 §3 的待答题或 §2.3 的验证行；**不改任何已冻结决策**，D1 契约的 Q1–Q18 一题不重开。

---

## 0. 起草期发现的覆盖缺口（2026-08-27）

| # | 缺口 | 来源 | 落到哪 |
|---|---|---|---|
| G1 | **展板 ④ 已在 D4 当天上板，D5 的展板待办不是它**。D4 §8 收尾清单与 `LEARNING-STATE.md` 头部都写着「④ 上板材料齐备，D5 上板」，但 8/27 的 `1024e12` 已把 ④ 与 ⑪ 落地，`w11Facts.ts` 的 `W11_STAGE_PLAN` 里 `rollback` 与 `false-active` 均为 `done: true`。D5 真正未上板的是 **① `lanes`** 与 **⑦ `handoff`**（两条仍为 `done: false`，`when` 写的就是 D5） | `w11Facts.ts` `W11_STAGE_PLAN`；`1024e12` | §2.1 第 4 / 5 项 + V5 / V6 |
| G2 | **① 与 ⑦ 的上板前提不同，不能并作一件事排**。⑦ 的数据来自主线 A 的对照说明，A 不成篇就没有格子可填（`when` 写的是「D5 对照说明成篇后」）；① 的数据在 D3 收口时就齐了，它排 D5 的理由是「触发链路终点在 D2 / D3 各不相同，早画会当天过时」，与 A 无依赖。**顺序上 ① 可以先做，⑦ 必须在 A 之后** | 可视化计划 §5 ①（第 194 行起）与 ⑦（第 350 行起） | §2.2 执行顺序 + §3 P1 |
| G3 | **周计划 §4 D5 的 A–E 里没有展板与 8081 发布这条线**。A–E 是「对照说明 / 口述 / 延迟重建 / 状态收口 / stretch」，而 D4 §6 把「展板上板 + 8081 重新发布」列为 D5 待办。两处并列时，哪一条属于最低交付边界、哪一条可顺延，没有写 | 周计划 §4 D5；D4 §6 | §1 判据表 + §2.5 时间盒 |
| G4 | **延迟重建（C）现在是两项，周计划只写了一项**。周计划 C 写的是「从 runbook 速查表盲重画一张开始」；`DEBT.md` 2026-08-27 的类 2 脚本条目把重建安排写成「**W11 D5 或下周**第一档」。两项都排今天会与主线 A / B 抢时间盒，且**两项的「盲」的对照物不同**：前者对照 `runbook.md` §4.1，后者对照 `day4-rollback-drill.md` §5.3 | 周计划 §4 D5 C；`DEBT.md` | §3 P3 + V3 / V4 |
| G5 | **类 2 最小样本脚本已按约定删除，DEBT 重建拿不到可重跑的对照**。D4 §2.1 第 5 条写的是「演练后删除」，仓库里 `reproduce-close-race.js` 不存在（`git log --diff-filter=A` 无记录）。重建那条债要复述「三种 close 时序各自的竞争语义与实测结果」，可用的对照只剩 §5.3 的文字。**重建时要不要重写脚本、重写算不算破坏「盲」**，没有约定 | D4 §2.1 第 5 条、§5.3；`DEBT.md` | §3 P3 追问 ③ |
| G6 | **公网面的收口判据口径 8/27 刚改，且 8081 发布本身会动其中两面**。周计划 §3 末条现在写的是「四面 + 80 `/showcase/` 子路径入口」（8080 已下线）。今天要发布展板，发布动的正是 8081 与 `/showcase/` 两个面——**最终回归跑在发布之前还是之后**决定了它证明的是哪一版产物 | 周计划 §3 末条；D4 §8 8080 下线条 | §3 P4 + V9 / V10 |
| G7 | **三项持久遗留 + 三项 stretch 的收口写法没定**。持久遗留：① 一次性 `cp` 白名单条目未收回（需 root，P6 冻结为持久遗留）；② `/etc/sudoers` L55 lighthouse 未注释（8/27 复核「利用面 0 仍成立」）；③ `authorized_keys` 第 3 行展板落盘密钥裸装无 `command=`（`BACKLOG.md` P1-9）。stretch：Java / S3 / Docker 三项本周未开工（Q18 已写前置条件）。W10 D5 的口径是「要么定论、要么显式转 backlog，**不许沉默消失**」，这六项今天各写成什么状态、落在哪个文件，没有写 | D4 §8；`BACKLOG.md` P1-9；周计划 §5 Q18；W10 D5 §1 判据 6 | §3 P5 + §8 结账表 |
| G8 | **W12 主题与 stretch 的关系没定**。`LEARNING-STATE.md` 写的是「W12『AI 使用进阶』主题与黑白名单完整审视维持下周一并推进」，而周计划 §4 D5 的 E 三项 stretch 若顺延，顺延的目的地就是 W12。两者是并列还是二选一，D5 收口时要写进「下周入口」 | `LEARNING-STATE.md` 头部；周计划 §4 D5 E | §3 P6 + §11 |

---

## 1. 今日唯一主线与验收句

**主线**：把 D1–D4 四天的事实收成一份**别人能照着判断的对照说明**，并用一次**不看笔记的口述**证明这四天的机制留在人身上而不只留在文件里。

```text
A 对照说明成篇（W9 六步 × 三种归属：被替掉 / 仍需人执行 / 本周明确不交）
→ B 能力检验口述（三问，当场修正计入掌握证据）
→ C 延迟重建（runbook 速查表盲重画 + DEBT 第一档，排序由 P3 定）
→ D 展板 ① 与 ⑦ 上板 + 8081 重新发布
→ E 状态收口（周计划勾选 + LEARNING-STATE + DEBT + 复盘 + 项目叙述 + 口语稿 + commit）
```

**今日验收句**（取自 `week11-plan.md` §3 最低交付边界第 5 条，不临时改）：

> **一份与 W9 手工部署的对照说明：同一件事手工要做哪几步、流水线替掉了哪几步、哪几步没有被替掉。**

**验收句的可证伪形态**（今天逐条验收，缺一即本周未收口）：

| # | 判据 | 怎么算过 |
|---|---|---|
| 1 | 对照说明有结构不是流水账 | W9 手工发布的每一步都落进**三种归属之一**，且「没被替掉」**分成两类分别写**：`nginx -t && reload` / 证书 / ufw 是**主动不交**（W9 收口成果，本周边界，将来可改）；「验证不绿要不要回滚」是**不能交**（Q12 已拍板由人判定）。两类写成同一类即判据 1 不过 |
| 2 | 第三类归属在册 | 「替掉了，但依赖一台会关机的机器」（controller 装在开发机，Q1 / Q2 显式接受过）作为**独立一格**出现，不并进「被替掉」 |
| 3 | 口述三问当场答 | B 的三问各口述一遍，**当场修正逐条记录**（沿用 W9 / W10 D5 形态：修正计入掌握证据，不计失分）；答不出的题如实记为未达成，不补看笔记后重答冒充通过 |
| 4 | 延迟重建是盲的 | C 全程不打开对照物（runbook 盲重画不看 `runbook.md` §4.1；DEBT 第一档不看 `day4-rollback-drill.md` §5.3）。中途翻了就地记「第几步翻的、翻的是什么」——这条比重建通过更重要，它是缺口的唯一直接证据 |
| 5 | 展板不画预测 | ① 与 ⑦ 上板后 `W11_STAGE_PLAN` 两条转 `done: true`；板上每一格的等级按实际证据标（`measured` / `contract` / `pending`），**没走过的路径不标 measured** |
| 6 | 线上与仓库一致 | 8081 发布后线上产物与本次构建一致（asset 指纹比对），公网面按 8/27 新口径全绿；服务器版本仍是已验证提交，无演练残留 |
| 7 | 收口不留悬案 | 最低交付边界六条逐条结账（§8）；G7 的六项（三项持久遗留 + 三项 stretch）**各有明确去向**，要么定论、要么显式转 `BACKLOG.md`，不沉默消失 |

> 判据 4 是今天的秤。四天的机制记住没有，不由写了多少字评价，由**不看对照物能重画出多少**评价。

---

## 2. 变更单（动手前冻结，四要素）

### 2.1 改动清单 —— 今天就这几项，别的都不动

**文档侧**

1. **对照说明成篇**（主线 A）。落点由 P1 定：本文件 §5、或 `week11-ci/notes/` 下单独一份对照文档。
2. **本文件 §6 / §7 / §8 / §9 现场填**：口述记录、延迟重建记录、W11 结账表、第二轮复盘与项目叙述。
3. **状态文件收口**：`week11-plan.md` §4 D5 勾选 + §3 六条边界结账留痕、`LEARNING-STATE.md` 指向 W12、`DEBT.md` 类 2 条目状态更新、`BACKLOG.md` 按 P5 的去向增条。

**展板侧（白名单展示资产）**

4. **① `lanes` 上板**：三条自动化轨道（GitHub Actions / Jenkins / W9 手工）+ 服务器单入口 + 轮询等待段（上限 5 分钟）。轮询失败被静默记 `No changes` 那条**只挂指针不重讲**（归 ⑥·2）。
5. **⑦ `handoff` 上板**：W9 六步 × 三种归属，两类「没被替掉」用**两种画法**（判据 1 / 2）。数据源 = 主线 A 的对照说明，A 未成篇则本项顺延。
6. `w11Facts.ts` 新增两组导出 + `W11_STAGE_PLAN` 两条翻 `done`；`verify:board` 加对应结构断言。

**发布侧**

7. **8081 重新发布**：走 `SHOWCASE-DEPLOY-PROTOCOL.md` + 部署 skill（本地 `deploy-showcase-8081` 还是远程触发由 P4 定）。**GitHub Pages 维持冻结，不解冻**。

**明确不改**

- 服务器配置（Nginx / 证书 / ufw / sudoers / `authorized_keys`）**一格不动**。今天没有计划内的 root 需求；若出现，按 D3 冻结的「绑定同一会话闭合」口径处理并记录。
- 业务代码不动。`server.js` 的类 2 修复已在 D4 上线（`2b9f87b`），今天不追加。
- Jenkinsfile 与 wrapper 不动。本周流水线内容已冻结。

### 2.2 执行顺序（提议，待本人拍板）

| 序 | 动作 | 依赖 | 说明 |
|---|---|---|---|
| 1 | C-1 前置核对（§2.3 C1–C5） | — | 服务器与仓库现状对齐，5 分钟 |
| 2 | **C 延迟重建**（两项或一项，P3 定） | 必须在 A 之前 | A 会把 W9 六步与本周五阶段重新写一遍，写完再重建就不盲了 |
| 3 | **A 对照说明** | C 完成 | 主线，验收句本体 |
| 4 | **B 口述三问** | A 完成 | 三问里两问的答案就是 A 的内容，A 先成篇 |
| 5 | 展板 ① 上板 | 与 A 无依赖（G2） | 时间紧时可与 3 并行安排 |
| 6 | 展板 ⑦ 上板 | A 成篇 | A 未成篇则顺延 |
| 7 | `typecheck` + `verify:board` + 8081 发布 | 5 / 6 完成 | 发布与最终回归的先后由 P4 定 |
| 8 | 公网面最终回归 + 残留核零 | 7 完成 | §2.3 V10 / V11 |
| 9 | E 状态收口 + 口语稿 + commit | 全部 | §13 收尾清单逐条 |

> **顺序的唯一硬约束是「C 在 A 之前」**（第 2 行）。其余可按当天时间盒调整。

### 2.3 验证 = 可证伪实验（动手前写死期望，实测栏当天填）

**前置核对（动手前，逐格填，空一格不进入执行）**

| # | 核对项 | 命令 / 位置 | 期望 | 实测 |
|---|---|---|---|---|
| C1 | 服务器运行版本 | `git -C <部署目录> rev-parse HEAD` | 等于 D4 最后一次已验证部署的提交（`2b9f87b`），无演练残留 | |
| C2 | 回滚基线两个指针 | `.previous_commit` / `.rollback_target` | 两值与 D4 §5.2 的收口记录一致（**取值当天读，不照抄推断**） | |
| C3 | 越权面未漂移 | `sudo -n -l` 条数；`authorized_keys` 行数与前缀 | 白名单仍 **9 条**；仍 3 行，第 3 行仍裸装无 `command=`（已记账，今天不收窄） | |
| C4 | 展板基线 | `yarn verify:board` | **934/934** 通过（上板前的基线，用于证明新增断言没放宽既有断言） | |
| C5 | 仓库状态 | `git status --short`；`git log origin/main -1` | 无意外改动；本地与 main 一致 | |

**执行后验证**

| # | 验证项 | 怎么做 | 期望 | 实测 |
|---|---|---|---|---|
| V1 | 对照说明覆盖完整 | 逐条对 W9 手工发布的每一步 | 每步都有归属，三类各非空；两类「没被替掉」分开写（判据 1 / 2） | |
| V2 | 口述三问留痕 | §6 三格 | 三问各有一段答与一段当场修正；未达成的如实标注 | |
| V3 | runbook 盲重画 | 不看 `runbook.md`，重画 §4.1 速查表，再逐项对照 | 产出差异计数（漏项 / 错项 / 多项各几条），**期望值不预设**；差异条目即缺口清单 | |
| V4 | DEBT 第一档重建 | 不看 §5.3，复述三条（探测时机为何必须在 close 前发起 / 三种 close 时序的竞争语义与实测结果 / EADDRINUSE 注入为何须绑同地址） | 三条都答出 + 两项掌握证据 → `DEBT.md` 改「已还」；卡档 → 按规则记「两次未通过」并顺延 | |
| V5 | 展板 ① 落地 | `W11_STAGE_PLAN` `lanes` | `done: true`；轨道终点按 D3 收口后的实际形态画（Jenkins 延伸到 Deploy / Verify） | |
| V6 | 展板 ⑦ 落地 | `W11_STAGE_PLAN` `handoff` | `done: true`；三类归属在数据层是三个分类值，不是三种颜色 | |
| V7 | 类型与构建 | `yarn typecheck`；`yarn build:showcase` | 均通过 | |
| V8 | 断言未放宽 | `yarn verify:board` | 计数 **> 934** 且既有断言一条未改、未放宽；新增断言全部量图形事实 | |
| V9 | 线上产物一致 | 8081 `/` 200；asset 指纹比对；`POST /auth/login` | 200 / 指纹一致 / 400（沿用发布脚本既有验收形态） | |
| V10 | 公网面回归 | 80、443、443 `/admin/`、8081 四面 + 80 `/showcase/` | 全部 200（8/27 新口径，**不含 8080**；8080 应无监听） | |
| V11 | 服务器残留核零 | `ss -lntp`；`git -C ... status`；`sudo -n -l` | 3000 仅 nodeapp；部署目录干净；白名单仍 9 条（发布不得顺手放宽） | |
| V12 | 状态收口 | 四个状态文件 + 口语稿 | 周计划 D5 勾选、`LEARNING-STATE` 指向 W12、`DEBT` 状态已更新、`BACKLOG` 增条、`day5-english-speaking.md` 入库 | |

### 2.4 回滚（动手前写好卸载路径）

| 层 | 触发 | 动作 |
|---|---|---|
| 文档 | 结论写错 | `git checkout -- <file>`，本地改动未提交前零成本 |
| 展板 | `verify:board` 红、或上板后发现证据等级标高了 | 回退 `w11Facts.ts` / 组件改动，**不放宽断言**；该块退回 `done: false` |
| 发布 | 8081 发布后 V9 / V10 任一不过 | 按 `SHOWCASE-DEPLOY-PROTOCOL.md` 回滚到上一份产物；展板线当天停，主线 A / B / C 不受影响 |
| 服务器 | 出现非预期改动 | 停止展板线，转 `runbook.md` 通用首查 |

### 2.5 止步条件与时间盒（触发即停）

1. **8081 发布后任一面非 200 且一次回滚未恢复** → 停止展板线，转 runbook，D 项记未完成。
2. **`verify:board` 出现红且不是新增断言导致** → 先定位，不改断言放宽。
3. **口述三问答不出** → 如实记未达成并排 W12 第一档，不补看笔记后重答。
4. **时间盒**：A + B 前半天；C 60–90 分钟；展板 ① ⑦ + 发布 90 分钟；E 收口 60 分钟。
   **超时的保留顺序**：A（验收句本体）> B > E（状态收口）> C > D（展板与发布）。**展板 ①⑦ 不在最低交付边界内，可顺延**（G3）。

### 2.6 连带文档

| 文档 | 改什么 | 触发条件 |
|---|---|---|
| `week11-plan.md` | §4 D5 勾选；§3 六条边界逐条结账留痕 | 必改 |
| `LEARNING-STATE.md` | 当前周 / Day、最近完成、下一入口指向 W12 | 必改 |
| `DEBT.md` | 类 2 脚本条目：已还 / 仍待还 + 重建结果 | 必改 |
| `BACKLOG.md` | G7 六项按 P5 的去向增条或更新 | P5 定后 |
| `week11-visualization-plan.md` | §10 排期第 4 批标完成；①⑦ 两节回填实际取值与偏差 | 上板后 |
| `week11-ci/notes/day5-english-speaking.md` | 当天口语稿（`DAILY-SPEAKING-PROTOCOL.md`） | 必建 |

---

## 3. 需要本人拍板的执行期决策（答完冻结，动手前不留空）

### P1（对照说明的粒度与落点）W9 手工发布取哪一份为准？三类归属的判据是什么？说明落在哪个文件？

**为什么问它**：G2。⑦ 的数据源就是这份说明，粒度定得比板细或比板粗，两处会各写一份互不对齐的口径。

**必答追问**：① W9 手工发布的「六步」以哪一份笔记的哪一节为准（`week9-deployment/notes/` 里有多处描述）；② 三类归属各自的判据写成什么可判真假的形态（「主动不交」与「不能交」的分界，Q12 是唯一依据还是还有别的）；③ 说明落进本文件 §5、还是单独一份 `week11-ci/notes/handoff-comparison.md`（单独成篇的代价是多一处要同步的事实源）。

> 答（本人）：

### P2（口述三问的形态与留痕）口述在什么条件下算过？当场修正怎么记才不失真？

**为什么问它**：判据 3。W9 / W10 两次都写了「当场修正计入掌握证据」，但没写「答到什么程度算答出」。

**必答追问**：① 三问是否连问不查资料、每问的时间上限；② 修正记成什么（首答原话 + 修正点 + 触发修正的是什么，还是只记结论）；③ 第三问「改需求预演」两个方向（再加一个部署目标 / 仓库转为私有）**都答还是二选一**——后者与 `BACKLOG.md` P1-8 的前置条件同源。

> 答（本人）：

### P3（延迟重建的排序与「盲」的边界）两项都做还是只做一项？类 2 那条要不要重写脚本？

**为什么问它**：G4 + G5。两项抢时间盒，且两项的对照物不同；类 2 的脚本已删，重建能拿到的只有 §5.3 的文字。

**必答追问**：① 两项都做还是只做一项，另一项顺延到哪（`DEBT.md` 写的是「W11 D5 **或下周**」，顺延本身是合规的，但要写明去向）；② runbook 盲重画的对照物是 8/27 更新后的 §4.1（四面 + `/showcase/`），重画时按哪一版记分——按新版还是按记忆里的五面版（**这一条会直接影响差异计数的含义**）；③ 类 2 第一档重建要不要重写最小样本脚本：重写能拿到可执行证据，但写脚本的过程本身会提示答案，**重写算不算破坏「盲」**；不重写时两项掌握证据取什么。

> 答（本人）：

### P4（8081 发布的通道与时机）本地发布还是远程触发？发布排在最终回归之前还是之后？

**为什么问它**：G6。发布动的是 8081 与 `/showcase/` 两个面，而这两个面同时是本周收口判据的一部分。

**必答追问**：① 走 `deploy-showcase-8081`（本地全链路）还是 `trigger-showcase-deploy`（远程触发，8/27 已有一次常规使用）——若走远程触发，则 **main 必须先合入今天的展板改动**，这一步的时机要排进 §2.2；② 最终回归跑在发布之后（证明的是新产物）还是发布之前（证明的是发布前基线），还是两次都跑；③ 发布失败一次后是否当天再试第二次，还是直接按 §2.5 第 1 条停线。

> 答（本人）：

### P5（六项遗留的收口去向）三项持久遗留 + 三项 stretch，本周收口时各写成什么状态？

**为什么问它**：G7。W10 D5 的口径是「不许沉默消失」，这六项今天不处理是可以的，**但必须各有一个显式去向**。

**必答追问**：① 一次性 `cp` 白名单条目（需 root 才能删）——继续记持久遗留，还是绑定下一次 root 需求；② L55 lighthouse 注释（8/27 复核利用面 0）——同上，且复核结论要不要写进 `BACKLOG.md` 而不只留在 D4 笔记里；③ `authorized_keys` 第 3 行裸装密钥（P1-9 已在册）——本周结束时它的档位是否要调；④ Java / S3 / Docker 三项 stretch：顺延 W12、转 `BACKLOG.md`、还是明确放弃（Q18 已写各自前置条件，放弃也要写依据）；⑤ 展板板头的「待做」计数怎么随之同步（当前三项待做写成节点，见 `LEARNING-STATE.md`）。

> 答（本人）：

### P6（W11 收口判据与 W12 入口）做到什么算本周收口？下周第一入口写什么？

**为什么问它**：G3 + G8。周计划 §3 有六条最低交付边界，但没写「六条之外的当天待办缺了算不算未收口」；`LEARNING-STATE.md` 里 W12 主题与 stretch 顺延是两条并列的线。

**必答追问**：① 六条边界逐条结账（§8 表），缺任一条即未收口这一点是否维持；② 展板 ①⑦ 与 8081 发布**不在六条之内**，今天没做完算不算本周未收口；③ W12 的主线写「AI 使用进阶 + 黑白名单完整审视」，还是先清 stretch；两者并列时哪一条是主线；④ 下周入口那句写在 `LEARNING-STATE.md` 的哪一节，指向哪个文件。

> 答（本人）：

---

## 4. 执行记录（滚动，2026-08-28 当天填）

### 时间盒

| 模块 | 计划 | 实际 |
|---|---|---|
| 前置核对 C1–C5 | 5 min | |
| C 延迟重建 | 60–90 min | |
| A 对照说明 | 前半天 | |
| B 口述三问 | 30 min | |
| D 展板 ①⑦ + 8081 发布 | 90 min | |
| E 状态收口 | 60 min | |

### 前置核对结果（C1–C5）

> 当天填，逐格对 §2.3 的期望栏。

### 验证结果（V1–V12）

> 当天填。期望栏已在 §2.3 写死，此处只填实测与是否一致。

---

## 5. A 对照说明（落点由 P1 定，骨架先摆）

**W9 手工发布的步骤 → 三种归属**

| # | W9 手工那一步 | 现在由谁做 | 归属 | 依据 |
|---|---|---|---|---|
| 1 | | | 被替掉 / 仍需人执行 / 主动不交 / 替掉了但依赖会关机的机器 | |

**两类「没被替掉」的分界**（判据 1，展开写）

- **主动不交**：`nginx -t && reload`、证书续期、ufw 规则。它们是 W9 的收口成果，本周边界（Q7）——**将来可以改**。
- **不能交**：验证不绿要不要回滚，由人判定（Q12）。**它不是额度问题**，与上一类不同源。

**第三类**（判据 2）：controller 装在开发机（Q1 / Q2），关机或休眠即流水线不存在。这一条是「替掉了，但依赖一台会关机的机器」。

---

## 6. B 能力检验：三问口述（当天现场填，形态沿用 W9 / W10 D5）

### 6.1 一次提交从推送到服务器换版本经过哪些层，每层的失败点是什么

> 首答：
> 当场修正：

### 6.2 两条会把坏版本放上线的路径分别是什么（测试假绿 / 部署后验证覆盖不到），各自先查什么

> 首答：
> 当场修正：

### 6.3 改需求预演：「再加一个部署目标」或「仓库转为私有」会先影响哪一层

> 首答：
> 当场修正：

---

## 7. C 延迟重建（当天现场填）

### 7.1 runbook §4.1 速查表盲重画

> 重画产出：
> 逐项对照后的差异计数（漏 / 错 / 多）：
> 差异条目 = runbook 的缺口清单：

### 7.2 DEBT 第一档：类 2 最小样本的构造与收尾逻辑

> 三条复述（不看 `day4-rollback-drill.md` §5.3）：
> 掌握证据两项：
> 结论（已还 / 卡档一次 / 顺延）：

---

## 8. W11 收口结账（P6 第 1 问的答案落这里）

### 8.1 最低交付边界六条

| # | 边界（`week11-plan.md` §3） | 证据 | 状态 |
|---|---|---|---|
| 1 | 一条能从代码提交走到服务器换版本的流水线，四项固定可复现 | | |
| 2 | 发布契约（身份 / 权限 / 凭据落点 / 部署单元 / 不动清单 / 失败判据） | | |
| 3 | 部署后验证复用 W10 证据形态，结果进构建日志 | | |
| 4 | 一次真实回滚演练，留命令输出 | | |
| 5 | 与 W9 手工部署的对照说明 | | |
| 6 | 当前基线公网面全部恢复绿（四面 + `/showcase/`） | | |

### 8.2 六项遗留的去向（P5 的答案落这里）

| 项 | 当前状态 | 去向 | 落在哪个文件 |
|---|---|---|---|
| 一次性 `cp` 白名单条目未收回 | | | |
| `/etc/sudoers` L55 lighthouse 未注释 | | | |
| `authorized_keys` 第 3 行裸装无 `command=` | | | |
| stretch · Java 最小 jar | | | |
| stretch · S3 归档 | | | |
| stretch · Docker 统一构建环境 | | | |

---

## 9. 第二轮 W11 复盘与项目叙述（本人写，AI 只挑毛病）

### 9.1 第二轮复盘

> 本周押错过什么、依据什么改的、哪一条是到执行期才暴露的：

### 9.2 5–10 分钟项目叙述（背景 → 问题 → 做法 → 结果 → **边界**）

> 边界一段必须写：这条流水线证明不了什么。

---

## 10. 今日明确不做

- 不解冻 GitHub Pages（`SHOWCASE-DEPLOY-PROTOCOL.md` 冻结状态维持）。
- 不改服务器任何配置；不收窄也不放宽 sudoers 与 `authorized_keys`。
- 不改业务代码；`server.js` 类 2 修复已在 D4 上线，今天不追加。
- 不改 Jenkinsfile 与 wrapper；本周流水线内容已冻结。
- 不重开 D1 契约的任何一题（Q1–Q18 全部已拍板）。
- 不启动 stretch 三项中的任何一项（除非 A / B / C / E 全部收口且时间盒有余，按周计划 §4 D5 E 的口径）。

---

## 11. 下周入口（W12，由 P6 定）

> 当天填。至少写清：主线主题、第一入口文件、本周顺延过去的项及其前置条件。

---

## 12. AI 辅助记录

- **协作模式声明（`AGENTS.md` §2）**：本日主线 A / B / C 属**导师模式**——三种归属的判据、三问口述、两项延迟重建都在黑名单内（W2 分层职责、W5 Node 底层、测试场景设计），AI 只提问、列候选与 review，欠债记账规则照常。展板 ①⑦ 的组件与数据层、8081 发布链路属**白名单展示资产**，按实现方模式的五项交付标准执行。
- 2026-08-27：AI 起草本文件——引用周计划 §3 / §4 D5、D4 §6 / §8、`DEBT.md` 2026-08-27 条目、可视化计划 §5 ①⑦ 与 §10 排期，搭四要素框架、写验证行与止步线、列 P1–P6。属白名单文档整理。
- **G1–G8 的性质**：AI 读上述文件并在本地核实三项事实——① `w11Facts.ts` 的 `W11_STAGE_PLAN` 里 `rollback` / `false-active` 已 `done: true`，`lanes` / `handoff` 仍 `false`；② 仓库内不存在 `reproduce-close-race.js`（`git log --diff-filter=A` 无新增记录）；③ 本地分支与 `origin/main` 一致——据此报出八处覆盖缺口。AI 的动作限于**指出矛盾与覆盖缺口、列候选、出题**，六题取值全部留空。
- **未代答**：P1–P6 全部待本人作答；对照说明的三类归属、三问的答案、两项重建的内容均未给。
- **起草期未触发 `DEBT.md` 记账**（白名单文档整理 + L1 事实核对）。执行期如再对黑名单知识点给到 L2，按规则当天记账。**注意**：`DEBT.md` 2026-08-27 的类 2 脚本条目**仍为「待还」**，§7.2 是它的重建入口。

---

## 13. 收尾清单

- [ ] P1–P6 本人作答并冻结（动手前）
- [ ] 前置核对 C1–C5 完成，无空格
- [ ] 四要素（改动清单 / 验证 / 回滚 / 止步）本人核对
- [ ] C 延迟重建按 P3 的排布执行，差异计数与「是否翻过对照物」如实记录
- [ ] A 对照说明成篇 = 验收句达成（判据 1 / 2）
- [ ] B 三问口述完成，当场修正逐条留痕（判据 3）
- [ ] 展板 ① 上板（V5）；⑦ 上板或按 §2.5 顺延并写明去向
- [ ] `yarn typecheck` / `build:showcase` / `verify:board`（计数 > 934，断言未放宽）
- [ ] 8081 发布 + 线上验证（V9）+ 公网面回归（V10）+ 残留核零（V11）
- [ ] W11 结账表 §8.1 六条逐条填；§8.2 六项遗留各有去向（判据 7）
- [ ] 第二轮复盘 + 5–10 分钟项目叙述（含边界段）
- [ ] 技术英语口语稿 `day5-english-speaking.md`（`DAILY-SPEAKING-PROTOCOL.md`）
- [ ] `week11-plan.md` §4 D5 勾选、`LEARNING-STATE.md` 指向 W12、`DEBT.md` 状态更新、`BACKLOG.md` 增条
- [ ] 本周新增脚本与文档入库 commit
