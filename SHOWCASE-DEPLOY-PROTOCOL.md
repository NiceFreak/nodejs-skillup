# 学习展板构建与部署规范

本文件定义学习展板从仓库事实到发布目标的通用发布边界。它供不同 AI 会话、电脑端和移动端共同使用；具体项目命令、路径和站点约束由对应 deploy skill 补充。

## 0. 当前发布目标（2026-08-14 起）

**GitHub Pages 上传已冻结。** 未经本人在当次会话中明确解冻，任何 AI 会话不得向 `nicefreak.github.io` 复制产物、提交或推送。

| | 状态 |
|---|---|
| 发布目标 | 服务器 `http://43.128.154.242:8081`（`shop-showcase` 站点，`dist-showcase/`，带登录门禁） |
| GitHub Pages `/skillup-week8/` | **冻结**：不再更新。线上停留在 W9 之前的版本，暂不撤下 |
| 冻结的是什么 | 只冻结「上传 / 复制产物 / 提交 Pages 仓库 / 推送」。**构建不冻结**——`VITE_SHOWCASE_ONLY=1` 构建仍是本地验证的必要手段，8081 的产物走的也是同一条构建，只是 base 不同（Pages 用 `--base=/skillup-week8/`，8081 用空 base） |

**冻结理由**（两条，缺一条这个决定都不完整）：

1. 已有自建服务器承载同一份展板，且 8081 带登录门禁；Pages 是公开、无门禁的第二份副本，继续更新等于维护两个发布目标。
2. 与既有边界同向：W9「部署上线」板设为 `reviewOnly`、W9 七份笔记设为 `reviewOnly`，理由都是「一台在跑的服务器的拓扑与排障判据不进对外展示」。冻结公开 Pages 是同一条边界的第三次应用。

**解冻条件**：本人在当次会话中明确说「解冻 Pages」或等价指令，并同时确认要发布的内容不含 `reviewOnly` 资产。解冻后仍按本文件 §4 全流程执行。

**解冻时必须先解决的一件事**（2026-08-14 核对时发现）：D4-c 给展板加了登录门禁之后，showcase 入口已经**不再零后端**——`AppShowcase.tsx` 引入了 `api.ts` 的 token，登录走后端 `/auth`。8081 由 Nginx 把 `/auth` 反代给 3000，所以门禁在那边是通的；而 Pages 没有后端，门禁将无人能登入，展板等于打不开。也就是说 Pages 现在即使解冻也发不出一个可用的版本，除非先决定门禁在纯静态目标上怎么处理。这条与冻结决定无关，是冻结期间顺带核出来的事实。

**线上旧产物**：`nicefreak.github.io/skillup-week8/` 现存内容按 2026-08-14 决定**暂不动**。它停在 W9 之前的版本，会随时间越来越旧——需要撤下时，那是一次独立的授权动作。

## 1. 使用口令

AI 已恢复仓库状态时，可以直接说：

> 更新并部署学习展板。

冻结期内（见 §0），这句口令默认指向**服务器 8081**，不指向 GitHub Pages。若本意是 Pages，必须显式说「解冻 Pages」。

新会话尚未读取仓库时，使用：

> 请先读取 `AGENTS.md`、`LEARNING-PROTOCOL.md`、`SHOWCASE-DEPLOY-PROTOCOL.md`、`LEARNING-STATE.md`、当天笔记和对应 deploy skill，再更新学习展板。

“更新”“构建”“部署”“提交”“推送”“开 PR”和“合并上线”是不同权限层级。没有得到明确授权时，只执行请求范围内的步骤。

## 2. 事实来源

开始前按顺序读取：

1. `AGENTS.md`、`LEARNING-PROTOCOL.md` 和本规范。
2. `LEARNING-STATE.md`、当前周计划、当天笔记及其直接引用的验证证据。
3. 对应 deploy skill、前端构建配置和 GitHub Pages 仓库当前状态。
4. 两个仓库的 `git status --short`、当前分支和与本次任务相关的 diff。

展板只能发布已经学习或明确标为“待验证”的内容。推断、作废数据和未完成主题不得伪装成已验收结论。

## 3. 发布不变量

以下各条对**任何**发布目标成立。写着「Pages」的几条在冻结期内不适用，但解冻后立即恢复。

- 静态发布版本必须能在无后端条件下完成其承诺的功能；需要 API 的管理功能不得混入纯静态入口（8081 是例外中的合规形态：它的 `/auth` 反代只服务登录门禁，展板本体仍零后端依赖）。
- 构建 base 必须与实际承载路径一致，静态资源不得指向开发机或错误根路径。Pages 用 `/skillup-week8/`，8081 用空 base——两者不可混用，混了就是整站资源 404。
- 源码仓库保存源码和内容，不提交其 gitignored 构建目录（`dist`、`dist-showcase`、`dist-admin443`）。
- `reviewOnly` 资产（W9 展板、W9 笔记、面试材料）不进任何公开无门禁的发布目标。这条是 §0 冻结决定的实质内容，不随冻结与否变化。
- 不直接推送或合并 `main`；是否 commit、push、开 PR 或 merge 必须遵循用户当次授权。
- 发布分支必须明确；功能分支构建成功不等于线上已经更新。
- **Pages（冻结期内不适用）**：Pages 仓库只保存发布产物和必要的入口说明；不覆盖 Pages 目录中明确要求保留的人工文档。

## 4. 标准流程

### 4.1 确认范围

1. 说明本次变更来源、目标页面和是否涉及布局或交互。
2. 区分源码更新、仅重新构建、复制发布产物、提交、PR 和上线。
3. 若展示内容依赖后端或发布目标不明确，停止部署并说明缺失条件。

### 4.2 发布前检查

1. 检查静态展示入口是否新增 `fetch`、API client 或其他运行时后端依赖。
2. 检查本次内容中的事实、数字、状态标签和引用路径。
3. 检查构建环境变量、base、路由模式和目标目录。
4. 对任何删除或覆盖操作，先解析并验证源目录、Pages 仓库根目录和目标子目录；不得对未验证变量执行递归删除。

### 4.3 构建与验证

1. 使用锁文件对应的包管理器安装依赖。
2. 运行 typecheck 和生产构建；警告若会影响发布，也必须处理或记录。
3. 验证输出 `index.html`、页面标题、hashed assets 和子路径引用。
4. 本地预览最终发布目录，检查默认入口和关键深链。
5. 内容或布局变化时至少检查一个桌面和一个移动视口；确认无空白、遮挡、横向页面溢出或不可操作控件。
6. 只有内容未变且构建产物结构未变时，才可省略重复视觉检查，并在结果中说明依据。

### 4.4 同步到发布目标

**服务器 8081（当前目标）**：构建产物落到服务器由本人执行的操作链完成——AI 出命令、本人核输出。属服务器操作，遵守权限速查表（`week9-deployment/notes/server-permission-cheatsheet.md`）：落盘目录属主是 `nodeapp`，一律 `sudo -u nodeapp`；scp 的目标目录必须先建。发布本身仍是独立授权动作。

**Pages（冻结期内不执行，解冻后恢复）**：

1. 先验证 Pages 仓库身份、当前分支和目标子目录。
2. 只替换生成产物；保留约定的 README、手写说明和其他站点内容。
3. 检查 Pages 仓库 diff，确认没有删除无关页面、泄露环境变量或混入源码仓库的临时文件。
4. 源码仓库与 Pages 仓库分别记录变更，不跨仓库混合提交。

### 4.5 外部操作

- 用户只要求“更新/构建”时：停在本地验证完成。
- 用户明确要求“提交”时：只提交与任务相关的文件。
- 用户明确要求“推送/开 PR”时：推送指定功能分支并创建对应 PR。
- 用户明确要求“上线”时：说清发布目标是 8081 还是 Pages、需要哪些前置条件；未经授权不代替用户合并或推送产物。
- 冻结期内收到指向 Pages 的部署请求：**先停下确认**，不默认按 8081 执行，也不默认解冻。
- 远程触发发布（`trigger-showcase-deploy` skill）：见下方 §4.5.1。

#### 4.5.1 远程触发发布的授权口径（2026-08-26 冻结，变更单 D1）

手机侧向 `ops/showcase-deploy` 分支写一条带 `requestId` 的 `trigger.json`，**该写入动作本身即为对这一次发布的授权**，不再另行确认。

**依据**：`requestId` 形如 `<UTC 时间戳>-<main sha7>`，与回执文件一一对应，且写入以一条 commit 留在 GitHub 上。相比本地脚本的「本人回车」，它多了可追溯的时刻、发起方与内容摘要，审计性更强而不是更弱。

**这条授权的范围只到「什么时候发」，不到「发什么」**：

- 发布内容固定取 `origin/main`。`trigger.json` 的 `targetRef` 按 schema 恒为 `main`，不是可配置项。
- pipeline 定义存放在 Jenkins 内，不从触发分支读取。因此能写触发分支的人无法让开发机执行任意脚本。
- `mainSha` 字段只作审计记录，幂等判断以 Jenkins 自己解析的 `origin/main` 为准，不采信触发方提供的值。

**成功判据只有回执**：`receipts/<requestId>.json` 的 `status` 为 `succeeded`。手机侧会话连不到 8081（出站被网络策略挡住，实测 `curl --max-time 12` 返回 `code=000`），因此「已触发」永远不等于「已发布」，没有回执时只能报「已触发，回执未到」。

**前置条件**：开发机需醒着。休眠期间轮询不运行，触发信号会滞留在分支上，表现为回执超时（变更单 D6；2026-08-26 曾以此形态实际发生过一次）。

**边界**：本条只覆盖 8081。冻结期内 Pages 不因本条获得任何授权。

**证据**：变更单 §4.2 九条可证伪验证全部达成，其中验证 4 / 5 / 7 / 8 由手机侧写信号复现（构建 8 / 10 / 12，全程无人操作 Jenkins），明细见 `week11-ci/notes/change-order-showcase-remote-trigger.md` §9.5′。

## 5. 完成证据

交付时至少报告：

- 变更了哪些展示内容。
- typecheck、build 和静态依赖扫描结果。
- 桌面 / 移动验证是否执行；若省略，说明原因。
- 涉及的每个仓库分别有哪些 diff、commit、分支或 PR。
- 当前线上是否已经更新；若没有，明确还差哪一步（8081 是产物落盘 + reload，Pages 是 push、PR 合并或 Pages 构建）。
- 走远程触发链路时，报告 `requestId` 与回执的 `status`、`deployedSha` 及 `checks` 各项取值。**没有回执就不报「已发布」**（§4.5.1）。

若任一步失败，不把“源码已修改”“本地构建成功”或“功能分支已推送”表述为“部署完成”。
