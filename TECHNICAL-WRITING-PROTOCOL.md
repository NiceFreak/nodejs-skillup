# 技术学习文案规范

本规范约束仓库中的用户可见技术文案，包括每日学习笔记、周计划与复盘、学习展板、runbook 摘要和验收说明。

目标是让读者快速识别技术对象、事实证据、结论与边界。它不要求把内容写得简短或口语化，而是要求每句话都承担明确的信息职责。

---

## 1. 核心表达顺序

优先按以下顺序组织一个结论：

```text
技术对象
→ 观察到的结果或证据
→ 可以支持的结论
→ 不能支持的结论、待验证项或下一步
```

示例：

```text
四项检查对三类故障产生 8 个实测结果，全部为绿，另有 4 格未复测。
这说明真实故障不一定触发现有检查的红色结果；不能把 8/19 的假输入红态验证当作端到端故障验证。
```

不要用悬念、比喻或情绪先占据读者注意力，再让读者自己还原技术对象。

```text
不推荐：九次判决里指对方向的只有 2 次，而且是同一把刀给的。
推荐：三个定位信号共做出 9 次判断；只有服务器内直连健康检查口的 2 次判断定位正确。
```

---

## 2. 事实纪律

文案优化不得改变事实强度。修改前先锁定以下内容：

- 日期、时刻、数量、单位、状态码、退出码和命令输出。
- `已实测 / 已拍板 / 待做`，以及 `事实 / 推断 / 待验证` 的等级。
- 历史计划、当前状态与后来决策之间的时间顺序。
- 技术机制成立的完整条件，不能为了短句删掉限定词。
- “没有执行”“执行后未触发”“不在职责范围”三种不同状态。

具体规则：

1. 未保留原始证据时，不写成已实测。
2. 只验证判据时，不扩大为已验证真实故障链路。
3. 根因方向已确定但机制未复现时，继续标为待验证。
4. 历史记录中的未来时态在阶段结束后必须补充实际结果，不能让“计划在 D5 做”长期冒充当前状态。
5. 相对时间只用于明确的当日记录。跨日展板、runbook 和状态摘要使用 `8/20`、`D4` 等稳定锚点，不写孤立的“今天、昨天、明天”。
6. 数据由代码计算时，文案继续使用计算结果，不手写可能漂移的数字。

技术机制不得被过度压缩。例如：

```text
不准确：location 级配置覆盖 server 级配置。
准确：location 内只要定义任一 proxy_set_header，就不再继承 server 级的整个同类指令集。
```

---

## 3. 语言约束

### 3.1 直接写技术对象

标题、标签和结论应点名真实对象：请求、进程、端口、反代、检查脚本、日志字段、阈值、证据等级。

避免用以下词替代技术概念：

```text
刀、劈、断口、闸、绿灯、判决、说谎、接力、销账、过夜、站出来说话
```

`红 / 绿` 可以作为已经定义的检查状态使用，但不能扩写成拟人化叙事。

### 3.2 一句话只承担一个主要判断

- 标题说明对象与结论，不制造悬念。
- 表格标签使用名词或明确动作，如“观测对象”“未触发条件”“验证证据”。
- 一个复习问题只考一个设计点；需要多个判断时拆成独立问题。
- 句子过长时按事实、原因、边界拆开，不依赖破折号串联多层意思。

### 3.3 删除表演性旁白

默认删除以下表达：

- “先自己答”“真正值钱的是”“最危险的是”“当场打脸”。
- “不是 X，而是 Y”式虚设对比。
- 为普通事实添加引号、感叹号或情绪评价。
- 只描述版面或写作动作、不增加技术信息的句子。

存在真实且容易混淆的技术对比时，可以保留“不是 X”。例如 `available 不是 free` 明确区分了两个实际字段。

### 3.4 英文只作准确分类

英文 kicker 应使用可翻译的技术类别，如 `request correlation`、`check results`、`evidence status`。不要使用口号、悬念或文学化英文来装饰中文内容。

### 3.5 归纳收拢不得超过材料

文案中的每个词都应能在原文、数据或已确认的事实中找到对应。读者应先核对内容，再评价句式。总结、结论条、复盘标题和展板 10 秒结论可以把零散点收拢成一个方向，但不得为句式工整引入原文没有的计数、映射或完备性。

```text
不推荐：三个错误 = 两类根因 + 一个共性。
推荐：三个错误分别追到两类根因，另有一个共性。这只覆盖这次遇到的三个，
      不能推出系统只有两类根因。
```

判定规则：

- 原文只列举了若干例子时，不把结果写成带计数与映射的等式。计数必须来自原文或数据，不能为对称或节奏补一个。
- 排比、对仗或等式暗示全集、唯一对应或完备分层而材料不支持时，删掉对称，只保留材料支持的部分。
- 素材本身开放或尚未验证时，结论保留开放表述，说明「这是当前列出的一部分，不是全部」，或省略全集断言。

---

## 4. 每日学习笔记

每日笔记首先是事实与推理记录，不是故事、战报或宣传稿。继续遵守 `LEARNING-PROTOCOL.md` 的既有结构，并对每个关键问题至少写清：

```text
目标：要证明什么
操作：实际做了什么
观察：命令、日志或行为显示什么
结论：证据支持什么
边界：仍未证明什么
```

记录错误或预测偏差时使用：

```text
原判断
→ 实际现象
→ 关键证据
→ 偏差类型或根因状态
→ 修正与待验证项
```

每日笔记不得：

- 把执行顺利写成“机制已掌握”。
- 把读代码得到的方向写成运行机制已验证。
- 把工具经验包装成可以从原理推出的结论。
- 为了形成完整叙事而补齐没有发生的步骤。
- 用大段感受性总结替代命令输出、状态变化和取舍理由。

允许保留个人感受，但应单列为复盘，不与事实证据混写。

---

## 5. 学习展板

展板用于扫描、比较和复习，应比每日笔记更压缩，但不能牺牲事实边界。

1. 专题标题直接回答“这页比较什么”或“实测结果是什么”。
2. 首屏先给对象、样本数和结论，再提供机制与原始证据。
3. 数字结论优先由数据计算，并由图形或结构断言交叉保护。
4. 图表标签必须能脱离颜色理解，不只写“好 / 坏”或依赖隐喻。
5. 复习门只隐藏答案，不使用“揭晓、第一刀、先猜”等表演性措辞。
6. 可见术语改变时同步修改 `aria-label`、按钮 accessible name 与说明文字。
7. 文案测试应守稳定事实或结构，不为某个修辞片段建立无意义的精确断言。
8. 纯文案修改仍需检查桌面和手机宽度下的溢出、截断与阅读顺序。
9. **事实完整优先于精简，但不优先于编码**（2026-08-25 补）。限定语的正确落法是与图相邻的短句
   或格内标注，不是把结论退回段落。确实无法与图相邻时，按
   `SHOWCASE-VISUAL-PROTOCOL.md` 的文字层级处理并注明理由。
10. 新建或改建一块板前先按 `SHOWCASE-VISUAL-PROTOCOL.md` 产出十列设计契约。
    本规范约束句子怎么写；视觉规范约束结论由什么承载、首屏如何组织以及用什么证据验收，两者同时成立。

---

## 6. 自检清单

新建或修改学习文案后，逐项检查：

- [ ] 标题是否点名技术对象和实际问题？
- [ ] 读者能否区分事实、推断、决定和待验证项？
- [ ] 日期、数字、单位、状态码和证据等级是否保持？
- [ ] 是否把局部验证扩大成端到端结论？
- [ ] 是否删掉了技术机制成立所需的限定条件？
- [ ] 是否存在失效的“今天 / 明天 / 补进 D5”等相对时态？
- [ ] 是否用比喻、拟人或口号替代了技术名词？
- [ ] 是否存在无必要的引号、长破折号或“不是 X，而是 Y”？
- [ ] 是否把举例性列举写成带计数或等式的封闭断言？对仗、排比或等式是否比材料更精确？
- [ ] 每个复习问题是否只包含一个设计点？
- [ ] 展板可见术语与 ARIA、测试断言是否同步？
- [ ] 本页同类对象是否 ≥3 个？若是，结论是否由非文字编码（位置 / 长度 / 对齐 / 矩阵）承载？
- [ ] 默认态首屏是否出现完整结论锚，而不是只有板头、导航或连续正文？
- [ ] 主路径屏数与字数是否落在视觉规范护栏内？若超出，是否记录不可下沉的限定语与拆页判断？
- [ ] 视觉记忆点是否来自技术关系，图标与动效是否各自承担明确语义？

如果精简与事实完整发生冲突，优先保留事实完整；通过拆句、表格或渐进展开降低阅读负担，不删除证据边界。

**这一条不适用于「是否使用非文字编码」**（2026-08-25 补）。事实完整从来不要求结论以段落形式呈现：
限定语与证据边界可以写在图旁、格内或折叠区，图形本身仍应承担结论。
把「不能牺牲事实边界」用作「所以只能写成段落」的理由，是对本条的误用。

---

## 7. Review 收口

技术文案 review 沿用 `AGENTS.md` 的阻断性问题标准：

- 事实、数字、时点、证据等级或技术机制漂移，属于阻断性问题。
- 隐喻导致读者需要反向推断技术对象，属于阻断性问题。
- 把举例性列举写成带计数或等式的封闭断言，让读者相信不存在的全集或精确映射，属于阻断性问题。
- 仅有更自然的措辞选择，但当前表达准确且无实际阅读代价，属于锦上添花或不列入发现。

完成标准不是“所有句子都能继续润色”，而是事实准确、边界清楚、主要阅读路径不再依赖修辞解码。
