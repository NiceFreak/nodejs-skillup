@AGENTS.md

# Claude Code 项目入口

执行任何仓库任务或委派前，完整读取 `LEARNING-PROTOCOL.md`，并按其中顺序恢复当前会话。开始执行前读取
`LEARNING-STATE.md` 列出的文件；不得根据摘要或局部内容推断未读部分。

第一次执行有副作用的操作前，完成 `LEARNING-PROTOCOL.md` 定义的一次性会话启动检查。普通笔记或代码编辑前
不重复检查。项目 permission 与 sandbox 配置见 `.claude/settings.json`。
