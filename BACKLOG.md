# BACKLOG — 7/31 后的候选任务与优先级

> 建立：2026-07-29。本期硬截止 **2026-07-31**，本文所有条目一律 **7/31 后** 再评估。
> 在此之前不启动任何一条——剩余两天只做 W6 最终总结（见 `LEARNING-STATE.md`「当前主线」）。

本文是 backlog 的**唯一优先级入口**。条目详情仍留在各自来源文件里，这里只做一句话描述、
规模估计和排序理由，避免两处描述漂移。

## 排序口径（功利优先，2026-07-29 确定）

| 档 | 判据 | 说明 |
|---|---|---|
| **P0** | 能直接作为面试加分项 | 能变成一句具体的、带数字或带边界的话；要么补当前短板，要么是别人讲不出的差异化项 |
| **P1** | 有助于投入真实项目工作 | 工程习惯、契约、可观测性、部署，能迁移到日常工作 |
| **P2** | 纯粹深度挖掘原理 | 认知上有价值，但对面试和工作的边际收益低 |

排序还叠加两个修正：**当前是短板的加权**（补短板 > 锦上添花），**已有资产可复用的加权**
（数据、脚本、笔记已就位的，单位投入产出更高）。

规模按 `AGENTS.md` 的 15–20 分钟小单元估：**小** = 1–2 单元，**中** = 3–6 单元，**大** = 需要单独排期。

---

## P0 · 面试直接加分

### P0-0 · 过一遍 DB 自测稿 —— 规模：小（半小时，**建议排在 P0-1 之前**）

打开 `interview-prep/db-review-sheet.md`，先只看第 0 节那 10 题自测清单，合上答案自己讲一遍，
答不出的才回去看对应小节。过完把 `interview-prep/backend-qa-sheet.md` 第 5 节和顶部强项分层表的
DB 从 ⚠️ 调到 ✅。

来源：`interview-prep/db-review-sheet.md`（2026-07-20 建立）；2026-07-30 核对确认**尚未自测**。

**为什么放在 P0 最前**：它是本文里**投入最低、且是其他 DB 条目前置**的一条。
材料已经写完了，不需要写代码、不需要跑实验，纯粹是把已有的东西过一遍。
在它完成之前，DB 在面试里只能按「做过但生疏、不主动挑起」处理——
也就是说 P0-1 把定性升级成数字之后，如果这一步没做，收益仍然拿不满。
编号用 P0-0 而不是插队重排，是因为 `week3-mongoose/notes/week3-plan.md` 已经引用了 P0-1。

### P0-1 · explain 建索引前后的耗时量化脚本 —— 规模：小

对 `orders.userId`、`orders.status + createdAt` 建索引前后各跑 `explain("executionStats")`，
把 `executionTimeMillis` / `totalDocsExamined` 前后对照打印出来。

来源：`week3-mongoose/notes/week3-plan.md` Backlog 第 1 条。

**为什么排第一**：这是当前投入产出比最高的一条。
`interview-prep/backend-qa-sheet.md` 把数据库标为 ⚠️「做过但未复盘固化」，并明确写了
「面试前若不复盘，就别主动挑起 DB 细节」——这是你三块强项里唯一的短板。
而 seed 数据（2000 用户 / ~4500 订单，含幂律复购与大促尖峰）**已经造好了**，
`week3-mongoose/notes/` 的定性对照（`collectionScans 3 → 0`）也已经做完。
只差把定性变成数字。做完之后你在面试里能说的从
「我看 `collectionScans` 和 `indexesUsed` 判断有没有走索引」
升级为「这个查询建索引前 `totalDocsExamined` 是 X、耗时 Y ms，建完变成 A / B」——
后者是能压住追问的答法，前者不是。

顺带修 `week2-express/src/match-index-explain.js` 不可运行（`LEARNING-STATE.md` 已记为非阻断）——
它和这条是同一块地方，一起做。

### P0-2 · 单 Agent Harness Lab —— 规模：中（按 spike 收口）

本地只读的单 Agent Harness，用现有前后端观察 tool call、trace、停止规则与 verifier。

来源：`README.md` backlog；方案见 `week7-ai/notes/single-agent-harness-lab-plan.md`。

**为什么在 P0**（2026-07-29 本人补充后定档，此前误列 P1 且档位待定）：
它不是一个孤立的技术玩具——本人工作中做的是 **AI 辅助生成自动化测试平台**，
这个 Lab 是对该平台**二期工作的技术展望**。这个锚点改变了它的性质：
不是「我业余搭了个 agent demo」，而是「我在做的这个平台，二期要解决 X，我先做了预研验证」。
后者是能主动抛出、并且能一路讲到工程取舍的题目，属于本 backlog 里天花板最高的一条。

**建议按 spike 收口，不按 Lab 收口**：`README.md` 里记的「场景和核心 loop / eval 契约尚未确定」
仍然成立，而完整 Lab 是「大」规模。但拿到面试价值**不需要做完**——
先锁定二期里一个具体问题（例如「生成的测试怎么判定可信」「什么时候该停止重试」），
只围绕它做最小 harness，把 tool call / trace / 停止规则 / verifier 四件事各验证一遍即可。
规模从「大」降到「中」，叙事完整度基本不变。**先定这个问题，再动手。**

### P0-3 · TypeScript 迁移练习（auth 竖切）—— 规模：小到中

把 auth 竖切迁移为 TS，用类型把 W4 的契约（service 返回形状、`req.auth`、错误收窄）再验收一遍。

来源：`README.md` backlog「TypeScript 迁移练习」（2026-07-15 决策，含完整理由与做法）。

**当前真实状态**（2026-07-29 本人澄清）：**工作中能用 TS**，
但如果面试被追问，需要提前查资料准备。所以缺口不是「不会」，
而是**「能用」和「能讲」之间的那一段**——这也是为什么规模比原估计小：
不需要学语法，需要的是把隐性使用经验变成显式的、能自圆其说的类型判断。

**因此调整练习重点**：不要停在「让它编译通过」。迁移时刻意挑面试会问的点各踩一遍——
`unknown` vs `any` 的边界、类型收窄怎么替代运行时断言、泛型在 repository 层怎么用才不过度、
`satisfies` 解决什么问题、以及为什么 `req.auth` 这类扩展要用声明合并而不是断言。
7/15 的方案本身仍然成立（迁移一段能空白重建的代码，认知负荷全落在类型上），
前置条件「auth 相关欠债已还清」看 `DEBT.md` 也已满足（①–⑧ 全部已还）。可以直接开工。

---

## P1 · 有助于真实项目工作

### P1-1 · Decimal128 → DTO / 序列化层重构 —— 规模：小

`totalAmount` 的 `Decimal128 → Number` 转换目前散在 service 层，收敛到 DTO / 序列化层。

来源：`week3-mongoose/notes/week3-plan.md` Backlog 第 3 条（Day 5 遗留，当时记档为「没吃透」）。

**为什么排 P1 第一**：它表面是重构，实际喂的是你**最强的那张牌**——分层判断力。
「金额精度在 Mongo / Node / JSON 之间怎么不丢，转换该放哪一层」是个能体现资深度的问题，
而且它正好把 W3 里你自己标记为「没吃透」的一处补上。小投入，直接加固主叙事。

### P1-2 · 后端上线 + CORS —— 规模：大

选免费额度（MongoDB Atlas M0 + Render）或付费轻量，后端加 CORS 中间件，
前端设 `VITE_API_BASE` 重构建，让管理后台真链路上线。

来源：`README.md` backlog「后端上线 + CORS」（2026-07-24 提出，含选型）。

**为什么是 P1 而不是 P0**：部署、环境变量、跨域配置是最贴近真实工作的一条，
但**CORS 的讲解价值几乎已经免费拿到了**——`week6-testing/notes/day3-fullstack-integration-validation.md`
和 `week8-fullstack/notes/deploy-pipeline-talk.md` 已经写清了同源策略、预检、以及
dev proxy 为什么把它藏起来。面试要的是能讲清，这部分不做部署也能答。
所以这条的增量收益主要在「真的上过线」的工程经验，不在面试话术。规模也确实大。

### P1-3 · 报表错误响应信封统一 —— 规模：小

`{ error }` 旧信封迁到 `{ code, message }`，前端 `readErrorMessage` 随后可简化。

来源：`week8-fullstack/README.md` Backlog；`LEARNING-STATE.md` 当前风险已记为非阻断。

**理由**：契约一致性，典型的真实项目收尾工作。做完顺带能在面试里说清「为什么错误响应要统一信封、
兼容期怎么过渡」。

### P1-4 · 测试异常清理路径加固 —— 规模：小

`dropDatabase()` 或 `disconnect()` 抛错时，后续资源释放和 `JWT_SECRET` 恢复会被跳过。

来源：`LEARNING-STATE.md` 当前风险第 1 条。

**理由**：真实的测试基建健壮性缺陷，正常路径不阻断但会在 CI 偶发失败时咬人。
`try/finally` 层面的小改动，属于该有的工程习惯。

### P1-5 · 401/403 日志噪音与服务端原因日志 —— 规模：小

预期 403 目前会输出完整错误堆栈造成日志噪音；401/403 缺服务端侧的拒绝原因记录。

来源：`LEARNING-STATE.md` 当前风险第 2 条与「已知非阻断遗留」。

**理由**：可观测性卫生。预期内的受控拒绝不该和真异常长得一样——这个区分本身也是能讲的点。

### P1-6 · 登录失败限流 —— 规模：中

登录失败次数限流（涉及 Redis 或等价存储）。

来源：`week6-testing/notes/day1-auth-flow-integration-testing.md` 第 141 行，
当时明确判定为「有价值的安全 backlog，不属于保护当前已有行为的测试补全」。

**理由**：真实生产安全需求，也是认证话题里常见的追问（「怎么防暴力破解」）。
但它是**新子系统**（引入 Redis），成本比其他 P1 条目高一档，所以排在这里而不是更前。

### P1-7 · `$lookup` 子管道优化 —— 规模：小

给关联查询的 `$lookup` 加子管道 / 提前 `$project` 裁字段，观察对 `docsExamined` 的影响。

来源：`week3-mongoose/notes/week3-plan.md` Backlog 第 2 条。

**理由**：真实的查询优化手段。建议**跟在 P0-1 之后顺手做**——同一套 explain 方法论、
同一批 seed 数据，边际成本很低。单独拎出来做则收益一般。

---

## P2 · 原理深挖

### P2-1 · 知识点 4 的 Stream 内存实测 —— 规模：小（分档可拆）

用真实文件测 `readFile` 整块 vs `createReadStream` 流式的内存峰值与首字节时间，
把展板知识点 4 从「判断模型 / 相对示意」升为「本人实测」。

来源：`week5-nodejs-internals/notes/day4-stream-backpressure.md` 第 88 行未测量边界；
展板 `w5Topics.ts` 知识点 4 的 `boundary`；实验设计与分档见
`week8-fullstack/notes/visualization-optimization-roadmap.md`「下一执行入口」第 2 条。

**为什么是 P2**：概念部分（该看 `heapUsed` / `external` / `arrayBuffers` / RSS、
Buffer 不等于 V8 heap、整块的风险来自大小 × 并发重叠）在 day4 笔记 §1.2–1.3 已经写完了，
你现在就能讲。补实测只是把标签从「模型」换成「实测」——面试里不会有人追问你的 RSS 具体数字，
真实项目里也只在遇到大文件场景才用得上。**它的定位就是把已有判断落成证据，这正是「纯深度」。**

**唯一有面试价值的副产品**：`fs.readFile` 有 2 GiB 硬上限（`ERR_FS_FILE_TOO_LARGE`），
和 `buffer.constants.MAX_LENGTH`（8 PiB）不是一回事——这是条具体、少有人知道的生产边界。
用 `truncate -s 3G` 建稀疏文件几秒就能验证，占 0 字节真实磁盘。
**如果时间只够做一点，就只做这一条验证**，其余分档可以不做。

注意：属 `AGENTS.md` 黑名单（W5「流与背压 demo」），AI 援助上限 L2，脚本由本人实现和运行。
要 L2 骨架会触发 `DEBT.md` 记账 + 一次延迟重建；停在 L1（测什么、坑在哪、怎么验证）不记债。

### P2-2 · 认证主链改真泳道 —— 规模：中

认证代表页顶部四列 lanes 目前是装饰性的，消息行没有对齐到那四列，实际是列表而非泳道。

来源：`week8-fullstack/notes/visualization-optimization-roadmap.md` 第四轮「明确不做」。

**为什么是 P2**：审计判定为剩余最大的表达力收益，但属于**代表页布局重写**。
而 `week8-fullstack/README.md` 已经写明「前端不作为验收重点」，
`LEARNING-STATE.md` 也把它按砍项顺序排在第一位。对面试和工作都不产生增量。
2026-07-29 的逐帧改造已经让这一页的可理解性上了一档，这条的紧迫性进一步下降。

### P2-4 · **八块板**按加严后的验收判据复核 —— 规模：**大**｜**已单开一条线**（2026-08-20）

**执行文档**：[`showcase-audit-line.md`](./week8-fullstack/notes/showcase-audit-line.md)。
本条只保留优先级与档位，具体范围表、判据、工作单元与进度记录都在那条线里，两处不重复描述。
**这条线不与学习主线同步开展**：它排队不排期，只在当天主线达到止步条件、且还有一个 15–20 分钟空档时推进（`LEARNING-PROTOCOL.md` §7）。

**做什么**：2026-08-18 把展板验收判据从「不读完整段落也能判断」加严为
**「遮住这块板的标题与结论段，仍能答出它的验收句」**，并把「图形只负责关系」改成
**「图形负责结论本身」**（见
[`visualization-optimization-roadmap.md`](./week8-fullstack/notes/visualization-optimization-roadmap.md) 第八轮）。

**2026-08-20 范围修正**：本条原来只写 W9 板十三块。本人指出「不要过分追求动效」那句的波及面更广，
核对后成立——8/18 的落地范围只覆盖 W10（返工）+ W9（延后），**其余各 tab 的板从未进入过新判据的范围**：
它们的组件最后一次改动**都停在 2026-08-12**，是在旧规则「图形只负责关系」下建成的；
8/18 的「存量清扫」(`d24038b`) 只推了 `styles.css` 的排版结论，**没碰内容形态**。
故复核对象 = **八块板**（`Auth` / `OAuth2` / `W3` / `W5` / `W6` / `W6Day4` / `W9` / `W10`），
证据与范围边界见 roadmap 第八轮「落地范围的补正」与核查线 §2。
（首次列范围时误把 8080 管理后台的 `Dashboard.tsx` 算进来、漏了 `OAuth2Panel.tsx`，当日更正。）

**规模从「中」升到「大」**，因此拆两阶段，**不许跳过第一阶段直接开改**：
1. **阶段一 · 只判定不改**：八块 × 每块一个 15–20 分钟单元，记「欠什么、值不值得改」。
   **视觉编码那一维八块已判完**；阅读负担那一维已有全量实测（见下），但三问的判断仍是人闸。
2. **阶段二 · 按判定结果返工**：改哪几块、改到什么程度，一次性决定；大概率不必八块全改。
   **2026-08-21 已开始**，执行范围冻结为核查线 §5.1.1 的四步排序。
   **第 1 步（折叠常驻外壳）已完成**：`W9` 常驻外壳 −64%、`W10` −56%，两块板的专题正文起点
   都进了第一屏；内容一字未删、专题正文一行未动；`verify:board` 421/421 与基线同数；未部署。
   **第 2 步上半（`W10` ⑤）已完成**：读者态 5905→4067px（−31%），单张演练卡 −49%，
   全部展开后 5941px 比改前多 36px——内容一字未删，省的全部来自层级；`verify:board` 421/421；未部署。
   **第 3 步（`W6Day4Board`）已完成**：展示态 5537→3184px（−42%），复习态的逐条回忆设计未动
   （九条纠错做成按视角给默认值的受控 details）；全展开后比改前少 105px，差值来自三处标题字号，
   逐块字数与改前完全一致；`verify:board` 421/421；未部署。
   **第 2 步下半（`W9` `evidence`）已完成**：复习态 4268→1712px（−60%，四轮最大）；
   代价是该板只有复习态，收起 17 条认知修正多一次点击（取舍已记录，反悔成本极低）；
   `verify:board` 421/421；未部署。
   **`W10` ⑦「红过才算数」已于 W10 整周收口后解冻并完成**：4346→3000px（−31%）；
   该轮故意折得比能折的少——`selfwatch`（静默常绿）是验收句的另一半，保持常驻。
   **第 4 步核实后判定「不做」**：其前提「首屏 0% 编码」是选择器漏报——四个专题首屏
   （`w5-runtime-owners` / `w5-sm-row` / `w5-pipe-stage` / `w9-exposure-card` / `w9-level`）
   其实都有位置编码，只是类名不在清单里。四个专题一行未改。**阅读负担这一支到此五轮收口。**
   **视觉编码那一维已开工**：共性欠口②（量的差距只写成数字）已落地——`W3Board` 的 `ExplainVisual`
   与 `W6Day4Board` 的 `EvidenceLayers` 共用一套行内长度编码，一次覆盖 6 个单元里的 4 个；
   类别型的格子一条不画（W6 那组整组不画），零画成短记号不画成空白；`verify:board` 421/421。
   **剩**：共性欠口①（`AuthBoard` + `W6Board` Day 3 互斥切换，2 单元，建议下一轮）、
   `W6Board` 常驻折叠的证据矩阵、`OAuth2Panel` 凭据卡（待裁决）、`W10` ⑤ 第二轮（待定）。
   **待本人定**：⑦ 降下来后 `W10` ⑤ 的 4.07 屏又是全站最重，而 ⑤ 已做过一轮——
   要不要对它做第二轮**不在原四步范围内**，未自行扩范围。

**判据在 2026-08-21 被补正过一次**（核查线 §3、§9）：原来的四条肯定面只检查「结论有没有被版面承载」，
不检查「承载之后读起来累不累」。补入**主路径 / 重复量 / 文案可读性**三问后，`W9Board` 的「无欠项」被撤回。
**因此不要用 8/20 的口径读下面的结论**——那一版只过了第一问。

**阶段一现状（2026-08-21）**：

- **视觉编码**：`OAuth2Panel`（凭据卡一处留裁决）、`W5Board`、`W10Board` 无欠项；
  `AuthBoard`、`W6Board` 各一处互斥切换（各 1 单元）；`W3Board`、`W6Day4Board` 各一处量欠图（各 2 单元）；
  `W9Board` 视觉编码可答，问题不在缺图。「面试准备」判定**不纳入**。
- **阅读负担（实测，非估算）**：把展板跑起来量了**八块板 + 面试板全部 45 个专题**的正文区高度。
  按平均屏数排：`W10Board` **2.88 屏（7/7 超一屏）** > `W6Board` 2.73（含 Day 4 子板 **5.12 屏**）
  > `W9Board` **2.03（11/13）** > `W5Board` 1.48 > 面试板 0.98 ≈ `AuthBoard` 0.97
  > `W3Board` 0.74 ≈ `OAuth2Panel` 0.73。方法与验证见核查线 §5.1。
- **两条横贯多块的共性欠口**：① 互斥切换（`AuthBoard` / `W6Board` Day 3 / `W3Board` / `W5Board` / `W6Day4Board`）；
  ② 量的差距只写成数字（`W3Board` 的 `ExplainVisual` 与 `W6Day4Board` 的 `EvidenceLayers`，`HBarChart` 已存在属复用）。
  这两条一次做完覆盖面大于逐块修。

**档位：维持 P2，暂不重定。** 上一版留的问题是「范围扩大后是否升档」，现在还不能答——
判据补正后**规模的大头从「有没有图」换成了「读起来累不累」**，而后者的两个最重的对象
（`W10Board` 7/7 超一屏、`W6Day4Board` 5.12 屏）都是 8/20 判定时**认为不必改或只需小改**的块。
减负的单元数要等本人过完三问才估得出，**在那之前不动档位**。
（实测数据是事实，可复核；「遮住 `h3` 能否答出验收句」「是否要读完多屏才知道重点」的判断仍是人闸。）

---

### P2-3 · 真实 OAuth2 接入 —— 规模：中

当前 OAuth2 是讲解型 demo，不接真实第三方 provider。

来源：`LEARNING-STATE.md`「已知非阻断遗留」。

**为什么是 P2**：`interview-prep/backend-qa-sheet.md` 已经把认证鉴权列为 ✅ 可复述强项，
OAuth2 的**凭据边界**（`state` 何时比对、`client_secret` 为什么不能进浏览器、
第一方会话与第三方 token 的区别）才是面试真正考的，而这部分你已经能讲。
接真实 provider 需要注册应用、配回调域名，增量主要是工程手感，不是理解。

---

## 明确不做

- **epoll / kqueue / IOCP 实现差异、TCP 重组、HTTP parser 内部实现**：
  `week5-nodejs-internals/notes/day3-threadpool-continuation.md` 第 210 行已给出投入产出结论并止步于
  「能提出正确假设并知道该测什么」。这个判断现在依然成立，不重开。

---

## 维护约定

- 新条目一律先进本文并给出档位，不散落到各周笔记里。
- 档位可以调，但要写明调整理由和日期。已发生一次：Harness Lab 由本人补充工作背景后，
  2026-07-29 从「P1 待定」提到 **P0-2**；TS 的缺口性质同日由「筛选项」更正为「能用但未到能讲」。
- 条目完成后移入下方记录，不直接删除，保留判断留痕。

### 已完成

（暂无）
