# W5 · Node.js 底层理解展示板 · 方案草案

> 状态：**草案 / 未定稿（2026-07-21）**。先写一版对齐方向，本周随进度再改。
> 定位：展示前端是白名单展示资产，本方案只规划「怎么把已学到的东西可视化」，不规划核心学习本身。

## 1. 目的

- 本周（W5，Node.js 底层）是核心保护周，但随时可能被面试等打断。
- 对策：**能学多少学多少，学到的都用带 UI 的方式可展示出来**。做展示时相当于一次复盘，partial 进度也不等于零。
- 载体：`week8-fullstack/src/frontend/`（AGENTS.md 白名单，AI 可维护）。

## 2. 边界（红线）

- ✅ AI 可做：把**本人已跑出的现象、数值、判断表**用 UI 呈现。
- ❌ AI 不做：事件循环 / 阻塞 / stream / worker 等**核心 demo 代码**（黑名单，援助上限 L2）。
- 分界：展示板画的是「本人产出的数据 + 本人推导的模型」，**不是**「输入任意代码→自动算执行顺序」的在线模拟器——后者等于替本人完成理解 demo。

## 3. 设计原则

1. **少字多象**：每主题以一个可视化的可观察现象为主角，文字降为旁注。
2. **真实数据驱动**：画的是本人实验测到的真实数值，不是装饰图形。
3. **粗颗粒**：每周 demo 仅约 10 分钟，做「几个能一眼看懂的大块」，不做细碎分步。

## 4. 展示板结构

在现有前端加一个与报表看板并列的视图，**每个主题一张卡**，固定模板：

| 字段 | 内容 |
|---|---|
| 状态 | 已完成 / 进行中 / 未开始 |
| 可观察证据 | 本人实验的真实输出（可视化主角） |
| 运行时判断 | 能迁移回业务的一句话结论 |
| 映射回 Week2–4 | 对应真实后端代码（bcrypt=CPU、mongo 查询=I/O…） |

D3–D5 未学到时先放「未开始」占位，学到哪填到哪，看板始终完整。

## 5. 各主题视觉呈现（定调，非最终）

- **D1 事件循环**（已完成）：队列泳道图，按本人脚本的真实输出序列高亮播放「预测 vs 实际」。
- **D2 CPU 阻塞**（已完成）：时间轴条形对照，`20ms→late 0` vs `2000ms→late 1904` 并排，一眼看出「CPU 越久 timer 越晚」。线程池实验完成后补一组 `UV_THREADPOOL_SIZE` 前后对照。
- **D3 stream / 背压**（未开始）：producer/consumer 缓冲条动画，高水位触发 drain 变色；旁边 `readFile` vs stream 内存占用对比条。
- **D4 错误边界**（未开始）：可点的「错误流向图」，点某类错误高亮它被谁捕获、到哪一层。
- **D5 worker**（未开始）：主线程阻塞 vs worker offload 两条「响应能力」时间线对照。

## 6. OAuth2 tab 返工（已完成，2026-07-21）

原「6 步有序列表 + 两张表格」的纯文字堆，已改成**授权码流程时序播放器**：

- 三条泳道（用户/浏览器 · 我们的后端 · 第三方），一步高亮一步。
- **前信道（过浏览器）vs 后信道（后端直连）**配色，让「client_secret 绝不过浏览器」一眼可见。
- 底部 4 件凭据（code / client_secret / 第三方 token / 本系统 JWT）随步骤高亮。
- 实现：`week8-fullstack/src/frontend/src/Dashboard.tsx` 的 `OAuth2FlowPanel` + `styles.css`；`yarn typecheck` / `yarn build` 通过。

## 7. 优先级

- **不重排 D1–D5 日序**（按依赖递进）。
- 改为**降低每天产出门槛**：每天只要求「一个可观察现象 + 一句业务判断」进看板。
- 时间紧时优先保 **D2（阻塞/线程池）与 D3（stream/背压）**——可观察 + 可视化 + 业务相关 ROI 最高；D4/D5 可更轻（D5 验收本就是串讲）。
- 计划里 section 3「锦上添花」维持不追。

## 8. 未定项（待本人拍板）

- 展示板做成 Dashboard 新 tab，还是独立路由？
- D1 泳道图是否要动画，还是静态高亮即可？
- 数据文件（如 `w5Topics.ts`）的内容以本人笔记为唯一来源，需逐条确认后再填。
- 展示板动手时机：等 D3+ 有实验产出后，还是先搭空壳逐步填。
